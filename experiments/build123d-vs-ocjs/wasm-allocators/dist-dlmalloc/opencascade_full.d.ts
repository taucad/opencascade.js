/**
 * Auxiliary class representing a part of the global progress scale allocated by a step of the progress scope, see `Message_ProgressScope::Next()`.
 *
 * A range object takes responsibility of advancing the progress by the size of allocated step, which is then performed depending on how it is used:
 *
 * - If {@link Message_ProgressScope | `Message_ProgressScope`} object is created using this range as argument, then this respondibility is taken over by that scope.
 * - Otherwise, a range advances progress directly upon destruction.
 *
 * A range object can be copied, the responsibility for progress advancement is then taken by the copy. The same range object may be used (either copied or used to create scope) only once. Any consequent attempts to use range will give no result on the progress; in debug mode, an assert message will be generated.
 * @see {@link Message_ProgressScope | `Message_ProgressScope`}
 */
export declare class Message_ProgressRange {
  /**
   * Constructor of the empty range.
   */
  constructor();
  /**
   * Copy constructor disarms the source.
   */
  constructor(theOther: Message_ProgressRange);
  /**
   * Returns true if ProgressIndicator signals UserBreak.
   */
  UserBreak(): boolean;
  /**
   * Returns false if ProgressIndicator signals UserBreak.
   */
  More(): boolean;
  /**
   * Returns true if this progress range is attached to some indicator.
   */
  IsActive(): boolean;
  /**
   * Closes the current range and advances indicator.
   */
  Close(): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

export declare class NCollection_BaseList {
  Extent(): number;
  /**
   * Length - number of nodes (legacy int-returning API, synonym of `Extent()`).
   */
  Length(): number;
  /**
   * Size - number of nodes.
   */
  Size(): number;
  IsEmpty(): boolean;
  /**
   * Returns attached allocator.
   */
  Allocator(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Abstract class which forms the root of the entire Transient class hierarchy.
 */
export declare class Standard_Transient {
  /** Returns true if the underlying handle is null. */
  isNull(): boolean;
  /** Releases the handle, setting it to null. */
  nullify(): void;
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor - does nothing.
   */
  constructor(a0: Standard_Transient);
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  IsInstance(theType: unknown): boolean;
  IsInstance(theTypeName: string): boolean;
  IsKind(theType: unknown): boolean;
  IsKind(theTypeName: string): boolean;
  This(): Standard_Transient;
  GetRefCount(): number;
  IncrementRefCounter(): void;
  DecrementRefCounter(): number;
  Delete(): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * A Location is a composite transition. It comprises a series of elementary reference coordinates, i.e. objects of type {@link TopLoc_Datum3D | `TopLoc_Datum3D`}, and the powers to which these objects are raised.
 */
export declare class TopLoc_Location {
  /**
   * Constructs an empty local coordinate system object. Note: A Location constructed from a default datum is said to be "empty".
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: TopLoc_Location);
  /**
   * Move constructor.
   */
  constructor(T: unknown);
  /**
   * Constructs the local coordinate system object defined by the transformation T. T invokes in turn, a {@link TopLoc_Datum3D | `TopLoc_Datum3D`} object.
   */
  constructor(D: unknown);
  /**
   * Returns true if this location is equal to the Identity transformation.
   */
  IsIdentity(): boolean;
  /**
   * Resets this location to the Identity transformation.
   */
  Identity(): void;
  /**
   * Returns the first elementary datum of the Location. Use the NextLocation function recursively to access the other data comprising this location. Exceptions Standard_NoSuchObject if this location is empty.
   */
  FirstDatum(): unknown;
  /**
   * Returns the power elevation of the first elementary datum. Exceptions Standard_NoSuchObject if this location is empty.
   */
  FirstPower(): number;
  /**
   * Returns a Location representing <me> without the first datum. We have the relation:
   *
   * <me> = `NextLocation()` * `FirstDatum()` ^ `FirstPower()` Exceptions Standard_NoSuchObject if this location is empty.
   */
  NextLocation(): TopLoc_Location;
  /**
   * Returns the transformation associated to the coordinate system.
   */
  Transformation(): unknown;
  /**
   * Returns the inverse of <me>.
   *
   * <me> * `Inverted()` is an Identity.
   */
  Inverted(): TopLoc_Location;
  /**
   * Returns <me> * <Other>, the elementary datums are concatenated.
   */
  Multiplied(Other: TopLoc_Location): TopLoc_Location;
  /**
   * Returns <me> / <Other>.
   */
  Divided(Other: TopLoc_Location): TopLoc_Location;
  /**
   * Returns <Other>.`Inverted()` * <me>.
   */
  Predivided(Other: TopLoc_Location): TopLoc_Location;
  /**
   * Returns me at the power <pwr>. If <pwr> is zero returns Identity. <pwr> can be lower than zero (usual meaning for powers).
   */
  Powered(pwr: number): TopLoc_Location;
  /**
   * Returns a hashed value for this local coordinate system. This value is used, with map tables, to store and retrieve the object easily.
   * @returns a computed hash code
   */
  HashCode(): number;
  /**
   * Returns true if this location and the location Other have the same elementary data, i.e. contain the same series of {@link TopLoc_Datum3D | `TopLoc_Datum3D`} and respective powers. This method is an alias for operator ==.
   */
  IsEqual(theOther: TopLoc_Location): boolean;
  /**
   * Returns true if this location and the location Other do not have the same elementary data, i.e. do not contain the same series of {@link TopLoc_Datum3D | `TopLoc_Datum3D`} and respective powers. This method is an alias for operator !=.
   */
  IsDifferent(theOther: TopLoc_Location): boolean;
  /**
   * Clear myItems.
   */
  Clear(): void;
  static ScalePrec(): number;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Defines a 3D cartesian point.
 */
export declare class gp_Pnt {
  /**
   * Creates a point with zero coordinates.
   */
  constructor();
  /**
   * Creates a point from a XYZ object.
   */
  constructor(theCoord: unknown);
  /**
   * Creates a point with its 3 cartesian's coordinates: theXp, theYp, theZp.
   */
  constructor(theXp: number, theYp: number, theZp: number);
  /**
   * Changes the coordinate of range theIndex: theIndex = 1 => X is modified theIndex = 2 => Y is modified theIndex = 3 => Z is modified Raised if theIndex != {1, 2, 3}.
   */
  SetCoord(theIndex: number, theXi: number): void;
  /**
   * For this point, assigns the values theXp, theYp and theZp to its three coordinates.
   */
  SetCoord(theXp: number, theYp: number, theZp: number): void;
  /**
   * Assigns the given value to the X coordinate of this point.
   */
  SetX(theX: number): void;
  /**
   * Assigns the given value to the Y coordinate of this point.
   */
  SetY(theY: number): void;
  /**
   * Assigns the given value to the Z coordinate of this point.
   */
  SetZ(theZ: number): void;
  /**
   * Assigns the three coordinates of theCoord to this point.
   */
  SetXYZ(theCoord: unknown): void;
  /**
   * Returns the coordinate of corresponding to the value of theIndex : theIndex = 1 => X is returned theIndex = 2 => Y is returned theIndex = 3 => Z is returned Raises OutOfRange if theIndex != {1, 2, 3}. Raised if theIndex != {1, 2, 3}.
   */
  Coord(theIndex: number): number;
  /**
   * For this point gives its three coordinates theXp, theYp and theZp.
   * @returns A result object with fields:
   * - `theXp`: updated value from the call.
   * - `theYp`: updated value from the call.
   * - `theZp`: updated value from the call.
   */
  Coord(theXp: number, theYp: number, theZp: number): { theXp: number; theYp: number; theZp: number };
  /**
   * For this point, returns its three coordinates as a XYZ object.
   */
  Coord(): unknown;
  /**
   * For this point, returns its X coordinate.
   */
  X(): number;
  /**
   * For this point, returns its Y coordinate.
   */
  Y(): number;
  /**
   * For this point, returns its Z coordinate.
   */
  Z(): number;
  /**
   * For this point, returns its three coordinates as a XYZ object.
   */
  XYZ(): unknown;
  /**
   * Returns the coordinates of this point. Note: This syntax allows direct modification of the returned value.
   */
  ChangeCoord(): unknown;
  /**
   * Assigns the result of the following expression to this point (theAlpha*this + theBeta*theP) / (theAlpha + theBeta).
   */
  BaryCenter(theAlpha: number, theP: gp_Pnt, theBeta: number): void;
  /**
   * Comparison Returns True if the distance between the two points is lower or equal to theLinearTolerance.
   */
  IsEqual(theOther: gp_Pnt, theLinearTolerance: number): boolean;
  /**
   * Computes the distance between two points.
   */
  Distance(theOther: gp_Pnt): number;
  /**
   * Computes the square distance between two points.
   */
  SquareDistance(theOther: gp_Pnt): number;
  /**
   * Performs the symmetrical transformation of a point with respect to the point theP which is the center of the symmetry.
   */
  Mirror(theP: gp_Pnt): void;
  Mirror(theA1: unknown): void;
  Mirror(theA2: gp_Ax2): void;
  /**
   * Performs the symmetrical transformation of a point with respect to an axis placement which is the axis of the symmetry.
   */
  Mirrored(theP: gp_Pnt): gp_Pnt;
  /**
   * Performs the symmetrical transformation of a point with respect to a plane. The axis placement theA2 locates the plane of the symmetry : (Location, XDirection, YDirection).
   */
  Mirrored(theA1: unknown): gp_Pnt;
  /**
   * Rotates a point. theA1 is the axis of the rotation. theAng is the angular value of the rotation in radians.
   */
  Mirrored(theA2: gp_Ax2): gp_Pnt;
  Rotate(theA1: unknown, theAng: number): void;
  Rotated(theA1: unknown, theAng: number): gp_Pnt;
  /**
   * Scales a point. theS is the scaling value.
   */
  Scale(theP: gp_Pnt, theS: number): void;
  Scaled(theP: gp_Pnt, theS: number): gp_Pnt;
  /**
   * Transforms a point with the transformation T.
   */
  Transform(theT: unknown): void;
  Transformed(theT: unknown): gp_Pnt;
  /**
   * Translates a point in the direction of the vector theV. The magnitude of the translation is the vector's magnitude.
   */
  Translate(theV: unknown): void;
  /**
   * Translates a point from the point theP1 to the point theP2.
   */
  Translate(theP1: gp_Pnt, theP2: gp_Pnt): void;
  Translated(theV: unknown): gp_Pnt;
  Translated(theP1: gp_Pnt, theP2: gp_Pnt): gp_Pnt;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes a right-handed coordinate system in 3D space. A coordinate system is defined by:
 *
 * - its origin (also referred to as its "Location point"), and
 * - three orthogonal unit vectors, termed respectively the "X Direction", the "Y Direction" and the "Direction" (also referred to as the "main Direction"). The "Direction" of the coordinate system is called its "main Direction" because whenever this unit vector is modified, the "X Direction" and the "Y Direction" are recomputed. However, when we modify either the "X Direction" or the "Y Direction", "Direction" is not modified. The "main Direction" is also the "Z Direction".
 * Since an Ax2 coordinate system is right-handed, its "main Direction" is always equal to the cross product of its "X Direction" and "Y Direction". (To define a left-handed coordinate system, use {@link gp_Ax3 | `gp_Ax3`}.) A coordinate system is used:
 * - to describe geometric entities, in particular to position them. The local coordinate system of a geometric entity serves the same purpose as the STEP function "axis placement two axes", or
 * - to define geometric transformations. Note: we refer to the "X Axis", "Y Axis" and "Z Axis", respectively, as to axes having:
 * - the origin of the coordinate system as their origin, and
 * - the unit vectors "X Direction", "Y Direction" and "main Direction", respectively, as their unit vectors. The "Z Axis" is also the "main Axis".
 */
export declare class gp_Ax2 {
  /**
   * Creates an object corresponding to the reference coordinate system (OXYZ).
   */
  constructor();
  /**
   * Creates a coordinate system at the origin with the given standard main direction. Replaces `gp::XOY()`, `gp::YOZ()`, `gp::ZOX()` static functions.
   */
  constructor(theV: gp_Dir_D);
  /**
   * Creates a coordinate system with an origin P, where V gives the "main Direction" (here, "X Direction" and "Y Direction" are defined automatically).
   */
  constructor(P: gp_Pnt, V: gp_Dir);
  /**
   * Creates a coordinate system with an origin P and standard main direction.
   */
  constructor(theP: gp_Pnt, theV: gp_Dir_D);
  /**
   * Creates an axis placement with an origin P such that:
   *
   * - N is the Direction, and
   * - the "X Direction" is normal to N, in the plane defined by the vectors (N, Vx): "X Direction" = (N ^ Vx) ^ N, Exception: raises ConstructionError if N and Vx are parallel (same or opposite orientation).
   */
  constructor(P: gp_Pnt, N: gp_Dir, Vx: gp_Dir);
  /**
   * Creates an axis placement with standard directions.
   */
  constructor(theP: gp_Pnt, theN: gp_Dir_D, theVx: gp_Dir_D);
  /**
   * Assigns the origin and "main Direction" of the axis A1 to this coordinate system, then recomputes its "X Direction" and "Y Direction". Note: The new "X Direction" is computed as follows: new "X Direction" = V1 ^(previous "X Direction" ^ V) where V is the "Direction" of A1. Exceptions Standard_ConstructionError if A1 is parallel to the "X Direction" of this coordinate system.
   */
  SetAxis(A1: unknown): void;
  /**
   * Changes the "main Direction" of this coordinate system, then recomputes its "X Direction" and "Y Direction". Note: the new "X Direction" is computed as follows: new "X Direction" = V ^ (previous "X Direction" ^ V) Exceptions Standard_ConstructionError if V is parallel to the "X Direction" of this coordinate system.
   */
  SetDirection(V: gp_Dir): void;
  /**
   * Changes the "Location" point (origin) of <me>.
   */
  SetLocation(theP: gp_Pnt): void;
  /**
   * Changes the "Xdirection" of <me>. The main direction "Direction" is not modified, the "Ydirection" is modified. If <Vx> is not normal to the main direction then <XDirection> is computed as follows XDirection = Direction ^ (Vx ^ Direction). Exceptions Standard_ConstructionError if Vx or Vy is parallel to the "main Direction" of this coordinate system.
   */
  SetXDirection(theVx: gp_Dir): void;
  /**
   * Changes the "Ydirection" of <me>. The main direction is not modified but the "Xdirection" is changed. If <Vy> is not normal to the main direction then "YDirection" is computed as follows YDirection = Direction ^ (<Vy> ^ Direction). Exceptions Standard_ConstructionError if Vx or Vy is parallel to the "main Direction" of this coordinate system.
   */
  SetYDirection(theVy: gp_Dir): void;
  /**
   * Computes the angular value, in radians, between the main direction of <me> and the main direction of <theOther>. Returns the angle between 0 and PI in radians.
   */
  Angle(theOther: gp_Ax2): number;
  /**
   * Returns the main axis of <me>. It is the "Location" point and the main "Direction".
   */
  Axis(): unknown;
  /**
   * Returns the main direction of <me>.
   */
  Direction(): gp_Dir;
  /**
   * Returns the "Location" point (origin) of <me>.
   */
  Location(): gp_Pnt;
  /**
   * Returns the "XDirection" of <me>.
   */
  XDirection(): gp_Dir;
  /**
   * Returns the "YDirection" of <me>.
   */
  YDirection(): gp_Dir;
  IsCoplanar(Other: gp_Ax2, LinearTolerance: number, AngularTolerance: number): boolean;
  /**
   * Returns True if: . the distance between <me> and the "Location" point of A1 is lower of equal to LinearTolerance and . the main direction of <me> and the direction of A1 are normal. Note: the tolerance criterion for angular equality is given by AngularTolerance.
   */
  IsCoplanar(A1: unknown, LinearTolerance: number, AngularTolerance: number): boolean;
  /**
   * Performs a symmetrical transformation of this coordinate system with respect to:
   *
   * - the point P, and assigns the result to this coordinate system. Warning This transformation is always performed on the origin. In case of a reflection with respect to a point:
   * - the main direction of the coordinate system is not changed, and
   * - the "X Direction" and the "Y Direction" are simply reversed In case of a reflection with respect to an axis or a plane:
   * - the transformation is applied to the "X Direction" and the "Y Direction", then
   * - the "main Direction" is recomputed as the cross product "X Direction" ^ "Y Direction". This maintains the right-handed property of the coordinate system.
   */
  Mirror(P: gp_Pnt): void;
  /**
   * Performs a symmetrical transformation of this coordinate system with respect to:
   *
   * - the axis A1, and assigns the result to this coordinate system. Warning This transformation is always performed on the origin. In case of a reflection with respect to a point:
   * - the main direction of the coordinate system is not changed, and
   * - the "X Direction" and the "Y Direction" are simply reversed In case of a reflection with respect to an axis or a plane:
   * - the transformation is applied to the "X Direction" and the "Y Direction", then
   * - the "main Direction" is recomputed as the cross product "X Direction" ^ "Y Direction". This maintains the right-handed property of the coordinate system.
   */
  Mirror(A1: unknown): void;
  /**
   * Performs a symmetrical transformation of this coordinate system with respect to:
   *
   * - the plane defined by the origin, "X Direction" and "Y Direction" of coordinate system A2 and assigns the result to this coordinate system. Warning This transformation is always performed on the origin. In case of a reflection with respect to a point:
   * - the main direction of the coordinate system is not changed, and
   * - the "X Direction" and the "Y Direction" are simply reversed In case of a reflection with respect to an axis or a plane:
   * - the transformation is applied to the "X Direction" and the "Y Direction", then
   * - the "main Direction" is recomputed as the cross product "X Direction" ^ "Y Direction". This maintains the right-handed property of the coordinate system.
   */
  Mirror(A2: gp_Ax2): void;
  /**
   * Performs a symmetrical transformation of this coordinate system with respect to:
   *
   * - the point P, and creates a new one. Warning This transformation is always performed on the origin. In case of a reflection with respect to a point:
   * - the main direction of the coordinate system is not changed, and
   * - the "X Direction" and the "Y Direction" are simply reversed In case of a reflection with respect to an axis or a plane:
   * - the transformation is applied to the "X Direction" and the "Y Direction", then
   * - the "main Direction" is recomputed as the cross product "X Direction" ^ "Y Direction". This maintains the right-handed property of the coordinate system.
   */
  Mirrored(P: gp_Pnt): gp_Ax2;
  /**
   * Performs a symmetrical transformation of this coordinate system with respect to:
   *
   * - the axis A1, and creates a new one. Warning This transformation is always performed on the origin. In case of a reflection with respect to a point:
   * - the main direction of the coordinate system is not changed, and
   * - the "X Direction" and the "Y Direction" are simply reversed In case of a reflection with respect to an axis or a plane:
   * - the transformation is applied to the "X Direction" and the "Y Direction", then
   * - the "main Direction" is recomputed as the cross product "X Direction" ^ "Y Direction". This maintains the right-handed property of the coordinate system.
   */
  Mirrored(A1: unknown): gp_Ax2;
  /**
   * Performs a symmetrical transformation of this coordinate system with respect to:
   *
   * - the plane defined by the origin, "X Direction" and "Y Direction" of coordinate system A2 and creates a new one. Warning This transformation is always performed on the origin. In case of a reflection with respect to a point:
   * - the main direction of the coordinate system is not changed, and
   * - the "X Direction" and the "Y Direction" are simply reversed In case of a reflection with respect to an axis or a plane:
   * - the transformation is applied to the "X Direction" and the "Y Direction", then
   * - the "main Direction" is recomputed as the cross product "X Direction" ^ "Y Direction". This maintains the right-handed property of the coordinate system.
   */
  Mirrored(A2: gp_Ax2): gp_Ax2;
  Rotate(theA1: unknown, theAng: number): void;
  /**
   * Rotates an axis placement. <theA1> is the axis of the rotation. theAng is the angular value of the rotation in radians.
   */
  Rotated(theA1: unknown, theAng: number): gp_Ax2;
  Scale(theP: gp_Pnt, theS: number): void;
  /**
   * Applies a scaling transformation on the axis placement. The "Location" point of the axisplacement is modified. Warnings: If the scale is negative: . the main direction of the axis placement is not changed. . The "XDirection" and the "YDirection" are reversed. So the axis placement stay right handed.
   */
  Scaled(theP: gp_Pnt, theS: number): gp_Ax2;
  Transform(theT: unknown): void;
  /**
   * Transforms an axis placement with a Trsf. The "Location" point, the "XDirection" and the "YDirection" are transformed with theT. The resulting main "Direction" of <me> is the cross product between the "XDirection" and the "YDirection" after transformation.
   */
  Transformed(theT: unknown): gp_Ax2;
  Translate(theV: unknown): void;
  Translate(theP1: gp_Pnt, theP2: gp_Pnt): void;
  /**
   * Translates an axis plaxement in the direction of the vector <theV>. The magnitude of the translation is the vector's magnitude.
   */
  Translated(theV: unknown): gp_Ax2;
  /**
   * Translates an axis placement from the point <theP1> to the point <theP2>.
   */
  Translated(theP1: gp_Pnt, theP2: gp_Pnt): gp_Ax2;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes a unit vector in 3`D` space. This unit vector is also called "Direction". See Also {@link gce_MakeDir | `gce_MakeDir`} which provides functions for more complex unit vector constructions {@link Geom_Direction | `Geom_Direction`} which provides additional functions for constructing unit vectors and works, in particular, with the parametric equations of unit vectors.
 */
export declare class gp_Dir {
  /**
   * Creates a direction corresponding to X axis.
   */
  constructor();
  /**
   * Creates a direction from a standard direction enumeration.
   */
  constructor(theDir: gp_Dir_D);
  /**
   * Normalizes the vector theV and creates a direction. Raises ConstructionError if theV.Magnitude() <= Resolution.
   * @remarks **Note:** Constexpr-compatible when input is already normalized.
   */
  constructor(theV: unknown);
  /**
   * Creates a direction from a triplet of coordinates. Raises ConstructionError if theCoord.Modulus() <= Resolution from gp.
   * @remarks **Note:** Constexpr-compatible when input is already normalized.
   */
  constructor(theCoord: unknown);
  constructor(a0: gp_Dir);
  /**
   * Creates a direction with its 3 cartesian coordinates. Raises ConstructionError if std::sqrt(theXv*theXv + theYv*theYv + theZv*theZv) <= Resolution Modification of the direction's coordinates If std::sqrt (theXv*theXv + theYv*theYv + theZv*theZv) <= Resolution from gp where theXv, theYv ,theZv are the new coordinates it is not possible to construct the direction and the method raises the exception ConstructionError.
   * @remarks **Note:** Constexpr-compatible when input is already normalized.
   */
  constructor(theXv: number, theYv: number, theZv: number);
  /**
   * For this unit vector, assigns the value Xi to:
   *
   * - the X coordinate if theIndex is 1, or
   * - the Y coordinate if theIndex is 2, or
   * - the Z coordinate if theIndex is 3, and then normalizes it. Warning: Remember that all the coordinates of a unit vector are implicitly modified when any single one is changed directly. Exceptions Standard_OutOfRange if theIndex is not 1, 2, or 3. Standard_ConstructionError if either of the following is less than or equal to `gp::Resolution()`:
   * - std::sqrt(Xv*Xv + Yv*Yv + Zv*Zv), or
   * - the modulus of the number triple formed by the new value theXi and the two other coordinates of this vector that were not directly modified.
   * @remarks **Note:** Constexpr-compatible when result is already normalized.
   */
  SetCoord(theIndex: number, theXi: number): void;
  /**
   * For this unit vector, assigns the values theXv, theYv and theZv to its three coordinates. Remember that all the coordinates of a unit vector are implicitly modified when any single one is changed directly.
   * @remarks **Note:** Constexpr-compatible when input is already normalized.
   */
  SetCoord(theXv: number, theYv: number, theZv: number): void;
  /**
   * Assigns the given value to the X coordinate of this unit vector.
   * @remarks **Note:** Constexpr-compatible when result is already normalized.
   */
  SetX(theX: number): void;
  /**
   * Assigns the given value to the Y coordinate of this unit vector.
   * @remarks **Note:** Constexpr-compatible when result is already normalized.
   */
  SetY(theY: number): void;
  /**
   * Assigns the given value to the Z coordinate of this unit vector.
   * @remarks **Note:** Constexpr-compatible when result is already normalized.
   */
  SetZ(theZ: number): void;
  /**
   * Assigns the three coordinates of theCoord to this unit vector.
   * @remarks **Note:** Constexpr-compatible when input is already normalized.
   */
  SetXYZ(theCoord: unknown): void;
  /**
   * Returns the coordinate of range theIndex : theIndex = 1 => X is returned theIndex = 2 => Y is returned theIndex = 3 => Z is returned Exceptions Standard_OutOfRange if theIndex is not 1, 2, or 3.
   */
  Coord(theIndex: number): number;
  /**
   * Returns for the unit vector its three coordinates theXv, theYv, and theZv.
   * @returns A result object with fields:
   * - `theXv`: updated value from the call.
   * - `theYv`: updated value from the call.
   * - `theZv`: updated value from the call.
   */
  Coord(theXv: number, theYv: number, theZv: number): { theXv: number; theYv: number; theZv: number };
  /**
   * Returns the X coordinate for a unit vector.
   */
  X(): number;
  /**
   * Returns the Y coordinate for a unit vector.
   */
  Y(): number;
  /**
   * Returns the Z coordinate for a unit vector.
   */
  Z(): number;
  /**
   * for this unit vector, returns its three coordinates as a number triple.
   */
  XYZ(): unknown;
  /**
   * Returns True if the angle between the two directions is lower or equal to theAngularTolerance.
   */
  IsEqual(theOther: gp_Dir, theAngularTolerance: number): boolean;
  /**
   * Returns True if the angle between this unit vector and the unit vector theOther is equal to Pi/2 (normal).
   */
  IsNormal(theOther: gp_Dir, theAngularTolerance: number): boolean;
  /**
   * Returns True if the angle between this unit vector and the unit vector theOther is equal to Pi (opposite).
   */
  IsOpposite(theOther: gp_Dir, theAngularTolerance: number): boolean;
  /**
   * Returns true if the angle between this unit vector and the unit vector theOther is equal to 0 or to Pi. Note: the tolerance criterion is given by theAngularTolerance.
   */
  IsParallel(theOther: gp_Dir, theAngularTolerance: number): boolean;
  /**
   * Computes the angular value in radians between <me> and <theOther>. This value is always positive in 3`D` space. Returns the angle in the range [0, PI].
   */
  Angle(theOther: gp_Dir): number;
  /**
   * Computes the angular value between <me> and <theOther>. <theVRef> is the direction of reference normal to <me> and <theOther> and its orientation gives the positive sense of rotation. If the cross product <me> ^ <theOther> has the same orientation as <theVRef> the angular value is positive else negative. Returns the angular value in the range -PI and PI (in radians). Raises DomainError if <me> and <theOther> are not parallel this exception is raised when <theVRef> is in the same plane as <me> and <theOther> The tolerance criterion is Resolution from package gp.
   */
  AngleWithRef(theOther: gp_Dir, theVRef: gp_Dir): number;
  /**
   * Computes the cross product between two directions Raises the exception ConstructionError if the two directions are parallel because the computed vector cannot be normalized to create a direction.
   * @remarks **Note:** Constexpr-compatible when result is already normalized.
   */
  Cross(theRight: gp_Dir): void;
  /**
   * Computes the triple vector product. <me> ^ (V1 ^ V2) Raises the exception ConstructionError if V1 and V2 are parallel or <me> and (V1^V2) are parallel because the computed vector can't be normalized to create a direction.
   * @remarks **Note:** Constexpr-compatible when result is already normalized.
   */
  Crossed(theRight: gp_Dir): gp_Dir;
  /**
   * @remarks **Note:** Constexpr-compatible when result is already normalized.
   */
  CrossCross(theV1: gp_Dir, theV2: gp_Dir): void;
  /**
   * Computes the double vector product this ^ (theV1 ^ theV2).
   *
   * - CrossCrossed creates a new unit vector. Exceptions Standard_ConstructionError if:
   * - theV1 and theV2 are parallel, or
   * - this unit vector and (theV1 ^ theV2) are parallel. This is because, in these conditions, the computed vector is null and cannot be normalized.
   * @remarks **Note:** Constexpr-compatible when result is already normalized.
   */
  CrossCrossed(theV1: gp_Dir, theV2: gp_Dir): gp_Dir;
  /**
   * Computes the scalar product.
   */
  Dot(theOther: gp_Dir): number;
  /**
   * Computes the triple scalar product <me> * (theV1 ^ theV2). Warnings : The computed vector theV1' = theV1 ^ theV2 is not normalized to create a unitary vector. So this method never raises an exception even if theV1 and theV2 are parallel.
   */
  DotCross(theV1: gp_Dir, theV2: gp_Dir): number;
  Reverse(): void;
  /**
   * Reverses the orientation of a direction geometric transformations Performs the symmetrical transformation of a direction with respect to the direction V which is the center of the symmetry.
   */
  Reversed(): gp_Dir;
  Mirror(theV: gp_Dir): void;
  Mirror(theA1: unknown): void;
  Mirror(theA2: gp_Ax2): void;
  /**
   * Performs the symmetrical transformation of a direction with respect to the direction theV which is the center of the symmetry.
   */
  Mirrored(theV: gp_Dir): gp_Dir;
  /**
   * Performs the symmetrical transformation of a direction with respect to an axis placement which is the axis of the symmetry.
   */
  Mirrored(theA1: unknown): gp_Dir;
  /**
   * Performs the symmetrical transformation of a direction with respect to a plane. The axis placement theA2 locates the plane of the symmetry : (Location, XDirection, YDirection).
   */
  Mirrored(theA2: gp_Ax2): gp_Dir;
  Rotate(theA1: unknown, theAng: number): void;
  /**
   * Rotates a direction. theA1 is the axis of the rotation. theAng is the angular value of the rotation in radians.
   */
  Rotated(theA1: unknown, theAng: number): gp_Dir;
  Transform(theT: unknown): void;
  /**
   * Transforms a direction with a "Trsf" from gp. Warnings : If the scale factor of the "Trsf" theT is negative then the direction <me> is reversed.
   */
  Transformed(theT: unknown): gp_Dir;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

export type gp_Dir_D = typeof gp_Dir_D[keyof typeof gp_Dir_D];
/**
 * {@link Standard | `Standard`} directions in 3`D` space for optimized constexpr construction.
 */
export declare const gp_Dir_D: {
  /**
   * Direction along positive X axis (1, 0, 0).
   */
  readonly X: 'X';
  /**
   * Direction along positive Y axis (0, 1, 0).
   */
  readonly Y: 'Y';
  /**
   * Direction along positive Z axis (0, 0, 1).
   */
  readonly Z: 'Z';
  /**
   * Direction along negative X axis (-1, 0, 0).
   */
  readonly NX: 'NX';
  /**
   * Direction along negative Y axis (0, -1, 0).
   */
  readonly NY: 'NY';
  /**
   * Direction along negative Z axis (0, 0, -1).
   */
  readonly NZ: 'NZ';
};

export type GeomAbs_Shape = typeof GeomAbs_Shape[keyof typeof GeomAbs_Shape];
/**
 * Provides information about the continuity of a curve:
 *
 * - C0: only geometric continuity.
 * - G1: for each point on the curve, the tangent vectors "on the right" and "on the left" are collinear with the same orientation.
 * - C1: continuity of the first derivative. The "C1" curve is also "G1" but, in addition, the tangent vectors " on the right" and "on the left" are equal.
 * - G2: for each point on the curve, the normalized normal vectors "on the right" and "on the left" are equal.
 * - C2: continuity of the second derivative.
 * - C3: continuity of the third derivative.
 * - CN: continuity of the N-th derivative, whatever is the value given for N (infinite order of continuity). Also provides information about the continuity of a surface:
 * - C0: only geometric continuity.
 * - C1: continuity of the first derivatives; any isoparametric (in U or V) of a surface "C1" is also "C1".
 * - G2: for BSpline curves only; "on the right" and "on the left" of a knot the computation of the "main curvature radii" and the "main directions" (when they exist) gives the same result.
 * - C2: continuity of the second derivative.
 * - C3: continuity of the third derivative.
 * - CN: continuity of any N-th derivative, whatever is the value given for N (infinite order of continuity). We may also say that a surface is "Ci" in u, and "Cj" in v to indicate the continuity of its derivatives up to the order i in the u parametric direction, and j in the v parametric direction.
 */
export declare const GeomAbs_Shape: {
  readonly GeomAbs_C0: 'GeomAbs_C0';
  readonly GeomAbs_G1: 'GeomAbs_G1';
  readonly GeomAbs_C1: 'GeomAbs_C1';
  readonly GeomAbs_G2: 'GeomAbs_G2';
  readonly GeomAbs_C2: 'GeomAbs_C2';
  readonly GeomAbs_C3: 'GeomAbs_C3';
  readonly GeomAbs_CN: 'GeomAbs_CN';
};

/**
 * Provides a triangulation for a surface, a set of surfaces, or more generally a shape.
 *
 * A triangulation consists of an approximate representation of the actual shape, using a collection of points and triangles. The points are located on the surface. The edges of the triangles connect adjacent points with a straight line that approximates the true curve on the surface.
 *
 * A triangulation comprises:
 *
 * - A table of 3D nodes (3D points on the surface).
 * - A table of triangles. Each triangle ({@link Poly_Triangle | `Poly_Triangle`} object) comprises a triplet of indices in the table of 3D nodes specific to the triangulation.
 * - An optional table of 2D nodes (2D points), parallel to the table of 3D nodes. 2D point are the (u, v) parameters of the corresponding 3D point on the surface approximated by the triangulation.
 * - An optional table of 3D vectors, parallel to the table of 3D nodes, defining normals to the surface at specified 3D point.
 * - An optional deflection, which maximizes the distance from a point on the surface to the corresponding point on its approximate triangulation.
 *
 * In many cases, algorithms do not need to work with the exact representation of a surface. A triangular representation induces simpler and more robust adjusting, faster performances, and the results are as good.
 */
export declare class Poly_Triangulation extends Standard_Transient {
  /**
   * Constructs an empty triangulation.
   */
  constructor();
  /**
   * Copy constructor for triangulation.
   */
  constructor(theTriangulation: Poly_Triangulation);
  /**
   * Constructs a triangulation from a set of triangles. The triangulation is initialized with 3D points from Nodes and triangles from Triangles.
   */
  constructor(Nodes: NCollection_Array1_gp_Pnt, Triangles: NCollection_Array1_Poly_Triangle);
  /**
   * Constructs a triangulation from a set of triangles. The triangulation is initialized with 3D points from Nodes, 2D points from UVNodes and triangles from Triangles, where coordinates of a 2D point from UVNodes are the (u, v) parameters of the corresponding 3D point from Nodes on the surface approximated by the constructed triangulation.
   */
  constructor(Nodes: NCollection_Array1_gp_Pnt, UVNodes: NCollection_Array1_gp_Pnt2d, Triangles: NCollection_Array1_Poly_Triangle);
  /**
   * Constructs a triangulation from a set of triangles. The triangulation is initialized with 3D points from Nodes, 2D points from UVNodes and triangles from Triangles, where coordinates of a 2D point from UVNodes are the (u, v) parameters of the corresponding 3D point from Nodes on the surface approximated by the constructed triangulation.
   */
  constructor(theNbNodes: number, theNbTriangles: number, theHasUVNodes: boolean);
  /**
   * Constructs a triangulation from a set of triangles. The triangulation is initialized without a triangle or a node, but capable of containing specified number of nodes and triangles.
   * @param theNbNodes number of nodes to allocate
   * @param theNbTriangles number of triangles to allocate
   * @param theHasUVNodes indicates whether 2D nodes will be associated with 3D ones, (i.e. to enable a 2D representation)
   * @param theHasNormals indicates whether normals will be given and associated with nodes
   */
  constructor(theNbNodes: number, theNbTriangles: number, theHasUVNodes: boolean, theHasNormals?: boolean);
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /**
   * Creates full copy of current triangulation.
   */
  Copy(): Poly_Triangulation;
  /**
   * Returns the deflection of this triangulation.
   */
  Deflection(): number;
  /**
   * Sets the deflection of this triangulation to theDeflection. See more on deflection in Polygon2D.
   */
  Deflection(theDeflection: number): void;
  /**
   * Returns initial set of parameters used to generate this triangulation.
   */
  Parameters(): unknown;
  /**
   * Updates initial set of parameters used to generate this triangulation.
   */
  Parameters(theParams: unknown): void;
  /**
   * Clears internal arrays of nodes and all attributes.
   */
  Clear(): void;
  /**
   * Returns TRUE if triangulation has some geometry.
   */
  HasGeometry(): boolean;
  /**
   * Returns the number of nodes for this triangulation.
   */
  NbNodes(): number;
  /**
   * Returns the number of triangles for this triangulation.
   */
  NbTriangles(): number;
  /**
   * Returns true if 2D nodes are associated with 3D nodes for this triangulation.
   */
  HasUVNodes(): boolean;
  /**
   * Returns true if nodal normals are defined.
   */
  HasNormals(): boolean;
  /**
   * Returns a node at the given index.
   * @param theIndex node index within [1, `NbNodes()`] range
   * @returns 3D point coordinates
   */
  Node(theIndex: number): gp_Pnt;
  /**
   * Sets a node coordinates.
   * @param theIndex node index within [1, `NbNodes()`] range
   * @param thePnt 3D point coordinates
   */
  SetNode(theIndex: number, thePnt: gp_Pnt): void;
  /**
   * Returns UV-node at the given index.
   * @param theIndex node index within [1, `NbNodes()`] range
   * @returns 2D point defining UV coordinates
   */
  UVNode(theIndex: number): unknown;
  /**
   * Sets an UV-node coordinates.
   * @param theIndex node index within [1, `NbNodes()`] range
   * @param thePnt UV coordinates
   */
  SetUVNode(theIndex: number, thePnt: unknown): void;
  /**
   * Returns triangle at the given index.
   * @param theIndex triangle index within [1, `NbTriangles()`] range
   * @returns triangle node indices, with each node defined within [1, `NbNodes()`] range
   */
  Triangle(theIndex: number): unknown;
  /**
   * Sets a triangle.
   * @param theIndex triangle index within [1, `NbTriangles()`] range
   * @param theTriangle triangle node indices, with each node defined within [1, `NbNodes()`] range
   */
  SetTriangle(theIndex: number, theTriangle: unknown): void;
  /**
   * Returns normal at the given index.
   * @param theIndex node index within [1, `NbNodes()`] range
   * @returns normalized 3D vector defining a surface normal
   */
  Normal(theIndex: number): gp_Dir;
  /**
   * Changes normal at the given index.
   * @param theIndex node index within [1, `NbNodes()`] range
   * @param theNormal normalized 3D vector defining a surface normal
   */
  SetNormal(theIndex: number, theNormal: gp_Dir): void;
  /**
   * Returns mesh purpose bits.
   */
  MeshPurpose(): number;
  /**
   * Sets mesh purpose bits.
   */
  SetMeshPurpose(thePurpose: number): void;
  /**
   * Returns cached min - max range of triangulation data, which is VOID by default (e.g, no cached information).
   */
  CachedMinMax(): unknown;
  /**
   * Sets a cached min - max range of this triangulation. The bounding box should exactly match actual range of triangulation data without a gap or transformation, or otherwise undefined behavior will be observed. Passing a VOID range invalidates the cache.
   */
  SetCachedMinMax(theBox: unknown): void;
  /**
   * Returns TRUE if there is some cached min - max range of this triangulation.
   */
  HasCachedMinMax(): boolean;
  /**
   * Updates cached min - max range of this triangulation with bounding box of nodal data.
   */
  UpdateCachedMinMax(): void;
  /**
   * Extends the passed box with bounding box of this triangulation. Uses cached min - max range when available and:
   *
   * - input transformation theTrsf has no rotation part;
   * - theIsAccurate is set to FALSE;
   * - no triangulation data available (e.g. it is deferred and not loaded). out] theBox bounding box to extend by this triangulation
   * @param theTrsf optional transformation
   * @param theIsAccurate when FALSE, allows using a cached min - max range of this triangulation even for non-identity transformation.
   * @param theBox Mutated in place; read the updated value from this argument after the call.
   * @returns FALSE if there is no any data to extend the passed box (no both triangulation and cached min - max range).
   */
  MinMax(theBox: unknown, theTrsf: unknown, theIsAccurate: boolean): boolean;
  /**
   * Returns TRUE if node positions are defined with double precision; TRUE by default.
   */
  IsDoublePrecision(): boolean;
  /**
   * Set if node positions should be defined with double or single precision for 3D and UV nodes. Raises exception if data was already allocated.
   */
  SetDoublePrecision(theIsDouble: boolean): void;
  /**
   * Method resizing internal arrays of nodes (synchronously for all attributes).
   * @param theNbNodes new number of nodes
   * @param theToCopyOld copy old nodes into the new array
   */
  ResizeNodes(theNbNodes: number, theToCopyOld: boolean): void;
  /**
   * Method resizing an internal array of triangles.
   * @param theNbTriangles new number of triangles
   * @param theToCopyOld copy old triangles into the new array
   */
  ResizeTriangles(theNbTriangles: number, theToCopyOld: boolean): void;
  /**
   * If an array for UV coordinates is not allocated yet, do it now.
   */
  AddUVNodes(): void;
  /**
   * Deallocates the UV nodes array.
   */
  RemoveUVNodes(): void;
  /**
   * If an array for normals is not allocated yet, do it now.
   */
  AddNormals(): void;
  /**
   * Deallocates the normals array.
   */
  RemoveNormals(): void;
  /**
   * Compute smooth normals by averaging triangle normals.
   */
  ComputeNormals(): void;
  /**
   * Returns the table of 3D points for read-only access or NULL if nodes array is undefined. `Poly_Triangulation::Node()` should be used instead when possible. Returned object should not be used after {@link Poly_Triangulation | `Poly_Triangulation`} destruction.
   */
  MapNodeArray(): NCollection_HArray1_gp_Pnt;
  /**
   * Returns the triangle array for read-only access or NULL if triangle array is undefined. `Poly_Triangulation::Triangle()` should be used instead when possible. Returned object should not be used after {@link Poly_Triangulation | `Poly_Triangulation`} destruction.
   */
  MapTriangleArray(): NCollection_HArray1_Poly_Triangle;
  /**
   * Returns the table of 2D nodes for read-only access or NULL if UV nodes array is undefined. `Poly_Triangulation::UVNode()` should be used instead when possible. Returned object should not be used after {@link Poly_Triangulation | `Poly_Triangulation`} destruction.
   */
  MapUVNodeArray(): NCollection_HArray1_gp_Pnt2d;
  /**
   * Returns the table of per-vertex normals for read-only access or NULL if normals array is undefined. `Poly_Triangulation::Normal()` should be used instead when possible. Returned object should not be used after {@link Poly_Triangulation | `Poly_Triangulation`} destruction.
   */
  MapNormalArray(): NCollection_HArray1_float;
  /**
   * Returns an internal array of triangles. `Triangle()`/SetTriangle() should be used instead in portable code.
   */
  InternalTriangles(): NCollection_Array1_Poly_Triangle;
  /**
   * Returns an internal array of nodes. `Node()`/SetNode() should be used instead in portable code.
   */
  InternalNodes(): unknown;
  /**
   * Returns an internal array of UV nodes. UBNode()/SetUVNode() should be used instead in portable code.
   */
  InternalUVNodes(): unknown;
  /**
   * Return an internal array of normals. `Normal()`/SetNormal() should be used instead in portable code.
   */
  InternalNormals(): NCollection_Array1_NCollection_Vec3_float;
  /**
   * @deprecated
   */
  SetNormals(theNormals: NCollection_HArray1_float): void;
  /**
   * @deprecated
   */
  Triangles(): NCollection_Array1_Poly_Triangle;
  /**
   * @deprecated
   */
  ChangeTriangles(): NCollection_Array1_Poly_Triangle;
  /**
   * @deprecated
   */
  ChangeTriangle(theIndex: number): unknown;
  NbDeferredNodes(): number;
  NbDeferredTriangles(): number;
  HasDeferredData(): boolean;
  LoadDeferredData(theFileSystem: unknown): boolean;
  DetachedLoadDeferredData(theFileSystem: unknown): Poly_Triangulation;
  UnloadDeferredData(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_XY {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_XY);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_XY): NCollection_Array1_gp_XY;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_XY): NCollection_Array1_gp_XY;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_XY): NCollection_Array1_gp_XY;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_NCollection_List_TopoDS_Shape extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_NCollection_List_TopoDS_Shape);
  /**
   * Move constructor.
   */
  constructor(theInitList: NCollection_List_TopoDS_Shape[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: NCollection_List_TopoDS_Shape[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_NCollection_List_TopoDS_Shape): NCollection_List_NCollection_List_TopoDS_Shape;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): NCollection_List_TopoDS_Shape;
  /**
   * Last item.
   */
  Last(): NCollection_List_TopoDS_Shape;
  /**
   * Append one item at the end.
   */
  Append(theItem: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_NCollection_List_TopoDS_Shape): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_NamedUnit {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_BOPDS_PaveBlock extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_BOPDS_PaveBlock);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_BOPDS_PaveBlock): NCollection_List_handle_BOPDS_PaveBlock;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_BOPDS_PaveBlock): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_BOPDS_PaveBlock): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_BOPDS_PaveBlock): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Geom2d_Curve {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Geom2d_Curve);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Geom2d_Curve): NCollection_Array1_handle_Geom2d_Curve;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Geom2d_Curve): NCollection_Array1_handle_Geom2d_Curve;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Geom2d_Curve): NCollection_Array1_handle_Geom2d_Curve;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_StepFEA_ElementRepresentation {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_StepFEA_ElementRepresentation);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_StepFEA_ElementRepresentation): NCollection_Sequence_handle_StepFEA_ElementRepresentation;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_StepFEA_ElementRepresentation): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_StepFEA_ElementRepresentation): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_StepFEA_ElementRepresentation): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_StepFEA_ElementRepresentation): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_StepFEA_ElementRepresentation): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_gp_Pnt2d {
  constructor();
  constructor(theOther: NCollection_DynamicArray_gp_Pnt2d);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_gp_Pnt2d, theOwnAllocator: boolean): NCollection_DynamicArray_gp_Pnt2d;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_gp_Pnt2d_1 extends NCollection_DynamicArray_gp_Pnt2d {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_gp_Pnt2d_2 extends NCollection_DynamicArray_gp_Pnt2d {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_gp_Pnt2d_3 extends NCollection_DynamicArray_gp_Pnt2d {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_gp_Pnt2d_4 extends NCollection_DynamicArray_gp_Pnt2d {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_DerivedUnitElement {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeRef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_CoEdgeRef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_CoEdgeRef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_CoEdgeRef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_1 extends NCollection_DynamicArray_BRepGraphInc_CoEdgeRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_2 extends NCollection_DynamicArray_BRepGraphInc_CoEdgeRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_3 extends NCollection_DynamicArray_BRepGraphInc_CoEdgeRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_4 extends NCollection_DynamicArray_BRepGraphInc_CoEdgeRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepShape_ShapeDimensionRepresentationItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepShape_ShapeDimensionRepresentationItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepShape_ShapeDimensionRepresentationItem): NCollection_Array1_StepShape_ShapeDimensionRepresentationItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepShape_ShapeDimensionRepresentationItem): NCollection_Array1_StepShape_ShapeDimensionRepresentationItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepShape_ShapeDimensionRepresentationItem): NCollection_Array1_StepShape_ShapeDimensionRepresentationItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepShape_GeometricSetSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepShape_GeometricSetSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepShape_GeometricSetSelect): NCollection_Array1_StepShape_GeometricSetSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepShape_GeometricSetSelect): NCollection_Array1_StepShape_GeometricSetSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepShape_GeometricSetSelect): NCollection_Array1_StepShape_GeometricSetSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_Approval {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_CameraModelD3MultiClippingInterectionSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_RWGltf_GltfPrimArrayData {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_RWGltf_GltfPrimArrayData);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_RWGltf_GltfPrimArrayData): NCollection_Sequence_RWGltf_GltfPrimArrayData;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_RWGltf_GltfPrimArrayData): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_RWGltf_GltfPrimArrayData): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_RWGltf_GltfPrimArrayData): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_RWGltf_GltfPrimArrayData): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_RWGltf_GltfPrimArrayData): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESSolid_Shell {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESSolid_Shell);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESSolid_Shell): NCollection_Array1_handle_IGESSolid_Shell;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESSolid_Shell): NCollection_Array1_handle_IGESSolid_Shell;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESSolid_Shell): NCollection_Array1_handle_IGESSolid_Shell;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_gp_XYZ {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array2_gp_XYZ);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: unknown);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): NCollection_Array2_gp_XYZ;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): NCollection_Array2_gp_XYZ;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_DateAndTimeItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_DateAndTimeItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_DateAndTimeItem): NCollection_Array1_StepAP214_DateAndTimeItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_DateAndTimeItem): NCollection_Array1_StepAP214_DateAndTimeItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_DateAndTimeItem): NCollection_Array1_StepAP214_DateAndTimeItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepElement_CurveElementSectionDefinition {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepElement_CurveElementSectionDefinition);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepElement_CurveElementSectionDefinition): NCollection_Array1_handle_StepElement_CurveElementSectionDefinition;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepElement_CurveElementSectionDefinition): NCollection_Array1_handle_StepElement_CurveElementSectionDefinition;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepElement_CurveElementSectionDefinition): NCollection_Array1_handle_StepElement_CurveElementSectionDefinition;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_MathRoot_NullInterval {
  constructor();
  constructor(theOther: NCollection_DynamicArray_MathRoot_NullInterval);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_MathRoot_NullInterval, theOwnAllocator: boolean): NCollection_DynamicArray_MathRoot_NullInterval;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_MathRoot_NullInterval_1 extends NCollection_DynamicArray_MathRoot_NullInterval {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_MathRoot_NullInterval_2 extends NCollection_DynamicArray_MathRoot_NullInterval {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_MathRoot_NullInterval_3 extends NCollection_DynamicArray_MathRoot_NullInterval {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_MathRoot_NullInterval_4 extends NCollection_DynamicArray_MathRoot_NullInterval {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepGeom_CartesianPoint {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_PresentationStyleSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_PresentationStyleSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_PresentationStyleSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_PresentationStyleSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_TDF_Attribute {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_TDF_Attribute);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_TDF_Attribute): NCollection_Sequence_handle_TDF_Attribute;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_TDF_Attribute): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_TDF_Attribute): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_TDF_Attribute): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_TDF_Attribute): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_TDF_Attribute): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_handle_Standard_Transient {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_handle_Standard_Transient);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_handle_Standard_Transient): NCollection_Array2_handle_Standard_Transient;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_handle_Standard_Transient): NCollection_Array2_handle_Standard_Transient;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_handle_Standard_Transient): NCollection_Array2_handle_Standard_Transient;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESAppli_Node {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESAppli_Node);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESAppli_Node): NCollection_Array1_handle_IGESAppli_Node;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESAppli_Node): NCollection_Array1_handle_IGESAppli_Node;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESAppli_Node): NCollection_Array1_handle_IGESAppli_Node;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_BoxCharacteristicSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_BoxCharacteristicSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_BoxCharacteristicSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_BoxCharacteristicSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_BOPTools_ConnexityBlock extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_BOPTools_ConnexityBlock);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_BOPTools_ConnexityBlock): NCollection_List_BOPTools_ConnexityBlock;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_BOPTools_ConnexityBlock): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_BOPTools_ConnexityBlock): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_BOPTools_ConnexityBlock): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Single hashed Map. This Map is used to store and retrieve keys in linear time.
 *
 * The `Iterator` class can be used to explore the content of the map. It is not wise to iterate and modify a map in parallel.
 *
 * To compute the hashcode of the key the function ::HashCode must be defined in the global namespace
 *
 * To compare two keys the function `IsEqual` must be defined in the global namespace.
 *
 * The performance of a Map is conditioned by its number of buckets that should be kept greater to the number of keys. This map has an automatic management of the number of buckets. It is resized when the number of Keys becomes greater than the number of buckets.
 *
 * If you have a fair idea of the number of objects you can save on automatic resizing by giving a number of buckets at creation or using the ReSize method. This should be consider only for crucial optimisation issues.
 */
export declare class NCollection_Map_BOPDS_Pair {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Add.
   */
  Add(theKey: unknown): boolean;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing object.
   */
  Added(theKey: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey: unknown): boolean;
  /**
   * Checks if this map contains all keys of another map. This function checks if this map contains all keys of another map.
   * @deprecated
   */
  Contains(theOther: unknown): boolean;
  /**
   * Remove.
   */
  Remove(K: unknown): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Checks if two maps contain exactly the same keys. This function compares the keys of this map and another map and returns true if they contain exactly the same keys.
   * @deprecated
   */
  IsEqual(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of union (aka addition, fuse, merge, boolean OR) operation between two given Maps The new Map contains the values that are contained either in the first map or in the second map or in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be passed as one of operands.
   * @deprecated
   */
  Union(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the boolean operation union (aka addition, fuse, merge, boolean OR) with another (given) Map. The result contains the values that were previously contained in this map or contained in the given (operand) map. This algorithm is similar to method `Union()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Unite(theOther: unknown): boolean;
  /**
   * Returns true if this and theMap have common elements.
   * @deprecated
   */
  HasIntersection(theMap: unknown): boolean;
  /**
   * Sets this Map to be the result of intersection (aka multiplication, common, boolean AND) operation between two given Maps. The new Map contains only the values that are contained in both map operands. All previous content of this Map is cleared. This same map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Intersection(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the intersection operation (aka multiplication, common, boolean AND) with another (given) Map. The result contains only the values that are contained in both this and the given maps. This algorithm is similar to method `Intersection()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Intersect(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation between two given Maps. The new Map contains only the values that are contained in the first map operands and not contained in the second one. All previous content of this Map is cleared.
   * @deprecated
   */
  Subtraction(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation with another (given) Map. The result contains only the values that were previously contained in this map and not contained in this map. This algorithm is similar to method `Subtract()` with two operands. Returns True if contents of this map is changed.
   * @deprecated
   */
  Subtract(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of symmetric difference (aka exclusive disjunction, boolean XOR) operation between two given Maps. The new Map contains the values that are contained only in the first or the second operand maps but not in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Difference(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the symmetric difference (aka exclusive disjunction, boolean XOR) operation with another (given) Map. The result contains the values that are contained only in this or the operand map, but not in both. This algorithm is similar to method `Difference()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Differ(theOther: unknown): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_Map_BOPDS_Pair_2 extends NCollection_Map_BOPDS_Pair {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_Map_BOPDS_Pair_3 extends NCollection_Map_BOPDS_Pair {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_BOPDS_Pair_4 extends NCollection_Map_BOPDS_Pair {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_BOPDS_Pair_5 extends NCollection_Map_BOPDS_Pair {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_BOPDS_Pair_6 extends NCollection_Map_BOPDS_Pair {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_BOPDS_Pair_7 extends NCollection_Map_BOPDS_Pair {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_CompSolidDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_CompSolidDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_CompSolidDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_CompSolidDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_CompSolidDef_1 extends NCollection_DynamicArray_BRepGraphInc_CompSolidDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CompSolidDef_2 extends NCollection_DynamicArray_BRepGraphInc_CompSolidDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CompSolidDef_3 extends NCollection_DynamicArray_BRepGraphInc_CompSolidDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CompSolidDef_4 extends NCollection_DynamicArray_BRepGraphInc_CompSolidDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Poly_Triangulation {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Poly_Triangulation);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Poly_Triangulation): NCollection_Sequence_handle_Poly_Triangulation;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Poly_Triangulation): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Poly_Triangulation): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Poly_Triangulation): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Poly_Triangulation): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Poly_Triangulation): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_NCollection_Vec3_float {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_NCollection_Vec3_float);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_NCollection_Vec3_float): NCollection_Array1_NCollection_Vec3_float;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_NCollection_Vec3_float): NCollection_Array1_NCollection_Vec3_float;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_NCollection_Vec3_float): NCollection_Array1_NCollection_Vec3_float;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_PersonOrganizationItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_PersonOrganizationItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_PersonOrganizationItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_PersonOrganizationItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_DocumentReferenceItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_DocumentReferenceItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_DocumentReferenceItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_DocumentReferenceItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESData_IGESEntity {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESData_IGESEntity);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESData_IGESEntity): NCollection_Array1_handle_IGESData_IGESEntity;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESData_IGESEntity): NCollection_Array1_handle_IGESData_IGESEntity;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESData_IGESEntity): NCollection_Array1_handle_IGESData_IGESEntity;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntTools_CommonPrt {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_IntTools_CommonPrt);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_IntTools_CommonPrt): NCollection_Sequence_IntTools_CommonPrt;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_IntTools_CommonPrt): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_IntTools_CommonPrt): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_IntTools_CommonPrt): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_IntTools_CommonPrt): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_IntTools_CommonPrt): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_SpecifiedItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_SpecifiedItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_SpecifiedItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_SpecifiedItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepShape_ConnectedFaceSet {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepShape_ConnectedFaceSet);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepShape_ConnectedFaceSet): NCollection_Array1_handle_StepShape_ConnectedFaceSet;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepShape_ConnectedFaceSet): NCollection_Array1_handle_StepShape_ConnectedFaceSet;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepShape_ConnectedFaceSet): NCollection_Array1_handle_StepShape_ConnectedFaceSet;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_Plate_PinpointConstraint {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_Plate_PinpointConstraint);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_Plate_PinpointConstraint): NCollection_Array1_Plate_PinpointConstraint;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_Plate_PinpointConstraint): NCollection_Array1_Plate_PinpointConstraint;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_Plate_PinpointConstraint): NCollection_Array1_Plate_PinpointConstraint;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntPatch_Point {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: unknown): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: unknown): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: unknown): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: unknown): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepShape_FaceBound {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepShape_FaceBound);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepShape_FaceBound): NCollection_Array1_handle_StepShape_FaceBound;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepShape_FaceBound): NCollection_Array1_handle_StepShape_FaceBound;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepShape_FaceBound): NCollection_Array1_handle_StepShape_FaceBound;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_TDF_Delta extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_TDF_Delta);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_TDF_Delta): NCollection_List_handle_TDF_Delta;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_TDF_Delta): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_TDF_Delta): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_TDF_Delta): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepElement_SurfaceSection {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepShape_ConnectedFaceSet {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_int {
  constructor();
  constructor(theOther: NCollection_DynamicArray_int);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_int, theOwnAllocator: boolean): NCollection_DynamicArray_int;
  Append(theValue: number): number;
  InsertAfter(theIndex: number, theValue: number): number;
  InsertBefore(theIndex: number, theValue: number): number;
  EraseLast(): void;
  Appended(): number;
  Value(theIndex: number): number;
  First(): number;
  ChangeFirst(): number;
  Last(): number;
  ChangeLast(): number;
  ChangeValue(theIndex: number): number;
  SetValue(theIndex: number, theValue: number): number;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_int_1 extends NCollection_DynamicArray_int {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_int_2 extends NCollection_DynamicArray_int {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_int_3 extends NCollection_DynamicArray_int {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_int_4 extends NCollection_DynamicArray_int {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_StepElement_ElementMaterial {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESSolid_Loop {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESSolid_Loop);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESSolid_Loop): NCollection_Array1_handle_IGESSolid_Loop;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESSolid_Loop): NCollection_Array1_handle_IGESSolid_Loop;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESSolid_Loop): NCollection_Array1_handle_IGESSolid_Loop;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_handle_Standard_Transient {
  constructor();
  constructor(theOther: NCollection_DynamicArray_handle_Standard_Transient);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_handle_Standard_Transient, theOwnAllocator: boolean): NCollection_DynamicArray_handle_Standard_Transient;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_handle_Standard_Transient_1 extends NCollection_DynamicArray_handle_Standard_Transient {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_handle_Standard_Transient_2 extends NCollection_DynamicArray_handle_Standard_Transient {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_handle_Standard_Transient_3 extends NCollection_DynamicArray_handle_Standard_Transient {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_handle_Standard_Transient_4 extends NCollection_DynamicArray_handle_Standard_Transient {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESGeom_Boundary {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESGeom_Boundary);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESGeom_Boundary): NCollection_Array1_handle_IGESGeom_Boundary;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESGeom_Boundary): NCollection_Array1_handle_IGESGeom_Boundary;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESGeom_Boundary): NCollection_Array1_handle_IGESGeom_Boundary;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect): NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect): NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect): NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepVisual_TessellatedStructuredItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepVisual_TessellatedStructuredItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepVisual_TessellatedStructuredItem): NCollection_Array1_handle_StepVisual_TessellatedStructuredItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepVisual_TessellatedStructuredItem): NCollection_Array1_handle_StepVisual_TessellatedStructuredItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepVisual_TessellatedStructuredItem): NCollection_Array1_handle_StepVisual_TessellatedStructuredItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepElement_SurfaceSection {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepElement_SurfaceSection);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepElement_SurfaceSection): NCollection_Array1_handle_StepElement_SurfaceSection;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepElement_SurfaceSection): NCollection_Array1_handle_StepElement_SurfaceSection;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepElement_SurfaceSection): NCollection_Array1_handle_StepElement_SurfaceSection;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_ChFiDS_ElSpine extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_ChFiDS_ElSpine);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_ChFiDS_ElSpine): NCollection_List_handle_ChFiDS_ElSpine;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_ChFiDS_ElSpine): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_ChFiDS_ElSpine): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_ChFiDS_ElSpine): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntTools_PntOn2Faces {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_IntTools_PntOn2Faces);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_IntTools_PntOn2Faces): NCollection_Sequence_IntTools_PntOn2Faces;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_IntTools_PntOn2Faces): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_IntTools_PntOn2Faces): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_IntTools_PntOn2Faces): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_IntTools_PntOn2Faces): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_IntTools_PntOn2Faces): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_gp_Pnt2d {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_gp_Pnt2d);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_gp_Pnt2d;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_gp_Pnt2d;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_2 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_3 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_4 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_5 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_6 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_7 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_OrganizationItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_OrganizationItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_OrganizationItem): NCollection_Array1_StepAP214_OrganizationItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_OrganizationItem): NCollection_Array1_StepAP214_OrganizationItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_OrganizationItem): NCollection_Array1_StepAP214_OrganizationItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepDimTol_DatumReference {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepDimTol_DatumReference);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepDimTol_DatumReference): NCollection_Array1_handle_StepDimTol_DatumReference;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepDimTol_DatumReference): NCollection_Array1_handle_StepDimTol_DatumReference;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepDimTol_DatumReference): NCollection_Array1_handle_StepDimTol_DatumReference;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1..Extent. See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedMap_handle_BOPDS_PaveBlock {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(theExtent: number): void;
  /**
   * Add adds a new key to the map.
   * @param theKey1 key to add
   * @returns index of the key (new or existing)
   */
  Add(theKey1: unknown): number;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing key.
   * @param theKey1 key to add
   * @returns const reference to the key in the map
   */
  Added(theKey1: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): boolean;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedMap_handle_BOPDS_PaveBlock_2 extends NCollection_IndexedMap_handle_BOPDS_PaveBlock {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedMap_handle_BOPDS_PaveBlock_3 extends NCollection_IndexedMap_handle_BOPDS_PaveBlock {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_BOPDS_PaveBlock_4 extends NCollection_IndexedMap_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_BOPDS_PaveBlock_5 extends NCollection_IndexedMap_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_BOPDS_PaveBlock_6 extends NCollection_IndexedMap_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_BOPDS_PaveBlock_7 extends NCollection_IndexedMap_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepVisual_PresentationStyleAssignment {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_TDocStd_Document {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_TDocStd_Document);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_TDocStd_Document): NCollection_Sequence_handle_TDocStd_Document;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_TDocStd_Document): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_TDocStd_Document): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_TDocStd_Document): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_TDocStd_Document): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_TDocStd_Document): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_ChFiDS_CircSection {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_ChFiDS_CircSection);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_ChFiDS_CircSection;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_ChFiDS_CircSection;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_WorkItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_WorkItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_WorkItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_WorkItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_TopoDS_Shape {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_TopoDS_Shape);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: TopoDS_Shape, theLower: number, theUpper: number);
  constructor(theBegin: TopoDS_Shape, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: TopoDS_Shape): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_TopoDS_Shape): NCollection_Array1_TopoDS_Shape;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_TopoDS_Shape): NCollection_Array1_TopoDS_Shape;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_TopoDS_Shape): NCollection_Array1_TopoDS_Shape;
  /**
   * @returns first element
   */
  First(): TopoDS_Shape;
  /**
   * @returns first element
   */
  ChangeFirst(): TopoDS_Shape;
  /**
   * @returns last element
   */
  Last(): TopoDS_Shape;
  /**
   * @returns last element
   */
  ChangeLast(): TopoDS_Shape;
  /**
   * Constant value access.
   */
  Value(theIndex: number): TopoDS_Shape;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): TopoDS_Shape;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): TopoDS_Shape;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): TopoDS_Shape;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: TopoDS_Shape): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_IntTools_CurveRangeSample extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_IntTools_CurveRangeSample);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_IntTools_CurveRangeSample): NCollection_List_IntTools_CurveRangeSample;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_IntTools_CurveRangeSample): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_IntTools_CurveRangeSample): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_IntTools_CurveRangeSample): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_CoEdgeDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_CoEdgeDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_CoEdgeDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_1 extends NCollection_DynamicArray_BRepGraphInc_CoEdgeDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_2 extends NCollection_DynamicArray_BRepGraphInc_CoEdgeDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_3 extends NCollection_DynamicArray_BRepGraphInc_CoEdgeDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_4 extends NCollection_DynamicArray_BRepGraphInc_CoEdgeDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_TCollection_ExtendedString {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_TCollection_ExtendedString);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_TCollection_ExtendedString): NCollection_Sequence_TCollection_ExtendedString;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_TCollection_ExtendedString): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_TCollection_ExtendedString): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_TCollection_ExtendedString): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_TCollection_ExtendedString): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_TCollection_ExtendedString): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepShape_ValueQualifier {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepShape_ValueQualifier);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepShape_ValueQualifier;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepShape_ValueQualifier;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_TextOrCharacter {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_TextOrCharacter);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_TextOrCharacter;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_TextOrCharacter;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_bool {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: boolean);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: boolean, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_StartRequestItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_StartRequestItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_StartRequestItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_StartRequestItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_Law_Function extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_Law_Function);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_Law_Function): NCollection_List_handle_Law_Function;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_Law_Function): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_Law_Function): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_Law_Function): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESDimen_LeaderArrow {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_gp_Pnt2d {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array2_gp_Pnt2d);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: unknown);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): NCollection_Array2_gp_Pnt2d;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): NCollection_Array2_gp_Pnt2d;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_AnnotationPlaneElement {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_AnnotationPlaneElement);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_AnnotationPlaneElement;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_AnnotationPlaneElement;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_double extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_double);
  /**
   * Move constructor.
   */
  constructor(theInitList: number[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: number[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_double): NCollection_List_double;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): number;
  /**
   * Last item.
   */
  Last(): number;
  /**
   * Append one item at the end.
   */
  Append(theItem: number): number;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_double): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: number): number;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_double): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_double): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_LayeredItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_LayeredItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_LayeredItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_LayeredItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_BOPAlgo_CheckResult extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_BOPAlgo_CheckResult);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_BOPAlgo_CheckResult): NCollection_List_BOPAlgo_CheckResult;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_BOPAlgo_CheckResult): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_BOPAlgo_CheckResult): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_BOPAlgo_CheckResult): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_ClassifiedItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_ClassifiedItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_ClassifiedItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_ClassifiedItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESAppli_FiniteElement {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESAppli_FiniteElement);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESAppli_FiniteElement): NCollection_Array1_handle_IGESAppli_FiniteElement;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESAppli_FiniteElement): NCollection_Array1_handle_IGESAppli_FiniteElement;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESAppli_FiniteElement): NCollection_Array1_handle_IGESAppli_FiniteElement;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESSolid_VertexList {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_Geom_BoundedCurve {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_AnnotationPlaneElement {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_AnnotationPlaneElement);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_AnnotationPlaneElement): NCollection_Array1_StepVisual_AnnotationPlaneElement;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_AnnotationPlaneElement): NCollection_Array1_StepVisual_AnnotationPlaneElement;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_AnnotationPlaneElement): NCollection_Array1_StepVisual_AnnotationPlaneElement;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_Poly_Triangle {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_Poly_Triangle);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_Poly_Triangle;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_Poly_Triangle;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem): NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem): NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem): NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_StepFEA_ElementGeometricRelationship {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_gp_Pnt {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_gp_Pnt);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_gp_Pnt): NCollection_Sequence_gp_Pnt;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: gp_Pnt): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_gp_Pnt): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: gp_Pnt): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_gp_Pnt): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: gp_Pnt): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_gp_Pnt): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_gp_Pnt): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: gp_Pnt): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_gp_Pnt): void;
  /**
   * First item access.
   */
  First(): gp_Pnt;
  /**
   * First item access.
   */
  ChangeFirst(): gp_Pnt;
  /**
   * Last item access.
   */
  Last(): gp_Pnt;
  /**
   * Last item access.
   */
  ChangeLast(): gp_Pnt;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): gp_Pnt;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): gp_Pnt;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: gp_Pnt): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): gp_Pnt;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): gp_Pnt;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_ExternalIdentificationItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_ExternalIdentificationItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_ExternalIdentificationItem): NCollection_Array1_StepAP214_ExternalIdentificationItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_ExternalIdentificationItem): NCollection_Array1_StepAP214_ExternalIdentificationItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_ExternalIdentificationItem): NCollection_Array1_StepAP214_ExternalIdentificationItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_TopoDS_Shape {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_TopoDS_Shape);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: TopoDS_Shape, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_TopoDS_Shape): NCollection_Array2_TopoDS_Shape;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_TopoDS_Shape): NCollection_Array2_TopoDS_Shape;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_TopoDS_Shape): NCollection_Array2_TopoDS_Shape;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: TopoDS_Shape): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfFZ {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfFZ);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfFZ, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfFZ;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfFZ_1 extends NCollection_DynamicArray_BOPDS_InterfFZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfFZ_2 extends NCollection_DynamicArray_BOPDS_InterfFZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfFZ_3 extends NCollection_DynamicArray_BOPDS_InterfFZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfFZ_4 extends NCollection_DynamicArray_BOPDS_InterfFZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepShape_Face {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Standard_Transient {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Standard_Transient);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Standard_Transient): NCollection_Array1_handle_Standard_Transient;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Standard_Transient): NCollection_Array1_handle_Standard_Transient;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Standard_Transient): NCollection_Array1_handle_Standard_Transient;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_gp_XYZ {
  constructor();
  constructor(theOther: NCollection_DynamicArray_gp_XYZ);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_gp_XYZ, theOwnAllocator: boolean): NCollection_DynamicArray_gp_XYZ;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_gp_XYZ_1 extends NCollection_DynamicArray_gp_XYZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_gp_XYZ_2 extends NCollection_DynamicArray_gp_XYZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_gp_XYZ_3 extends NCollection_DynamicArray_gp_XYZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_gp_XYZ_4 extends NCollection_DynamicArray_gp_XYZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_gp_Pnt {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_gp_Pnt);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: gp_Pnt);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: gp_Pnt, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_gp_Pnt;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_gp_Pnt;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_IntTools_SurfaceRangeSample extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_IntTools_SurfaceRangeSample);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_IntTools_SurfaceRangeSample): NCollection_List_IntTools_SurfaceRangeSample;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_IntTools_SurfaceRangeSample): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_IntTools_SurfaceRangeSample): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_IntTools_SurfaceRangeSample): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_SolidDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_SolidDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_SolidDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_SolidDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_SolidDef_1 extends NCollection_DynamicArray_BRepGraphInc_SolidDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_SolidDef_2 extends NCollection_DynamicArray_BRepGraphInc_SolidDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_SolidDef_3 extends NCollection_DynamicArray_BRepGraphInc_SolidDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_SolidDef_4 extends NCollection_DynamicArray_BRepGraphInc_SolidDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_XCAFDimTolObjects_DimensionModif {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_XCAFDimTolObjects_DimensionModif);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_XCAFDimTolObjects_DimensionModif): NCollection_Sequence_XCAFDimTolObjects_DimensionModif;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_XCAFDimTolObjects_DimensionModif): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_XCAFDimTolObjects_DimensionModif): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_DimensionModif): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_DimensionModif): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_DimensionModif): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_TessellatedEdgeOrVertex {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_TessellatedEdgeOrVertex);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_TessellatedEdgeOrVertex;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_TessellatedEdgeOrVertex;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_handle_StepGeom_CartesianPoint {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_SecurityClassificationItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_SecurityClassificationItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_SecurityClassificationItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_SecurityClassificationItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepShape_GeometricSetSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepShape_GeometricSetSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepShape_GeometricSetSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepShape_GeometricSetSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_gp_Vec {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_gp_Vec);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_gp_Vec;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_gp_Vec;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_DocumentReferenceItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_DocumentReferenceItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_DocumentReferenceItem): NCollection_Array1_StepAP214_DocumentReferenceItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_DocumentReferenceItem): NCollection_Array1_StepAP214_DocumentReferenceItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_DocumentReferenceItem): NCollection_Array1_StepAP214_DocumentReferenceItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_ApprovedItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_ApprovedItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_ApprovedItem): NCollection_Array1_StepAP203_ApprovedItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_ApprovedItem): NCollection_Array1_StepAP203_ApprovedItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_ApprovedItem): NCollection_Array1_StepAP203_ApprovedItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Geom2d_BSplineCurve {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Geom2d_BSplineCurve);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Geom2d_BSplineCurve): NCollection_Array1_handle_Geom2d_BSplineCurve;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Geom2d_BSplineCurve): NCollection_Array1_handle_Geom2d_BSplineCurve;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Geom2d_BSplineCurve): NCollection_Array1_handle_Geom2d_BSplineCurve;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepFEA_CurveElementEndOffset {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepFEA_CurveElementEndOffset);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepFEA_CurveElementEndOffset): NCollection_Array1_handle_StepFEA_CurveElementEndOffset;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepFEA_CurveElementEndOffset): NCollection_Array1_handle_StepFEA_CurveElementEndOffset;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepFEA_CurveElementEndOffset): NCollection_Array1_handle_StepFEA_CurveElementEndOffset;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_STEPSelections_AssemblyLink {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_BOPDS_Pave extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_BOPDS_Pave);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_BOPDS_Pave): NCollection_List_BOPDS_Pave;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_BOPDS_Pave): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_BOPDS_Pave): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_BOPDS_Pave): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_unsignedchar {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_unsignedchar);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: string, theLower: number, theUpper: number);
  constructor(theBegin: string, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: string): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_unsignedchar): NCollection_Array1_unsignedchar;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_unsignedchar): NCollection_Array1_unsignedchar;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_unsignedchar): NCollection_Array1_unsignedchar;
  /**
   * @returns first element
   */
  First(): string;
  /**
   * @returns first element
   */
  ChangeFirst(): string;
  /**
   * @returns last element
   */
  Last(): string;
  /**
   * @returns last element
   */
  ChangeLast(): string;
  /**
   * Constant value access.
   */
  Value(theIndex: number): string;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): string;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): string;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): string;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: string): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_Lin {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_Lin);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_Lin): NCollection_Array1_gp_Lin;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_Lin): NCollection_Array1_gp_Lin;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_Lin): NCollection_Array1_gp_Lin;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_SolidRef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_SolidRef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_SolidRef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_SolidRef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_SolidRef_1 extends NCollection_DynamicArray_BRepGraphInc_SolidRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_SolidRef_2 extends NCollection_DynamicArray_BRepGraphInc_SolidRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_SolidRef_3 extends NCollection_DynamicArray_BRepGraphInc_SolidRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_SolidRef_4 extends NCollection_DynamicArray_BRepGraphInc_SolidRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESSolid_VertexList {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESSolid_VertexList);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESSolid_VertexList): NCollection_Array1_handle_IGESSolid_VertexList;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESSolid_VertexList): NCollection_Array1_handle_IGESSolid_VertexList;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESSolid_VertexList): NCollection_Array1_handle_IGESSolid_VertexList;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_CertifiedItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_CertifiedItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_CertifiedItem): NCollection_Array1_StepAP203_CertifiedItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_CertifiedItem): NCollection_Array1_StepAP203_CertifiedItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_CertifiedItem): NCollection_Array1_StepAP203_CertifiedItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Expr_GeneralExpression {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Expr_GeneralExpression);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Expr_GeneralExpression): NCollection_Sequence_handle_Expr_GeneralExpression;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Expr_GeneralExpression): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Expr_GeneralExpression): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_GeneralExpression): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_GeneralExpression): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_GeneralExpression): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepRepr_PropertyDefinitionRepresentation {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntSurf_PathPoint {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: unknown): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: unknown): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: unknown): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: unknown): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_MAT_Arc {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_MAT_Arc);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_MAT_Arc): NCollection_Sequence_handle_MAT_Arc;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_MAT_Arc): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_MAT_Arc): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_MAT_Arc): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_MAT_Arc): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_MAT_Arc): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepElement_CurveElementEndReleasePacket {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_AutoDesignGeneralOrgItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_FillStyleSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_FillStyleSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_FillStyleSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_FillStyleSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESSolid_Face {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESSolid_Face);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESSolid_Face): NCollection_Array1_handle_IGESSolid_Face;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESSolid_Face): NCollection_Array1_handle_IGESSolid_Face;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESSolid_Face): NCollection_Array1_handle_IGESSolid_Face;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_TDF_AttributeDelta extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_TDF_AttributeDelta);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_TDF_AttributeDelta): NCollection_List_handle_TDF_AttributeDelta;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_TDF_AttributeDelta): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_TDF_AttributeDelta): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_TDF_AttributeDelta): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_TCollection_ExtendedString {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: NCollection_Sequence_TCollection_ExtendedString);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): NCollection_Sequence_TCollection_ExtendedString;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): NCollection_Sequence_TCollection_ExtendedString;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: NCollection_Sequence_TCollection_ExtendedString): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_BRepOffset_Interval extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_BRepOffset_Interval);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_BRepOffset_Interval): NCollection_List_BRepOffset_Interval;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_BRepOffset_Interval): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_BRepOffset_Interval): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_BRepOffset_Interval): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESDimen_LeaderArrow {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESDimen_LeaderArrow);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESDimen_LeaderArrow): NCollection_Array1_handle_IGESDimen_LeaderArrow;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESDimen_LeaderArrow): NCollection_Array1_handle_IGESDimen_LeaderArrow;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESDimen_LeaderArrow): NCollection_Array1_handle_IGESDimen_LeaderArrow;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_double {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_double);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: number, theLower: number, theUpper: number);
  constructor(theBegin: number, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: number): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_double): NCollection_Array1_double;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_double): NCollection_Array1_double;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_double): NCollection_Array1_double;
  /**
   * @returns first element
   */
  First(): number;
  /**
   * @returns first element
   */
  ChangeFirst(): number;
  /**
   * @returns last element
   */
  Last(): number;
  /**
   * @returns last element
   */
  ChangeLast(): number;
  /**
   * Constant value access.
   */
  Value(theIndex: number): number;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): number;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): number;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): number;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: number): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: unknown, theItem: TopoDS_Shape): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: unknown, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: unknown, theItem: TopoDS_Shape): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: unknown, theItem: TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: unknown, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown, theItem: TopoDS_Shape): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): TopoDS_Shape;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): TopoDS_Shape;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: unknown): TopoDS_Shape;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: unknown): TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: unknown): TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_2 extends NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_3 extends NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_4 extends NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_5 extends NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_6 extends NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_7 extends NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepDimTol_DatumSystemOrReference {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepDimTol_DatumSystemOrReference);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepDimTol_DatumSystemOrReference): NCollection_Array1_StepDimTol_DatumSystemOrReference;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepDimTol_DatumSystemOrReference): NCollection_Array1_StepDimTol_DatumSystemOrReference;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepDimTol_DatumSystemOrReference): NCollection_Array1_StepDimTol_DatumSystemOrReference;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_HLRAlgo_PolyData {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_HLRAlgo_PolyData);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_HLRAlgo_PolyData): NCollection_Array1_handle_HLRAlgo_PolyData;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_HLRAlgo_PolyData): NCollection_Array1_handle_HLRAlgo_PolyData;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_HLRAlgo_PolyData): NCollection_Array1_handle_HLRAlgo_PolyData;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_IFSelect_Selection {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_IFSelect_Selection);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_IFSelect_Selection): NCollection_Sequence_handle_IFSelect_Selection;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_IFSelect_Selection): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_IFSelect_Selection): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_IFSelect_Selection): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_IFSelect_Selection): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_IFSelect_Selection): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_TCollection_ExtendedString {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_TCollection_ExtendedString);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_TCollection_ExtendedString): NCollection_Array1_TCollection_ExtendedString;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_TCollection_ExtendedString): NCollection_Array1_TCollection_ExtendedString;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_TCollection_ExtendedString): NCollection_Array1_TCollection_ExtendedString;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepShape_OrientedClosedShell {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_Plate_PinpointConstraint {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_Plate_PinpointConstraint);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_Plate_PinpointConstraint): NCollection_Sequence_Plate_PinpointConstraint;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_Plate_PinpointConstraint): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_Plate_PinpointConstraint): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_Plate_PinpointConstraint): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_Plate_PinpointConstraint): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_Plate_PinpointConstraint): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepDimTol_DatumSystemOrReference {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepDimTol_DatumSystemOrReference);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepDimTol_DatumSystemOrReference;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepDimTol_DatumSystemOrReference;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_gp_Vec2d {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_gp_Vec2d);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_gp_Vec2d;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_gp_Vec2d;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntTools_Root {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_IntTools_Root);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_IntTools_Root): NCollection_Sequence_IntTools_Root;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_IntTools_Root): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_IntTools_Root): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_IntTools_Root): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_IntTools_Root): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_IntTools_Root): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_TCollection_AsciiString extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_TCollection_AsciiString);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_TCollection_AsciiString): NCollection_List_TCollection_AsciiString;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_TCollection_AsciiString): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_TCollection_AsciiString): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_TCollection_AsciiString): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_1 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_2 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_3 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_4 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_Dir {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_Dir);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: gp_Dir, theLower: number, theUpper: number);
  constructor(theBegin: gp_Dir, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: gp_Dir): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_Dir): NCollection_Array1_gp_Dir;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_Dir): NCollection_Array1_gp_Dir;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_Dir): NCollection_Array1_gp_Dir;
  /**
   * @returns first element
   */
  First(): gp_Dir;
  /**
   * @returns first element
   */
  ChangeFirst(): gp_Dir;
  /**
   * @returns last element
   */
  Last(): gp_Dir;
  /**
   * @returns last element
   */
  ChangeLast(): gp_Dir;
  /**
   * Constant value access.
   */
  Value(theIndex: number): gp_Dir;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): gp_Dir;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): gp_Dir;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): gp_Dir;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: gp_Dir): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_ShapeAnalysis_FreeBoundData {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepMesh_Circle {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepMesh_Circle);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepMesh_Circle, theOwnAllocator: boolean): NCollection_DynamicArray_BRepMesh_Circle;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepMesh_Circle_1 extends NCollection_DynamicArray_BRepMesh_Circle {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepMesh_Circle_2 extends NCollection_DynamicArray_BRepMesh_Circle {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepMesh_Circle_3 extends NCollection_DynamicArray_BRepMesh_Circle {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepMesh_Circle_4 extends NCollection_DynamicArray_BRepMesh_Circle {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESGeom_TransformationMatrix {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESGeom_TransformationMatrix);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESGeom_TransformationMatrix): NCollection_Array1_handle_IGESGeom_TransformationMatrix;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESGeom_TransformationMatrix): NCollection_Array1_handle_IGESGeom_TransformationMatrix;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESGeom_TransformationMatrix): NCollection_Array1_handle_IGESGeom_TransformationMatrix;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_GroupItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_GroupItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_GroupItem): NCollection_Array1_StepAP214_GroupItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_GroupItem): NCollection_Array1_StepAP214_GroupItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_GroupItem): NCollection_Array1_StepAP214_GroupItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_AutoDesignReferencingItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignReferencingItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_AutoDesignReferencingItem): NCollection_Array1_StepAP214_AutoDesignReferencingItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_AutoDesignReferencingItem): NCollection_Array1_StepAP214_AutoDesignReferencingItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_AutoDesignReferencingItem): NCollection_Array1_StepAP214_AutoDesignReferencingItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_1 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_2 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_3 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_4 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_DateItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_DateItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_DateItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_DateItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_Geom_Curve {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1..Extent. See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedMap_Message_MetricType {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(theExtent: number): void;
  /**
   * Add adds a new key to the map.
   * @param theKey1 key to add
   * @returns index of the key (new or existing)
   */
  Add(theKey1: unknown): number;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing key.
   * @param theKey1 key to add
   * @returns const reference to the key in the map
   */
  Added(theKey1: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): boolean;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedMap_Message_MetricType_2 extends NCollection_IndexedMap_Message_MetricType {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedMap_Message_MetricType_3 extends NCollection_IndexedMap_Message_MetricType {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_Message_MetricType_4 extends NCollection_IndexedMap_Message_MetricType {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_Message_MetricType_5 extends NCollection_IndexedMap_Message_MetricType {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_Message_MetricType_6 extends NCollection_IndexedMap_Message_MetricType {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_Message_MetricType_7 extends NCollection_IndexedMap_Message_MetricType {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1..Extent. See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedMap_TCollection_AsciiString {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(theExtent: number): void;
  /**
   * Add adds a new key to the map.
   * @param theKey1 key to add
   * @returns index of the key (new or existing)
   */
  Add(theKey1: unknown): number;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing key.
   * @param theKey1 key to add
   * @returns const reference to the key in the map
   */
  Added(theKey1: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): boolean;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedMap_TCollection_AsciiString_2 extends NCollection_IndexedMap_TCollection_AsciiString {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedMap_TCollection_AsciiString_3 extends NCollection_IndexedMap_TCollection_AsciiString {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TCollection_AsciiString_4 extends NCollection_IndexedMap_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TCollection_AsciiString_5 extends NCollection_IndexedMap_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TCollection_AsciiString_6 extends NCollection_IndexedMap_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TCollection_AsciiString_7 extends NCollection_IndexedMap_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepRepr_RepresentationItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepRepr_RepresentationItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepRepr_RepresentationItem): NCollection_Array1_handle_StepRepr_RepresentationItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepRepr_RepresentationItem): NCollection_Array1_handle_StepRepr_RepresentationItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepRepr_RepresentationItem): NCollection_Array1_handle_StepRepr_RepresentationItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_handle_StepGeom_SurfacePatch {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: unknown);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_ShellRef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_ShellRef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_ShellRef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_ShellRef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_ShellRef_1 extends NCollection_DynamicArray_BRepGraphInc_ShellRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ShellRef_2 extends NCollection_DynamicArray_BRepGraphInc_ShellRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ShellRef_3 extends NCollection_DynamicArray_BRepGraphInc_ShellRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ShellRef_4 extends NCollection_DynamicArray_BRepGraphInc_ShellRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TDF_Label_int {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: number): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: number): number;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: number): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: number): number;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): number;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): number;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TDF_Label_int_2 extends NCollection_DataMap_TDF_Label_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TDF_Label_int_3 extends NCollection_DataMap_TDF_Label_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TDF_Label_int_4 extends NCollection_DataMap_TDF_Label_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TDF_Label_int_5 extends NCollection_DataMap_TDF_Label_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TDF_Label_int_6 extends NCollection_DataMap_TDF_Label_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TDF_Label_int_7 extends NCollection_DataMap_TDF_Label_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESGraph_Color {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESGraph_Color);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESGraph_Color): NCollection_Array1_handle_IGESGraph_Color;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESGraph_Color): NCollection_Array1_handle_IGESGraph_Color;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESGraph_Color): NCollection_Array1_handle_IGESGraph_Color;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepShape_Edge {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_MAT_BasicElt {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_MAT_BasicElt);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_MAT_BasicElt): NCollection_Sequence_handle_MAT_BasicElt;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_MAT_BasicElt): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_MAT_BasicElt): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_MAT_BasicElt): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_MAT_BasicElt): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_MAT_BasicElt): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_TCollection_AsciiString {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_TCollection_AsciiString);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_TCollection_AsciiString;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_TCollection_AsciiString;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_int_bool {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: number, theItem: boolean): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: number, theItem: boolean): boolean;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: number, theItem: boolean): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: number, theItem: boolean): boolean;
  /**
   * IsBound.
   */
  IsBound(theKey: number): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: number): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: number): boolean;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: number): boolean;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: number): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_int_bool_2 extends NCollection_DataMap_int_bool {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_int_bool_3 extends NCollection_DataMap_int_bool {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_bool_4 extends NCollection_DataMap_int_bool {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_bool_5 extends NCollection_DataMap_int_bool {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_bool_6 extends NCollection_DataMap_int_bool {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_bool_7 extends NCollection_DataMap_int_bool {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_IntSurf_PntOn2S extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_IntSurf_PntOn2S);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_IntSurf_PntOn2S): NCollection_List_IntSurf_PntOn2S;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_IntSurf_PntOn2S): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_IntSurf_PntOn2S): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_IntSurf_PntOn2S): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepShape_Shell {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepShape_Shell);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepShape_Shell;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepShape_Shell;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_2 extends NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_3 extends NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_4 extends NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_5 extends NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_6 extends NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_7 extends NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepGeom_PcurveOrSurface {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepGeom_PcurveOrSurface);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepGeom_PcurveOrSurface;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepGeom_PcurveOrSurface;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_Document {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_Document);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_Document): NCollection_Array1_handle_StepBasic_Document;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_Document): NCollection_Array1_handle_StepBasic_Document;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_Document): NCollection_Array1_handle_StepBasic_Document;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_BRepExtrema_SolutionElem {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_BRepExtrema_SolutionElem);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_BRepExtrema_SolutionElem): NCollection_Sequence_BRepExtrema_SolutionElem;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_BRepExtrema_SolutionElem): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_BRepExtrema_SolutionElem): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_BRepExtrema_SolutionElem): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_BRepExtrema_SolutionElem): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_BRepExtrema_SolutionElem): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Single hashed Map. This Map is used to store and retrieve keys in linear time.
 *
 * The `Iterator` class can be used to explore the content of the map. It is not wise to iterate and modify a map in parallel.
 *
 * To compute the hashcode of the key the function ::HashCode must be defined in the global namespace
 *
 * To compare two keys the function `IsEqual` must be defined in the global namespace.
 *
 * The performance of a Map is conditioned by its number of buckets that should be kept greater to the number of keys. This map has an automatic management of the number of buckets. It is resized when the number of Keys becomes greater than the number of buckets.
 *
 * If you have a fair idea of the number of objects you can save on automatic resizing by giving a number of buckets at creation or using the ReSize method. This should be consider only for crucial optimisation issues.
 */
export declare class NCollection_Map_handle_TNaming_NamedShape {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Add.
   */
  Add(theKey: unknown): boolean;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing object.
   */
  Added(theKey: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey: unknown): boolean;
  /**
   * Checks if this map contains all keys of another map. This function checks if this map contains all keys of another map.
   * @deprecated
   */
  Contains(theOther: unknown): boolean;
  /**
   * Remove.
   */
  Remove(K: unknown): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Checks if two maps contain exactly the same keys. This function compares the keys of this map and another map and returns true if they contain exactly the same keys.
   * @deprecated
   */
  IsEqual(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of union (aka addition, fuse, merge, boolean OR) operation between two given Maps The new Map contains the values that are contained either in the first map or in the second map or in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be passed as one of operands.
   * @deprecated
   */
  Union(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the boolean operation union (aka addition, fuse, merge, boolean OR) with another (given) Map. The result contains the values that were previously contained in this map or contained in the given (operand) map. This algorithm is similar to method `Union()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Unite(theOther: unknown): boolean;
  /**
   * Returns true if this and theMap have common elements.
   * @deprecated
   */
  HasIntersection(theMap: unknown): boolean;
  /**
   * Sets this Map to be the result of intersection (aka multiplication, common, boolean AND) operation between two given Maps. The new Map contains only the values that are contained in both map operands. All previous content of this Map is cleared. This same map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Intersection(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the intersection operation (aka multiplication, common, boolean AND) with another (given) Map. The result contains only the values that are contained in both this and the given maps. This algorithm is similar to method `Intersection()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Intersect(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation between two given Maps. The new Map contains only the values that are contained in the first map operands and not contained in the second one. All previous content of this Map is cleared.
   * @deprecated
   */
  Subtraction(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation with another (given) Map. The result contains only the values that were previously contained in this map and not contained in this map. This algorithm is similar to method `Subtract()` with two operands. Returns True if contents of this map is changed.
   * @deprecated
   */
  Subtract(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of symmetric difference (aka exclusive disjunction, boolean XOR) operation between two given Maps. The new Map contains the values that are contained only in the first or the second operand maps but not in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Difference(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the symmetric difference (aka exclusive disjunction, boolean XOR) operation with another (given) Map. The result contains the values that are contained only in this or the operand map, but not in both. This algorithm is similar to method `Difference()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Differ(theOther: unknown): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_Map_handle_TNaming_NamedShape_2 extends NCollection_Map_handle_TNaming_NamedShape {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_Map_handle_TNaming_NamedShape_3 extends NCollection_Map_handle_TNaming_NamedShape {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_TNaming_NamedShape_4 extends NCollection_Map_handle_TNaming_NamedShape {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_TNaming_NamedShape_5 extends NCollection_Map_handle_TNaming_NamedShape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_TNaming_NamedShape_6 extends NCollection_Map_handle_TNaming_NamedShape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_TNaming_NamedShape_7 extends NCollection_Map_handle_TNaming_NamedShape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepShape_OrientedEdge {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Standard_Transient {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Standard_Transient);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Standard_Transient): NCollection_Sequence_handle_Standard_Transient;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Standard_Transient): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Standard_Transient): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Standard_Transient): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Standard_Transient): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Standard_Transient): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_Bnd_Box {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_Bnd_Box);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_Bnd_Box;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_Bnd_Box;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_XYZ {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_XYZ);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_XYZ): NCollection_Array1_gp_XYZ;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_XYZ): NCollection_Array1_gp_XYZ;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_XYZ): NCollection_Array1_gp_XYZ;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_2 extends NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_3 extends NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_4 extends NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_5 extends NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_6 extends NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_7 extends NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_bool {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_bool);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: boolean, theLower: number, theUpper: number);
  constructor(theBegin: boolean, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: boolean): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_bool): NCollection_Array1_bool;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_bool): NCollection_Array1_bool;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_bool): NCollection_Array1_bool;
  /**
   * @returns first element
   */
  First(): boolean;
  /**
   * @returns first element
   */
  ChangeFirst(): boolean;
  /**
   * @returns last element
   */
  Last(): boolean;
  /**
   * @returns last element
   */
  ChangeLast(): boolean;
  /**
   * Constant value access.
   */
  Value(theIndex: number): boolean;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): boolean;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): boolean;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): boolean;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: boolean): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_AppParCurves_ConstraintCouple {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_AppParCurves_ConstraintCouple);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_AppParCurves_ConstraintCouple): NCollection_Array1_AppParCurves_ConstraintCouple;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_AppParCurves_ConstraintCouple): NCollection_Array1_AppParCurves_ConstraintCouple;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_AppParCurves_ConstraintCouple): NCollection_Array1_AppParCurves_ConstraintCouple;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_PresentedItemSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_PresentedItemSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_PresentedItemSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_PresentedItemSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Standard_Persistent {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Standard_Persistent);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Standard_Persistent): NCollection_Array1_handle_Standard_Persistent;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Standard_Persistent): NCollection_Array1_handle_Standard_Persistent;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Standard_Persistent): NCollection_Array1_handle_Standard_Persistent;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_Organization {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_Organization);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_Organization): NCollection_Array1_handle_StepBasic_Organization;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_Organization): NCollection_Array1_handle_StepBasic_Organization;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_Organization): NCollection_Array1_handle_StepBasic_Organization;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepGeom_CartesianPoint {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_int {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_int);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: number, theLower: number, theUpper: number);
  constructor(theBegin: number, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: number): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_int): NCollection_Array1_int;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_int): NCollection_Array1_int;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_int): NCollection_Array1_int;
  /**
   * @returns first element
   */
  First(): number;
  /**
   * @returns first element
   */
  ChangeFirst(): number;
  /**
   * @returns last element
   */
  Last(): number;
  /**
   * @returns last element
   */
  ChangeLast(): number;
  /**
   * Constant value access.
   */
  Value(theIndex: number): number;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): number;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): number;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): number;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: number): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect): NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect): NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect): NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_ShapeFix_WireSegment {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_ShapeFix_WireSegment);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_ShapeFix_WireSegment): NCollection_Sequence_ShapeFix_WireSegment;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_ShapeFix_WireSegment): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_ShapeFix_WireSegment): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_ShapeFix_WireSegment): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_ShapeFix_WireSegment): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_ShapeFix_WireSegment): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_StepElement_ElementMaterial {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_StepElement_ElementMaterial);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_StepElement_ElementMaterial): NCollection_Sequence_handle_StepElement_ElementMaterial;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_StepElement_ElementMaterial): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_StepElement_ElementMaterial): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_StepElement_ElementMaterial): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_StepElement_ElementMaterial): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_StepElement_ElementMaterial): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_2 extends NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_3 extends NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_4 extends NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_5 extends NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_6 extends NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_7 extends NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_handle_Adaptor3d_Surface {
  constructor();
  constructor(theOther: NCollection_DynamicArray_handle_Adaptor3d_Surface);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_handle_Adaptor3d_Surface, theOwnAllocator: boolean): NCollection_DynamicArray_handle_Adaptor3d_Surface;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_handle_Adaptor3d_Surface_1 extends NCollection_DynamicArray_handle_Adaptor3d_Surface {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_handle_Adaptor3d_Surface_2 extends NCollection_DynamicArray_handle_Adaptor3d_Surface {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_handle_Adaptor3d_Surface_3 extends NCollection_DynamicArray_handle_Adaptor3d_Surface {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_handle_Adaptor3d_Surface_4 extends NCollection_DynamicArray_handle_Adaptor3d_Surface {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher): NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: TopoDS_Shape, theItem: number): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: TopoDS_Shape, theItem: number): number;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: TopoDS_Shape, theItem: number): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: TopoDS_Shape, theItem: number): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: TopoDS_Shape, theItem: number): number;
  /**
   * Contains.
   */
  Contains(theKey1: TopoDS_Shape): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: TopoDS_Shape, theItem: number): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: TopoDS_Shape): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): TopoDS_Shape;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): number;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): number;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: TopoDS_Shape): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: TopoDS_Shape): number;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: TopoDS_Shape): number;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: TopoDS_Shape): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_2 extends NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_3 extends NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_4 extends NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_5 extends NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_6 extends NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_7 extends NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_TDF_Label {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_TDF_Label);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_TDF_Label;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_TDF_Label;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1..Extent. See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher): NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(theExtent: number): void;
  /**
   * Add adds a new key to the map.
   * @param theKey1 key to add
   * @returns index of the key (new or existing)
   */
  Add(theKey1: TopoDS_Shape): number;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing key.
   * @param theKey1 key to add
   * @returns const reference to the key in the map
   */
  Added(theKey1: TopoDS_Shape): TopoDS_Shape;
  /**
   * Contains.
   */
  Contains(theKey1: TopoDS_Shape): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: TopoDS_Shape): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: TopoDS_Shape): boolean;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): TopoDS_Shape;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: TopoDS_Shape): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_2 extends NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_3 extends NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_4 extends NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_5 extends NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_6 extends NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_7 extends NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_InvisibleItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_InvisibleItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_InvisibleItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_InvisibleItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_1 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_2 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_3 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_4 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_PersonAndOrganizationItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_PersonAndOrganizationItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_PersonAndOrganizationItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_PersonAndOrganizationItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_double {
  constructor();
  constructor(theOther: NCollection_DynamicArray_double);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_double, theOwnAllocator: boolean): NCollection_DynamicArray_double;
  Append(theValue: number): number;
  InsertAfter(theIndex: number, theValue: number): number;
  InsertBefore(theIndex: number, theValue: number): number;
  EraseLast(): void;
  Appended(): number;
  Value(theIndex: number): number;
  First(): number;
  ChangeFirst(): number;
  Last(): number;
  ChangeLast(): number;
  ChangeValue(theIndex: number): number;
  SetValue(theIndex: number, theValue: number): number;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_double_1 extends NCollection_DynamicArray_double {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_double_2 extends NCollection_DynamicArray_double {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_double_3 extends NCollection_DynamicArray_double {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_double_4 extends NCollection_DynamicArray_double {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepFEA_DegreeOfFreedom {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepFEA_DegreeOfFreedom);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepFEA_DegreeOfFreedom;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepFEA_DegreeOfFreedom;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_AsciiString_int {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: number): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: number): number;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: number): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: number): number;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): number;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): number;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_int_2 extends NCollection_DataMap_TCollection_AsciiString_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_int_3 extends NCollection_DataMap_TCollection_AsciiString_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_int_4 extends NCollection_DataMap_TCollection_AsciiString_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_int_5 extends NCollection_DataMap_TCollection_AsciiString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_int_6 extends NCollection_DataMap_TCollection_AsciiString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_int_7 extends NCollection_DataMap_TCollection_AsciiString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject): NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_float {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: number);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: number, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1..Extent. See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedMap_TDF_Label {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(theExtent: number): void;
  /**
   * Add adds a new key to the map.
   * @param theKey1 key to add
   * @returns index of the key (new or existing)
   */
  Add(theKey1: unknown): number;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing key.
   * @param theKey1 key to add
   * @returns const reference to the key in the map
   */
  Added(theKey1: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): boolean;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedMap_TDF_Label_2 extends NCollection_IndexedMap_TDF_Label {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedMap_TDF_Label_3 extends NCollection_IndexedMap_TDF_Label {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TDF_Label_4 extends NCollection_IndexedMap_TDF_Label {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TDF_Label_5 extends NCollection_IndexedMap_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TDF_Label_6 extends NCollection_IndexedMap_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_TDF_Label_7 extends NCollection_IndexedMap_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_ShellDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_ShellDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_ShellDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_ShellDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_ShellDef_1 extends NCollection_DynamicArray_BRepGraphInc_ShellDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ShellDef_2 extends NCollection_DynamicArray_BRepGraphInc_ShellDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ShellDef_3 extends NCollection_DynamicArray_BRepGraphInc_ShellDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ShellDef_4 extends NCollection_DynamicArray_BRepGraphInc_ShellDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepVisual_CurveStyleFontPattern {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepGeom_SurfaceBoundary {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepGeom_SurfaceBoundary);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepGeom_SurfaceBoundary): NCollection_Array1_StepGeom_SurfaceBoundary;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepGeom_SurfaceBoundary): NCollection_Array1_StepGeom_SurfaceBoundary;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepGeom_SurfaceBoundary): NCollection_Array1_StepGeom_SurfaceBoundary;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_handle_Geom_Surface {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_handle_Geom_Surface);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_handle_Geom_Surface): NCollection_Array2_handle_Geom_Surface;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_handle_Geom_Surface): NCollection_Array2_handle_Geom_Surface;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_handle_Geom_Surface): NCollection_Array2_handle_Geom_Surface;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepRepr_MaterialPropertyRepresentation {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_TNaming_NamedShape extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_TNaming_NamedShape);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_TNaming_NamedShape): NCollection_List_handle_TNaming_NamedShape;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_TNaming_NamedShape): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_TNaming_NamedShape): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_TNaming_NamedShape): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_Units_Quantity {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_AutoDesignDatedItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignDatedItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_AutoDesignDatedItem): NCollection_Array1_StepAP214_AutoDesignDatedItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_AutoDesignDatedItem): NCollection_Array1_StepAP214_AutoDesignDatedItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_AutoDesignDatedItem): NCollection_Array1_StepAP214_AutoDesignDatedItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_TCollection_AsciiString {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_TCollection_AsciiString);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_TCollection_AsciiString): NCollection_Sequence_TCollection_AsciiString;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_TCollection_AsciiString): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_TCollection_AsciiString): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_TCollection_AsciiString): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_TCollection_AsciiString): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_TCollection_AsciiString): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_TopoDS_Shape extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_TopoDS_Shape);
  /**
   * Move constructor.
   */
  constructor(theInitList: TopoDS_Shape[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: TopoDS_Shape[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): TopoDS_Shape;
  /**
   * Last item.
   */
  Last(): TopoDS_Shape;
  /**
   * Append one item at the end.
   */
  Append(theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_TopoDS_Shape): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_TopoDS_Shape): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_TopoDS_Shape): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: TopoDS_Shape): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): TopoDS_Shape;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_AppParCurves_MultiCurve {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_AppParCurves_MultiCurve);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_AppParCurves_MultiCurve): NCollection_Sequence_AppParCurves_MultiCurve;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_AppParCurves_MultiCurve): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_AppParCurves_MultiCurve): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_AppParCurves_MultiCurve): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_AppParCurves_MultiCurve): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_AppParCurves_MultiCurve): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_Geom2d_BoundedCurve {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepElement_VolumeElementPurposeMember {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_Standard_Transient {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_DateTimeItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_DateTimeItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_DateTimeItem): NCollection_Array1_StepAP203_DateTimeItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_DateTimeItem): NCollection_Array1_StepAP203_DateTimeItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_DateTimeItem): NCollection_Array1_StepAP203_DateTimeItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_Units_Unit {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepGeom_TrimmingSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepGeom_TrimmingSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepGeom_TrimmingSelect): NCollection_Array1_StepGeom_TrimmingSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepGeom_TrimmingSelect): NCollection_Array1_StepGeom_TrimmingSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepGeom_TrimmingSelect): NCollection_Array1_StepGeom_TrimmingSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_gp_Vec {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_gp_Vec);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_gp_Vec): NCollection_Array2_gp_Vec;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_gp_Vec): NCollection_Array2_gp_Vec;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_gp_Vec): NCollection_Array2_gp_Vec;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_ChFiDS_SurfData {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_ChFiDS_SurfData);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_ChFiDS_SurfData): NCollection_Sequence_handle_ChFiDS_SurfData;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_ChFiDS_SurfData): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_ChFiDS_SurfData): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_ChFiDS_SurfData): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_ChFiDS_SurfData): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_ChFiDS_SurfData): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher): NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: TopoDS_Shape, theItem: unknown): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: TopoDS_Shape): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: TopoDS_Shape, theItem: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: TopoDS_Shape): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): TopoDS_Shape;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): unknown;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: TopoDS_Shape): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: TopoDS_Shape): unknown;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_2 extends NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_3 extends NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_4 extends NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_5 extends NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_6 extends NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_7 extends NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESAppli_FiniteElement {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_Geom_BSplineCurve {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_FaceRef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_FaceRef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_FaceRef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_FaceRef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_FaceRef_1 extends NCollection_DynamicArray_BRepGraphInc_FaceRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_FaceRef_2 extends NCollection_DynamicArray_BRepGraphInc_FaceRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_FaceRef_3 extends NCollection_DynamicArray_BRepGraphInc_FaceRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_FaceRef_4 extends NCollection_DynamicArray_BRepGraphInc_FaceRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_int extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_int);
  /**
   * Move constructor.
   */
  constructor(theInitList: number[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: number[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_int): NCollection_List_int;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): number;
  /**
   * Last item.
   */
  Last(): number;
  /**
   * Append one item at the end.
   */
  Append(theItem: number): number;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_int): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: number): number;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_int): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_int): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Single hashed Map. This Map is used to store and retrieve keys in linear time.
 *
 * The `Iterator` class can be used to explore the content of the map. It is not wise to iterate and modify a map in parallel.
 *
 * To compute the hashcode of the key the function ::HashCode must be defined in the global namespace
 *
 * To compare two keys the function `IsEqual` must be defined in the global namespace.
 *
 * The performance of a Map is conditioned by its number of buckets that should be kept greater to the number of keys. This map has an automatic management of the number of buckets. It is resized when the number of Keys becomes greater than the number of buckets.
 *
 * If you have a fair idea of the number of objects you can save on automatic resizing by giving a number of buckets at creation or using the ReSize method. This should be consider only for crucial optimisation issues.
 */
export declare class NCollection_Map_int {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Add.
   */
  Add(theKey: number): boolean;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing object.
   */
  Added(theKey: number): number;
  /**
   * Contains.
   */
  Contains(theKey: number): boolean;
  /**
   * Checks if this map contains all keys of another map. This function checks if this map contains all keys of another map.
   * @deprecated
   */
  Contains(theOther: unknown): boolean;
  /**
   * Remove.
   */
  Remove(K: number): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Checks if two maps contain exactly the same keys. This function compares the keys of this map and another map and returns true if they contain exactly the same keys.
   * @deprecated
   */
  IsEqual(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of union (aka addition, fuse, merge, boolean OR) operation between two given Maps The new Map contains the values that are contained either in the first map or in the second map or in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be passed as one of operands.
   * @deprecated
   */
  Union(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the boolean operation union (aka addition, fuse, merge, boolean OR) with another (given) Map. The result contains the values that were previously contained in this map or contained in the given (operand) map. This algorithm is similar to method `Union()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Unite(theOther: unknown): boolean;
  /**
   * Returns true if this and theMap have common elements.
   * @deprecated
   */
  HasIntersection(theMap: unknown): boolean;
  /**
   * Sets this Map to be the result of intersection (aka multiplication, common, boolean AND) operation between two given Maps. The new Map contains only the values that are contained in both map operands. All previous content of this Map is cleared. This same map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Intersection(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the intersection operation (aka multiplication, common, boolean AND) with another (given) Map. The result contains only the values that are contained in both this and the given maps. This algorithm is similar to method `Intersection()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Intersect(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation between two given Maps. The new Map contains only the values that are contained in the first map operands and not contained in the second one. All previous content of this Map is cleared.
   * @deprecated
   */
  Subtraction(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation with another (given) Map. The result contains only the values that were previously contained in this map and not contained in this map. This algorithm is similar to method `Subtract()` with two operands. Returns True if contents of this map is changed.
   * @deprecated
   */
  Subtract(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of symmetric difference (aka exclusive disjunction, boolean XOR) operation between two given Maps. The new Map contains the values that are contained only in the first or the second operand maps but not in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Difference(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the symmetric difference (aka exclusive disjunction, boolean XOR) operation with another (given) Map. The result contains the values that are contained only in this or the operand map, but not in both. This algorithm is similar to method `Difference()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Differ(theOther: unknown): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_Map_int_2 extends NCollection_Map_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_Map_int_3 extends NCollection_Map_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_int_4 extends NCollection_Map_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_int_5 extends NCollection_Map_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_int_6 extends NCollection_Map_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_int_7 extends NCollection_Map_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfVF {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfVF);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfVF, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfVF;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfVF_1 extends NCollection_DynamicArray_BOPDS_InterfVF {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVF_2 extends NCollection_DynamicArray_BOPDS_InterfVF {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVF_3 extends NCollection_DynamicArray_BOPDS_InterfVF {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVF_4 extends NCollection_DynamicArray_BOPDS_InterfVF {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_Curve {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_Curve);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_Curve, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_Curve;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_Curve_1 extends NCollection_DynamicArray_BOPDS_Curve {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_Curve_2 extends NCollection_DynamicArray_BOPDS_Curve {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_Curve_3 extends NCollection_DynamicArray_BOPDS_Curve {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_Curve_4 extends NCollection_DynamicArray_BOPDS_Curve {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_gp_Pnt {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_gp_Pnt);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: gp_Pnt, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_gp_Pnt): NCollection_Array2_gp_Pnt;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_gp_Pnt): NCollection_Array2_gp_Pnt;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_gp_Pnt): NCollection_Array2_gp_Pnt;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: gp_Pnt): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_Transfer_Finder {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: TopoDS_Shape): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): TopoDS_Shape;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_2 extends NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_3 extends NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_4 extends NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_5 extends NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_6 extends NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_7 extends NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_Poly_Triangulation extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_Poly_Triangulation);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_Poly_Triangulation): NCollection_List_handle_Poly_Triangulation;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_Poly_Triangulation): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_Poly_Triangulation): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_Poly_Triangulation): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect): NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect): NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect): NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_AutoDesignDatedItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignDatedItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_AutoDesignDatedItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_AutoDesignDatedItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_SecurityClassificationItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_SecurityClassificationItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_SecurityClassificationItem): NCollection_Array1_StepAP214_SecurityClassificationItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_SecurityClassificationItem): NCollection_Array1_StepAP214_SecurityClassificationItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_SecurityClassificationItem): NCollection_Array1_StepAP214_SecurityClassificationItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: unknown, theItem: NCollection_List_int): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: unknown, theItem: NCollection_List_int): NCollection_List_int;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: unknown, theItem: NCollection_List_int): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: unknown, theItem: NCollection_List_int): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: unknown, theItem: NCollection_List_int): NCollection_List_int;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown, theItem: NCollection_List_int): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): NCollection_List_int;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): NCollection_List_int;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: unknown): NCollection_List_int;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: unknown): NCollection_List_int;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: unknown): NCollection_List_int;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_2 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_3 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_4 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_5 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_6 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_7 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepFEA_CurveElementInterval {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_int_int {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: number, theItem: number): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: number, theItem: number): number;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: number, theItem: number): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: number, theItem: number): number;
  /**
   * IsBound.
   */
  IsBound(theKey: number): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: number): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: number): number;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: number): number;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: number): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_int_int_2 extends NCollection_DataMap_int_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_int_int_3 extends NCollection_DataMap_int_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_int_4 extends NCollection_DataMap_int_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_int_5 extends NCollection_DataMap_int_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_int_6 extends NCollection_DataMap_int_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_int_7 extends NCollection_DataMap_int_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Geom2d_BezierCurve {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Geom2d_BezierCurve);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Geom2d_BezierCurve): NCollection_Array1_handle_Geom2d_BezierCurve;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Geom2d_BezierCurve): NCollection_Array1_handle_Geom2d_BezierCurve;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Geom2d_BezierCurve): NCollection_Array1_handle_Geom2d_BezierCurve;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_AppParCurves_MultiPoint {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_handle_StepGeom_SurfacePatch {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_handle_StepGeom_SurfacePatch);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_handle_StepGeom_SurfacePatch): NCollection_Array2_handle_StepGeom_SurfacePatch;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_handle_StepGeom_SurfacePatch): NCollection_Array2_handle_StepGeom_SurfacePatch;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_handle_StepGeom_SurfacePatch): NCollection_Array2_handle_StepGeom_SurfacePatch;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_STEPSelections_AssemblyLink {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_STEPSelections_AssemblyLink);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_STEPSelections_AssemblyLink): NCollection_Sequence_handle_STEPSelections_AssemblyLink;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_STEPSelections_AssemblyLink): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_STEPSelections_AssemblyLink): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_STEPSelections_AssemblyLink): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_STEPSelections_AssemblyLink): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_STEPSelections_AssemblyLink): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepFEA_CurveElementEndOffset {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Storage_Root {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Storage_Root);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Storage_Root): NCollection_Sequence_handle_Storage_Root;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Storage_Root): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Storage_Root): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Storage_Root): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Storage_Root): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Storage_Root): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_double {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: number): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_ExternalIdentificationItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_ExternalIdentificationItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_ExternalIdentificationItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_ExternalIdentificationItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_handle_StepGeom_CartesianPoint {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: unknown);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfZZ {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfZZ);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfZZ, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfZZ;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfZZ_1 extends NCollection_DynamicArray_BOPDS_InterfZZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfZZ_2 extends NCollection_DynamicArray_BOPDS_InterfZZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfZZ_3 extends NCollection_DynamicArray_BOPDS_InterfZZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfZZ_4 extends NCollection_DynamicArray_BOPDS_InterfZZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_HLRAlgo_PolyHidingData {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_HLRAlgo_PolyHidingData);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_HLRAlgo_PolyHidingData): NCollection_Array1_HLRAlgo_PolyHidingData;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_HLRAlgo_PolyHidingData): NCollection_Array1_HLRAlgo_PolyHidingData;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_HLRAlgo_PolyHidingData): NCollection_Array1_HLRAlgo_PolyHidingData;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_1 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_2 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_3 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_4 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_unsignedchar extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_unsignedchar);
  /**
   * Move constructor.
   */
  constructor(theInitList: string[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: string[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_unsignedchar): NCollection_List_unsignedchar;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): string;
  /**
   * Last item.
   */
  Last(): string;
  /**
   * Append one item at the end.
   */
  Append(theItem: string): string;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_unsignedchar): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: string): string;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_unsignedchar): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_unsignedchar): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_int {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: number): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_UncertaintyMeasureWithUnit {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepDimTol_DatumReferenceElement {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepDimTol_DatumReferenceElement);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepDimTol_DatumReferenceElement): NCollection_Array1_handle_StepDimTol_DatumReferenceElement;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepDimTol_DatumReferenceElement): NCollection_Array1_handle_StepDimTol_DatumReferenceElement;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepDimTol_DatumReferenceElement): NCollection_Array1_handle_StepDimTol_DatumReferenceElement;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif): NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepFEA_NodeRepresentation {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_StepFEA_ElementRepresentation {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_MAT2d_Connexion {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_MAT2d_Connexion);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_MAT2d_Connexion): NCollection_Sequence_handle_MAT2d_Connexion;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_MAT2d_Connexion): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_MAT2d_Connexion): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_MAT2d_Connexion): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_MAT2d_Connexion): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_MAT2d_Connexion): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_CompoundDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_CompoundDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_CompoundDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_CompoundDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_CompoundDef_1 extends NCollection_DynamicArray_BRepGraphInc_CompoundDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CompoundDef_2 extends NCollection_DynamicArray_BRepGraphInc_CompoundDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CompoundDef_3 extends NCollection_DynamicArray_BRepGraphInc_CompoundDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_CompoundDef_4 extends NCollection_DynamicArray_BRepGraphInc_CompoundDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_gp_Pnt2d {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_gp_Pnt2d);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_gp_Pnt2d): NCollection_Sequence_gp_Pnt2d;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_gp_Pnt2d): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_gp_Pnt2d): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_gp_Pnt2d): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_gp_Pnt2d): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_gp_Pnt2d): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_DraughtingCalloutElement {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_DraughtingCalloutElement);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_DraughtingCalloutElement): NCollection_Array1_StepVisual_DraughtingCalloutElement;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_DraughtingCalloutElement): NCollection_Array1_StepVisual_DraughtingCalloutElement;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_DraughtingCalloutElement): NCollection_Array1_StepVisual_DraughtingCalloutElement;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Geom2d_BoundedCurve {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Geom2d_BoundedCurve);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Geom2d_BoundedCurve): NCollection_Sequence_handle_Geom2d_BoundedCurve;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Geom2d_BoundedCurve): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Geom2d_BoundedCurve): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Geom2d_BoundedCurve): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Geom2d_BoundedCurve): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Geom2d_BoundedCurve): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepFEA_NodeRepresentation {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepFEA_NodeRepresentation);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepFEA_NodeRepresentation): NCollection_Array1_handle_StepFEA_NodeRepresentation;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepFEA_NodeRepresentation): NCollection_Array1_handle_StepFEA_NodeRepresentation;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepFEA_NodeRepresentation): NCollection_Array1_handle_StepFEA_NodeRepresentation;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_PresentedItemSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_PresentedItemSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_PresentedItemSelect): NCollection_Array1_StepAP214_PresentedItemSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_PresentedItemSelect): NCollection_Array1_StepAP214_PresentedItemSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_PresentedItemSelect): NCollection_Array1_StepAP214_PresentedItemSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_2 extends NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_3 extends NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_4 extends NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_5 extends NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_6 extends NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_7 extends NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_AppParCurves_ConstraintCouple {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_AppParCurves_ConstraintCouple);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_AppParCurves_ConstraintCouple;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_AppParCurves_ConstraintCouple;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_Extrema_POnCurv2d {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_Extrema_POnCurv2d);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_Extrema_POnCurv2d): NCollection_Sequence_Extrema_POnCurv2d;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_Extrema_POnCurv2d): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_Extrema_POnCurv2d): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnCurv2d): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnCurv2d): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnCurv2d): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_ApprovalItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_ApprovalItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_ApprovalItem): NCollection_Array1_StepAP214_ApprovalItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_ApprovalItem): NCollection_Array1_StepAP214_ApprovalItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_ApprovalItem): NCollection_Array1_StepAP214_ApprovalItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_OccurrenceDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_OccurrenceDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_OccurrenceDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_OccurrenceDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_1 extends NCollection_DynamicArray_BRepGraphInc_OccurrenceDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_2 extends NCollection_DynamicArray_BRepGraphInc_OccurrenceDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_3 extends NCollection_DynamicArray_BRepGraphInc_OccurrenceDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_4 extends NCollection_DynamicArray_BRepGraphInc_OccurrenceDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment): NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment): NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment): NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_double {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: number);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_NCollection_Sequence_int extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_NCollection_Sequence_int);
  /**
   * Move constructor.
   */
  constructor(theInitList: NCollection_Sequence_int[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: NCollection_Sequence_int[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_NCollection_Sequence_int): NCollection_List_NCollection_Sequence_int;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): NCollection_Sequence_int;
  /**
   * Last item.
   */
  Last(): NCollection_Sequence_int;
  /**
   * Append one item at the end.
   */
  Append(theItem: NCollection_Sequence_int): NCollection_Sequence_int;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_NCollection_Sequence_int): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: NCollection_Sequence_int): NCollection_Sequence_int;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_NCollection_Sequence_int): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_NCollection_Sequence_int): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_ContractedItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_ContractedItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_ContractedItem): NCollection_Array1_StepAP203_ContractedItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_ContractedItem): NCollection_Array1_StepAP203_ContractedItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_ContractedItem): NCollection_Array1_StepAP203_ContractedItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESGeom_TransformationMatrix {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData): NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_Organization {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Single hashed Map. This Map is used to store and retrieve keys in linear time.
 *
 * The `Iterator` class can be used to explore the content of the map. It is not wise to iterate and modify a map in parallel.
 *
 * To compute the hashcode of the key the function ::HashCode must be defined in the global namespace
 *
 * To compare two keys the function `IsEqual` must be defined in the global namespace.
 *
 * The performance of a Map is conditioned by its number of buckets that should be kept greater to the number of keys. This map has an automatic management of the number of buckets. It is resized when the number of Keys becomes greater than the number of buckets.
 *
 * If you have a fair idea of the number of objects you can save on automatic resizing by giving a number of buckets at creation or using the ReSize method. This should be consider only for crucial optimisation issues.
 */
export declare class NCollection_Map_TCollection_AsciiString {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Add.
   */
  Add(theKey: unknown): boolean;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing object.
   */
  Added(theKey: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey: unknown): boolean;
  /**
   * Checks if this map contains all keys of another map. This function checks if this map contains all keys of another map.
   * @deprecated
   */
  Contains(theOther: unknown): boolean;
  /**
   * Remove.
   */
  Remove(K: unknown): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Checks if two maps contain exactly the same keys. This function compares the keys of this map and another map and returns true if they contain exactly the same keys.
   * @deprecated
   */
  IsEqual(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of union (aka addition, fuse, merge, boolean OR) operation between two given Maps The new Map contains the values that are contained either in the first map or in the second map or in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be passed as one of operands.
   * @deprecated
   */
  Union(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the boolean operation union (aka addition, fuse, merge, boolean OR) with another (given) Map. The result contains the values that were previously contained in this map or contained in the given (operand) map. This algorithm is similar to method `Union()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Unite(theOther: unknown): boolean;
  /**
   * Returns true if this and theMap have common elements.
   * @deprecated
   */
  HasIntersection(theMap: unknown): boolean;
  /**
   * Sets this Map to be the result of intersection (aka multiplication, common, boolean AND) operation between two given Maps. The new Map contains only the values that are contained in both map operands. All previous content of this Map is cleared. This same map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Intersection(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the intersection operation (aka multiplication, common, boolean AND) with another (given) Map. The result contains only the values that are contained in both this and the given maps. This algorithm is similar to method `Intersection()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Intersect(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation between two given Maps. The new Map contains only the values that are contained in the first map operands and not contained in the second one. All previous content of this Map is cleared.
   * @deprecated
   */
  Subtraction(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation with another (given) Map. The result contains only the values that were previously contained in this map and not contained in this map. This algorithm is similar to method `Subtract()` with two operands. Returns True if contents of this map is changed.
   * @deprecated
   */
  Subtract(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of symmetric difference (aka exclusive disjunction, boolean XOR) operation between two given Maps. The new Map contains the values that are contained only in the first or the second operand maps but not in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Difference(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the symmetric difference (aka exclusive disjunction, boolean XOR) operation with another (given) Map. The result contains the values that are contained only in this or the operand map, but not in both. This algorithm is similar to method `Difference()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Differ(theOther: unknown): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_Map_TCollection_AsciiString_2 extends NCollection_Map_TCollection_AsciiString {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_Map_TCollection_AsciiString_3 extends NCollection_Map_TCollection_AsciiString {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TCollection_AsciiString_4 extends NCollection_Map_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TCollection_AsciiString_5 extends NCollection_Map_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TCollection_AsciiString_6 extends NCollection_Map_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TCollection_AsciiString_7 extends NCollection_Map_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfEF {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfEF);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfEF, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfEF;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfEF_1 extends NCollection_DynamicArray_BOPDS_InterfEF {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEF_2 extends NCollection_DynamicArray_BOPDS_InterfEF {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEF_3 extends NCollection_DynamicArray_BOPDS_InterfEF {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEF_4 extends NCollection_DynamicArray_BOPDS_InterfEF {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_BRepCheck_Status extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_BRepCheck_Status);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_BRepCheck_Status): NCollection_List_BRepCheck_Status;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_BRepCheck_Status): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_BRepCheck_Status): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_BRepCheck_Status): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_int {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_int);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: number, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_int): NCollection_Array2_int;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_int): NCollection_Array2_int;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_int): NCollection_Array2_int;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: number): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_Person {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_Product {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_Product);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_Product): NCollection_Array1_handle_StepBasic_Product;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_Product): NCollection_Array1_handle_StepBasic_Product;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_Product): NCollection_Array1_handle_StepBasic_Product;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_1 extends NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_2 extends NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_3 extends NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_4 extends NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_2 extends NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_3 extends NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_4 extends NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_5 extends NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_6 extends NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_7 extends NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_FaceDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_FaceDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_FaceDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_FaceDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_FaceDef_1 extends NCollection_DynamicArray_BRepGraphInc_FaceDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_FaceDef_2 extends NCollection_DynamicArray_BRepGraphInc_FaceDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_FaceDef_3 extends NCollection_DynamicArray_BRepGraphInc_FaceDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_FaceDef_4 extends NCollection_DynamicArray_BRepGraphInc_FaceDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepShape_OrientedClosedShell {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepShape_OrientedClosedShell);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepShape_OrientedClosedShell): NCollection_Array1_handle_StepShape_OrientedClosedShell;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepShape_OrientedClosedShell): NCollection_Array1_handle_StepShape_OrientedClosedShell;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepShape_OrientedClosedShell): NCollection_Array1_handle_StepShape_OrientedClosedShell;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepShape_OrientedEdge {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepShape_OrientedEdge);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepShape_OrientedEdge): NCollection_Array1_handle_StepShape_OrientedEdge;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepShape_OrientedEdge): NCollection_Array1_handle_StepShape_OrientedEdge;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepShape_OrientedEdge): NCollection_Array1_handle_StepShape_OrientedEdge;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_ChFiDS_CircSection {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_ChFiDS_CircSection);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_ChFiDS_CircSection): NCollection_Array1_ChFiDS_CircSection;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_ChFiDS_CircSection): NCollection_Array1_ChFiDS_CircSection;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_ChFiDS_CircSection): NCollection_Array1_ChFiDS_CircSection;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntTools_Curve {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_IntTools_Curve);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_IntTools_Curve): NCollection_Sequence_IntTools_Curve;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_IntTools_Curve): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_IntTools_Curve): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_IntTools_Curve): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_IntTools_Curve): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_IntTools_Curve): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepShape_Shell {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepShape_Shell);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepShape_Shell): NCollection_Array1_StepShape_Shell;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepShape_Shell): NCollection_Array1_StepShape_Shell;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepShape_Shell): NCollection_Array1_StepShape_Shell;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_WorkItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_WorkItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_WorkItem): NCollection_Array1_StepAP203_WorkItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_WorkItem): NCollection_Array1_StepAP203_WorkItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_WorkItem): NCollection_Array1_StepAP203_WorkItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_TCollection_ExtendedString {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_TCollection_ExtendedString);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_TCollection_ExtendedString;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_TCollection_ExtendedString;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_int_NCollection_PackedMap_int {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: number, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: number, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: number, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: number, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: number): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: number): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: number): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: number): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: number): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_int_NCollection_PackedMap_int_2 extends NCollection_DataMap_int_NCollection_PackedMap_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_int_NCollection_PackedMap_int_3 extends NCollection_DataMap_int_NCollection_PackedMap_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_NCollection_PackedMap_int_4 extends NCollection_DataMap_int_NCollection_PackedMap_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_NCollection_PackedMap_int_5 extends NCollection_DataMap_int_NCollection_PackedMap_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_NCollection_PackedMap_int_6 extends NCollection_DataMap_int_NCollection_PackedMap_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_NCollection_PackedMap_int_7 extends NCollection_DataMap_int_NCollection_PackedMap_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepMesh_Vertex {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepMesh_Vertex);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepMesh_Vertex, theOwnAllocator: boolean): NCollection_DynamicArray_BRepMesh_Vertex;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepMesh_Vertex_1 extends NCollection_DynamicArray_BRepMesh_Vertex {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepMesh_Vertex_2 extends NCollection_DynamicArray_BRepMesh_Vertex {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepMesh_Vertex_3 extends NCollection_DynamicArray_BRepMesh_Vertex {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepMesh_Vertex_4 extends NCollection_DynamicArray_BRepMesh_Vertex {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher): NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Contains.
   */
  Contains(theKey1: TopoDS_Shape): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: TopoDS_Shape, theItem: NCollection_List_TopoDS_Shape): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: TopoDS_Shape): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): TopoDS_Shape;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): NCollection_List_TopoDS_Shape;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): NCollection_List_TopoDS_Shape;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: TopoDS_Shape): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_2 extends NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_3 extends NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_4 extends NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_5 extends NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_6 extends NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_7 extends NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_HLRAlgo_TriangleData {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_HLRAlgo_TriangleData);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_HLRAlgo_TriangleData): NCollection_Array1_HLRAlgo_TriangleData;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_HLRAlgo_TriangleData): NCollection_Array1_HLRAlgo_TriangleData;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_HLRAlgo_TriangleData): NCollection_Array1_HLRAlgo_TriangleData;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_DirectionCountSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_DirectionCountSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_DirectionCountSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_DirectionCountSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DoubleMap is used to bind pairs (Key1,Key2) and retrieve them in linear time.
 *
 * See Map from NCollection for a discussion about the number of buckets
 */
export declare class NCollection_DoubleMap_int_TDF_Label {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds the pair (Key1, Key2).
   */
  Bind(theKey1: number, theKey2: unknown): void;
  /**
   * TryBind binds the pair (Key1, Key2) only if neither key is already bound.
   * @param theKey1 first key to bind
   * @param theKey2 second key to bind
   * @returns true if pair was successfully bound, false if either key already exists
   */
  TryBind(theKey1: number, theKey2: unknown): boolean;
  /**
   * - AreBound
   */
  AreBound(theKey1: number, theKey2: unknown): boolean;
  /**
   * IsBound1.
   */
  IsBound1(theKey1: number): boolean;
  /**
   * IsBound2.
   */
  IsBound2(theKey2: unknown): boolean;
  /**
   * UnBind1.
   */
  UnBind1(theKey1: number): boolean;
  /**
   * UnBind2.
   */
  UnBind2(theKey2: unknown): boolean;
  /**
   * Find the Key1 and return pointer to Key2 or NULL if Key1 is not bound.
   * @param theKey1 Key1 to find
   * @returns pointer to Key2 or NULL if Key1 is not found
   */
  Seek1(theKey1: number): unknown;
  /**
   * Find the Key2 and return pointer to Key1 or NULL if not bound.
   * @param theKey2 Key2 to find
   * @returns pointer to Key1 if Key2 has been found
   */
  Seek2(theKey2: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DoubleMap_int_TDF_Label_2 extends NCollection_DoubleMap_int_TDF_Label {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DoubleMap_int_TDF_Label_3 extends NCollection_DoubleMap_int_TDF_Label {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1..Extent. See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedMap_handle_Standard_Transient {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(theExtent: number): void;
  /**
   * Add adds a new key to the map.
   * @param theKey1 key to add
   * @returns index of the key (new or existing)
   */
  Add(theKey1: unknown): number;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing key.
   * @param theKey1 key to add
   * @returns const reference to the key in the map
   */
  Added(theKey1: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): boolean;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedMap_handle_Standard_Transient_2 extends NCollection_IndexedMap_handle_Standard_Transient {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedMap_handle_Standard_Transient_3 extends NCollection_IndexedMap_handle_Standard_Transient {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_Standard_Transient_4 extends NCollection_IndexedMap_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_Standard_Transient_5 extends NCollection_IndexedMap_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_Standard_Transient_6 extends NCollection_IndexedMap_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_Standard_Transient_7 extends NCollection_IndexedMap_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepFEA_CurveElementEndRelease {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepFEA_CurveElementEndRelease);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepFEA_CurveElementEndRelease): NCollection_Array1_handle_StepFEA_CurveElementEndRelease;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepFEA_CurveElementEndRelease): NCollection_Array1_handle_StepFEA_CurveElementEndRelease;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepFEA_CurveElementEndRelease): NCollection_Array1_handle_StepFEA_CurveElementEndRelease;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_ApprovalItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_ApprovalItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_ApprovalItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_ApprovalItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_handle_Standard_Transient {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: unknown);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_GroupItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_GroupItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_GroupItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_GroupItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_TopoDS_Face {
  constructor();
  constructor(theOther: NCollection_DynamicArray_TopoDS_Face);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_TopoDS_Face, theOwnAllocator: boolean): NCollection_DynamicArray_TopoDS_Face;
  Append(theValue: TopoDS_Face): TopoDS_Face;
  InsertAfter(theIndex: number, theValue: TopoDS_Face): TopoDS_Face;
  InsertBefore(theIndex: number, theValue: TopoDS_Face): TopoDS_Face;
  EraseLast(): void;
  Appended(): TopoDS_Face;
  Value(theIndex: number): TopoDS_Face;
  First(): TopoDS_Face;
  ChangeFirst(): TopoDS_Face;
  Last(): TopoDS_Face;
  ChangeLast(): TopoDS_Face;
  ChangeValue(theIndex: number): TopoDS_Face;
  SetValue(theIndex: number, theValue: TopoDS_Face): TopoDS_Face;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_TopoDS_Face_1 extends NCollection_DynamicArray_TopoDS_Face {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_TopoDS_Face_2 extends NCollection_DynamicArray_TopoDS_Face {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_TopoDS_Face_3 extends NCollection_DynamicArray_TopoDS_Face {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_TopoDS_Face_4 extends NCollection_DynamicArray_TopoDS_Face {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESDraw_ConnectPoint {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_DateAndTimeItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_DateAndTimeItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_DateAndTimeItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_DateAndTimeItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_TopoDS_Shape {
  constructor();
  constructor(theOther: NCollection_DynamicArray_TopoDS_Shape);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_TopoDS_Shape, theOwnAllocator: boolean): NCollection_DynamicArray_TopoDS_Shape;
  Append(theValue: TopoDS_Shape): TopoDS_Shape;
  InsertAfter(theIndex: number, theValue: TopoDS_Shape): TopoDS_Shape;
  InsertBefore(theIndex: number, theValue: TopoDS_Shape): TopoDS_Shape;
  EraseLast(): void;
  Appended(): TopoDS_Shape;
  Value(theIndex: number): TopoDS_Shape;
  First(): TopoDS_Shape;
  ChangeFirst(): TopoDS_Shape;
  Last(): TopoDS_Shape;
  ChangeLast(): TopoDS_Shape;
  ChangeValue(theIndex: number): TopoDS_Shape;
  SetValue(theIndex: number, theValue: TopoDS_Shape): TopoDS_Shape;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_TopoDS_Shape_1 extends NCollection_DynamicArray_TopoDS_Shape {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_TopoDS_Shape_2 extends NCollection_DynamicArray_TopoDS_Shape {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_TopoDS_Shape_3 extends NCollection_DynamicArray_TopoDS_Shape {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_TopoDS_Shape_4 extends NCollection_DynamicArray_TopoDS_Shape {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_ClassifiedItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_ClassifiedItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_ClassifiedItem): NCollection_Array1_StepAP203_ClassifiedItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_ClassifiedItem): NCollection_Array1_StepAP203_ClassifiedItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_ClassifiedItem): NCollection_Array1_StepAP203_ClassifiedItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepGeom_TrimmingSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepGeom_TrimmingSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepGeom_TrimmingSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepGeom_TrimmingSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Expr_NamedUnknown {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Expr_NamedUnknown);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Expr_NamedUnknown): NCollection_Array1_handle_Expr_NamedUnknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Expr_NamedUnknown): NCollection_Array1_handle_Expr_NamedUnknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Expr_NamedUnknown): NCollection_Array1_handle_Expr_NamedUnknown;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem): NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem): NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem): NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_IntPatch_Line {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_IntPatch_Line);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_IntPatch_Line): NCollection_Sequence_handle_IntPatch_Line;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_IntPatch_Line): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_IntPatch_Line): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_IntPatch_Line): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_IntPatch_Line): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_IntPatch_Line): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Geom2d_Curve {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Geom2d_Curve);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Geom2d_Curve): NCollection_Sequence_handle_Geom2d_Curve;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Geom2d_Curve): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Geom2d_Curve): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Geom2d_Curve): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Geom2d_Curve): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Geom2d_Curve): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfVV {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfVV);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfVV, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfVV;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfVV_1 extends NCollection_DynamicArray_BOPDS_InterfVV {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVV_2 extends NCollection_DynamicArray_BOPDS_InterfVV {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVV_3 extends NCollection_DynamicArray_BOPDS_InterfVV {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVV_4 extends NCollection_DynamicArray_BOPDS_InterfVV {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_TCollection_HAsciiString {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_TCollection_HAsciiString);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_TCollection_HAsciiString): NCollection_Sequence_handle_TCollection_HAsciiString;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_TCollection_HAsciiString): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_TCollection_HAsciiString): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_TCollection_HAsciiString): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_TCollection_HAsciiString): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_TCollection_HAsciiString): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_Approval {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_Approval);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_Approval): NCollection_Array1_handle_StepBasic_Approval;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_Approval): NCollection_Array1_handle_StepBasic_Approval;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_Approval): NCollection_Array1_handle_StepBasic_Approval;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Expr_NamedFunction {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Expr_NamedFunction);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Expr_NamedFunction): NCollection_Sequence_handle_Expr_NamedFunction;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Expr_NamedFunction): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Expr_NamedFunction): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_NamedFunction): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_NamedFunction): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_NamedFunction): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_FillStyleSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_FillStyleSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_FillStyleSelect): NCollection_Array1_StepVisual_FillStyleSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_FillStyleSelect): NCollection_Array1_StepVisual_FillStyleSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_FillStyleSelect): NCollection_Array1_StepVisual_FillStyleSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_2 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_3 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_4 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_5 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_6 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_7 extends NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_2 extends NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_3 extends NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_4 extends NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_5 extends NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_6 extends NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_7 extends NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_PCDM_Reference {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_PCDM_Reference);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_PCDM_Reference): NCollection_Sequence_PCDM_Reference;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_PCDM_Reference): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_PCDM_Reference): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_PCDM_Reference): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_PCDM_Reference): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_PCDM_Reference): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_PersonAndOrganizationItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_PersonAndOrganizationItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_PersonAndOrganizationItem): NCollection_Array1_StepAP214_PersonAndOrganizationItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_PersonAndOrganizationItem): NCollection_Array1_StepAP214_PersonAndOrganizationItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_PersonAndOrganizationItem): NCollection_Array1_StepAP214_PersonAndOrganizationItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_TCollection_HAsciiString {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_AutoDesignGroupedItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignGroupedItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_AutoDesignGroupedItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_AutoDesignGroupedItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Geom_Curve {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Geom_Curve);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Geom_Curve): NCollection_Array1_handle_Geom_Curve;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Geom_Curve): NCollection_Array1_handle_Geom_Curve;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Geom_Curve): NCollection_Array1_handle_Geom_Curve;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepDimTol_ToleranceZoneTarget {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepDimTol_ToleranceZoneTarget);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepDimTol_ToleranceZoneTarget;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepDimTol_ToleranceZoneTarget;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1..Extent. See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedMap_handle_TDF_Attribute {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(theExtent: number): void;
  /**
   * Add adds a new key to the map.
   * @param theKey1 key to add
   * @returns index of the key (new or existing)
   */
  Add(theKey1: unknown): number;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing key.
   * @param theKey1 key to add
   * @returns const reference to the key in the map
   */
  Added(theKey1: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): boolean;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedMap_handle_TDF_Attribute_2 extends NCollection_IndexedMap_handle_TDF_Attribute {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedMap_handle_TDF_Attribute_3 extends NCollection_IndexedMap_handle_TDF_Attribute {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_TDF_Attribute_4 extends NCollection_IndexedMap_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_TDF_Attribute_5 extends NCollection_IndexedMap_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_TDF_Attribute_6 extends NCollection_IndexedMap_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_handle_TDF_Attribute_7 extends NCollection_IndexedMap_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfFF {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfFF);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfFF, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfFF;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfFF_1 extends NCollection_DynamicArray_BOPDS_InterfFF {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfFF_2 extends NCollection_DynamicArray_BOPDS_InterfFF {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfFF_3 extends NCollection_DynamicArray_BOPDS_InterfFF {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfFF_4 extends NCollection_DynamicArray_BOPDS_InterfFF {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepDimTol_GeometricToleranceModifier {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepDimTol_GeometricToleranceModifier);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepDimTol_GeometricToleranceModifier): NCollection_Array1_StepDimTol_GeometricToleranceModifier;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepDimTol_GeometricToleranceModifier): NCollection_Array1_StepDimTol_GeometricToleranceModifier;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepDimTol_GeometricToleranceModifier): NCollection_Array1_StepDimTol_GeometricToleranceModifier;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESData_LineFontEntity {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESData_ViewKindEntity {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESData_ViewKindEntity);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESData_ViewKindEntity): NCollection_Array1_handle_IGESData_ViewKindEntity;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESData_ViewKindEntity): NCollection_Array1_handle_IGESData_ViewKindEntity;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESData_ViewKindEntity): NCollection_Array1_handle_IGESData_ViewKindEntity;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_1 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_2 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_3 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_4 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_HLRAlgo_PolyHidingData {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_HLRAlgo_PolyHidingData);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_HLRAlgo_PolyHidingData;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_HLRAlgo_PolyHidingData;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_Quantity_Color {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_Quantity_Color);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_Quantity_Color): NCollection_Array1_Quantity_Color;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_Quantity_Color): NCollection_Array1_Quantity_Color;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_Quantity_Color): NCollection_Array1_Quantity_Color;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_Person {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_Person);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_Person): NCollection_Array1_handle_StepBasic_Person;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_Person): NCollection_Array1_handle_StepBasic_Person;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_Person): NCollection_Array1_handle_StepBasic_Person;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_gp_Pnt2d extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_gp_Pnt2d);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_gp_Pnt2d): NCollection_List_gp_Pnt2d;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_gp_Pnt2d): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_gp_Pnt2d): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_gp_Pnt2d): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_TessellatedEdgeOrVertex {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_TessellatedEdgeOrVertex);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_TessellatedEdgeOrVertex): NCollection_Array1_StepVisual_TessellatedEdgeOrVertex;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_TessellatedEdgeOrVertex): NCollection_Array1_StepVisual_TessellatedEdgeOrVertex;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_TessellatedEdgeOrVertex): NCollection_Array1_StepVisual_TessellatedEdgeOrVertex;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_2 extends NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_3 extends NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_4 extends NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_5 extends NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_6 extends NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_7 extends NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_1 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_2 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_3 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_4 extends NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_Geom2d_BSplineCurve {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_TCollection_AsciiString_int {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: unknown, theItem: number): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: unknown, theItem: number): number;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: unknown, theItem: number): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: unknown, theItem: number): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: unknown, theItem: number): number;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown, theItem: number): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): number;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): number;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: unknown): number;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: unknown): number;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_int_2 extends NCollection_IndexedDataMap_TCollection_AsciiString_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_int_3 extends NCollection_IndexedDataMap_TCollection_AsciiString_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_int_4 extends NCollection_IndexedDataMap_TCollection_AsciiString_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_int_5 extends NCollection_IndexedDataMap_TCollection_AsciiString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_int_6 extends NCollection_IndexedDataMap_TCollection_AsciiString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_int_7 extends NCollection_IndexedDataMap_TCollection_AsciiString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Single hashed Map. This Map is used to store and retrieve keys in linear time.
 *
 * The `Iterator` class can be used to explore the content of the map. It is not wise to iterate and modify a map in parallel.
 *
 * To compute the hashcode of the key the function ::HashCode must be defined in the global namespace
 *
 * To compare two keys the function `IsEqual` must be defined in the global namespace.
 *
 * The performance of a Map is conditioned by its number of buckets that should be kept greater to the number of keys. This map has an automatic management of the number of buckets. It is resized when the number of Keys becomes greater than the number of buckets.
 *
 * If you have a fair idea of the number of objects you can save on automatic resizing by giving a number of buckets at creation or using the ReSize method. This should be consider only for crucial optimisation issues.
 */
export declare class NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Add.
   */
  Add(theKey: TopoDS_Shape): boolean;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing object.
   */
  Added(theKey: TopoDS_Shape): TopoDS_Shape;
  /**
   * Contains.
   */
  Contains(theKey: TopoDS_Shape): boolean;
  /**
   * Checks if this map contains all keys of another map. This function checks if this map contains all keys of another map.
   * @deprecated
   */
  Contains(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): boolean;
  /**
   * Remove.
   */
  Remove(K: TopoDS_Shape): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Checks if two maps contain exactly the same keys. This function compares the keys of this map and another map and returns true if they contain exactly the same keys.
   * @deprecated
   */
  IsEqual(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): boolean;
  /**
   * Sets this Map to be the result of union (aka addition, fuse, merge, boolean OR) operation between two given Maps The new Map contains the values that are contained either in the first map or in the second map or in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be passed as one of operands.
   * @deprecated
   */
  Union(theLeft: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher, theRight: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Apply to this Map the boolean operation union (aka addition, fuse, merge, boolean OR) with another (given) Map. The result contains the values that were previously contained in this map or contained in the given (operand) map. This algorithm is similar to method `Union()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Unite(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): boolean;
  /**
   * Returns true if this and theMap have common elements.
   * @deprecated
   */
  HasIntersection(theMap: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): boolean;
  /**
   * Sets this Map to be the result of intersection (aka multiplication, common, boolean AND) operation between two given Maps. The new Map contains only the values that are contained in both map operands. All previous content of this Map is cleared. This same map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Intersection(theLeft: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher, theRight: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Apply to this Map the intersection operation (aka multiplication, common, boolean AND) with another (given) Map. The result contains only the values that are contained in both this and the given maps. This algorithm is similar to method `Intersection()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Intersect(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): boolean;
  /**
   * Sets this Map to be the result of subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation between two given Maps. The new Map contains only the values that are contained in the first map operands and not contained in the second one. All previous content of this Map is cleared.
   * @deprecated
   */
  Subtraction(theLeft: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher, theRight: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Apply to this Map the subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation with another (given) Map. The result contains only the values that were previously contained in this map and not contained in this map. This algorithm is similar to method `Subtract()` with two operands. Returns True if contents of this map is changed.
   * @deprecated
   */
  Subtract(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): boolean;
  /**
   * Sets this Map to be the result of symmetric difference (aka exclusive disjunction, boolean XOR) operation between two given Maps. The new Map contains the values that are contained only in the first or the second operand maps but not in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Difference(theLeft: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher, theRight: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Apply to this Map the symmetric difference (aka exclusive disjunction, boolean XOR) operation with another (given) Map. The result contains the values that are contained only in this or the operand map, but not in both. This algorithm is similar to method `Difference()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Differ(theOther: NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_2 extends NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_3 extends NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_4 extends NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_5 extends NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_6 extends NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_7 extends NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESDimen_GeneralNote {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESDimen_GeneralNote);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESDimen_GeneralNote): NCollection_Array1_handle_IGESDimen_GeneralNote;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESDimen_GeneralNote): NCollection_Array1_handle_IGESDimen_GeneralNote;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESDimen_GeneralNote): NCollection_Array1_handle_IGESDimen_GeneralNote;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_RenderingPropertiesSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_RenderingPropertiesSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_RenderingPropertiesSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_RenderingPropertiesSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_ExtendedString_int {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: number): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: number): number;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: number): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: number): number;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): number;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): number;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_int_2 extends NCollection_DataMap_TCollection_ExtendedString_int {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_int_3 extends NCollection_DataMap_TCollection_ExtendedString_int {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_int_4 extends NCollection_DataMap_TCollection_ExtendedString_int {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_int_5 extends NCollection_DataMap_TCollection_ExtendedString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_int_6 extends NCollection_DataMap_TCollection_ExtendedString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_int_7 extends NCollection_DataMap_TCollection_ExtendedString_int {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_HLRAlgo_BiPoint extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: unknown): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: unknown): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_gp_XYZ {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_gp_XYZ);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_gp_XYZ): NCollection_Array2_gp_XYZ;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_gp_XYZ): NCollection_Array2_gp_XYZ;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_gp_XYZ): NCollection_Array2_gp_XYZ;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_HLRAlgo_TriangleData {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_HLRAlgo_TriangleData);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_HLRAlgo_TriangleData;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_HLRAlgo_TriangleData;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_HLRAlgo_Interference extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_HLRAlgo_Interference);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_HLRAlgo_Interference): NCollection_List_HLRAlgo_Interference;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_HLRAlgo_Interference): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_HLRAlgo_Interference): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_HLRAlgo_Interference): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepShape_ShapeDimensionRepresentationItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepShape_ShapeDimensionRepresentationItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepShape_ShapeDimensionRepresentationItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepShape_ShapeDimensionRepresentationItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_2 extends NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_3 extends NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_4 extends NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_5 extends NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_6 extends NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_7 extends NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_StyleContextSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_StyleContextSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_StyleContextSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_StyleContextSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_AutoDesignReferencingItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignReferencingItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_AutoDesignReferencingItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_AutoDesignReferencingItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember): NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember): NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember): NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_int {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_int);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_int): NCollection_Sequence_int;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: number): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_int): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: number): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_int): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: number): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_int): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_int): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: number): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_int): void;
  /**
   * First item access.
   */
  First(): number;
  /**
   * First item access.
   */
  ChangeFirst(): number;
  /**
   * Last item access.
   */
  Last(): number;
  /**
   * Last item access.
   */
  ChangeLast(): number;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): number;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): number;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: number): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): number;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): number;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Geom_BoundedCurve {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Geom_BoundedCurve);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Geom_BoundedCurve): NCollection_Sequence_handle_Geom_BoundedCurve;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Geom_BoundedCurve): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Geom_BoundedCurve): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Geom_BoundedCurve): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Geom_BoundedCurve): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Geom_BoundedCurve): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_1 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_2 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_3 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_4 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntSurf_InteriorPoint {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: unknown): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: unknown): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: unknown): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: unknown): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: NCollection_List_double): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: NCollection_List_double): NCollection_List_double;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: NCollection_List_double): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: NCollection_List_double): NCollection_List_double;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): NCollection_List_double;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): NCollection_List_double;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): NCollection_List_double;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepShape_Edge {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepShape_Edge);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepShape_Edge): NCollection_Array1_handle_StepShape_Edge;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepShape_Edge): NCollection_Array1_handle_StepShape_Edge;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepShape_Edge): NCollection_Array1_handle_StepShape_Edge;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_Trsf {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_Trsf);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_Trsf): NCollection_Array1_gp_Trsf;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_Trsf): NCollection_Array1_gp_Trsf;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_Trsf): NCollection_Array1_gp_Trsf;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_handle_NCollection_HArray1_int {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_handle_NCollection_HArray1_int);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_handle_NCollection_HArray1_int): NCollection_Array2_handle_NCollection_HArray1_int;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_handle_NCollection_HArray1_int): NCollection_Array2_handle_NCollection_HArray1_int;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_handle_NCollection_HArray1_int): NCollection_Array2_handle_NCollection_HArray1_int;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_DirectionCountSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_DirectionCountSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_DirectionCountSelect): NCollection_Array1_StepVisual_DirectionCountSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_DirectionCountSelect): NCollection_Array1_StepVisual_DirectionCountSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_DirectionCountSelect): NCollection_Array1_StepVisual_DirectionCountSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Geom_BezierCurve {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Geom_BezierCurve);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Geom_BezierCurve): NCollection_Array1_handle_Geom_BezierCurve;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Geom_BezierCurve): NCollection_Array1_handle_Geom_BezierCurve;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Geom_BezierCurve): NCollection_Array1_handle_Geom_BezierCurve;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_gp_XY {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_gp_XY);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_gp_XY): NCollection_Sequence_gp_XY;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_gp_XY): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_gp_XY): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_gp_XY): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_gp_XY): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_gp_XY): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_gp_Pnt extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_gp_Pnt);
  /**
   * Move constructor.
   */
  constructor(theInitList: gp_Pnt[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: gp_Pnt[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_gp_Pnt): NCollection_List_gp_Pnt;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): gp_Pnt;
  /**
   * Last item.
   */
  Last(): gp_Pnt;
  /**
   * Append one item at the end.
   */
  Append(theItem: gp_Pnt): gp_Pnt;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_gp_Pnt): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: gp_Pnt): gp_Pnt;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_gp_Pnt): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_gp_Pnt): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_VertexRef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_VertexRef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_VertexRef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_VertexRef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_VertexRef_1 extends NCollection_DynamicArray_BRepGraphInc_VertexRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_VertexRef_2 extends NCollection_DynamicArray_BRepGraphInc_VertexRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_VertexRef_3 extends NCollection_DynamicArray_BRepGraphInc_VertexRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_VertexRef_4 extends NCollection_DynamicArray_BRepGraphInc_VertexRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_Point {
  constructor();
  constructor(theOther: unknown);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: unknown, theOwnAllocator: boolean): unknown;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_Point_1 extends NCollection_DynamicArray_BOPDS_Point {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_Point_2 extends NCollection_DynamicArray_BOPDS_Point {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_Point_3 extends NCollection_DynamicArray_BOPDS_Point {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_Point_4 extends NCollection_DynamicArray_BOPDS_Point {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation): NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation): NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation): NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_OrganizationItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_OrganizationItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_OrganizationItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_OrganizationItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_DateTimeItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_DateTimeItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_DateTimeItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_DateTimeItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_float {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_float);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: number, theLower: number, theUpper: number);
  constructor(theBegin: number, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: number): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_float): NCollection_Array1_float;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_float): NCollection_Array1_float;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_float): NCollection_Array1_float;
  /**
   * @returns first element
   */
  First(): number;
  /**
   * @returns first element
   */
  ChangeFirst(): number;
  /**
   * @returns last element
   */
  Last(): number;
  /**
   * @returns last element
   */
  ChangeLast(): number;
  /**
   * Constant value access.
   */
  Value(theIndex: number): number;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): number;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): number;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): number;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: number): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_double {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_double);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_double): NCollection_Sequence_double;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: number): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_double): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: number): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_double): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: number): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_double): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_double): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: number): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_double): void;
  /**
   * First item access.
   */
  First(): number;
  /**
   * First item access.
   */
  ChangeFirst(): number;
  /**
   * Last item access.
   */
  Last(): number;
  /**
   * Last item access.
   */
  ChangeLast(): number;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): number;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): number;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: number): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): number;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): number;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_Bnd_Box {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_Bnd_Box);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_Bnd_Box): NCollection_Array1_Bnd_Box;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_Bnd_Box): NCollection_Array1_Bnd_Box;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_Bnd_Box): NCollection_Array1_Bnd_Box;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_1 extends NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_2 extends NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_3 extends NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_4 extends NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepDimTol_DatumReferenceCompartment {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_TextOrCharacter {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_TextOrCharacter);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_TextOrCharacter): NCollection_Array1_StepVisual_TextOrCharacter;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_TextOrCharacter): NCollection_Array1_StepVisual_TextOrCharacter;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_TextOrCharacter): NCollection_Array1_StepVisual_TextOrCharacter;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_StartRequestItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_StartRequestItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_StartRequestItem): NCollection_Array1_StepAP203_StartRequestItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_StartRequestItem): NCollection_Array1_StepAP203_StartRequestItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_StartRequestItem): NCollection_Array1_StepAP203_StartRequestItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_Standard_Transient {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepElement_MeasureOrUnspecifiedValue {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepElement_MeasureOrUnspecifiedValue);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepElement_MeasureOrUnspecifiedValue): NCollection_Array1_StepElement_MeasureOrUnspecifiedValue;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepElement_MeasureOrUnspecifiedValue): NCollection_Array1_StepElement_MeasureOrUnspecifiedValue;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepElement_MeasureOrUnspecifiedValue): NCollection_Array1_StepElement_MeasureOrUnspecifiedValue;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntRes2d_IntersectionPoint {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: unknown): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: unknown): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: unknown): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: unknown): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_TCollection_ExtendedString extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_TCollection_ExtendedString);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_TCollection_ExtendedString): NCollection_List_TCollection_ExtendedString;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_TCollection_ExtendedString): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_TCollection_ExtendedString): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_TCollection_ExtendedString): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_SurfaceStyleElementSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_SurfaceStyleElementSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_SurfaceStyleElementSelect): NCollection_Array1_StepVisual_SurfaceStyleElementSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_SurfaceStyleElementSelect): NCollection_Array1_StepVisual_SurfaceStyleElementSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_SurfaceStyleElementSelect): NCollection_Array1_StepVisual_SurfaceStyleElementSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_double {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: number);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: number, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepShape_FaceBound {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_BRep_PointRepresentation extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: unknown): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: unknown): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepFEA_ElementRepresentation {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfVE {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfVE);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfVE, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfVE;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfVE_1 extends NCollection_DynamicArray_BOPDS_InterfVE {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVE_2 extends NCollection_DynamicArray_BOPDS_InterfVE {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVE_3 extends NCollection_DynamicArray_BOPDS_InterfVE {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVE_4 extends NCollection_DynamicArray_BOPDS_InterfVE {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_Extrema_POnSurf {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_Extrema_POnSurf);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_Extrema_POnSurf): NCollection_Sequence_Extrema_POnSurf;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_Extrema_POnSurf): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_Extrema_POnSurf): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnSurf): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnSurf): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnSurf): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_TCollection_AsciiString {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_TCollection_AsciiString);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_TCollection_AsciiString): NCollection_Array1_TCollection_AsciiString;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_TCollection_AsciiString): NCollection_Array1_TCollection_AsciiString;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_TCollection_AsciiString): NCollection_Array1_TCollection_AsciiString;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_PersonOrganizationItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_PersonOrganizationItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_PersonOrganizationItem): NCollection_Array1_StepAP203_PersonOrganizationItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_PersonOrganizationItem): NCollection_Array1_StepAP203_PersonOrganizationItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_PersonOrganizationItem): NCollection_Array1_StepAP203_PersonOrganizationItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_AutoDesignGroupedItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignGroupedItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_AutoDesignGroupedItem): NCollection_Array1_StepAP214_AutoDesignGroupedItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_AutoDesignGroupedItem): NCollection_Array1_StepAP214_AutoDesignGroupedItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_AutoDesignGroupedItem): NCollection_Array1_StepAP214_AutoDesignGroupedItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_ChangeRequestItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_ChangeRequestItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_ChangeRequestItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_ChangeRequestItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_DraughtingCalloutElement {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_DraughtingCalloutElement);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_DraughtingCalloutElement;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_DraughtingCalloutElement;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_TCollection_HExtendedString {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_TCollection_HExtendedString);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_TCollection_HExtendedString): NCollection_Sequence_handle_TCollection_HExtendedString;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_TCollection_HExtendedString): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_TCollection_HExtendedString): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_TCollection_HExtendedString): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_TCollection_HExtendedString): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_TCollection_HExtendedString): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_AutoDesignDateAndPersonItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESGraph_TextDisplayTemplate {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESDraw_ConnectPoint {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESDefs_TabularData {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_StyleContextSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_StyleContextSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_StyleContextSelect): NCollection_Array1_StepVisual_StyleContextSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_StyleContextSelect): NCollection_Array1_StepVisual_StyleContextSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_StyleContextSelect): NCollection_Array1_StepVisual_StyleContextSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_ChildRef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_ChildRef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_ChildRef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_ChildRef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_ChildRef_1 extends NCollection_DynamicArray_BRepGraphInc_ChildRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ChildRef_2 extends NCollection_DynamicArray_BRepGraphInc_ChildRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ChildRef_3 extends NCollection_DynamicArray_BRepGraphInc_ChildRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_ChildRef_4 extends NCollection_DynamicArray_BRepGraphInc_ChildRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfEE {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfEE);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfEE, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfEE;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfEE_1 extends NCollection_DynamicArray_BOPDS_InterfEE {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEE_2 extends NCollection_DynamicArray_BOPDS_InterfEE {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEE_3 extends NCollection_DynamicArray_BOPDS_InterfEE {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEE_4 extends NCollection_DynamicArray_BOPDS_InterfEE {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_TDocStd_ApplicationDelta {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_TDocStd_ApplicationDelta);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_TDocStd_ApplicationDelta): NCollection_Sequence_handle_TDocStd_ApplicationDelta;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_TDocStd_ApplicationDelta): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_TDocStd_ApplicationDelta): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_TDocStd_ApplicationDelta): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_TDocStd_ApplicationDelta): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_TDocStd_ApplicationDelta): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_Vec {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_Vec);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_Vec): NCollection_Array1_gp_Vec;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_Vec): NCollection_Array1_gp_Vec;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_Vec): NCollection_Array1_gp_Vec;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_BoxCharacteristicSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_BoxCharacteristicSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_BoxCharacteristicSelect): NCollection_Array1_StepVisual_BoxCharacteristicSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_BoxCharacteristicSelect): NCollection_Array1_StepVisual_BoxCharacteristicSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_BoxCharacteristicSelect): NCollection_Array1_StepVisual_BoxCharacteristicSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_StepElement_CurveElementSectionDefinition {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit): NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit): NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit): NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepShape_ValueQualifier {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepShape_ValueQualifier);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepShape_ValueQualifier): NCollection_Array1_StepShape_ValueQualifier;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepShape_ValueQualifier): NCollection_Array1_StepShape_ValueQualifier;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepShape_ValueQualifier): NCollection_Array1_StepShape_ValueQualifier;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepFEA_CurveElementInterval {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepFEA_CurveElementInterval);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepFEA_CurveElementInterval): NCollection_Array1_handle_StepFEA_CurveElementInterval;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepFEA_CurveElementInterval): NCollection_Array1_handle_StepFEA_CurveElementInterval;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepFEA_CurveElementInterval): NCollection_Array1_handle_StepFEA_CurveElementInterval;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_Pnt {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_Pnt);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: gp_Pnt, theLower: number, theUpper: number);
  constructor(theBegin: gp_Pnt, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: gp_Pnt): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_Pnt): NCollection_Array1_gp_Pnt;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_Pnt): NCollection_Array1_gp_Pnt;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_Pnt): NCollection_Array1_gp_Pnt;
  /**
   * @returns first element
   */
  First(): gp_Pnt;
  /**
   * @returns first element
   */
  ChangeFirst(): gp_Pnt;
  /**
   * @returns last element
   */
  Last(): gp_Pnt;
  /**
   * @returns last element
   */
  ChangeLast(): gp_Pnt;
  /**
   * Constant value access.
   */
  Value(theIndex: number): gp_Pnt;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): gp_Pnt;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): gp_Pnt;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): gp_Pnt;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: gp_Pnt): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_TopoDS_Shape {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: NCollection_Sequence_TopoDS_Shape);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): NCollection_Sequence_TopoDS_Shape;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): NCollection_Sequence_TopoDS_Shape;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: TopoDS_Shape): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: NCollection_Sequence_TopoDS_Shape): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Units_Quantity {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Units_Quantity);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Units_Quantity): NCollection_Sequence_handle_Units_Quantity;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Units_Quantity): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Units_Quantity): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Quantity): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Quantity): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Quantity): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_PresentationStyleSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_PresentationStyleSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_PresentationStyleSelect): NCollection_Array1_StepVisual_PresentationStyleSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_PresentationStyleSelect): NCollection_Array1_StepVisual_PresentationStyleSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_PresentationStyleSelect): NCollection_Array1_StepVisual_PresentationStyleSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_Storage_Root {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_AsciiString_RWObj_Material {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_RWObj_Material_2 extends NCollection_DataMap_TCollection_AsciiString_RWObj_Material {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_RWObj_Material_3 extends NCollection_DataMap_TCollection_AsciiString_RWObj_Material {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_RWObj_Material_4 extends NCollection_DataMap_TCollection_AsciiString_RWObj_Material {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_RWObj_Material_5 extends NCollection_DataMap_TCollection_AsciiString_RWObj_Material {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_RWObj_Material_6 extends NCollection_DataMap_TCollection_AsciiString_RWObj_Material {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_RWObj_Material_7 extends NCollection_DataMap_TCollection_AsciiString_RWObj_Material {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_int_TopoDS_Shape {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: number, theItem: TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: number, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: number, theItem: TopoDS_Shape): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: number, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * IsBound.
   */
  IsBound(theKey: number): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: number): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: number): TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: number): TopoDS_Shape;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: number): TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_int_TopoDS_Shape_2 extends NCollection_DataMap_int_TopoDS_Shape {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_int_TopoDS_Shape_3 extends NCollection_DataMap_int_TopoDS_Shape {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_TopoDS_Shape_4 extends NCollection_DataMap_int_TopoDS_Shape {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_TopoDS_Shape_5 extends NCollection_DataMap_int_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_TopoDS_Shape_6 extends NCollection_DataMap_int_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_TopoDS_Shape_7 extends NCollection_DataMap_int_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESData_IGESEntity {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_ChangeRequestItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_ChangeRequestItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_ChangeRequestItem): NCollection_Array1_StepAP203_ChangeRequestItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_ChangeRequestItem): NCollection_Array1_StepAP203_ChangeRequestItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_ChangeRequestItem): NCollection_Array1_StepAP203_ChangeRequestItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_Message_Alert extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_Message_Alert);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_Message_Alert): NCollection_List_handle_Message_Alert;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_Message_Alert): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_Message_Alert): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_Message_Alert): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESGeom_Boundary {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_2 extends NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_3 extends NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_4 extends NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_5 extends NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_6 extends NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_7 extends NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_VertexDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_VertexDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_VertexDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_VertexDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_VertexDef_1 extends NCollection_DynamicArray_BRepGraphInc_VertexDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_VertexDef_2 extends NCollection_DynamicArray_BRepGraphInc_VertexDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_VertexDef_3 extends NCollection_DynamicArray_BRepGraphInc_VertexDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_VertexDef_4 extends NCollection_DynamicArray_BRepGraphInc_VertexDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: TopoDS_Shape): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): TopoDS_Shape;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_2 extends NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_3 extends NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_4 extends NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_5 extends NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_6 extends NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_7 extends NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_BRep_CurveRepresentation extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_BRep_CurveRepresentation);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_BRep_CurveRepresentation): NCollection_List_handle_BRep_CurveRepresentation;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_BRep_CurveRepresentation): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_BRep_CurveRepresentation): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_BRep_CurveRepresentation): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepDimTol_DatumReference {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Single hashed Map. This Map is used to store and retrieve keys in linear time.
 *
 * The `Iterator` class can be used to explore the content of the map. It is not wise to iterate and modify a map in parallel.
 *
 * To compute the hashcode of the key the function ::HashCode must be defined in the global namespace
 *
 * To compare two keys the function `IsEqual` must be defined in the global namespace.
 *
 * The performance of a Map is conditioned by its number of buckets that should be kept greater to the number of keys. This map has an automatic management of the number of buckets. It is resized when the number of Keys becomes greater than the number of buckets.
 *
 * If you have a fair idea of the number of objects you can save on automatic resizing by giving a number of buckets at creation or using the ReSize method. This should be consider only for crucial optimisation issues.
 */
export declare class NCollection_Map_handle_TDF_Attribute {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Add.
   */
  Add(theKey: unknown): boolean;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing object.
   */
  Added(theKey: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey: unknown): boolean;
  /**
   * Checks if this map contains all keys of another map. This function checks if this map contains all keys of another map.
   * @deprecated
   */
  Contains(theOther: unknown): boolean;
  /**
   * Remove.
   */
  Remove(K: unknown): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Checks if two maps contain exactly the same keys. This function compares the keys of this map and another map and returns true if they contain exactly the same keys.
   * @deprecated
   */
  IsEqual(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of union (aka addition, fuse, merge, boolean OR) operation between two given Maps The new Map contains the values that are contained either in the first map or in the second map or in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be passed as one of operands.
   * @deprecated
   */
  Union(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the boolean operation union (aka addition, fuse, merge, boolean OR) with another (given) Map. The result contains the values that were previously contained in this map or contained in the given (operand) map. This algorithm is similar to method `Union()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Unite(theOther: unknown): boolean;
  /**
   * Returns true if this and theMap have common elements.
   * @deprecated
   */
  HasIntersection(theMap: unknown): boolean;
  /**
   * Sets this Map to be the result of intersection (aka multiplication, common, boolean AND) operation between two given Maps. The new Map contains only the values that are contained in both map operands. All previous content of this Map is cleared. This same map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Intersection(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the intersection operation (aka multiplication, common, boolean AND) with another (given) Map. The result contains only the values that are contained in both this and the given maps. This algorithm is similar to method `Intersection()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Intersect(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation between two given Maps. The new Map contains only the values that are contained in the first map operands and not contained in the second one. All previous content of this Map is cleared.
   * @deprecated
   */
  Subtraction(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation with another (given) Map. The result contains only the values that were previously contained in this map and not contained in this map. This algorithm is similar to method `Subtract()` with two operands. Returns True if contents of this map is changed.
   * @deprecated
   */
  Subtract(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of symmetric difference (aka exclusive disjunction, boolean XOR) operation between two given Maps. The new Map contains the values that are contained only in the first or the second operand maps but not in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Difference(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the symmetric difference (aka exclusive disjunction, boolean XOR) operation with another (given) Map. The result contains the values that are contained only in this or the operand map, but not in both. This algorithm is similar to method `Difference()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Differ(theOther: unknown): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_Map_handle_TDF_Attribute_2 extends NCollection_Map_handle_TDF_Attribute {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_Map_handle_TDF_Attribute_3 extends NCollection_Map_handle_TDF_Attribute {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_TDF_Attribute_4 extends NCollection_Map_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_TDF_Attribute_5 extends NCollection_Map_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_TDF_Attribute_6 extends NCollection_Map_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_TDF_Attribute_7 extends NCollection_Map_handle_TDF_Attribute {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_NamedUnit {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_NamedUnit);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_NamedUnit): NCollection_Array1_handle_StepBasic_NamedUnit;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_NamedUnit): NCollection_Array1_handle_StepBasic_NamedUnit;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_NamedUnit): NCollection_Array1_handle_StepBasic_NamedUnit;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_Geom2d_Curve {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESDefs_TabularData {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESDefs_TabularData);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESDefs_TabularData): NCollection_Array1_handle_IGESDefs_TabularData;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESDefs_TabularData): NCollection_Array1_handle_IGESDefs_TabularData;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESDefs_TabularData): NCollection_Array1_handle_IGESDefs_TabularData;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_EdgeDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_EdgeDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_EdgeDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_EdgeDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_EdgeDef_1 extends NCollection_DynamicArray_BRepGraphInc_EdgeDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_EdgeDef_2 extends NCollection_DynamicArray_BRepGraphInc_EdgeDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_EdgeDef_3 extends NCollection_DynamicArray_BRepGraphInc_EdgeDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_EdgeDef_4 extends NCollection_DynamicArray_BRepGraphInc_EdgeDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepGeom_CompositeCurveSegment {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepGeom_CompositeCurveSegment);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepGeom_CompositeCurveSegment): NCollection_Array1_handle_StepGeom_CompositeCurveSegment;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepGeom_CompositeCurveSegment): NCollection_Array1_handle_StepGeom_CompositeCurveSegment;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepGeom_CompositeCurveSegment): NCollection_Array1_handle_StepGeom_CompositeCurveSegment;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESGeom_CurveOnSurface {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESGeom_CurveOnSurface);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESGeom_CurveOnSurface): NCollection_Array1_handle_IGESGeom_CurveOnSurface;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESGeom_CurveOnSurface): NCollection_Array1_handle_IGESGeom_CurveOnSurface;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESGeom_CurveOnSurface): NCollection_Array1_handle_IGESGeom_CurveOnSurface;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepFEA_DegreeOfFreedom {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepFEA_DegreeOfFreedom);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepFEA_DegreeOfFreedom): NCollection_Array1_StepFEA_DegreeOfFreedom;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepFEA_DegreeOfFreedom): NCollection_Array1_StepFEA_DegreeOfFreedom;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepFEA_DegreeOfFreedom): NCollection_Array1_StepFEA_DegreeOfFreedom;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_TDF_Label {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_TDF_Label);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_TDF_Label): NCollection_Sequence_TDF_Label;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_TDF_Label): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_TDF_Label): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_TDF_Label): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_TDF_Label): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_TDF_Label): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepElement_VolumeElementPurposeMember {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepElement_VolumeElementPurposeMember);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepElement_VolumeElementPurposeMember): NCollection_Array1_handle_StepElement_VolumeElementPurposeMember;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepElement_VolumeElementPurposeMember): NCollection_Array1_handle_StepElement_VolumeElementPurposeMember;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepElement_VolumeElementPurposeMember): NCollection_Array1_handle_StepElement_VolumeElementPurposeMember;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_PCDM_Document {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_PCDM_Document);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_PCDM_Document): NCollection_Sequence_handle_PCDM_Document;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_PCDM_Document): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_PCDM_Document): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_PCDM_Document): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_PCDM_Document): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_PCDM_Document): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESGraph_Color {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESDimen_GeneralNote {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_CameraModelD3MultiClippingUnionSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition): NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_NodeId {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_NodeId);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_NodeId, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_NodeId;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_1 extends NCollection_DynamicArray_BRepGraph_NodeId {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_2 extends NCollection_DynamicArray_BRepGraph_NodeId {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_3 extends NCollection_DynamicArray_BRepGraph_NodeId {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_NodeId_4 extends NCollection_DynamicArray_BRepGraph_NodeId {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship): NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepRepr_ShapeAspect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepRepr_ShapeAspect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepRepr_ShapeAspect): NCollection_Array1_handle_StepRepr_ShapeAspect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepRepr_ShapeAspect): NCollection_Array1_handle_StepRepr_ShapeAspect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepRepr_ShapeAspect): NCollection_Array1_handle_StepRepr_ShapeAspect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject): NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_1 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_2 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_3 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_4 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfEZ {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfEZ);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfEZ, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfEZ;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfEZ_1 extends NCollection_DynamicArray_BOPDS_InterfEZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEZ_2 extends NCollection_DynamicArray_BOPDS_InterfEZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEZ_3 extends NCollection_DynamicArray_BOPDS_InterfEZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfEZ_4 extends NCollection_DynamicArray_BOPDS_InterfEZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_Poly_Triangle extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_Poly_Triangle);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_Poly_Triangle): NCollection_List_Poly_Triangle;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_Poly_Triangle): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_Poly_Triangle): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_Poly_Triangle): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_int_NCollection_List_TopoDS_Shape {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: number, theItem: NCollection_List_TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: number, theItem: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: number, theItem: NCollection_List_TopoDS_Shape): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: number, theItem: NCollection_List_TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * IsBound.
   */
  IsBound(theKey: number): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: number): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: number): NCollection_List_TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: number): NCollection_List_TopoDS_Shape;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: number): NCollection_List_TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_int_NCollection_List_TopoDS_Shape_2 extends NCollection_DataMap_int_NCollection_List_TopoDS_Shape {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_int_NCollection_List_TopoDS_Shape_3 extends NCollection_DataMap_int_NCollection_List_TopoDS_Shape {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_NCollection_List_TopoDS_Shape_4 extends NCollection_DataMap_int_NCollection_List_TopoDS_Shape {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_NCollection_List_TopoDS_Shape_5 extends NCollection_DataMap_int_NCollection_List_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_NCollection_List_TopoDS_Shape_6 extends NCollection_DataMap_int_NCollection_List_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_NCollection_List_TopoDS_Shape_7 extends NCollection_DataMap_int_NCollection_List_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_Document {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepVisual_TessellatedItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepVisual_TessellatedItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepVisual_TessellatedItem): NCollection_Array1_handle_StepVisual_TessellatedItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepVisual_TessellatedItem): NCollection_Array1_handle_StepVisual_TessellatedItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepVisual_TessellatedItem): NCollection_Array1_handle_StepVisual_TessellatedItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_2 extends NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_3 extends NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_4 extends NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_5 extends NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_6 extends NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_7 extends NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepDimTol_ToleranceZoneTarget {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_bool {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_bool);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_bool): NCollection_Sequence_bool;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: boolean): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_bool): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: boolean): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_bool): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: boolean): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_bool): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_bool): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: boolean): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_bool): void;
  /**
   * First item access.
   */
  First(): boolean;
  /**
   * First item access.
   */
  ChangeFirst(): boolean;
  /**
   * Last item access.
   */
  Last(): boolean;
  /**
   * Last item access.
   */
  ChangeLast(): boolean;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): boolean;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): boolean;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: boolean): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): boolean;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_unsignedchar {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: string);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: string, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Geom_BSplineCurve {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Geom_BSplineCurve);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Geom_BSplineCurve): NCollection_Array1_handle_Geom_BSplineCurve;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Geom_BSplineCurve): NCollection_Array1_handle_Geom_BSplineCurve;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Geom_BSplineCurve): NCollection_Array1_handle_Geom_BSplineCurve;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_gp_Pnt_handle_Standard_Transient {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: gp_Pnt, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: gp_Pnt, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: gp_Pnt, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: gp_Pnt, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: gp_Pnt): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: gp_Pnt): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: gp_Pnt): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: gp_Pnt): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: gp_Pnt): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_gp_Pnt_handle_Standard_Transient_2 extends NCollection_DataMap_gp_Pnt_handle_Standard_Transient {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_gp_Pnt_handle_Standard_Transient_3 extends NCollection_DataMap_gp_Pnt_handle_Standard_Transient {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_gp_Pnt_handle_Standard_Transient_4 extends NCollection_DataMap_gp_Pnt_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_gp_Pnt_handle_Standard_Transient_5 extends NCollection_DataMap_gp_Pnt_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_gp_Pnt_handle_Standard_Transient_6 extends NCollection_DataMap_gp_Pnt_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_gp_Pnt_handle_Standard_Transient_7 extends NCollection_DataMap_gp_Pnt_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_Expr_GeneralExpression {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_Expr_GeneralExpression);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_Expr_GeneralExpression): NCollection_Array1_handle_Expr_GeneralExpression;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_Expr_GeneralExpression): NCollection_Array1_handle_Expr_GeneralExpression;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_Expr_GeneralExpression): NCollection_Array1_handle_Expr_GeneralExpression;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESSolid_Loop {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Transfer_Finder {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Transfer_Finder);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Transfer_Finder): NCollection_Sequence_handle_Transfer_Finder;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Transfer_Finder): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Transfer_Finder): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Transfer_Finder): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Transfer_Finder): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Transfer_Finder): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock {
  constructor();
  constructor(theOther: NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock, theOwnAllocator: boolean): NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock;
  Append(theValue: NCollection_List_handle_BOPDS_PaveBlock): unknown;
  InsertAfter(theIndex: number, theValue: NCollection_List_handle_BOPDS_PaveBlock): unknown;
  InsertBefore(theIndex: number, theValue: NCollection_List_handle_BOPDS_PaveBlock): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: NCollection_List_handle_BOPDS_PaveBlock): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_1 extends NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_2 extends NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_3 extends NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_4 extends NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_TDF_Attribute extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_TDF_Attribute);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_TDF_Attribute): NCollection_List_handle_TDF_Attribute;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_TDF_Attribute): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_TDF_Attribute): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_TDF_Attribute): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepVisual_TessellatedStructuredItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_Units_Token {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepDimTol_GeometricToleranceModifier {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepDimTol_GeometricToleranceModifier);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepDimTol_GeometricToleranceModifier;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepDimTol_GeometricToleranceModifier;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_TDF_Label extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_TDF_Label);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_TDF_Label): NCollection_List_TDF_Label;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_TDF_Label): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_TDF_Label): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_TDF_Label): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_Standard_GUID extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_Standard_GUID);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_Standard_GUID): NCollection_List_Standard_GUID;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_Standard_GUID): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_Standard_GUID): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_Standard_GUID): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_Extrema_POnCurv {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_Extrema_POnCurv);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_Extrema_POnCurv): NCollection_Sequence_Extrema_POnCurv;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_Extrema_POnCurv): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_Extrema_POnCurv): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnCurv): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnCurv): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_Extrema_POnCurv): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_ChFiDS_SurfData {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_Poly_Triangle {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_Poly_Triangle);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_Poly_Triangle): NCollection_Array1_Poly_Triangle;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_Poly_Triangle): NCollection_Array1_Poly_Triangle;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_Poly_Triangle): NCollection_Array1_Poly_Triangle;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_2 extends NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_3 extends NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_4 extends NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_5 extends NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_6 extends NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_7 extends NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_TopoDS_Shape {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array2_TopoDS_Shape);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: TopoDS_Shape);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): NCollection_Array2_TopoDS_Shape;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): NCollection_Array2_TopoDS_Shape;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepRepr_ShapeAspect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_gp_Trsf {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_gp_Trsf);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_gp_Trsf): NCollection_Sequence_gp_Trsf;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_gp_Trsf): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_gp_Trsf): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_gp_Trsf): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_gp_Trsf): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_gp_Trsf): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepDimTol_DatumReferenceElement {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepElement_CurveElementSectionDefinition {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESAppli_Node {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Units_Token {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Units_Token);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Units_Token): NCollection_Sequence_handle_Units_Token;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Units_Token): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Units_Token): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Token): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Token): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Token): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember): NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember): NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember): NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepShape_ConnectedEdgeSet {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepShape_ConnectedEdgeSet);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepShape_ConnectedEdgeSet): NCollection_Array1_handle_StepShape_ConnectedEdgeSet;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepShape_ConnectedEdgeSet): NCollection_Array1_handle_StepShape_ConnectedEdgeSet;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepShape_ConnectedEdgeSet): NCollection_Array1_handle_StepShape_ConnectedEdgeSet;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_ApprovedItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_ApprovedItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_ApprovedItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_ApprovedItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_TopoDS_Shape {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_TopoDS_Shape);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_TopoDS_Shape): NCollection_Sequence_TopoDS_Shape;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: TopoDS_Shape): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_TopoDS_Shape): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: TopoDS_Shape): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_TopoDS_Shape): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: TopoDS_Shape): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_TopoDS_Shape): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_TopoDS_Shape): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: TopoDS_Shape): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_TopoDS_Shape): void;
  /**
   * First item access.
   */
  First(): TopoDS_Shape;
  /**
   * First item access.
   */
  ChangeFirst(): TopoDS_Shape;
  /**
   * Last item access.
   */
  Last(): TopoDS_Shape;
  /**
   * Last item access.
   */
  ChangeLast(): TopoDS_Shape;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): TopoDS_Shape;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): TopoDS_Shape;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: TopoDS_Shape): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): TopoDS_Shape;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): TopoDS_Shape;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_Product {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_TCollection_AsciiString {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: NCollection_Sequence_TCollection_AsciiString);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): NCollection_Sequence_TCollection_AsciiString;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): NCollection_Sequence_TCollection_AsciiString;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: NCollection_Sequence_TCollection_AsciiString): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_std_pair_unsignedint_int {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_std_pair_unsignedint_int);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_std_pair_unsignedint_int): NCollection_Array1_std_pair_unsignedint_int;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_std_pair_unsignedint_int): NCollection_Array1_std_pair_unsignedint_int;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_std_pair_unsignedint_int): NCollection_Array1_std_pair_unsignedint_int;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_Bnd_Range extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_Bnd_Range);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_Bnd_Range): NCollection_List_Bnd_Range;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_Bnd_Range): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_Bnd_Range): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_Bnd_Range): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_gp_XYZ {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_gp_XYZ);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_gp_XYZ;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_gp_XYZ;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESData_ViewKindEntity {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_HLRAlgo_PolyShellData {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_HLRAlgo_PolyShellData);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_HLRAlgo_PolyShellData): NCollection_Array1_handle_HLRAlgo_PolyShellData;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_HLRAlgo_PolyShellData): NCollection_Array1_handle_HLRAlgo_PolyShellData;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_HLRAlgo_PolyShellData): NCollection_Array1_handle_HLRAlgo_PolyShellData;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_Pnt2d {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_Pnt2d);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_Pnt2d): NCollection_Array1_gp_Pnt2d;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_Pnt2d): NCollection_Array1_gp_Pnt2d;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_Pnt2d): NCollection_Array1_gp_Pnt2d;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_BOPTools_CoupleOfShape extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_BOPTools_CoupleOfShape);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_BOPTools_CoupleOfShape): NCollection_List_BOPTools_CoupleOfShape;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_BOPTools_CoupleOfShape): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_BOPTools_CoupleOfShape): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_BOPTools_CoupleOfShape): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESData_LineFontEntity {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESData_LineFontEntity);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESData_LineFontEntity): NCollection_Array1_handle_IGESData_LineFontEntity;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESData_LineFontEntity): NCollection_Array1_handle_IGESData_LineFontEntity;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESData_LineFontEntity): NCollection_Array1_handle_IGESData_LineFontEntity;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_ContractedItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_ContractedItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_ContractedItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_ContractedItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Single hashed Map. This Map is used to store and retrieve keys in linear time.
 *
 * The `Iterator` class can be used to explore the content of the map. It is not wise to iterate and modify a map in parallel.
 *
 * To compute the hashcode of the key the function ::HashCode must be defined in the global namespace
 *
 * To compare two keys the function `IsEqual` must be defined in the global namespace.
 *
 * The performance of a Map is conditioned by its number of buckets that should be kept greater to the number of keys. This map has an automatic management of the number of buckets. It is resized when the number of Keys becomes greater than the number of buckets.
 *
 * If you have a fair idea of the number of objects you can save on automatic resizing by giving a number of buckets at creation or using the ReSize method. This should be consider only for crucial optimisation issues.
 */
export declare class NCollection_Map_TDF_Label {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Add.
   */
  Add(theKey: unknown): boolean;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing object.
   */
  Added(theKey: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey: unknown): boolean;
  /**
   * Checks if this map contains all keys of another map. This function checks if this map contains all keys of another map.
   * @deprecated
   */
  Contains(theOther: unknown): boolean;
  /**
   * Remove.
   */
  Remove(K: unknown): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Checks if two maps contain exactly the same keys. This function compares the keys of this map and another map and returns true if they contain exactly the same keys.
   * @deprecated
   */
  IsEqual(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of union (aka addition, fuse, merge, boolean OR) operation between two given Maps The new Map contains the values that are contained either in the first map or in the second map or in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be passed as one of operands.
   * @deprecated
   */
  Union(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the boolean operation union (aka addition, fuse, merge, boolean OR) with another (given) Map. The result contains the values that were previously contained in this map or contained in the given (operand) map. This algorithm is similar to method `Union()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Unite(theOther: unknown): boolean;
  /**
   * Returns true if this and theMap have common elements.
   * @deprecated
   */
  HasIntersection(theMap: unknown): boolean;
  /**
   * Sets this Map to be the result of intersection (aka multiplication, common, boolean AND) operation between two given Maps. The new Map contains only the values that are contained in both map operands. All previous content of this Map is cleared. This same map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Intersection(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the intersection operation (aka multiplication, common, boolean AND) with another (given) Map. The result contains only the values that are contained in both this and the given maps. This algorithm is similar to method `Intersection()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Intersect(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation between two given Maps. The new Map contains only the values that are contained in the first map operands and not contained in the second one. All previous content of this Map is cleared.
   * @deprecated
   */
  Subtraction(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation with another (given) Map. The result contains only the values that were previously contained in this map and not contained in this map. This algorithm is similar to method `Subtract()` with two operands. Returns True if contents of this map is changed.
   * @deprecated
   */
  Subtract(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of symmetric difference (aka exclusive disjunction, boolean XOR) operation between two given Maps. The new Map contains the values that are contained only in the first or the second operand maps but not in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Difference(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the symmetric difference (aka exclusive disjunction, boolean XOR) operation with another (given) Map. The result contains the values that are contained only in this or the operand map, but not in both. This algorithm is similar to method `Difference()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Differ(theOther: unknown): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_Map_TDF_Label_2 extends NCollection_Map_TDF_Label {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_Map_TDF_Label_3 extends NCollection_Map_TDF_Label {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TDF_Label_4 extends NCollection_Map_TDF_Label {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TDF_Label_5 extends NCollection_Map_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TDF_Label_6 extends NCollection_Map_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_TDF_Label_7 extends NCollection_Map_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepShape_ConnectedEdgeSet {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_int {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: number);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP203_CertifiedItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP203_CertifiedItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP203_CertifiedItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP203_CertifiedItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_DerivedUnitElement {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_DerivedUnitElement);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_DerivedUnitElement): NCollection_Array1_handle_StepBasic_DerivedUnitElement;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_DerivedUnitElement): NCollection_Array1_handle_StepBasic_DerivedUnitElement;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_DerivedUnitElement): NCollection_Array1_handle_StepBasic_DerivedUnitElement;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepFEA_CurveElementEndRelease {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Units_Unit {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Units_Unit);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Units_Unit): NCollection_Sequence_handle_Units_Unit;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Units_Unit): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Units_Unit): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Unit): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Unit): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Units_Unit): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepDimTol_DatumReferenceModifier {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepDimTol_DatumReferenceModifier);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepDimTol_DatumReferenceModifier): NCollection_Array1_StepDimTol_DatumReferenceModifier;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepDimTol_DatumReferenceModifier): NCollection_Array1_StepDimTol_DatumReferenceModifier;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepDimTol_DatumReferenceModifier): NCollection_Array1_StepDimTol_DatumReferenceModifier;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: unknown, theItem: unknown): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: unknown, theItem: unknown): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: unknown, theItem: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown, theItem: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): unknown;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: unknown): unknown;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_2 extends NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_3 extends NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_4 extends NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_5 extends NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_6 extends NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_7 extends NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_FaceInfo {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_FaceInfo);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_FaceInfo, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_FaceInfo;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_FaceInfo_1 extends NCollection_DynamicArray_BOPDS_FaceInfo {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_FaceInfo_2 extends NCollection_DynamicArray_BOPDS_FaceInfo {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_FaceInfo_3 extends NCollection_DynamicArray_BOPDS_FaceInfo {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_FaceInfo_4 extends NCollection_DynamicArray_BOPDS_FaceInfo {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_ExtendedString_unsignedchar {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: string): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: string): string;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: string): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: string): string;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): string;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): string;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): string;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_unsignedchar_2 extends NCollection_DataMap_TCollection_ExtendedString_unsignedchar {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_unsignedchar_3 extends NCollection_DataMap_TCollection_ExtendedString_unsignedchar {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_unsignedchar_4 extends NCollection_DataMap_TCollection_ExtendedString_unsignedchar {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_unsignedchar_5 extends NCollection_DataMap_TCollection_ExtendedString_unsignedchar {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_unsignedchar_6 extends NCollection_DataMap_TCollection_ExtendedString_unsignedchar {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_unsignedchar_7 extends NCollection_DataMap_TCollection_ExtendedString_unsignedchar {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_handle_NCollection_HSequence_int {
  constructor();
  constructor(theOther: NCollection_DynamicArray_handle_NCollection_HSequence_int);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_handle_NCollection_HSequence_int, theOwnAllocator: boolean): NCollection_DynamicArray_handle_NCollection_HSequence_int;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_handle_NCollection_HSequence_int_1 extends NCollection_DynamicArray_handle_NCollection_HSequence_int {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_handle_NCollection_HSequence_int_2 extends NCollection_DynamicArray_handle_NCollection_HSequence_int {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_handle_NCollection_HSequence_int_3 extends NCollection_DynamicArray_handle_NCollection_HSequence_int {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_handle_NCollection_HSequence_int_4 extends NCollection_DynamicArray_handle_NCollection_HSequence_int {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepBasic_ProductContext {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_RenderingPropertiesSelect {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_RenderingPropertiesSelect);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_RenderingPropertiesSelect): NCollection_Array1_StepVisual_RenderingPropertiesSelect;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_RenderingPropertiesSelect): NCollection_Array1_StepVisual_RenderingPropertiesSelect;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_RenderingPropertiesSelect): NCollection_Array1_StepVisual_RenderingPropertiesSelect;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_DateItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_DateItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_DateItem): NCollection_Array1_StepAP214_DateItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_DateItem): NCollection_Array1_StepAP214_DateItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_DateItem): NCollection_Array1_StepAP214_DateItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepDimTol_DatumReferenceModifier {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepDimTol_DatumReferenceModifier);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepDimTol_DatumReferenceModifier;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepDimTol_DatumReferenceModifier;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESGraph_TextFontDef {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1..Extent. See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedMap_double {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(theExtent: number): void;
  /**
   * Add adds a new key to the map.
   * @param theKey1 key to add
   * @returns index of the key (new or existing)
   */
  Add(theKey1: number): number;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing key.
   * @param theKey1 key to add
   * @returns const reference to the key in the map
   */
  Added(theKey1: number): number;
  /**
   * Contains.
   */
  Contains(theKey1: number): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: number): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: number): boolean;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): number;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: number): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedMap_double_2 extends NCollection_IndexedMap_double {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedMap_double_3 extends NCollection_IndexedMap_double {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_double_4 extends NCollection_IndexedMap_double {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_double_5 extends NCollection_IndexedMap_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_double_6 extends NCollection_IndexedMap_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedMap_double_7 extends NCollection_IndexedMap_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject): NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_LayeredItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_LayeredItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_LayeredItem): NCollection_Array1_StepVisual_LayeredItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_LayeredItem): NCollection_Array1_StepVisual_LayeredItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_LayeredItem): NCollection_Array1_StepVisual_LayeredItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BOPDS_InterfVZ {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BOPDS_InterfVZ);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BOPDS_InterfVZ, theOwnAllocator: boolean): NCollection_DynamicArray_BOPDS_InterfVZ;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BOPDS_InterfVZ_1 extends NCollection_DynamicArray_BOPDS_InterfVZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVZ_2 extends NCollection_DynamicArray_BOPDS_InterfVZ {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVZ_3 extends NCollection_DynamicArray_BOPDS_InterfVZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BOPDS_InterfVZ_4 extends NCollection_DynamicArray_BOPDS_InterfVZ {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_TCollection_HAsciiString {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_TCollection_HAsciiString);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_TCollection_HAsciiString): NCollection_Array1_handle_TCollection_HAsciiString;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_TCollection_HAsciiString): NCollection_Array1_handle_TCollection_HAsciiString;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_TCollection_HAsciiString): NCollection_Array1_handle_TCollection_HAsciiString;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_handle_NCollection_HArray1_double {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: unknown);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepGeom_CompositeCurveSegment {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_1 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_2 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_3 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_4 extends NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_handle_Geom_Surface {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: unknown);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket): NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket): NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket): NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_double {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_double);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: number, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_double): NCollection_Array2_double;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_double): NCollection_Array2_double;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_double): NCollection_Array2_double;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: number): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_handle_NCollection_HArray1_double {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_handle_NCollection_HArray1_double);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_handle_NCollection_HArray1_double): NCollection_Array2_handle_NCollection_HArray1_double;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_handle_NCollection_HArray1_double): NCollection_Array2_handle_NCollection_HArray1_double;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_handle_NCollection_HArray1_double): NCollection_Array2_handle_NCollection_HArray1_double;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_WireRef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_WireRef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_WireRef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_WireRef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_WireRef_1 extends NCollection_DynamicArray_BRepGraphInc_WireRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_WireRef_2 extends NCollection_DynamicArray_BRepGraphInc_WireRef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_WireRef_3 extends NCollection_DynamicArray_BRepGraphInc_WireRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_WireRef_4 extends NCollection_DynamicArray_BRepGraphInc_WireRef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_int {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: number);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: number, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepGeom_PcurveOrSurface {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepGeom_PcurveOrSurface);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepGeom_PcurveOrSurface): NCollection_Array1_StepGeom_PcurveOrSurface;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepGeom_PcurveOrSurface): NCollection_Array1_StepGeom_PcurveOrSurface;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepGeom_PcurveOrSurface): NCollection_Array1_StepGeom_PcurveOrSurface;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepVisual_CurveStyleFontPattern {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepVisual_CurveStyleFontPattern);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepVisual_CurveStyleFontPattern): NCollection_Array1_handle_StepVisual_CurveStyleFontPattern;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepVisual_CurveStyleFontPattern): NCollection_Array1_handle_StepVisual_CurveStyleFontPattern;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepVisual_CurveStyleFontPattern): NCollection_Array1_handle_StepVisual_CurveStyleFontPattern;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation): NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation): NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation): NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Single hashed Map. This Map is used to store and retrieve keys in linear time.
 *
 * The `Iterator` class can be used to explore the content of the map. It is not wise to iterate and modify a map in parallel.
 *
 * To compute the hashcode of the key the function ::HashCode must be defined in the global namespace
 *
 * To compare two keys the function `IsEqual` must be defined in the global namespace.
 *
 * The performance of a Map is conditioned by its number of buckets that should be kept greater to the number of keys. This map has an automatic management of the number of buckets. It is resized when the number of Keys becomes greater than the number of buckets.
 *
 * If you have a fair idea of the number of objects you can save on automatic resizing by giving a number of buckets at creation or using the ReSize method. This should be consider only for crucial optimisation issues.
 */
export declare class NCollection_Map_handle_BOPDS_PaveBlock {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assign. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Add.
   */
  Add(theKey: unknown): boolean;
  /**
   * Added: add a new key if not yet in the map, and return reference to either newly added or previously existing object.
   */
  Added(theKey: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey: unknown): boolean;
  /**
   * Checks if this map contains all keys of another map. This function checks if this map contains all keys of another map.
   * @deprecated
   */
  Contains(theOther: unknown): boolean;
  /**
   * Remove.
   */
  Remove(K: unknown): boolean;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Checks if two maps contain exactly the same keys. This function compares the keys of this map and another map and returns true if they contain exactly the same keys.
   * @deprecated
   */
  IsEqual(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of union (aka addition, fuse, merge, boolean OR) operation between two given Maps The new Map contains the values that are contained either in the first map or in the second map or in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be passed as one of operands.
   * @deprecated
   */
  Union(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the boolean operation union (aka addition, fuse, merge, boolean OR) with another (given) Map. The result contains the values that were previously contained in this map or contained in the given (operand) map. This algorithm is similar to method `Union()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Unite(theOther: unknown): boolean;
  /**
   * Returns true if this and theMap have common elements.
   * @deprecated
   */
  HasIntersection(theMap: unknown): boolean;
  /**
   * Sets this Map to be the result of intersection (aka multiplication, common, boolean AND) operation between two given Maps. The new Map contains only the values that are contained in both map operands. All previous content of this Map is cleared. This same map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Intersection(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the intersection operation (aka multiplication, common, boolean AND) with another (given) Map. The result contains only the values that are contained in both this and the given maps. This algorithm is similar to method `Intersection()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Intersect(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation between two given Maps. The new Map contains only the values that are contained in the first map operands and not contained in the second one. All previous content of this Map is cleared.
   * @deprecated
   */
  Subtraction(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the subtraction (aka set-theoretic difference, relative complement, exclude, cut, boolean NOT) operation with another (given) Map. The result contains only the values that were previously contained in this map and not contained in this map. This algorithm is similar to method `Subtract()` with two operands. Returns True if contents of this map is changed.
   * @deprecated
   */
  Subtract(theOther: unknown): boolean;
  /**
   * Sets this Map to be the result of symmetric difference (aka exclusive disjunction, boolean XOR) operation between two given Maps. The new Map contains the values that are contained only in the first or the second operand maps but not in both. All previous content of this Map is cleared. This map (result of the boolean operation) can also be used as one of operands.
   * @deprecated
   */
  Difference(theLeft: unknown, theRight: unknown): void;
  /**
   * Apply to this Map the symmetric difference (aka exclusive disjunction, boolean XOR) operation with another (given) Map. The result contains the values that are contained only in this or the operand map, but not in both. This algorithm is similar to method `Difference()`. Returns True if contents of this map is changed.
   * @deprecated
   */
  Differ(theOther: unknown): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_Map_handle_BOPDS_PaveBlock_2 extends NCollection_Map_handle_BOPDS_PaveBlock {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_Map_handle_BOPDS_PaveBlock_3 extends NCollection_Map_handle_BOPDS_PaveBlock {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_BOPDS_PaveBlock_4 extends NCollection_Map_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_BOPDS_PaveBlock_5 extends NCollection_Map_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_BOPDS_PaveBlock_6 extends NCollection_Map_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_Map_handle_BOPDS_PaveBlock_7 extends NCollection_Map_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP203_SpecifiedItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP203_SpecifiedItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP203_SpecifiedItem): NCollection_Array1_StepAP203_SpecifiedItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP203_SpecifiedItem): NCollection_Array1_StepAP203_SpecifiedItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP203_SpecifiedItem): NCollection_Array1_StepAP203_SpecifiedItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefId {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefId);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefId, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefId;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefId_1 extends NCollection_DynamicArray_BRepGraph_RefId {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_2 extends NCollection_DynamicArray_BRepGraph_RefId {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_3 extends NCollection_DynamicArray_BRepGraph_RefId {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefId_4 extends NCollection_DynamicArray_BRepGraph_RefId {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_handle_NCollection_HArray1_int {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: unknown);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESSolid_Face {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed sequences. Inherits from both `NCollection_Sequence<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted sequence functionality.
 */
export declare class NCollection_HSequence_handle_TCollection_HExtendedString {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from sequence.
   * @param theOther the sequence to copy from
   */
  constructor(theOther: any);
  /**
   * Returns const reference to the underlying sequence.
   */
  Sequence(): any;
  /**
   * Returns mutable reference to the underlying sequence.
   */
  ChangeSequence(): any;
  /**
   * Append single item.
   * @param theItem the item to append
   */
  Append(theItem: unknown): void;
  /**
   * Append another sequence.
   * @param theSequence the sequence to append
   */
  Append(theSequence: any): void;
  /**
   * Append single item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: unknown): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: unknown, theItem: NCollection_List_handle_BOPDS_PaveBlock): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: unknown, theItem: NCollection_List_handle_BOPDS_PaveBlock): NCollection_List_handle_BOPDS_PaveBlock;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: unknown, theItem: NCollection_List_handle_BOPDS_PaveBlock): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: unknown, theItem: NCollection_List_handle_BOPDS_PaveBlock): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: unknown, theItem: NCollection_List_handle_BOPDS_PaveBlock): NCollection_List_handle_BOPDS_PaveBlock;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown, theItem: NCollection_List_handle_BOPDS_PaveBlock): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): NCollection_List_handle_BOPDS_PaveBlock;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): NCollection_List_handle_BOPDS_PaveBlock;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: unknown): NCollection_List_handle_BOPDS_PaveBlock;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: unknown): NCollection_List_handle_BOPDS_PaveBlock;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: unknown): NCollection_List_handle_BOPDS_PaveBlock;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_2 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_3 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_4 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_5 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_6 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_7 extends NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TDF_Label_TDF_Label {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TDF_Label_TDF_Label_2 extends NCollection_DataMap_TDF_Label_TDF_Label {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TDF_Label_TDF_Label_3 extends NCollection_DataMap_TDF_Label_TDF_Label {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TDF_Label_TDF_Label_4 extends NCollection_DataMap_TDF_Label_TDF_Label {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TDF_Label_TDF_Label_5 extends NCollection_DataMap_TDF_Label_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TDF_Label_TDF_Label_6 extends NCollection_DataMap_TDF_Label_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TDF_Label_TDF_Label_7 extends NCollection_DataMap_TDF_Label_TDF_Label {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Geom_Curve {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Geom_Curve);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Geom_Curve): NCollection_Sequence_handle_Geom_Curve;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Geom_Curve): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Geom_Curve): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Geom_Curve): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Geom_Curve): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Geom_Curve): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_2 extends NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_3 extends NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_4 extends NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_5 extends NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_6 extends NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_7 extends NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESGraph_TextDisplayTemplate {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESGraph_TextDisplayTemplate);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESGraph_TextDisplayTemplate): NCollection_Array1_handle_IGESGraph_TextDisplayTemplate;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESGraph_TextDisplayTemplate): NCollection_Array1_handle_IGESGraph_TextDisplayTemplate;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESGraph_TextDisplayTemplate): NCollection_Array1_handle_IGESGraph_TextDisplayTemplate;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_int_handle_MAT_BasicElt {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: number, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: number, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: number, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: number, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: number): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: number): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: number): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: number): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: number): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_int_handle_MAT_BasicElt_2 extends NCollection_DataMap_int_handle_MAT_BasicElt {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_int_handle_MAT_BasicElt_3 extends NCollection_DataMap_int_handle_MAT_BasicElt {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_handle_MAT_BasicElt_4 extends NCollection_DataMap_int_handle_MAT_BasicElt {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_handle_MAT_BasicElt_5 extends NCollection_DataMap_int_handle_MAT_BasicElt {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_handle_MAT_BasicElt_6 extends NCollection_DataMap_int_handle_MAT_BasicElt {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_int_handle_MAT_BasicElt_7 extends NCollection_DataMap_int_handle_MAT_BasicElt {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif): NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepVisual_SurfaceStyleElementSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepVisual_SurfaceStyleElementSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepVisual_SurfaceStyleElementSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepVisual_SurfaceStyleElementSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_ChFiDS_Stripe extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_ChFiDS_Stripe);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_ChFiDS_Stripe): NCollection_List_handle_ChFiDS_Stripe;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_ChFiDS_Stripe): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_ChFiDS_Stripe): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_ChFiDS_Stripe): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepFEA_ElementRepresentation {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepFEA_ElementRepresentation);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepFEA_ElementRepresentation): NCollection_Array1_handle_StepFEA_ElementRepresentation;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepFEA_ElementRepresentation): NCollection_Array1_handle_StepFEA_ElementRepresentation;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepFEA_ElementRepresentation): NCollection_Array1_handle_StepFEA_ElementRepresentation;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_gp_Pnt2d {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_gp_Pnt2d);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_gp_Pnt2d): NCollection_Array2_gp_Pnt2d;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_gp_Pnt2d): NCollection_Array2_gp_Pnt2d;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_gp_Pnt2d): NCollection_Array2_gp_Pnt2d;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Simple list to link items together keeping the first and the last one. Inherits BaseList, adding the data item to each node.
 */
export declare class NCollection_List_handle_BRepMesh_DiscretAlgoFactory extends NCollection_BaseList {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_List_handle_BRepMesh_DiscretAlgoFactory);
  /**
   * Move constructor.
   */
  constructor(theInitList: unknown[]);
  /**
   * Initializer list constructor.
   * @param theInitList initializer list of elements to populate the list
   * @param theAllocator optional allocator for memory management
   */
  constructor(theInitList: unknown[], theAllocator?: unknown);
  /**
   * Replace this list by the items of another list (theOther parameter). This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_List_handle_BRepMesh_DiscretAlgoFactory): NCollection_List_handle_BRepMesh_DiscretAlgoFactory;
  /**
   * Clear this list.
   */
  Clear(theAllocator: unknown): void;
  /**
   * First item.
   */
  First(): unknown;
  /**
   * Last item.
   */
  Last(): unknown;
  /**
   * Append one item at the end.
   */
  Append(theItem: unknown): unknown;
  /**
   * Append one item at the end.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Append(theOther: NCollection_List_handle_BRepMesh_DiscretAlgoFactory): void;
  /**
   * Prepend one item at the beginning.
   */
  Prepend(theItem: unknown): unknown;
  /**
   * Prepend one item at the beginning.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theOther: NCollection_List_handle_BRepMesh_DiscretAlgoFactory): void;
  /**
   * RemoveFirst item.
   */
  RemoveFirst(): void;
  /**
   * Reverse the list.
   */
  Reverse(): void;
  /**
   * Exchange the content of two lists without re-allocations. Swaps all internal state including allocators, ensuring correct deallocation. Existing iterators remain valid but will point to the other list's elements.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_List_handle_BRepMesh_DiscretAlgoFactory): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_AutoDesignPresentedItemSelect {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The class Array2 represents bi-dimensional arrays of fixed size known at run time. The ranges of indices are user defined.
 *
 * Class allocates one 1D array storing full data (all Rows and Columns) and extra 1D array storing pointers to each Row.
 *
 * Warning: Programs clients of such class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * for (i = A.LowerRow(); i <= A.UpperRow(); i++) for (j = A.LowerCol(); j <= A.UpperCol(); j++)
 *
 * Zero-based (size_t) construction mode: `NCollection_Array2(size_t theNbRows, size_t theNbCols)` creates a zero-based array (`LowerRow()`==0, `LowerCol()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variant `NCollection_Array2(pointer, size_t, size_t)` wraps an existing flat row-major buffer and does NOT own the memory.
 */
export declare class NCollection_Array2_handle_Geom_BezierSurface {
  /**
   * Empty constructor; should be used with caution.
   * @see `Resize()`
   * @see `Move()`
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array2_handle_Geom_BezierSurface);
  /**
   * Zero-based constructor: allocates theNbRows x theNbCols elements with lower bounds 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theNbRows: number, theNbCols: number);
  /**
   * Constructor.
   */
  constructor(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  /**
   * C array-based constructor.
   */
  constructor(theBegin: unknown, theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number);
  static BeginPosition(theRowLower: number, argNo1: number, theColLower: number, theColUpper: number): number;
  static LastPosition(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number): number;
  /**
   * Size (number of items).
   */
  Size(): number;
  /**
   * Length (legacy int-returning API).
   */
  Length(): number;
  /**
   * Returns number of rows.
   */
  NbRows(): number;
  /**
   * Returns number of columns.
   */
  NbColumns(): number;
  /**
   * Returns length of the row, i.e. number of columns.
   */
  RowLength(): number;
  /**
   * Returns length of the column, i.e. number of rows.
   */
  ColLength(): number;
  /**
   * LowerRow.
   */
  LowerRow(): number;
  /**
   * UpperRow.
   */
  UpperRow(): number;
  /**
   * LowerCol.
   */
  LowerCol(): number;
  /**
   * UpperCol.
   */
  UpperCol(): number;
  /**
   * Updates lower row.
   */
  UpdateLowerRow(theLowerRow: number): void;
  /**
   * Updates lower column.
   */
  UpdateLowerCol(theLowerCol: number): void;
  /**
   * Updates upper row.
   */
  UpdateUpperRow(theUpperRow: number): void;
  /**
   * Updates upper column.
   */
  UpdateUpperCol(theUpperCol: number): void;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: NCollection_Array2_handle_Geom_BezierSurface): NCollection_Array2_handle_Geom_BezierSurface;
  /**
   * Replaces this array by a copy of theOther array. Row and column bounds are copied from theOther.
   */
  Assign(theOther: unknown): unknown;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array2_handle_Geom_BezierSurface): NCollection_Array2_handle_Geom_BezierSurface;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same dimensions as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: unknown): unknown;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array2_handle_Geom_BezierSurface): NCollection_Array2_handle_Geom_BezierSurface;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will be left uninitialized and should not be used anymore.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: unknown): unknown;
  /**
   * SetValue.
   */
  SetValue(theRow: number, theCol: number, theItem: unknown): void;
  /**
   * SetValue.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /**
   * Zero-based Resize: resizes to theNbRows x theNbCols, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to specified bounds. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies elements in linear (row-major) order. No re-allocation is done if dimensions are unchanged.
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  /**
   * Resizes the array preserving 2D element layout. When theToCopyData is false, the array is re-allocated without preserving data. When theToCopyData is true, copies min(oldNbRows,newNbRows) x min(oldNbCols,newNbCols) elements from the top-left corner of the old array to the top-left corner of the new, preserving relative (row, col) offsets from lower bounds. Trimming or growing as needed. No re-allocation is done if dimensions are unchanged.
   * @param theRowLower new lower Row of array
   * @param theRowUpper new upper Row of array
   * @param theColLower new lower Column of array
   * @param theColUpper new upper Column of array
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theRowLower: number, theRowUpper: number, theColLower: number, theColUpper: number, theToCopyData: boolean): void;
  /**
   * Zero-based ResizeWithTrim: resizes preserving 2D layout, keeping lower bounds unchanged. No re-allocation is done if dimensions are unchanged.
   * @param theNbRows new number of rows
   * @param theNbCols new number of columns
   * @param theToCopyData flag to copy existing data into new array
   */
  ResizeWithTrim(theNbRows: number, theNbCols: number, theToCopyData: boolean): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: NCollection_DynamicArray_BRepGraph_NodeId): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: NCollection_DynamicArray_BRepGraph_NodeId): NCollection_DynamicArray_BRepGraph_NodeId;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: NCollection_DynamicArray_BRepGraph_NodeId): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: NCollection_DynamicArray_BRepGraph_NodeId): NCollection_DynamicArray_BRepGraph_NodeId;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): NCollection_DynamicArray_BRepGraph_NodeId;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): NCollection_DynamicArray_BRepGraph_NodeId;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): NCollection_DynamicArray_BRepGraph_NodeId;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_2 extends NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_3 extends NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_4 extends NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_5 extends NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_6 extends NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_7 extends NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: number): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: number): number;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: number): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: number): number;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): number;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): number;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_RefUID {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_RefUID);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_RefUID, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_RefUID;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_RefUID_1 extends NCollection_DynamicArray_BRepGraph_RefUID {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefUID_2 extends NCollection_DynamicArray_BRepGraph_RefUID {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefUID_3 extends NCollection_DynamicArray_BRepGraph_RefUID {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_RefUID_4 extends NCollection_DynamicArray_BRepGraph_RefUID {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem): NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem): NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem): NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_IntTools_Range {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_IntTools_Range);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_IntTools_Range): NCollection_Sequence_IntTools_Range;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_IntTools_Range): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_IntTools_Range): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_IntTools_Range): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_IntTools_Range): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_IntTools_Range): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_gp_Vec2d {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_gp_Vec2d);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_gp_Vec2d): NCollection_Array1_gp_Vec2d;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_gp_Vec2d): NCollection_Array1_gp_Vec2d;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_gp_Vec2d): NCollection_Array1_gp_Vec2d;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_gp_XY {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_gp_XY);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_gp_XY;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_gp_XY;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_Quantity_Color {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_Quantity_Color);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_Quantity_Color;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_Quantity_Color;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepShape_Face {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepShape_Face);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepShape_Face): NCollection_Array1_handle_StepShape_Face;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepShape_Face): NCollection_Array1_handle_StepShape_Face;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepShape_Face): NCollection_Array1_handle_StepShape_Face;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_Standard_Persistent {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_IGESGraph_TextFontDef {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_IGESGraph_TextFontDef);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_IGESGraph_TextFontDef): NCollection_Array1_handle_IGESGraph_TextFontDef;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_IGESGraph_TextFontDef): NCollection_Array1_handle_IGESGraph_TextFontDef;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_IGESGraph_TextFontDef): NCollection_Array1_handle_IGESGraph_TextFontDef;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepGeom_SurfaceBoundary {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepGeom_SurfaceBoundary);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepGeom_SurfaceBoundary;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepGeom_SurfaceBoundary;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_NCollection_Sequence_handle_Geom2d_Geometry {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_NCollection_Sequence_handle_Geom2d_Geometry);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_NCollection_Sequence_handle_Geom2d_Geometry): NCollection_Sequence_NCollection_Sequence_handle_Geom2d_Geometry;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_NCollection_Sequence_handle_Geom2d_Geometry): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_NCollection_Sequence_handle_Geom2d_Geometry): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_TDF_Label {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_TDF_Label);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_TDF_Label): NCollection_Array1_TDF_Label;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_TDF_Label): NCollection_Array1_TDF_Label;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_TDF_Label): NCollection_Array1_TDF_Label;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepVisual_PresentationStyleAssignment {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepVisual_PresentationStyleAssignment);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepVisual_PresentationStyleAssignment): NCollection_Array1_handle_StepVisual_PresentationStyleAssignment;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepVisual_PresentationStyleAssignment): NCollection_Array1_handle_StepVisual_PresentationStyleAssignment;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepVisual_PresentationStyleAssignment): NCollection_Array1_handle_StepVisual_PresentationStyleAssignment;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_ExtendedString_double {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: number): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: number): number;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: number): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: number): number;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): number;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): number;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): number;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_double_2 extends NCollection_DataMap_TCollection_ExtendedString_double {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_double_3 extends NCollection_DataMap_TCollection_ExtendedString_double {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_double_4 extends NCollection_DataMap_TCollection_ExtendedString_double {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_double_5 extends NCollection_DataMap_TCollection_ExtendedString_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_double_6 extends NCollection_DataMap_TCollection_ExtendedString_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_ExtendedString_double_7 extends NCollection_DataMap_TCollection_ExtendedString_double {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_ExtremaPC_ExtremumResult {
  constructor();
  constructor(theOther: NCollection_DynamicArray_ExtremaPC_ExtremumResult);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_ExtremaPC_ExtremumResult, theOwnAllocator: boolean): NCollection_DynamicArray_ExtremaPC_ExtremumResult;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_ExtremaPC_ExtremumResult_1 extends NCollection_DynamicArray_ExtremaPC_ExtremumResult {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_ExtremaPC_ExtremumResult_2 extends NCollection_DynamicArray_ExtremaPC_ExtremumResult {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_ExtremaPC_ExtremumResult_3 extends NCollection_DynamicArray_ExtremaPC_ExtremumResult {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_ExtremaPC_ExtremumResult_4 extends NCollection_DynamicArray_ExtremaPC_ExtremumResult {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_TopoDS_Shape {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_TopoDS_Shape);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: TopoDS_Shape);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: TopoDS_Shape, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_TopoDS_Shape;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_TopoDS_Shape;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 2D arrays. Inherits from both `NCollection_Array2<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted 2D array functionality.
 */
export declare class NCollection_HArray2_gp_Pnt {
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array2_gp_Pnt);
  /**
   * Constructor with bounds.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number);
  /**
   * Constructor with bounds and initial value.
   * @param theRowLow lower row bound
   * @param theRowUpp upper row bound
   * @param theColLow lower column bound
   * @param theColUpp upper column bound
   * @param theValue initial value for all elements
   */
  constructor(theRowLow: number, theRowUpp: number, theColLow: number, theColUpp: number, theValue: gp_Pnt);
  /**
   * Returns const reference to the underlying array.
   */
  Array2(): NCollection_Array2_gp_Pnt;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray2(): NCollection_Array2_gp_Pnt;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESSolid_Shell {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_handle_Expr_NamedExpression {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_handle_Expr_NamedExpression);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_handle_Expr_NamedExpression): NCollection_Sequence_handle_Expr_NamedExpression;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_handle_Expr_NamedExpression): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_handle_Expr_NamedExpression): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_NamedExpression): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_NamedExpression): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_handle_Expr_NamedExpression): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: TopoDS_Shape): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: TopoDS_Shape): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: TopoDS_Shape): TopoDS_Shape;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): TopoDS_Shape;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): TopoDS_Shape;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): TopoDS_Shape;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_2 extends NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_3 extends NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_4 extends NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_5 extends NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_6 extends NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_7 extends NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: Definition of a sequence of elements indexed by an Integer in range of 1..n
 */
export declare class NCollection_Sequence_HLRBRep_ShapeBounds {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Constructor.
   */
  constructor(theAllocator: unknown);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Sequence_HLRBRep_ShapeBounds);
  /**
   * Method for consistency with other collections.
   * @returns Lower bound (inclusive) for iteration.
   */
  static Lower(): number;
  /**
   * Method for consistency with other collections.
   * @returns Upper bound (inclusive) for iteration.
   */
  Upper(): number;
  /**
   * Empty query.
   */
  IsEmpty(): boolean;
  /**
   * Reverse sequence.
   */
  Reverse(): void;
  /**
   * Exchange two members.
   */
  Exchange(I: number, J: number): void;
  /**
   * Static deleter to be passed to BaseSequence.
   * @returns A result object with fields:
   * - `theAl`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static delNode(theNode: unknown): { theAl: unknown; [Symbol.dispose](): void };
  /**
   * Clear the items out, take a new allocator if non null.
   */
  Clear(theAllocator: unknown): void;
  /**
   * Replace this sequence by the items of theOther. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_Sequence_HLRBRep_ShapeBounds): NCollection_Sequence_HLRBRep_ShapeBounds;
  /**
   * Remove one item.
   */
  Remove(theIndex: number): void;
  /**
   * Remove range of items.
   */
  Remove(theFromIndex: number, theToIndex: number): void;
  /**
   * Append one item.
   */
  Append(theItem: unknown): void;
  /**
   * Append one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Append(theSeq: NCollection_Sequence_HLRBRep_ShapeBounds): void;
  /**
   * Prepend one item.
   */
  Prepend(theItem: unknown): void;
  /**
   * Prepend one item.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Prepend(theSeq: NCollection_Sequence_HLRBRep_ShapeBounds): void;
  /**
   * InsertBefore theIndex theItem.
   */
  InsertBefore(theIndex: number, theItem: unknown): void;
  InsertBefore(theIndex: number, theSeq: NCollection_Sequence_HLRBRep_ShapeBounds): void;
  /**
   * InsertAfter the position of iterator.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  InsertAfter(theIndex: number, theSeq: NCollection_Sequence_HLRBRep_ShapeBounds): void;
  /**
   * InsertAfter the position of iterator.
   */
  InsertAfter(theIndex: number, theItem: unknown): void;
  /**
   * Split in two sequences.
   * @param theSeq Mutated in place; read the updated value from this argument after the call.
   */
  Split(theIndex: number, theSeq: NCollection_Sequence_HLRBRep_ShapeBounds): void;
  /**
   * First item access.
   */
  First(): unknown;
  /**
   * First item access.
   */
  ChangeFirst(): unknown;
  /**
   * Last item access.
   */
  Last(): unknown;
  /**
   * Last item access.
   */
  ChangeLast(): unknown;
  /**
   * Constant item access by theIndex.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable item access by theIndex.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * Set item value by theIndex.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_IGESGeom_CurveOnSurface {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepElement_MeasureOrUnspecifiedValue {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepElement_MeasureOrUnspecifiedValue);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepElement_MeasureOrUnspecifiedValue;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepElement_MeasureOrUnspecifiedValue;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraph_UID {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraph_UID);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraph_UID, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraph_UID;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraph_UID_1 extends NCollection_DynamicArray_BRepGraph_UID {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_UID_2 extends NCollection_DynamicArray_BRepGraph_UID {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraph_UID_3 extends NCollection_DynamicArray_BRepGraph_UID {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraph_UID_4 extends NCollection_DynamicArray_BRepGraph_UID {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_TCollection_HAsciiString {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_StepAP214_AutoDesignDateAndTimeItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher): NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: TopoDS_Shape, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: TopoDS_Shape, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: TopoDS_Shape): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: TopoDS_Shape): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: TopoDS_Shape): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: TopoDS_Shape): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_2 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_3 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_4 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_5 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_6 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_7 extends NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_2 extends NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_3 extends NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_4 extends NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_5 extends NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_6 extends NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_7 extends NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_StepVisual_InvisibleItem {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_StepVisual_InvisibleItem);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_StepVisual_InvisibleItem): NCollection_Array1_StepVisual_InvisibleItem;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_StepVisual_InvisibleItem): NCollection_Array1_StepVisual_InvisibleItem;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_StepVisual_InvisibleItem): NCollection_Array1_StepVisual_InvisibleItem;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: The DataMap is a Map to store keys with associated Items. See Map from NCollection for a discussion about the number of buckets.
 *
 * The DataMap can be seen as an extended array where the Keys are the indices. For this reason the operator () is defined on DataMap to fetch an Item from a Key. So the following syntax can be used :
 *
 * anItem = aMap(aKey); aMap(aKey) = anItem;
 *
 * This analogy has its limit. aMap(aKey) = anItem can be done only if aKey was previously bound to an item in the map.
 */
export declare class NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance {
  /**
   * Empty Constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Bind binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns true if Key was not bound already
   */
  Bind(theKey: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map.
   * @param theKey key to add/update
   * @param theItem new item; overrides value previously bound to the key (uses destroy+reconstruct)
   * @returns pointer to modifiable Item
   */
  Bound(theKey: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if Key was newly bound, false if Key already existed (no replacement)
   */
  TryBind(theKey: unknown, theItem: unknown): boolean;
  /**
   * TryBound binds Item to Key in map only if Key is not yet bound.
   * @param theKey key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey: unknown, theItem: unknown): unknown;
  /**
   * IsBound.
   */
  IsBound(theKey: unknown): boolean;
  /**
   * UnBind removes Item Key pair from map.
   */
  UnBind(theKey: unknown): boolean;
  /**
   * Seek returns pointer to Item by Key. Returns NULL is Key was not bound.
   */
  Seek(theKey: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL is Key was not bound.
   */
  ChangeSeek(theKey: unknown): unknown;
  /**
   * ChangeFind returns modifiable Item by Key. Raises if Key was not bound.
   */
  ChangeFind(theKey: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_2 extends NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_3 extends NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_4 extends NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_5 extends NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_6 extends NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_7 extends NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * The class `NCollection_Array1` represents unidimensional arrays of fixed size known at run time. The range of the index is user defined. An array1 can be constructed with a "C array". This functionality is useful to call methods expecting an Array1. It allows to carry the bounds inside the arrays.
 *
 * Examples:
 *
 * ```
 * Itemtab[100];//anexamplewithaCarray NCollection_Array1<Item>ttab(tab[0],1,100); NCollection_Array1<Item>tttab(ttab(10),10,20);//asliceofttab
 * ```
 *
 * If you want to reindex an array from 1 to Length do:
 *
 * ```
 * NCollection_Array1<Item>tab1(tab(tab.Lower()),1,tab.Length());
 * ```
 *
 * Warning: Programs client of such a class must be independent of the range of the first element. Then, a C++ for loop must be written like this
 *
 * ```
 * for(i=A.Lower();i<=A.Upper();i++)
 * ```
 *
 * Zero-based (size_t) construction mode: Use `NCollection_Array1(size_t theSize)` or `NCollection_Array1(pointer, size_t)` to create a zero-based array (`Lower()`==0). In this mode `At()`/ChangeAt() and STL iterators are the preferred access path - they address elements directly without any offset subtraction. Buffer-reuse variants do NOT own the memory and will not free it on destruction.
 *
 * ```
 * intaBuffer[100]; NCollection_Array1<int>aZero(100);//allocates,lower=0 NCollection_Array1<int>aWrap(aBuffer,100);//wrapsaBuffer,lower=0,notowner for(size_ti=0;i<aWrap.Size();++i) aWrap.At(i)=static_cast<int>(i);
 * ```
 */
export declare class NCollection_Array1_handle_StepBasic_ProductContext {
  constructor();
  /**
   * Zero-based constructor: allocates theSize elements with lower bound 0. Use `At()`/ChangeAt() or STL iterators for optimal access (no offset subtraction).
   */
  constructor(theSize: number);
  /**
   * Copy constructor.
   */
  constructor(theOther: NCollection_Array1_handle_StepBasic_ProductContext);
  constructor(theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number);
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer?: boolean);
  /**
   * Initialise the items with theValue.
   */
  Init(theValue: unknown): void;
  /**
   * Size query.
   */
  Size(): number;
  /**
   * Length query (legacy int-returning API).
   */
  Length(): number;
  /**
   * Return TRUE if array has zero length.
   */
  IsEmpty(): boolean;
  /**
   * Lower bound.
   */
  Lower(): number;
  /**
   * Upper bound.
   */
  Upper(): number;
  /**
   * Replaces this array by a copy of theOther array. Bounds and length are copied from theOther. When this array wraps an external (non-owned) buffer:
   *
   * - if theOther has the same length, values are copied in place into the external buffer and ownership is unchanged;
   * - if theOther has a different length, this array detaches from the external buffer and allocates a fresh owned buffer. Use `CopyValues()` to preserve this array's bounds.
   */
  Assign(theOther: NCollection_Array1_handle_StepBasic_ProductContext): NCollection_Array1_handle_StepBasic_ProductContext;
  /**
   * Copies values from theOther array without changing this array bounds. This array should be pre-allocated and have the same length as theOther; otherwise exception Standard_DimensionMismatch is thrown.
   */
  CopyValues(theOther: NCollection_Array1_handle_StepBasic_ProductContext): NCollection_Array1_handle_StepBasic_ProductContext;
  /**
   * Move assignment. This array will borrow all the data from theOther. The moved object will keep pointer to the memory buffer and range, but it will not free the buffer on destruction.
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Move(theOther: NCollection_Array1_handle_StepBasic_ProductContext): NCollection_Array1_handle_StepBasic_ProductContext;
  /**
   * @returns first element
   */
  First(): unknown;
  /**
   * @returns first element
   */
  ChangeFirst(): unknown;
  /**
   * @returns last element
   */
  Last(): unknown;
  /**
   * @returns last element
   */
  ChangeLast(): unknown;
  /**
   * Constant value access.
   */
  Value(theIndex: number): unknown;
  /**
   * Variable value access.
   */
  ChangeValue(theIndex: number): unknown;
  /**
   * 0-based checked access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  At(theIndex: number): unknown;
  /**
   * 0-based checked mutable access independent of `Lower()`/Upper().
   * @param theIndex 0-based index in [0, `Size()`-1]
   */
  ChangeAt(theIndex: number): unknown;
  /**
   * Set value.
   */
  SetValue(theIndex: number, theItem: unknown): void;
  /**
   * Changes the lowest bound. Do not move data.
   */
  UpdateLowerBound(theLower: number): void;
  /**
   * Changes the upper bound. Do not move data.
   */
  UpdateUpperBound(theUpper: number): void;
  /**
   * Resizes the array to specified bounds. No re-allocation will be done if length of array does not change, but existing values will not be discarded if theToCopyData set to FALSE.
   * @param theLower new lower bound of array
   * @param theUpper new upper bound of array
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theLower: number, theUpper: number, theToCopyData: boolean): void;
  /**
   * Resizes the array to theSize elements, keeping the lower bound unchanged.
   * @param theSize new number of elements
   * @param theToCopyData flag to copy existing data into new array
   */
  Resize(theSize: number, theToCopyData: boolean): void;
  IsDeletable(): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Purpose: An indexed map is used to store keys and to bind an index to them. Each new key stored in the map gets an index. Index are incremented as keys are stored in the map. A key can be found by the index and an index by the key. No key but the last can be removed so the indices are in the range 1.. Extent. An Item is stored with each key.
 *
 * This class is similar to IndexedMap from NCollection with the Item as a new feature. Note the important difference on the operator (). In the IndexedMap this operator returns the Key. In the IndexedDataMap this operator returns the Item.
 *
 * See the class Map from NCollection for a discussion about the number of buckets.
 */
export declare class NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Copy constructor.
   */
  constructor(theOther: unknown);
  /**
   * Exchange the content of two maps without re-allocations. Notice that allocators will be swapped as well!
   * @param theOther Mutated in place; read the updated value from this argument after the call.
   */
  Exchange(theOther: unknown): void;
  /**
   * Returns const reference to the hasher.
   */
  GetHasher(): unknown;
  /**
   * Assignment. This method does not change the internal allocator.
   */
  Assign(theOther: unknown): unknown;
  /**
   * ReSize.
   */
  ReSize(N: number): void;
  /**
   * Returns the Index of already bound Key or appends new Key with specified Item value.
   * @param theKey1 Key to search (and to bind, if it was not bound already)
   * @param theItem Item value to set for newly bound Key; ignored if Key was already bound
   * @returns index of Key
   */
  Add(theKey1: unknown, theItem: unknown): number;
  /**
   * TryBound binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns reference to existing or newly bound Item
   */
  TryBound(theKey1: unknown, theItem: unknown): unknown;
  /**
   * TryBind binds Item to Key only if Key is not yet bound.
   * @param theKey1 key to add
   * @param theItem item to bind if Key is not yet bound
   * @returns true if key was newly added, false if key already existed
   */
  TryBind(theKey1: unknown, theItem: unknown): boolean;
  /**
   * Bind binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns true if Key was not bound already
   */
  Bind(theKey1: unknown, theItem: unknown): boolean;
  /**
   * Bound binds Item to Key in map; overwrites value if Key already exists.
   * @param theKey1 key to add/update
   * @param theItem new item; overrides value previously bound to the key
   * @returns pointer to modifiable Item
   */
  Bound(theKey1: unknown, theItem: unknown): unknown;
  /**
   * Contains.
   */
  Contains(theKey1: unknown): boolean;
  /**
   * Substitute.
   */
  Substitute(theIndex: number, theKey1: unknown, theItem: unknown): void;
  /**
   * Swaps two elements with the given indices.
   */
  Swap(theIndex1: number, theIndex2: number): void;
  /**
   * RemoveLast.
   */
  RemoveLast(): void;
  /**
   * Remove the key of the given index. Caution! The index of the last key can be changed.
   */
  RemoveFromIndex(theIndex: number): void;
  /**
   * Remove the given key. Caution! The index of the last key can be changed.
   */
  RemoveKey(theKey1: unknown): void;
  /**
   * FindKey.
   */
  FindKey(theIndex: number): unknown;
  /**
   * FindFromIndex.
   */
  FindFromIndex(theIndex: number): unknown;
  /**
   * ChangeFromIndex.
   */
  ChangeFromIndex(theIndex: number): unknown;
  /**
   * FindIndex.
   */
  FindIndex(theKey1: unknown): number;
  /**
   * ChangeFromKey.
   */
  ChangeFromKey(theKey1: unknown): unknown;
  /**
   * Seek returns pointer to Item by Key. Returns NULL if Key was not found.
   */
  Seek(theKey1: unknown): unknown;
  /**
   * ChangeSeek returns modifiable pointer to Item by Key. Returns NULL if Key was not found.
   */
  ChangeSeek(theKey1: unknown): unknown;
  /**
   * Clear data. If doReleaseMemory is false then the table of buckets is not released and will be reused.
   */
  Clear(doReleaseMemory: boolean): void;
  /**
   * Clear data and reset allocator.
   */
  Clear(theAllocator: unknown): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  /**
   * Constructor.
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_2 extends NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor.
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor (legacy int-taking).
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_3 extends NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor (legacy int-taking).
     */
    constructor(theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (copy).
   * @param theHasher custom hasher instance
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_4 extends NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (copy).
     * @param theHasher custom hasher instance
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_5 extends NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_6 extends NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

  /**
   * Constructor with custom hasher (move).
   * @param theHasher custom hasher instance (moved)
   * @param theNbBuckets initial number of buckets
   * @param theAllocator custom memory allocator
   */
  export declare class NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_7 extends NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient {
    /**
     * Constructor with custom hasher (move).
     * @param theHasher custom hasher instance (moved)
     * @param theNbBuckets initial number of buckets
     * @param theAllocator custom memory allocator
     */
    constructor(theHasher: unknown, theNbBuckets: number, theAllocator: unknown);
  }

/**
 * Template class for Handle-managed 1D arrays. Inherits from both `NCollection_Array1<TheItemType>` and {@link Standard_Transient | `Standard_Transient`}, providing reference-counted array functionality.
 */
export declare class NCollection_HArray1_handle_StepRepr_RepresentationItem {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Copy constructor from array.
   * @param theOther the array to copy from
   */
  constructor(theOther: any);
  /**
   * Constructor with bounds.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   */
  constructor(theLower: number, theUpper: number);
  /**
   * Constructor with bounds and initial value.
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theValue initial value for all elements
   */
  constructor(theLower: number, theUpper: number, theValue: unknown);
  /**
   * Constructor from C array.
   * @param theBegin reference to the first element of a C array
   * @param theLower lower bound of the array
   * @param theUpper upper bound of the array
   * @param theUseBuffer flag indicating whether to use external buffer (must be explicit)
   */
  constructor(theBegin: unknown, theLower: number, theUpper: number, theUseBuffer: boolean);
  /**
   * Returns const reference to the underlying array.
   */
  Array1(): any;
  /**
   * Returns mutable reference to the underlying array.
   */
  ChangeArray1(): any;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Class `NCollection_DynamicArray` (dynamic array of objects).
 *
 * The array's indices always start at 0.
 *
 * The Vector is always created with 0 length. It can be enlarged by two means:
 *
 * 1. Calling the method Append (val) - then "val" is added to the end of the vector (the vector length is incremented)
 * 2. Calling the method SetValue (i, val) - if "i" is greater than or equal to the current length of the vector, the vector is enlarged to accomo- date this index
 *
 * The methods Append and SetValue return a non-const reference to the copied object inside the vector. This reference is guaranteed to be valid until the vector is destroyed. It can be used to access the vector member directly or to pass its address to other data structures.
 *
 * The vector iterator remembers the length of the vector at the moment of the creation or initialisation of the iterator. Therefore the iteration begins at index 0 and stops at the index equal to (remembered_length-1). It is OK to enlarge the vector during the iteration.
 */
export declare class NCollection_DynamicArray_BRepGraphInc_WireDef {
  constructor();
  constructor(theOther: NCollection_DynamicArray_BRepGraphInc_WireDef);
  Size(): number;
  Length(): number;
  Lower(): number;
  Upper(): number;
  IsEmpty(): boolean;
  Assign(theOther: NCollection_DynamicArray_BRepGraphInc_WireDef, theOwnAllocator: boolean): NCollection_DynamicArray_BRepGraphInc_WireDef;
  Append(theValue: unknown): unknown;
  InsertAfter(theIndex: number, theValue: unknown): unknown;
  InsertBefore(theIndex: number, theValue: unknown): unknown;
  EraseLast(): void;
  Appended(): unknown;
  Value(theIndex: number): unknown;
  First(): unknown;
  ChangeFirst(): unknown;
  Last(): unknown;
  ChangeLast(): unknown;
  ChangeValue(theIndex: number): unknown;
  SetValue(theIndex: number, theValue: unknown): unknown;
  Clear(theReleaseMemory: boolean): void;
  SetIncrement(theIncrement: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

  export declare class NCollection_DynamicArray_BRepGraphInc_WireDef_1 extends NCollection_DynamicArray_BRepGraphInc_WireDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_WireDef_2 extends NCollection_DynamicArray_BRepGraphInc_WireDef {
    constructor(theIncrement: number);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_WireDef_3 extends NCollection_DynamicArray_BRepGraphInc_WireDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

  export declare class NCollection_DynamicArray_BRepGraphInc_WireDef_4 extends NCollection_DynamicArray_BRepGraphInc_WireDef {
    constructor(theIncrement: number, theAllocator: unknown);
  }

/**
 * The class Cut provides Boolean cut operation between arguments and tools (Boolean Subtraction).
 */
export declare class BRepAlgoAPI_Cut extends BRepAlgoAPI_BooleanOperation {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Empty constructor <PF> - PaveFiller object that is carried out.
   */
  constructor(PF: unknown);
  /**
   * Empty constructor.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape);
  /**
   * Constructor with two shapes <S1> -argument <S2> -tool <anOperation> - the type of the operation Obsolete.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape, theRange: Message_ProgressRange);
  /**
   * Constructor with two shapes <S1> -argument <S2> -tool <anOperation> - the type of the operation Obsolete.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape, aDSF: unknown);
  /**
   * Empty constructor.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape, aDSF: unknown, bFWD: boolean);
  /**
   * Constructor with two shapes <S1> -argument <S2> -tool <anOperation> - the type of the operation <PF> - PaveFiller object that is carried out Obsolete.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape, aDSF: unknown, bFWD?: boolean, theRange?: Message_ProgressRange);
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class contains API level of the General Fuse algorithm.
 *
 * Additionally to the options defined in the base class, the algorithm has the following options:
 *
 * - *Safe processing mode* - allows to avoid modification of the input shapes during the operation (by default it is off);
 * - *Gluing options* - allows to speed up the calculation of the intersections on the special cases, in which some sub-shapes are coinciding.
 * - *Disabling the check for inverted solids* - Disables/Enables the check of the input solids for inverted status (holes in the space). The default value is TRUE, i.e. the check is performed. Setting this flag to FALSE for inverted solids, most likely will lead to incorrect results.
 * - *Disabling history collection* - allows disabling the collection of the history of shapes modifications during the operation.
 *
 * It returns the following Error statuses:
 *
 * - 0 - in case of success;
 * - *BOPAlgo_AlertTooFewArguments* - in case there are no enough arguments to perform the operation;
 * - *BOPAlgo_AlertIntersectionFailed* - in case the intersection of the arguments has failed;
 * - *BOPAlgo_AlertBuilderFailed* - in case building of the result shape has failed.
 *
 * Warnings statuses from underlying DS Filler and Builder algorithms are collected in the report.
 *
 * The class provides possibility to simplify the resulting shape by unification of the tangential edges and faces. It is performed by the method *SimplifyResult*. See description of this method for more details.
 */
export declare class BRepAlgoAPI_BuilderAlgo extends BRepAlgoAPI_Algo {
  constructor();
  constructor(thePF: unknown);
  SetArguments(theLS: NCollection_List_TopoDS_Shape): void;
  Arguments(): NCollection_List_TopoDS_Shape;
  SetNonDestructive(theFlag: boolean): void;
  NonDestructive(): boolean;
  SetGlue(theGlue: unknown): void;
  Glue(): unknown;
  SetCheckInverted(theCheck: boolean): void;
  CheckInverted(): boolean;
  Build(theRange: Message_ProgressRange): void;
  SimplifyResult(theUnifyEdges: boolean, theUnifyFaces: boolean, theAngularTol: number): void;
  Modified(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  Generated(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  IsDeleted(S: TopoDS_Shape): boolean;
  HasModified(): boolean;
  HasGenerated(): boolean;
  HasDeleted(): boolean;
  SetToFillHistory(theHistFlag: boolean): void;
  HasHistory(): boolean;
  SectionEdges(): NCollection_List_TopoDS_Shape;
  DSFiller(): unknown;
  Builder(): unknown;
  History(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Provides the root interface for the API algorithms.
 */
export declare class BRepAlgoAPI_Algo extends BRepBuilderAPI_MakeShape {
  /**
   * Returns a shape built by the shape construction algorithm. Does not check if the shape is built.
   */
  Shape(): TopoDS_Shape;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The root API class for performing Boolean Operations on arbitrary shapes.
 *
 * The arguments of the operation are divided in two groups - *Objects* and *Tools*. Each group can contain any number of shapes, but each shape should be valid in terms of *{@link BRepCheck_Analyzer | `BRepCheck_Analyzer`}* and *{@link BOPAlgo_ArgumentAnalyzer | `BOPAlgo_ArgumentAnalyzer`}*. The algorithm builds the splits of the given arguments using the intersection results and combines the result of Boolean Operation of given type:
 *
 * - *FUSE* - union of two groups of objects;
 * - *COMMON* - intersection of two groups of objects;
 * - *CUT* - subtraction of one group from the other;
 * - *SECTION* - section edges and vertices of all arguments;
 *
 * The rules for the arguments and type of the operation are the following:
 *
 * - For Boolean operation *FUSE* all arguments should have equal dimensions;
 * - For Boolean operation *CUT* the minimal dimension of *Tools* should not be less than the maximal dimension of *Objects*;
 * - For Boolean operation *COMMON* the arguments can have any dimension.
 * - For Boolean operation *SECTION* the arguments can be of any type.
 *
 * Additionally to the errors of the base class the algorithm returns the following Errors:
 *
 * - *BOPAlgo_AlertBOPNotSet* - in case the type of Boolean Operation is not set.
 */
export declare class BRepAlgoAPI_BooleanOperation extends BRepAlgoAPI_BuilderAlgo {
  constructor();
  constructor(thePF: unknown);
  Shape1(): TopoDS_Shape;
  Shape2(): TopoDS_Shape;
  SetTools(theLS: NCollection_List_TopoDS_Shape): void;
  Tools(): NCollection_List_TopoDS_Shape;
  SetOperation(theBOP: unknown): void;
  Operation(): unknown;
  Build(theRange: Message_ProgressRange): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The class provides Boolean fusion operation between arguments and tools (Boolean Union).
 */
export declare class BRepAlgoAPI_Fuse extends BRepAlgoAPI_BooleanOperation {
  /**
   * Empty constructor.
   */
  constructor();
  /**
   * Empty constructor <PF> - PaveFiller object that is carried out.
   */
  constructor(PF: unknown);
  /**
   * Empty constructor.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape);
  /**
   * Constructor with two shapes <S1> -argument <S2> -tool <anOperation> - the type of the operation Obsolete.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape, theRange: Message_ProgressRange);
  /**
   * Constructor with two shapes <S1> -argument <S2> -tool <anOperation> - the type of the operation Obsolete.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape, aDSF: unknown);
  /**
   * Constructor with two shapes <S1> -argument <S2> -tool <anOperation> - the type of the operation <PF> - PaveFiller object that is carried out Obsolete.
   */
  constructor(S1: TopoDS_Shape, S2: TopoDS_Shape, aDSF: unknown, theRange?: Message_ProgressRange);
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The abstract class MakeSweep is the root class of swept primitives. Sweeps are objects you obtain by sweeping a profile along a path. The profile can be any topology and the path is usually a curve or a wire. The profile generates objects according to the following rules:
 *
 * - Vertices generate Edges
 * - Edges generate Faces.
 * - Wires generate Shells.
 * - Faces generate Solids.
 * - Shells generate Composite Solids. You are not allowed to sweep Solids and Composite Solids. Two kinds of sweeps are implemented in the BRepPrimAPI package:
 * - The linear sweep called a Prism
 * - The rotational sweep called a Revol Swept constructions along complex profiles such as BSpline curves are also available in the BRepOffsetAPI package..
 */
export declare class BRepPrimAPI_MakeSweep extends BRepBuilderAPI_MakeShape {
  /**
   * Returns the `TopoDS` Shape of the bottom of the sweep.
   */
  FirstShape(): TopoDS_Shape;
  /**
   * Returns the `TopoDS` Shape of the top of the sweep.
   */
  LastShape(): TopoDS_Shape;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes functions to build cylinders or portions of cylinders. A MakeCylinder object provides a framework for:
 *
 * - defining the construction of a cylinder,
 * - implementing the construction algorithm, and
 * - consulting the result.
 */
export declare class BRepPrimAPI_MakeCylinder extends BRepPrimAPI_MakeOneAxis {
  /**
   * Make a cylinder.
   * @param R cylinder radius
   * @param H cylinder height
   */
  constructor(R: number, H: number);
  /**
   * Make a cylinder (part cylinder).
   * @param R cylinder radius
   * @param H cylinder height
   * @param Angle defines the missing portion of the cylinder
   */
  constructor(R: number, H: number, Angle: number);
  /**
   * Make a cylinder of radius R and length H.
   * @param Axes coordinate system for the construction of the cylinder
   * @param R cylinder radius
   * @param H cylinder height
   */
  constructor(Axes: gp_Ax2, R: number, H: number);
  /**
   * Make a cylinder of radius R and length H with angle H. Constructs.
   *
   * - a cylinder of radius R and height H, or
   * - a portion of cylinder of radius R and height H, and of the angle Angle defining the missing portion of the cylinder. The cylinder is constructed about the "Z Axis" of either:
   * - the global coordinate system, or
   * - the local coordinate system Axes. It is limited in this coordinate system as follows:
   * - in the v parametric direction (the Z axis), by the two parameter values 0 and H,
   * - and in the u parametric direction (the rotation angle around the Z Axis), in the case of a portion of a cylinder, by the two parameter values 0 and Angle. Angle is given in radians. The resulting shape is composed of:
   * - a lateral cylindrical face,
   * - two planar faces in the planes z = 0 and z = H (in the case of a complete cylinder, these faces are circles), and
   * - in case of a portion of a cylinder, two additional planar faces to close the shape.(two rectangles in the planes u = 0 and u = Angle). Exceptions Standard_DomainError if:
   * - R is less than or equal to `Precision::Confusion()`, or
   * - H is less than or equal to `Precision::Confusion()`.
   */
  constructor(Axes: gp_Ax2, R: number, H: number, Angle: number);
  /**
   * Returns the algorithm.
   */
  Cylinder(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The abstract class MakeOneAxis is the root class of algorithms used to construct rotational primitives.
 */
export declare class BRepPrimAPI_MakeOneAxis extends BRepBuilderAPI_MakeShape {
  /**
   * Stores the solid in myShape.
   */
  Build(theRange: Message_ProgressRange): void;
  /**
   * Returns the lateral face of the rotational primitive.
   */
  Face(): TopoDS_Face;
  /**
   * Returns the constructed rotational primitive as a shell.
   */
  Shell(): unknown;
  /**
   * Returns the constructed rotational primitive as a solid.
   */
  Solid(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes functions to build parallelepiped boxes. A MakeBox object provides a framework for:
 *
 * - defining the construction of a box,
 * - implementing the construction algorithm, and
 * - consulting the result. Constructs a box such that its sides are parallel to the axes of
 * - the global coordinate system, or
 * - the local coordinate system Axis. and
 * - with a corner at (0, 0, 0) and of size (dx, dy, dz), or
 * - with a corner at point P and of size (dx, dy, dz), or
 * - with corners at points P1 and P2. Exceptions Standard_DomainError if: dx, dy, dz are less than or equal to `Precision::Confusion()`, or
 * - the vector joining the points P1 and P2 has a component projected onto the global coordinate system less than or equal to `Precision::Confusion()`. In these cases, the box would be flat.
 */
export declare class BRepPrimAPI_MakeBox extends BRepBuilderAPI_MakeShape {
  /**
   * Default constructor.
   */
  constructor();
  /**
   * Make a box with corners P1,P2.
   */
  constructor(P1: gp_Pnt, P2: gp_Pnt);
  /**
   * Make a box with a corner at 0,0,0 and the other dx,dy,dz.
   */
  constructor(dx: number, dy: number, dz: number);
  /**
   * Make a box with a corner at P and size dx, dy, dz.
   */
  constructor(P: gp_Pnt, dx: number, dy: number, dz: number);
  /**
   * Make a box with Ax2 (the left corner and the axis) and size dx, dy, dz.
   */
  constructor(Axes: gp_Ax2, dx: number, dy: number, dz: number);
  /**
   * Init a box with corners thePnt1, thePnt2.
   */
  Init(thePnt1: gp_Pnt, thePnt2: gp_Pnt): void;
  /**
   * Init a box with a corner at 0,0,0 and the other theDX, theDY, theDZ.
   */
  Init(theDX: number, theDY: number, theDZ: number): void;
  /**
   * Init a box with a corner at thePnt and size theDX, theDY, theDZ.
   */
  Init(thePnt: gp_Pnt, theDX: number, theDY: number, theDZ: number): void;
  /**
   * Init a box with Ax2 (the left corner and the theAxes) and size theDX, theDY, theDZ.
   */
  Init(theAxes: gp_Ax2, theDX: number, theDY: number, theDZ: number): void;
  /**
   * Returns the internal algorithm.
   */
  Wedge(): unknown;
  /**
   * Stores the solid in myShape.
   */
  Build(theRange: Message_ProgressRange): void;
  /**
   * Returns the constructed box as a shell.
   */
  Shell(): unknown;
  /**
   * Returns the constructed box as a solid.
   */
  Solid(): unknown;
  /**
   * Returns ZMin face.
   */
  BottomFace(): TopoDS_Face;
  /**
   * Returns XMin face.
   */
  BackFace(): TopoDS_Face;
  /**
   * Returns XMax face.
   */
  FrontFace(): TopoDS_Face;
  /**
   * Returns YMin face.
   */
  LeftFace(): TopoDS_Face;
  /**
   * Returns YMax face.
   */
  RightFace(): TopoDS_Face;
  /**
   * Returns ZMax face.
   */
  TopFace(): TopoDS_Face;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * This is a common interface for meshing algorithms instantiated by Mesh Factory and implemented by plugins.
 */
export declare class BRepMesh_DiscretRoot extends Standard_Transient {
  /**
   * Set the shape to triangulate.
   */
  SetShape(theShape: TopoDS_Shape): void;
  Shape(): TopoDS_Shape;
  /**
   * Returns true if triangualtion was performed and has success.
   */
  IsDone(): boolean;
  /**
   * Compute triangulation for set shape.
   */
  Perform(theRange: Message_ProgressRange): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Builds the mesh of a shape with respect of their correctly triangulated parts.
 */
export declare class BRepMesh_IncrementalMesh extends BRepMesh_DiscretRoot {
  constructor();
  constructor(theShape: TopoDS_Shape, theLinDeflection: number);
  constructor(theShape: TopoDS_Shape, theParameters: unknown);
  constructor(theShape: TopoDS_Shape, theParameters: unknown, theRange: Message_ProgressRange);
  constructor(theShape: TopoDS_Shape, theLinDeflection: number, isRelative: boolean);
  constructor(theShape: TopoDS_Shape, theLinDeflection: number, isRelative: boolean, theAngDeflection: number);
  constructor(theShape: TopoDS_Shape, theLinDeflection: number, isRelative?: boolean, theAngDeflection?: number, isInParallel?: boolean);
  Perform(theRange: Message_ProgressRange): void;
  Perform(theContext: unknown, theRange: Message_ProgressRange): void;
  Parameters(): unknown;
  ChangeParameters(): unknown;
  IsModified(): boolean;
  GetStatusFlags(): number;
  static IsParallelDefault(): boolean;
  static SetParallelDefault(isInParallel: boolean): void;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * N-Side Filling This algorithm avoids to build a face from:
 *
 * - a set of edges defining the bounds of the face and some constraints the surface of the face has to satisfy
 * - a set of edges and points defining some constraints the support surface has to satisfy
 * - an initial surface to deform for satisfying the constraints
 * - a set of parameters to control the constraints.
 *
 * The support surface of the face is computed by deformation of the initial surface in order to satisfy the given constraints. The set of bounding edges defines the wire of the face.
 *
 * If no initial surface is given, the algorithm computes it automatically. If the set of edges is not connected (Free constraint) missing edges are automatically computed.
 *
 * Limitations:
 *
 * - If some constraints are not compatible The algorithm does not take them into account. So the constraints will not be satisfyed in an area containing the incompatibilitries.
 * - The constraints defining the bound of the face have to be entered in order to have a continuous wire.
 *
 * Other Applications:
 *
 * - Deformation of a face to satisfy internal constraints
 * - Deformation of a face to improve Gi continuity with connected faces
 */
export declare class BRepOffsetAPI_MakeFilling extends BRepBuilderAPI_MakeShape {
  /**
   * Constructs a wire filling object defined by.
   *
   * - the energy minimizing criterion Degree
   * - the number of points on the curve NbPntsOnCur
   * - the number of iterations NbIter
   * - the Boolean Anisotropie
   * - the 2D tolerance Tol2d
   * - the 3D tolerance Tol3d
   * - the angular tolerance TolAng
   * - the tolerance for curvature TolCur
   * - the highest polynomial degree MaxDeg
   * - the greatest number of segments MaxSeg. If the Boolean Anistropie is true, the algorithm's performance is better in cases where the ratio of the length U and the length V indicate a great difference between the two. In other words, when the surface is, for example, extremely long.
   */
  constructor(Degree?: number, NbPtsOnCur?: number, NbIter?: number, Anisotropie?: boolean, Tol2d?: number, Tol3d?: number, TolAng?: number, TolCurv?: number, MaxDeg?: number, MaxSegments?: number);
  /**
   * Sets the values of Tolerances used to control the constraint. Tol2d: Tol3d: it is the maximum distance allowed between the support surface and the constraints TolAng: it is the maximum angle allowed between the normal of the surface and the constraints TolCurv: it is the maximum difference of curvature allowed between the surface and the constraint.
   */
  SetConstrParam(Tol2d: number, Tol3d: number, TolAng: number, TolCurv: number): void;
  /**
   * Sets the parameters used for resolution. The default values of these parameters have been chosen for a good ratio quality/performance. Degree: it is the order of energy criterion to minimize for computing the deformation of the surface. The default value is 3 The recommended value is i+2 where i is the maximum order of the constraints. NbPtsOnCur: it is the average number of points for discretisation of the edges. NbIter: it is the maximum number of iterations of the process. For each iteration the number of discretisation points is increased. Anisotropie:
   */
  SetResolParam(Degree: number, NbPtsOnCur: number, NbIter: number, Anisotropie: boolean): void;
  /**
   * Sets the parameters used to approximate the filling surface. These include:
   *
   * - MaxDeg - the highest degree which the polynomial defining the filling surface can have
   * - MaxSegments - the greatest number of segments which the filling surface can have.
   */
  SetApproxParam(MaxDeg: number, MaxSegments: number): void;
  /**
   * Loads the initial surface Surf to begin the construction of the surface. This optional function is useful if the surface resulting from construction for the algorithm is likely to be complex.
   * The support surface of the face under construction is computed by a deformation of Surf which satisfies the given constraints. The set of bounding edges defines the wire of the face. If no initial surface is given, the algorithm computes it automatically. If the set of edges is not connected (Free constraint), missing edges are automatically computed.
   * Important: the initial surface must have orthogonal local coordinates, i.e. partial derivatives dS/du and dS/dv must be orthogonal at each point of surface. If this condition breaks, distortions of resulting surface are possible.
   */
  LoadInitSurface(Surf: TopoDS_Face): void;
  /**
   * Adds a punctual constraint.
   */
  Add(Point: gp_Pnt): number;
  /**
   * Adds a free constraint on a face. The corresponding edge has to be automatically recomputed. It is always a bound.
   */
  Add(Support: TopoDS_Face, Order: GeomAbs_Shape): number;
  /**
   * Adds a new constraint which also defines an edge of the wire of the face Order: Order of the constraint: GeomAbs_C0 : the surface has to pass by 3D representation of the edge GeomAbs_G1 : the surface has to pass by 3D representation of the edge and to respect tangency with the first face of the edge GeomAbs_G2 : the surface has to pass by 3D representation of the edge and to respect tangency and curvature with the first face of the edge. Raises ConstructionError if the edge has no representation on a face and Order is GeomAbs_G1 or GeomAbs_G2.
   */
  Add(Constr: TopoDS_Edge, Order: GeomAbs_Shape, IsBound: boolean): number;
  /**
   * Adds a new constraint which also defines an edge of the wire of the face Order: Order of the constraint: GeomAbs_C0 : the surface has to pass by 3D representation of the edge GeomAbs_G1 : the surface has to pass by 3D representation of the edge and to respect tangency with the given face GeomAbs_G2 : the surface has to pass by 3D representation of the edge and to respect tangency and curvature with the given face. Raises ConstructionError if the edge has no 2d representation on the given face.
   */
  Add(Constr: TopoDS_Edge, Support: TopoDS_Face, Order: GeomAbs_Shape, IsBound: boolean): number;
  /**
   * Adds a punctual constraint.
   */
  Add(U: number, V: number, Support: TopoDS_Face, Order: GeomAbs_Shape): number;
  /**
   * Builds the resulting faces.
   */
  Build(theRange: Message_ProgressRange): void;
  /**
   * Tests whether computation of the filling plate has been completed.
   */
  IsDone(): boolean;
  /**
   * Returns the list of shapes generated from the shape .
   */
  Generated(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Returns the maximum distance between the result and the constraints. This is set at construction time.
   */
  G0Error(): number;
  /**
   * Returns the maximum distance attained between the result and the constraint Index. This is set at construction time.
   */
  G0Error(Index: number): number;
  /**
   * Returns the maximum angle between the result and the constraints. This is set at construction time.
   */
  G1Error(): number;
  /**
   * Returns the maximum angle between the result and the constraints. This is set at construction time.
   */
  G1Error(Index: number): number;
  /**
   * Returns the maximum angle between the result and the constraints. This is set at construction time.
   */
  G2Error(): number;
  /**
   * Returns the greatest difference in curvature found between the result and the constraint Index.
   */
  G2Error(Index: number): number;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes functions to build a loft. This is a shell or a solid passing through a set of sections in a given sequence. Usually sections are wires, but the first and the last sections may be vertices (punctual sections).
 */
export declare class BRepOffsetAPI_ThruSections extends BRepBuilderAPI_MakeShape {
  /**
   * Initializes an algorithm for building a shell or a solid passing through a set of sections, where:
   *
   * - isSolid is set to true if the construction algorithm is required to build a solid or to false if it is required to build a shell (the default value),
   * - ruled is set to true if the faces generated between the edges of two consecutive wires are ruled surfaces or to false (the default value) if they are smoothed out by approximation,
   * - pres3d defines the precision criterion used by the approximation algorithm; the default value is 1.0e-6. Use AddWire and AddVertex to define the successive sections of the shell or solid to be built.
   */
  constructor(isSolid?: boolean, ruled?: boolean, pres3d?: number);
  /**
   * Initializes this algorithm for building a shell or a solid passing through a set of sections, where:
   *
   * - isSolid is set to true if this construction algorithm is required to build a solid or to false if it is required to build a shell. false is the default value;
   * - ruled is set to true if the faces generated between the edges of two consecutive wires are ruled surfaces or to false (the default value) if they are smoothed out by approximation,
   * - pres3d defines the precision criterion used by the approximation algorithm; the default value is 1.0e-6. Use AddWire and AddVertex to define the successive sections of the shell or solid to be built.
   */
  Init(isSolid: boolean, ruled: boolean, pres3d: number): void;
  /**
   * Adds the wire wire to the set of sections through which the shell or solid is built. Use the Build function to construct the shape.
   */
  AddWire(wire: TopoDS_Wire): void;
  /**
   * Adds the vertex Vertex (punctual section) to the set of sections through which the shell or solid is built. A vertex may be added to the set of sections only as first or last section. At least one wire must be added to the set of sections by the method AddWire. Use the Build function to construct the shape.
   */
  AddVertex(aVertex: unknown): void;
  /**
   * Sets/unsets the option to compute origin and orientation on wires to avoid twisted results and update wires to have same number of edges.
   */
  CheckCompatibility(check: boolean): void;
  /**
   * Define the approximation algorithm.
   */
  SetSmoothing(UseSmoothing: boolean): void;
  /**
   * Define the type of parametrization used in the approximation.
   */
  SetParType(ParType: unknown): void;
  /**
   * Define the Continuity used in the approximation.
   */
  SetContinuity(C: GeomAbs_Shape): void;
  /**
   * define the Weights associed to the criterium used in the optimization.
   *
   * if Wi <= 0
   */
  SetCriteriumWeight(W1: number, W2: number, W3: number): void;
  /**
   * Define the maximal U degree of result surface.
   */
  SetMaxDegree(MaxDeg: number): void;
  /**
   * returns the type of parametrization used in the approximation
   */
  ParType(): unknown;
  /**
   * returns the Continuity used in the approximation
   */
  Continuity(): GeomAbs_Shape;
  /**
   * returns the maximal U degree of result surface
   */
  MaxDegree(): number;
  /**
   * Define the approximation algorithm.
   */
  UseSmoothing(): boolean;
  /**
   * returns the Weights associed to the criterium used in the optimization.
   * @returns A result object with fields:
   * - `W1`: updated value from the call.
   * - `W2`: updated value from the call.
   * - `W3`: updated value from the call.
   */
  CriteriumWeight(W1: number, W2: number, W3: number): { W1: number; W2: number; W3: number };
  /**
   * This is called by `Shape()`. It does nothing but may be redefined.
   */
  Build(theRange: Message_ProgressRange): void;
  /**
   * Returns the `TopoDS` Shape of the bottom of the loft if solid.
   */
  FirstShape(): TopoDS_Shape;
  /**
   * Returns the `TopoDS` Shape of the top of the loft if solid.
   */
  LastShape(): TopoDS_Shape;
  /**
   * if Ruled Returns the Face generated by each edge except the last wire if smoothed Returns the Face generated by each edge of the first wire
   */
  GeneratedFace(Edge: TopoDS_Shape): TopoDS_Shape;
  /**
   * Sets the mutable input state. If true then the input profile can be modified inside the thrusection operation. Default value is true.
   */
  SetMutableInput(theIsMutableInput: boolean): void;
  /**
   * Returns a list of new shapes generated from the shape S by the shell-generating algorithm. This function is redefined from `BRepBuilderAPI_MakeShape::Generated`. S can be an edge or a vertex of a given Profile (see methods AddWire and AddVertex).
   */
  Generated(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Returns the list of original wires.
   */
  Wires(): NCollection_List_TopoDS_Shape;
  /**
   * Returns the current mutable input state.
   */
  IsMutableInput(): boolean;
  /**
   * Returns the status of thrusection operation.
   */
  GetStatus(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * This class provides for a framework to construct a shell or a solid along a spine consisting in a wire. To produce a solid, the initial wire must be closed. Two approaches are used:
 *
 * - definition by section
 * - by a section and a scaling law
 * - by addition of successive intermediary sections
 * - definition by sweep mode.
 * - pseudo-Frenet
 * - constant
 * - binormal constant
 * - normal defined by a surface support
 * - normal defined by a guiding contour. The two global approaches can also be combined. You can also close the surface later in order to form a solid. Warning: some limitations exist - Mode with auxiliary spine is incompatible with hometetic laws - Mode with auxiliary spine and keep contact produce only CO surface.
 */
export declare class BRepOffsetAPI_MakePipeShell extends BRepPrimAPI_MakeSweep {
  /**
   * Constructs the shell-generating framework defined by the wire Spine. Sets an sweep's mode If no mode are set, the mode use in MakePipe is used.
   */
  constructor(Spine: TopoDS_Wire);
  /**
   * Sets a Frenet or a CorrectedFrenet trihedron to perform the sweeping If IsFrenet is false, a corrected Frenet trihedron is used.
   */
  SetMode(IsFrenet: boolean): void;
  /**
   * Sets a fixed trihedron to perform the sweeping all sections will be parallel.
   */
  SetMode(Axe: gp_Ax2): void;
  /**
   * Sets a fixed BiNormal direction to perform the sweeping. Angular relations between the section(s) and <BiNormal> will be constant.
   */
  SetMode(BiNormal: gp_Dir): void;
  /**
   * Sets support to the spine to define the BiNormal of the trihedron, like the normal to the surfaces. Warning: To be effective, Each edge of the <spine> must have a representation on one face of<SpineSupport>.
   */
  SetMode(SpineSupport: TopoDS_Shape): boolean;
  /**
   * Sets an auxiliary spine to define the Normal For each Point of the Spine P, an Point Q is evaluated on <AuxiliarySpine> If <CurvilinearEquivalence> Q split <AuxiliarySpine> with the same length ratio than P split <Spline>. Else the plan define by P and the tangent to the <Spine> intersect <AuxiliarySpine> in Q. If <KeepContact> equals BRepFill_NoContact: The Normal is defined by the vector PQ.
   * If <KeepContact> equals BRepFill_Contact: The Normal is defined to achieve that the sweeped section is in contact to the auxiliarySpine. The width of section is constant all along the path. In other words, the auxiliary spine lies on the swept surface, but not necessarily is a boundary of this surface.
   * However, the auxiliary spine has to be close enough to the main spine to provide intersection with any section all along the path.
   * If <KeepContact> equals BRepFill_ContactOnBorder: The auxiliary spine becomes a boundary of the swept surface and the width of section varies along the path. Give section to sweep. Possibilities are:
   *
   * - Give one or several section
   * - Give one profile and an homotetic law.
   * - Automatic compute of correspondence between spine, and section on the sweeped shape
   * - correspondence between spine, and section on the sweeped shape defined by a vertex of the spine
   */
  SetMode(AuxiliarySpine: TopoDS_Wire, CurvilinearEquivalence: boolean, KeepContact: unknown): void;
  /**
   * Sets a Discrete trihedron to perform the sweeping.
   */
  SetDiscreteMode(): void;
  /**
   * Adds the section Profile to this framework. First and last sections may be punctual, so the shape Profile may be both wire and vertex. Correspondent point on spine is computed automatically. If WithContact is true, the section is translated to be in contact with the spine. If WithCorrection is true, the section is rotated to be orthogonal to the spine?s tangent in the correspondent point. This option has no sense if the section is punctual (Profile is of type {@link TopoDS_Vertex | `TopoDS_Vertex`}).
   */
  Add(Profile: TopoDS_Shape, WithContact: boolean, WithCorrection: boolean): void;
  /**
   * Adds the section Profile to this framework. Correspondent point on the spine is given by Location. Warning: To be effective, it is not recommended to combine methods Add and SetLaw.
   */
  Add(Profile: TopoDS_Shape, Location: unknown, WithContact: boolean, WithCorrection: boolean): void;
  /**
   * Sets the evolution law defined by the wire Profile with its position (Location, WithContact, WithCorrection are the same options as in methods Add) and a homotetic law defined by the function L. Warning: To be effective, it is not recommended to combine methods Add and SetLaw.
   */
  SetLaw(Profile: TopoDS_Shape, L: unknown, WithContact: boolean, WithCorrection: boolean): void;
  /**
   * Sets the evolution law defined by the wire Profile with its position (Location, WithContact, WithCorrection are the same options as in methods Add) and a homotetic law defined by the function L. Warning: To be effective, it is not recommended to combine methods Add and SetLaw.
   */
  SetLaw(Profile: TopoDS_Shape, L: unknown, Location: unknown, WithContact: boolean, WithCorrection: boolean): void;
  /**
   * Removes the section Profile from this framework.
   */
  Delete(Profile: TopoDS_Shape): void;
  /**
   * Returns true if this tool object is ready to build the shape, i.e. has a definition for the wire section Profile.
   */
  IsReady(): boolean;
  /**
   * Get a status, when Simulate or Build failed. It can be BRepBuilderAPI_PipeDone, BRepBuilderAPI_PipeNotDone, BRepBuilderAPI_PlaneNotIntersectGuide, BRepBuilderAPI_ImpossibleContact.
   */
  GetStatus(): unknown;
  /**
   * Sets the following tolerance values.
   *
   * - 3D tolerance Tol3d
   * - boundary tolerance BoundTol
   * - angular tolerance TolAngular.
   */
  SetTolerance(Tol3d: number, BoundTol: number, TolAngular: number): void;
  /**
   * Define the maximum V degree of resulting surface.
   */
  SetMaxDegree(NewMaxDegree: number): void;
  /**
   * Define the maximum number of spans in V-direction on resulting surface.
   */
  SetMaxSegments(NewMaxSegments: number): void;
  /**
   * Set the flag that indicates attempt to approximate a C1-continuous surface if a swept surface proved to be C0.
   */
  SetForceApproxC1(ForceApproxC1: boolean): void;
  /**
   * Sets the transition mode to manage discontinuities on the swept shape caused by fractures on the spine. The transition mode can be BRepBuilderAPI_Transformed (default value), BRepBuilderAPI_RightCorner, BRepBuilderAPI_RoundCorner:
   *
   * - RepBuilderAPI_Transformed: discontinuities are treated by modification of the sweeping mode. The pipe is "transformed" at the fractures of the spine. This mode assumes building a self-intersected shell.
   * - BRepBuilderAPI_RightCorner: discontinuities are treated like right corner. Two pieces of the pipe corresponding to two adjacent segments of the spine are extended and intersected at a fracture of the spine.
   * - BRepBuilderAPI_RoundCorner: discontinuities are treated like round corner. The corner is treated as rotation of the profile around an axis which passes through the point of the spine's fracture. This axis is based on cross product of directions tangent to the adjacent segments of the spine at their common point.
   * Warnings The mode BRepBuilderAPI_RightCorner provides a valid result if intersection of two pieces of the pipe (corresponding to two adjacent segments of the spine) in the neighborhood of the spine?s fracture is connected and planar. This condition can be violated if the spine is non-linear in some neighborhood of the fracture or if the profile was set with a scaling law. The last mode, BRepBuilderAPI_RoundCorner, will assuredly provide a good result only if a profile was set with option WithCorrection = True, i.e. it is strictly orthogonal to the spine.
   */
  SetTransitionMode(Mode: unknown): void;
  /**
   * Simulates the resulting shape by calculating its cross-sections. The spine is divided by this cross-sections into (NumberOfSection - 1) equal parts, the number of cross-sections is NumberOfSection. The cross-sections are wires and they are returned in the list Result. This gives a rapid preview of the resulting shape, which will be obtained using the settings you have provided. Raises NotDone if <me> it is not Ready.
   * @param Result Mutated in place; read the updated value from this argument after the call.
   */
  Simulate(NumberOfSection: number, Result: NCollection_List_TopoDS_Shape): void;
  /**
   * Builds the resulting shape (redefined from MakeShape).
   */
  Build(theRange: Message_ProgressRange): void;
  /**
   * Transforms the sweeping Shell in Solid. If a propfile is not closed returns False.
   */
  MakeSolid(): boolean;
  /**
   * Returns the `TopoDS` Shape of the bottom of the sweep.
   */
  FirstShape(): TopoDS_Shape;
  /**
   * Returns the `TopoDS` Shape of the top of the sweep.
   */
  LastShape(): TopoDS_Shape;
  /**
   * Returns a list of new shapes generated from the shape S by the shell-generating algorithm. This function is redefined from BRepOffsetAPI_MakeShape::Generated. S can be an edge or a vertex of a given Profile (see methods Add).
   */
  Generated(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  ErrorOnSurface(): number;
  /**
   * Sets the build history flag. If set to True, the pipe shell will store the history of the sections and the spine, which can be used for further modifications or analysis.
   */
  SetIsBuildHistory(theIsBuildHistory: boolean): void;
  /**
   * Returns the build history flag. If True, the pipe shell stores the history of the sections and the spine.
   */
  IsBuildHistory(): boolean;
  /**
   * Returns the list of original profiles.
   * @param theProfiles Mutated in place; read the updated value from this argument after the call.
   */
  Profiles(theProfiles: NCollection_List_TopoDS_Shape): void;
  /**
   * Returns the spine.
   */
  Spine(): TopoDS_Wire;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Provides methods to build edges.
 *
 * The methods have the following syntax, where TheCurve is one of Lin, Circ, ...
 *
 * Create(C : TheCurve)
 *
 * Makes an edge on the whole curve. Add vertices on finite curves.
 *
 * Create(C : TheCurve; p1,p2 : Real)
 *
 * Make an edge on the curve between parameters p1 and p2. if p2 < p1 the edge will be REVERSED. If p1 or p2 is infinite the curve will be open in that direction. Vertices are created for finite values of p1 and p2.
 *
 * Create(C : TheCurve; P1, P2 : Pnt from gp)
 *
 * Make an edge on the curve between the points P1 and P2. The points are projected on the curve and the previous method is used. An error is raised if the points are not on the curve.
 *
 * Create(C : TheCurve; V1, V2 : Vertex from `TopoDS`)
 *
 * Make an edge on the curve between the vertices V1 and V2. Same as the previous but no vertices are created. If a vertex is Null the curve will be open in this direction.
 */
export declare class BRepBuilderAPI_MakeEdge extends BRepBuilderAPI_MakeShape {
  constructor();
  constructor(L: unknown);
  constructor(L: unknown);
  constructor(L: unknown);
  constructor(L: unknown);
  constructor(L: unknown);
  constructor(L: Geom_Curve);
  constructor(V1: unknown, V2: unknown);
  constructor(P1: gp_Pnt, P2: gp_Pnt);
  constructor(L: unknown, S: unknown);
  constructor(L: unknown, p1: number, p2: number);
  constructor(L: unknown, P1: gp_Pnt, P2: gp_Pnt);
  constructor(L: unknown, V1: unknown, V2: unknown);
  constructor(L: unknown, p1: number, p2: number);
  constructor(L: unknown, P1: gp_Pnt, P2: gp_Pnt);
  constructor(L: unknown, V1: unknown, V2: unknown);
  constructor(L: unknown, p1: number, p2: number);
  constructor(L: unknown, P1: gp_Pnt, P2: gp_Pnt);
  constructor(L: unknown, V1: unknown, V2: unknown);
  constructor(L: unknown, p1: number, p2: number);
  constructor(L: unknown, P1: gp_Pnt, P2: gp_Pnt);
  constructor(L: unknown, V1: unknown, V2: unknown);
  constructor(L: unknown, p1: number, p2: number);
  constructor(L: unknown, P1: gp_Pnt, P2: gp_Pnt);
  constructor(L: unknown, V1: unknown, V2: unknown);
  constructor(L: Geom_Curve, p1: number, p2: number);
  constructor(L: Geom_Curve, P1: gp_Pnt, P2: gp_Pnt);
  constructor(L: Geom_Curve, V1: unknown, V2: unknown);
  constructor(L: unknown, S: unknown, p1: number, p2: number);
  constructor(L: unknown, S: unknown, P1: gp_Pnt, P2: gp_Pnt);
  constructor(L: unknown, S: unknown, V1: unknown, V2: unknown);
  constructor(L: Geom_Curve, P1: gp_Pnt, P2: gp_Pnt, p1: number, p2: number);
  constructor(L: Geom_Curve, V1: unknown, V2: unknown, p1: number, p2: number);
  constructor(L: unknown, S: unknown, P1: gp_Pnt, P2: gp_Pnt, p1: number, p2: number);
  /**
   * The general method to directly create an edge is to give.
   *
   * - a 3D curve C as the support (geometric domain) of the edge,
   * - two vertices V1 and V2 to limit the curve (definition of the restriction of the edge), and
   * - two real values p1 and p2 which are the parameters for the vertices V1 and V2 on the curve. The curve may be defined as a 2d curve in the parametric space of a surface: a pcurve. The surface on which the edge is built is then kept at the level of the edge. The default tolerance will be associated with this edge. Rules applied to the arguments: For the curve:
   * - The curve must not be a 'null handle'.
   * - If the curve is a trimmed curve the basis curve is used. For the vertices:
   * - Vertices may be null shapes. When V1 or V2 is null the edge is open in the corresponding direction and the parameter value p1 or p2 must be infinite (remember that `Precision::Infinite()` defines an infinite value).
   * - The two vertices must be identical if they have the same 3D location. Identical vertices are used in particular when the curve is closed. For the parameters:
   * - The parameters must be in the parametric range of the curve (or the basis curve if the curve is trimmed). If this condition is not satisfied the edge is not built, and the Error function will return BRepAPI_ParameterOutOfRange.
   * - Parameter values must not be equal. If this condition is not satisfied (i.e. if | p1 - p2 | ) the edge is not built, and the Error function will return BRepAPI_LineThroughIdenticPoints. Parameter values are expected to be given in increasing order: C->FirstParameter()
   * - If the parameter values are given in decreasing order the vertices are switched, i.e. the "first vertex" is on the point of parameter p2 and the "second vertex" is on the point of parameter p1. In such a case, to keep the original intent of the construction, the edge will be oriented "reversed".
   * - On a periodic curve the parameter values p1 and p2 are adjusted by adding or subtracting the period to obtain p1 in the parametric range of the curve, and p2] such that [ p1 , where Period is the period of the curve.
   * - A parameter value may be infinite. The edge is open in the corresponding direction. However the corresponding vertex must be a null shape. If this condition is not satisfied the edge is not built, and the Error function will return BRepAPI_PointWithInfiniteParameter.
   * - The distance between the vertex and the point evaluated on the curve with the parameter, must be lower than the precision of the vertex. If this condition is not satisfied the edge is not built, and the Error function will return BRepAPI_DifferentsPointAndParameter. Other edge constructions
   * - The parameter values can be omitted, they will be computed by projecting the vertices on the curve. Note that projection is the only way to evaluate the parameter values of the vertices on the curve: vertices must be given on the curve, i.e. the distance from a vertex to the curve must be less than or equal to the precision of the vertex. If this condition is not satisfied the edge is not built, and the Error function will return BRepAPI_PointProjectionFailed.
   * - 3D points can be given in place of vertices. Vertices will be created from the points (with the default topological precision `Precision::Confusion()`). Note:
   * - Giving vertices is useful when creating a connected edge.
   * - If the parameter values correspond to the extremities of a closed curve, points must be identical, or at least coincident. If this condition is not satisfied the edge is not built, and the Error function will return BRepAPI_DifferentPointsOnClosedCurve.
   * - The vertices or points can be omitted if the parameter values are given. The points will be computed from the parameters on the curve. The vertices or points and the parameter values can be omitted. The first and last parameters of the curve will then be used.
   *
   * Auxiliary methods
   */
  constructor(L: unknown, S: unknown, V1: unknown, V2: unknown, p1: number, p2: number);
  Init(C: Geom_Curve): void;
  Init(C: unknown, S: unknown): void;
  Init(C: Geom_Curve, p1: number, p2: number): void;
  Init(C: Geom_Curve, P1: gp_Pnt, P2: gp_Pnt): void;
  Init(C: Geom_Curve, V1: unknown, V2: unknown): void;
  Init(C: unknown, S: unknown, p1: number, p2: number): void;
  Init(C: unknown, S: unknown, P1: gp_Pnt, P2: gp_Pnt): void;
  Init(C: unknown, S: unknown, V1: unknown, V2: unknown): void;
  Init(C: Geom_Curve, P1: gp_Pnt, P2: gp_Pnt, p1: number, p2: number): void;
  Init(C: Geom_Curve, V1: unknown, V2: unknown, p1: number, p2: number): void;
  Init(C: unknown, S: unknown, P1: gp_Pnt, P2: gp_Pnt, p1: number, p2: number): void;
  /**
   * Defines or redefines the arguments for the construction of an edge. This function is currently used after the empty constructor BRepAPI_MakeEdge().
   */
  Init(C: unknown, S: unknown, V1: unknown, V2: unknown, p1: number, p2: number): void;
  /**
   * Returns true if the edge is built.
   */
  IsDone(): boolean;
  /**
   * Returns the construction status.
   *
   * - BRepBuilderAPI_EdgeDone if the edge is built, or
   * - another value of the `BRepBuilderAPI_EdgeError` enumeration indicating the reason of construction failure.
   */
  Error(): unknown;
  /**
   * Returns the constructed edge. Exceptions StdFail_NotDone if the edge is not built.
   */
  Edge(): TopoDS_Edge;
  /**
   * Returns the first vertex of the edge. May be Null.
   */
  Vertex1(): unknown;
  /**
   * Returns the second vertex of the edge. May be Null.
   *
   * Warning The returned vertex in each function corresponds respectively to
   *
   * - the lowest, or
   * - the highest parameter on the curve along which the edge is built. It does not correspond to the first or second vertex given at the time of the construction, if the edge is oriented reversed. Exceptions StdFail_NotDone if the edge is not built.
   */
  Vertex2(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Implements the methods of MakeShape for the constant topology modifications. The methods are implemented when the modification uses a Modifier from {@link BRepTools | `BRepTools`}. Some of them have to be redefined if the modification is implemented with another tool (see Transform from {@link BRepBuilderAPI | `BRepBuilderAPI`} for example). The {@link BRepBuilderAPI | `BRepBuilderAPI`} package provides the following frameworks to perform modifications of this sort:
 *
 * - {@link BRepBuilderAPI_Copy | `BRepBuilderAPI_Copy`} to produce the copy of a shape,
 * - {@link BRepBuilderAPI_Transform | `BRepBuilderAPI_Transform`} and {@link BRepBuilderAPI_GTransform | `BRepBuilderAPI_GTransform`} to apply a geometric transformation to a shape,
 * - {@link BRepBuilderAPI_NurbsConvert | `BRepBuilderAPI_NurbsConvert`} to convert the whole geometry of a shape into NURBS geometry,
 * - {@link BRepOffsetAPI_DraftAngle | `BRepOffsetAPI_DraftAngle`} to build a tapered shape.
 */
export declare class BRepBuilderAPI_ModifyShape extends BRepBuilderAPI_MakeShape {
  /**
   * Returns the list of shapes modified from the shape .
   */
  Modified(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Returns the modified shape corresponding to . S can correspond to the entire initial shape or to its subshape. Exceptions Standard_NoSuchObject if S is not the initial shape or a subshape of the initial shape to which the transformation has been applied. Raises NoSuchObject from {@link Standard | `Standard`} if S is not the initial shape or a sub-shape of the initial shape.
   */
  ModifiedShape(S: TopoDS_Shape): TopoDS_Shape;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * This is the root class for all shape constructions. It stores the result.
 *
 * It provides deferred methods to trace the history of sub-shapes.
 */
export declare class BRepBuilderAPI_MakeShape extends BRepBuilderAPI_Command {
  /**
   * This is called by `Shape()`. It does nothing but may be redefined.
   */
  Build(theRange: Message_ProgressRange): void;
  /**
   * Returns a shape built by the shape construction algorithm. Raises exception StdFail_NotDone if the shape was not built.
   */
  Shape(): TopoDS_Shape;
  /**
   * Returns the list of shapes generated from the shape .
   */
  Generated(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Returns the list of shapes modified from the shape .
   */
  Modified(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Returns true if the shape S has been deleted.
   */
  IsDeleted(S: TopoDS_Shape): boolean;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes functions to build wires from edges. A wire can be built from any number of edges. To build a wire you first initialize the construction, then add edges in sequence. An unlimited number of edges can be added. The initialization of construction is done with:
 *
 * - no edge (an empty wire), or
 * - edges of an existing wire, or
 * - up to four connectable edges. In order to be added to a wire under construction, an edge (unless it is the first one) must satisfy the following condition: one of its vertices must be geometrically coincident with one of the vertices of the wire (provided that the highest tolerance factor is assigned to the two vertices). It could also be the same vertex.
 * - The given edge is shared by the wire if it contains:
 * - two vertices, identical to two vertices of the wire under construction (a general case of the wire closure), or
 * - one vertex, identical to a vertex of the wire under construction; the other vertex not being geometrically coincident with another vertex of the wire.
 * - In other cases, when one of the vertices of the edge is simply geometrically coincident with a vertex of the wire under construction (provided that the highest tolerance factor is assigned to the two vertices), the given edge is first copied and the coincident vertex is replaced in this new edge, by the coincident vertex of the wire. Note: it is possible to build non manifold wires using this construction tool. A MakeWire object provides a framework for:
 * - initializing the construction of a wire,
 * - adding edges to the wire under construction, and
 * - consulting the result.
 */
export declare class BRepBuilderAPI_MakeWire extends BRepBuilderAPI_MakeShape {
  /**
   * Constructs an empty wire framework, to which edges are added using the Add function. As soon as the wire contains one edge, it can return with the use of the function Wire. Warning The function Error will return BRepBuilderAPI_EmptyWire if it is called before at least one edge is added to the wire under construction.
   */
  constructor();
  /**
   * Make a Wire from an edge.
   */
  constructor(E: TopoDS_Edge);
  /**
   * Make a Wire from a Wire. useful for adding later.
   */
  constructor(W: TopoDS_Wire);
  /**
   * Make a Wire from two edges.
   */
  constructor(E1: TopoDS_Edge, E2: TopoDS_Edge);
  /**
   * Add an edge to a wire.
   */
  constructor(W: TopoDS_Wire, E: TopoDS_Edge);
  /**
   * Make a Wire from three edges.
   */
  constructor(E1: TopoDS_Edge, E2: TopoDS_Edge, E3: TopoDS_Edge);
  /**
   * Make a Wire from four edges. Constructs a wire.
   *
   * - from the {@link TopoDS_Wire | `TopoDS_Wire`} W composed of the edge E, or
   * - from edge E, or
   * - from two edges E1 and E2, or
   * - from three edges E1, E2 and E3, or
   * - from four edges E1, E2, E3 and E4. Further edges can be added using the function Add. Given edges are added in a sequence.
   * Each of them must be connectable to the wire under construction, and so must satisfy the following condition (unless it is the first edge of the wire): one of its vertices must be geometrically coincident with one of the vertices of the wire (provided that the highest tolerance factor is assigned to the two vertices). It could also be the same vertex. Warning If an edge is not connectable to the wire under construction it is not added.
   * The function Error will return BRepBuilderAPI_DisconnectedWire, the function IsDone will return false and the function Wire will raise an error, until a new connectable edge is added.
   */
  constructor(E1: TopoDS_Edge, E2: TopoDS_Edge, E3: TopoDS_Edge, E4: TopoDS_Edge);
  /**
   * Adds the edge E to the wire under construction.
   * E must be connectable to the wire under construction, and, unless it is the first edge of the wire, must satisfy the following condition: one of its vertices must be geometrically coincident with one of the vertices of the wire (provided that the highest tolerance factor is assigned to the two vertices). It could also be the same vertex. Warning If E is not connectable to the wire under construction it is not added.
   * The function Error will return BRepBuilderAPI_DisconnectedWire, the function IsDone will return false and the function Wire will raise an error, until a new connectable edge is added.
   */
  Add(E: TopoDS_Edge): void;
  /**
   * Add the edges of <W> to the current wire.
   */
  Add(W: TopoDS_Wire): void;
  /**
   * Adds the edges of <L> to the current wire. The edges are not to be consecutive. But they are to be all connected geometrically or topologically. If some of them are not connected the Status give DisconnectedWire but the "Maker" is `Done()` and you can get the partial result. (i.e. connected to the first edgeof the list <L>).
   */
  Add(L: NCollection_List_TopoDS_Shape): void;
  /**
   * Returns true if this algorithm contains a valid wire. IsDone returns false if:
   *
   * - there are no edges in the wire, or
   * - the last edge which you tried to add was not connectable.
   */
  IsDone(): boolean;
  /**
   * Returns the construction status.
   *
   * - BRepBuilderAPI_WireDone if the wire is built, or
   * - another value of the `BRepBuilderAPI_WireError` enumeration indicating why the construction failed.
   */
  Error(): unknown;
  /**
   * Returns the constructed wire; or the part of the wire under construction already built. Exceptions StdFail_NotDone if a wire is not built.
   */
  Wire(): TopoDS_Wire;
  /**
   * Returns the last edge added to the wire under construction. Warning.
   *
   * - This edge can be different from the original one (the argument of the function Add, for instance,)
   * - A null edge is returned if there are no edges in the wire under construction, or if the last edge which you tried to add was not connectable..
   */
  Edge(): TopoDS_Edge;
  /**
   * Returns the last vertex of the last edge added to the wire under construction. Warning A null vertex is returned if there are no edges in the wire under construction, or if the last edge which you tried to add was not connectableR.
   */
  Vertex(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Root class for all commands in {@link BRepBuilderAPI | `BRepBuilderAPI`}.
 *
 * Provides :
 *
 * - Managements of the notDone flag.
 * - Catching of exceptions (not implemented).
 * - Logging (not implemented).
 */
export declare class BRepBuilderAPI_Command {
  IsDone(): boolean;
  /**
   * Raises NotDone if done is false.
   */
  Check(): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

export type ChFi3d_FilletShape = typeof ChFi3d_FilletShape[keyof typeof ChFi3d_FilletShape];
/**
 * Lists the types of fillet shapes. These include the following:
 *
 * - ChFi3d_Rational (default value), which is the standard NURBS representation of circles,
 * - ChFi3d_QuasiAngular, which is a NURBS representation of circles where the parameters match those of the circle,
 * - ChFi3d_Polynomial, which corresponds to a polynomial approximation of circles. This type facilitates the implementation of the construction algorithm.
 */
export declare const ChFi3d_FilletShape: {
  readonly ChFi3d_Rational: 'ChFi3d_Rational';
  readonly ChFi3d_QuasiAngular: 'ChFi3d_QuasiAngular';
  readonly ChFi3d_Polynomial: 'ChFi3d_Polynomial';
};

/**
 * Describes functions to build fillets on the broken edges of a shell or solid. A MakeFillet object provides a framework for:
 *
 * - initializing the construction algorithm with a given shape,
 * - acquiring the data characterizing the fillets,
 * - building the fillets and constructing the resulting shape, and
 * - consulting the result.
 */
export declare class BRepFilletAPI_MakeFillet extends BRepFilletAPI_LocalOperation {
  /**
   * Initializes the computation of the fillets. <FShape> sets the type of fillet surface. The default value is ChFi3d_Rational (classical nurbs representation of circles). ChFi3d_QuasiAngular corresponds to a nurbs representation of circles which parameterisation matches the circle one. ChFi3d_Polynomial corresponds to a polynomial representation of circles.
   */
  constructor(S: TopoDS_Shape, FShape?: ChFi3d_FilletShape);
  SetParams(Tang: number, Tesp: number, T2d: number, TApp3d: number, TolApp2d: number, Fleche: number): void;
  /**
   * Changes the parameters of continiuity InternalContinuity to produce fillet'surfaces with an continuity Ci (i=0,1 or 2). By defaultInternalContinuity = GeomAbs_C1. AngularTolerance is the G1 tolerance between fillet and support'faces.
   */
  SetContinuity(InternalContinuity: GeomAbs_Shape, AngularTolerance: number): void;
  /**
   * Adds a fillet contour in the builder (builds a contour of tangent edges). The Radius must be set after.
   */
  Add(E: TopoDS_Edge): void;
  /**
   * Adds a fillet description in the builder.
   *
   * - builds a contour of tangent edges,
   * - sets the radius.
   */
  Add(Radius: number, E: TopoDS_Edge): void;
  /**
   * Adds a fillet description in the builder.
   *
   * - builds a contour of tangent edges,
   * - sest the radius evolution law.
   */
  Add(L: unknown, E: TopoDS_Edge): void;
  /**
   * Adds a fillet description in the builder.
   *
   * - builds a contour of tangent edges,
   * - sets the radius evolution law interpolating the values given in the array UandR :
   *
   * p2d.X() = relative parameter on the spine [0,1] p2d.Y() = value of the radius.
   */
  Add(UandR: NCollection_Array1_gp_Pnt2d, E: TopoDS_Edge): void;
  /**
   * Adds a fillet description in the builder.
   *
   * - builds a contour of tangent edges,
   * - sets a linear radius evolution law between the first and last vertex of the spine.
   */
  Add(R1: number, R2: number, E: TopoDS_Edge): void;
  /**
   * Sets the parameters of the fillet along the contour of index IC generated using the Add function in the internal data structure of this algorithm, where Radius is the radius of the fillet.
   */
  SetRadius(Radius: number, IC: number, IinC: number): void;
  /**
   * Sets the parameters of the fillet along the contour of index IC generated using the Add function in the internal data structure of this algorithm, where the radius of the fillet evolves according to the evolution law L, between the first and last vertices of the contour of index IC.
   */
  SetRadius(L: unknown, IC: number, IinC: number): void;
  /**
   * Sets the parameters of the fillet along the contour of index IC generated using the Add function in the internal data structure of this algorithm, where the radius of the fillet evolves according to the evolution law which interpolates the set of parameter and radius pairs given in the array UandR as follows:
   *
   * - the X coordinate of a point in UandR defines a relative parameter on the contour (i.e. a parameter between 0 and 1),
   * - the Y coordinate of a point in UandR gives the corresponding value of the radius, and the radius evolves between the first and last vertices of the contour of index IC.
   */
  SetRadius(UandR: NCollection_Array1_gp_Pnt2d, IC: number, IinC: number): void;
  /**
   * Assigns Radius as the radius of the fillet on the edge E.
   */
  SetRadius(Radius: number, IC: number, E: TopoDS_Edge): void;
  SetRadius(Radius: number, IC: number, V: unknown): void;
  /**
   * Sets the parameters of the fillet along the contour of index IC generated using the Add function in the internal data structure of this algorithm, where the radius of the fillet evolves according to a linear evolution law defined from R1 to R2, between the first and last vertices of the contour of index IC.
   */
  SetRadius(R1: number, R2: number, IC: number, IinC: number): void;
  /**
   * Erases the radius information on the contour of index IC in the internal data structure of this algorithm. Use the SetRadius function to reset this data. Warning Nothing is done if IC is outside the bounds of the table of contours.
   */
  ResetContour(IC: number): void;
  /**
   * Returns true if the radius of the fillet along the contour of index IC in the internal data structure of this algorithm is constant, Warning False is returned if IC is outside the bounds of the table of contours or if E does not belong to the contour of index IC.
   */
  IsConstant(IC: number): boolean;
  /**
   * Returns true if the radius of the fillet along the edge E of the contour of index IC in the internal data structure of this algorithm is constant. Warning False is returned if IC is outside the bounds of the table of contours or if E does not belong to the contour of index IC.
   */
  IsConstant(IC: number, E: TopoDS_Edge): boolean;
  /**
   * Returns the radius of the fillet along the contour of index IC in the internal data structure of this algorithm Warning.
   *
   * - Use this function only if the radius is constant.
   * - -1. is returned if IC is outside the bounds of the table of contours or if E does not belong to the contour of index IC.
   */
  Radius(IC: number): number;
  /**
   * Returns the radius of the fillet along the edge E of the contour of index IC in the internal data structure of this algorithm. Warning.
   *
   * - Use this function only if the radius is constant.
   * - -1 is returned if IC is outside the bounds of the table of contours or if E does not belong to the contour of index IC.
   */
  Radius(IC: number, E: TopoDS_Edge): number;
  GetBounds(IC: number, E: TopoDS_Edge, F: number, L: number): { returnValue: boolean; F: number; L: number };
  GetLaw(IC: number, E: TopoDS_Edge): unknown;
  SetLaw(IC: number, E: TopoDS_Edge, L: unknown): void;
  /**
   * Assigns FShape as the type of fillet shape built by this algorithm.
   */
  SetFilletShape(FShape: ChFi3d_FilletShape): void;
  /**
   * Returns the type of fillet shape built by this algorithm.
   */
  GetFilletShape(): ChFi3d_FilletShape;
  /**
   * Returns the number of contours generated using the Add function in the internal data structure of this algorithm.
   */
  NbContours(): number;
  /**
   * Returns the index of the contour in the internal data structure of this algorithm which contains the edge E of the shape. This function returns 0 if the edge E does not belong to any contour. Warning This index can change if a contour is removed from the internal data structure of this algorithm using the function Remove.
   */
  Contour(E: TopoDS_Edge): number;
  /**
   * Returns the number of edges in the contour of index I in the internal data structure of this algorithm. Warning Returns 0 if I is outside the bounds of the table of contours.
   */
  NbEdges(I: number): number;
  /**
   * Returns the edge of index J in the contour of index I in the internal data structure of this algorithm. Warning Returns a null shape if:
   *
   * - I is outside the bounds of the table of contours, or
   * - J is outside the bounds of the table of edges of the index I contour.
   */
  Edge(I: number, J: number): TopoDS_Edge;
  /**
   * Removes the contour in the internal data structure of this algorithm which contains the edge E of the shape. Warning Nothing is done if the edge E does not belong to the contour in the internal data structure of this algorithm.
   */
  Remove(E: TopoDS_Edge): void;
  /**
   * Returns the length of the contour of index IC in the internal data structure of this algorithm. Warning Returns -1. if IC is outside the bounds of the table of contours.
   */
  Length(IC: number): number;
  /**
   * Returns the first vertex of the contour of index IC in the internal data structure of this algorithm. Warning Returns a null shape if IC is outside the bounds of the table of contours.
   */
  FirstVertex(IC: number): unknown;
  /**
   * Returns the last vertex of the contour of index IC in the internal data structure of this algorithm. Warning Returns a null shape if IC is outside the bounds of the table of contours.
   */
  LastVertex(IC: number): unknown;
  /**
   * Returns the curvilinear abscissa of the vertex V on the contour of index IC in the internal data structure of this algorithm. Warning Returns -1. if:
   *
   * - IC is outside the bounds of the table of contours, or
   * - V is not on the contour of index IC.
   */
  Abscissa(IC: number, V: unknown): number;
  /**
   * Returns the relative curvilinear abscissa (i.e. between 0 and 1) of the vertex V on the contour of index IC in the internal data structure of this algorithm. Warning Returns -1. if:
   *
   * - IC is outside the bounds of the table of contours, or
   * - V is not on the contour of index IC.
   */
  RelativeAbscissa(IC: number, V: unknown): number;
  /**
   * Returns true if the contour of index IC in the internal data structure of this algorithm is closed and tangential at the point of closure. Warning Returns false if IC is outside the bounds of the table of contours.
   */
  ClosedAndTangent(IC: number): boolean;
  /**
   * Returns true if the contour of index IC in the internal data structure of this algorithm is closed. Warning Returns false if IC is outside the bounds of the table of contours.
   */
  Closed(IC: number): boolean;
  /**
   * Builds the fillets on all the contours in the internal data structure of this algorithm and constructs the resulting shape. Use the function IsDone to verify that the filleted shape is built. Use the function Shape to retrieve the filleted shape. Warning The construction of fillets implements highly complex construction algorithms.
   * Consequently, there may be instances where the algorithm fails, for example if the data defining the radius of the fillet is not compatible with the geometry of the initial shape. There is no initial analysis of errors and they only become evident at the construction stage. Additionally, in the current software release, the following cases are not handled:
   *
   * - the end point of the contour is the point of intersection of 4 or more edges of the shape, or
   * - the intersection of the fillet with a face which limits the contour is not fully contained in this face.
   */
  Build(theRange: Message_ProgressRange): void;
  /**
   * Reinitializes this algorithm, thus canceling the effects of the Build function. This function allows modifications to be made to the contours and fillet parameters in order to rebuild the shape.
   */
  Reset(): void;
  /**
   * Returns the internal topology building algorithm.
   */
  Builder(): unknown;
  /**
   * Returns the list of shapes generated from the shape <EorV>.
   */
  Generated(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Returns the list of shapes modified from the shape <F>.
   */
  Modified(S: TopoDS_Shape): NCollection_List_TopoDS_Shape;
  /**
   * Returns true if the shape S has been deleted.
   */
  IsDeleted(S: TopoDS_Shape): boolean;
  /**
   * returns the number of surfaces after the shape creation.
   */
  NbSurfaces(): number;
  /**
   * Return the faces created for surface *.*
   */
  NewFaces(I: number): NCollection_List_TopoDS_Shape;
  Simulate(IC: number): void;
  NbSurf(IC: number): number;
  Sect(IC: number, IS: number): NCollection_HArray1_ChFiDS_CircSection;
  /**
   * Returns the number of contours where the computation of the fillet failed.
   */
  NbFaultyContours(): number;
  /**
   * for each I in [1.. NbFaultyContours] returns the index IC of the contour where the computation of the fillet failed. the method NbEdges(IC) gives the number of edges in the contour IC the method Edge(IC,ie) gives the edge number ie of the contour IC
   */
  FaultyContour(I: number): number;
  /**
   * returns the number of surfaces which have been computed on the contour IC
   */
  NbComputedSurfaces(IC: number): number;
  /**
   * returns the surface number IS concerning the contour IC
   */
  ComputedSurface(IC: number, IS: number): unknown;
  /**
   * returns the number of vertices where the computation failed
   */
  NbFaultyVertices(): number;
  /**
   * returns the vertex where the computation failed
   */
  FaultyVertex(IV: number): unknown;
  /**
   * returns true if a part of the result has been computed if the filling in a corner failed a shape with a hole is returned
   */
  HasResult(): boolean;
  /**
   * if (`HasResult()`) returns the partial result
   */
  BadShape(): TopoDS_Shape;
  /**
   * returns the status concerning the contour IC in case of error ChFiDS_Ok : the computation is Ok ChFiDS_StartsolFailure : the computation can't start, perhaps the the radius is too big ChFiDS_TwistedSurface : the computation failed because of a twisted surface ChFiDS_WalkingFailure : there is a problem in the walking ChFiDS_Error: other error different from above
   */
  StripeStatus(IC: number): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Construction of fillets on the edges of a Shell.
 */
export declare class BRepFilletAPI_LocalOperation extends BRepBuilderAPI_MakeShape {
  /**
   * Adds a contour in the builder (builds a contour of tangent edges).
   */
  Add(E: TopoDS_Edge): void;
  /**
   * Reset the contour of index IC, there is nomore information in the contour.
   */
  ResetContour(IC: number): void;
  /**
   * Number of contours.
   */
  NbContours(): number;
  /**
   * Returns the index of the contour containing the edge E, returns 0 if E doesn't belong to any contour.
   */
  Contour(E: TopoDS_Edge): number;
  /**
   * Number of Edges in the contour I.
   */
  NbEdges(I: number): number;
  /**
   * Returns the Edge J in the contour I.
   */
  Edge(I: number, J: number): TopoDS_Edge;
  /**
   * remove the contour containing the Edge E.
   */
  Remove(E: TopoDS_Edge): void;
  /**
   * returns the length the contour of index IC.
   */
  Length(IC: number): number;
  /**
   * Returns the first Vertex of the contour of index IC.
   */
  FirstVertex(IC: number): unknown;
  /**
   * Returns the last Vertex of the contour of index IC.
   */
  LastVertex(IC: number): unknown;
  /**
   * returns the abscissa of the vertex V on the contour of index IC.
   */
  Abscissa(IC: number, V: unknown): number;
  /**
   * returns the relative abscissa([0.,1.]) of the vertex V on the contour of index IC.
   */
  RelativeAbscissa(IC: number, V: unknown): number;
  /**
   * returns true if the contour of index IC is closed an tangent.
   */
  ClosedAndTangent(IC: number): boolean;
  /**
   * returns true if the contour of index IC is closed
   */
  Closed(IC: number): boolean;
  /**
   * Reset all the fields updated by Build operation and leave the algorithm in the same state than before build call. It allows contours and radius modifications to build the result another time.
   */
  Reset(): void;
  Simulate(IC: number): void;
  NbSurf(IC: number): number;
  Sect(IC: number, IS: number): NCollection_HArray1_ChFiDS_CircSection;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

export type TopAbs_ShapeEnum = typeof TopAbs_ShapeEnum[keyof typeof TopAbs_ShapeEnum];
/**
 * Identifies various topological shapes. This enumeration allows you to use dynamic typing of shapes. The values are listed in order of complexity, from the most complex to the most simple i.e. COMPOUND > COMPSOLID > SOLID > .... > VERTEX > SHAPE. Any shape can contain simpler shapes in its definition. Abstract topological data structure describes a basic entity, the shape (present in this enumeration as the SHAPE value), which can be divided into the following component topologies:
 *
 * - COMPOUND: A group of any of the shapes below.
 * - COMPSOLID: A set of solids connected by their faces. This expands the notions of WIRE and SHELL to solids.
 * - SOLID: A part of 3D space bounded by shells.
 * - SHELL: A set of faces connected by some of the edges of their wire boundaries. A shell can be open or closed.
 * - FACE: Part of a plane (in 2D geometry) or a surface (in 3D geometry) bounded by a closed wire. Its geometry is constrained (trimmed) by contours.
 * - WIRE: A sequence of edges connected by their vertices. It can be open or closed depending on whether the edges are linked or not.
 * - EDGE: A single dimensional shape corresponding to a curve, and bound by a vertex at each extremity.
 * - VERTEX: A zero-dimensional shape corresponding to a point in geometry.
 */
export declare const TopAbs_ShapeEnum: {
  readonly TopAbs_COMPOUND: 'TopAbs_COMPOUND';
  readonly TopAbs_COMPSOLID: 'TopAbs_COMPSOLID';
  readonly TopAbs_SOLID: 'TopAbs_SOLID';
  readonly TopAbs_SHELL: 'TopAbs_SHELL';
  readonly TopAbs_FACE: 'TopAbs_FACE';
  readonly TopAbs_WIRE: 'TopAbs_WIRE';
  readonly TopAbs_EDGE: 'TopAbs_EDGE';
  readonly TopAbs_VERTEX: 'TopAbs_VERTEX';
  readonly TopAbs_SHAPE: 'TopAbs_SHAPE';
};

/**
 * Describes a circle in 3D space. A circle is defined by its radius and, as with any conic curve, is positioned in space with a right-handed coordinate system ({@link gp_Ax2 | `gp_Ax2`} object) where:
 *
 * - the origin is the center of the circle, and
 * - the origin, "X Direction" and "Y Direction" define the plane of the circle. This coordinate system is the local coordinate system of the circle. The "main Direction" of this coordinate system is the vector normal to the plane of the circle. The axis, of which the origin and unit vector are respectively the origin and "main Direction" of the local coordinate system, is termed the "Axis" or "main Axis" of the circle.
 * The "main Direction" of the local coordinate system gives an explicit orientation to the circle (definition of the trigonometric sense), determining the direction in which the parameter increases along the circle. The {@link Geom_Circle | `Geom_Circle`} circle is parameterized by an angle: P(U) = O + R*std::cos(U)*XDir + R*Sin(U)*YDir, where:
 * - P is the point of parameter U,
 * - O, XDir and YDir are respectively the origin, "X Direction" and "Y Direction" of its local coordinate system,
 * - R is the radius of the circle. The "X Axis" of the local coordinate system therefore defines the origin of the parameter of the circle. The parameter is the angle with this "X Direction". A circle is a closed and periodic curve. The period is 2.*Pi and the parameter range is [ 0, 2.*Pi [.
 */
export declare class Geom_Circle extends Geom_Conic {
  /**
   * Constructs a circle by conversion of the {@link gp_Circ | `gp_Circ`} circle C.
   */
  constructor(C: unknown);
  /**
   * Constructs a circle of radius Radius, where A2 locates the circle and defines its orientation in 3D space such that:
   *
   * - the center of the circle is the origin of A2,
   * - the origin, "X Direction" and "Y Direction" of A2 define the plane of the circle,
   * - A2 is the local coordinate system of the circle. Note: It is possible to create a circle where Radius is equal to 0.0. raised if Radius < 0.
   */
  constructor(A2: gp_Ax2, Radius: number);
  /**
   * Set <me> so that <me> has the same geometric properties as C.
   */
  SetCirc(C: unknown): void;
  /**
   * Assigns the value R to the radius of this circle. Note: it is possible to have a circle with a radius equal to 0.0. Exceptions - Standard_ConstructionError if R is negative.
   */
  SetRadius(R: number): void;
  /**
   * returns the non transient circle from gp with the same geometric properties as <me>.
   */
  Circ(): unknown;
  /**
   * Returns the radius of this circle.
   */
  Radius(): number;
  /**
   * Computes the parameter on the reversed circle for the point of parameter U on this circle. For a circle, the returned value is: 2.*Pi - U.
   */
  ReversedParameter(U: number): number;
  /**
   * Returns the eccentricity e = 0 for a circle.
   */
  Eccentricity(): number;
  /**
   * Returns the value of the first parameter of this circle. This is 0.0, which gives the start point of this circle, or The start point and end point of a circle are coincident.
   */
  FirstParameter(): number;
  /**
   * Returns the value of the last parameter of this circle. This is 2.*Pi, which gives the end point of this circle. The start point and end point of a circle are coincident.
   */
  LastParameter(): number;
  /**
   * returns True.
   */
  IsClosed(): boolean;
  /**
   * returns True.
   */
  IsPeriodic(): boolean;
  /**
   * Returns the point of parameter U. P = C + R * Cos (U) * XDir + R * Sin (U) * YDir where C is the center of the circle , XDir the XDirection and YDir the YDirection of the circle's local coordinate system.
   */
  EvalD0(U: number): gp_Pnt;
  /**
   * Returns the point of parameter U and the first derivative.
   */
  EvalD1(U: number): Geom_Curve_ResD1;
  /**
   * Returns the point of parameter U, the first and second derivatives.
   */
  EvalD2(U: number): Geom_Curve_ResD2;
  /**
   * Returns the point of parameter U, the first, second and third derivatives.
   */
  EvalD3(U: number): Geom_Curve_ResD3;
  /**
   * Returns the vector corresponding to the derivative for the order of derivation N. Raised if N < 1.
   */
  EvalDN(U: number, N: number): unknown;
  /**
   * Applies the transformation T to this circle.
   */
  Transform(T: unknown): void;
  /**
   * Creates a new object which is a copy of this circle.
   */
  Copy(): Geom_Geometry;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The abstract class Geometry for 3D space is the root class of all geometric objects from the Geom package. It describes the common behavior of these objects when:
 *
 * - applying geometric transformations to objects, and
 * - constructing objects by geometric transformation (including copying).
 * Warning Only transformations which do not modify the nature of the geometry can be applied to Geom objects: this is the case with translations, rotations, symmetries and scales; this is also the case with {@link gp_Trsf | `gp_Trsf`} composite transformations which are used to define the geometric transformations applied using the Transform or Transformed functions.
 * Note: Geometry defines the "prototype" of the abstract method Transform which is defined for each concrete type of derived object. All other transformations are implemented using the Transform method.
 */
export declare class Geom_Geometry extends Standard_Transient {
  /**
   * Performs the symmetrical transformation of a Geometry with respect to the point P which is the center of the symmetry.
   */
  Mirror(P: gp_Pnt): void;
  /**
   * Performs the symmetrical transformation of a Geometry with respect to an axis placement which is the axis of the symmetry.
   */
  Mirror(A1: unknown): void;
  /**
   * Performs the symmetrical transformation of a Geometry with respect to a plane. The axis placement A2 locates the plane of the symmetry : (Location, XDirection, YDirection).
   */
  Mirror(A2: gp_Ax2): void;
  /**
   * Rotates a Geometry. A1 is the axis of the rotation. Ang is the angular value of the rotation in radians.
   */
  Rotate(A1: unknown, Ang: number): void;
  /**
   * Scales a Geometry. S is the scaling value.
   */
  Scale(P: gp_Pnt, S: number): void;
  /**
   * Translates a Geometry. V is the vector of the translation.
   */
  Translate(V: unknown): void;
  /**
   * Translates a Geometry from the point P1 to the point P2.
   */
  Translate(P1: gp_Pnt, P2: gp_Pnt): void;
  /**
   * Transformation of a geometric object. This transformation can be a translation, a rotation, a symmetry, a scaling or a complex transformation obtained by combination of the previous elementaries transformations. (see class Transformation of the package Geom).
   */
  Transform(T: unknown): void;
  Mirrored(P: gp_Pnt): Geom_Geometry;
  Mirrored(A1: unknown): Geom_Geometry;
  Mirrored(A2: gp_Ax2): Geom_Geometry;
  Rotated(A1: unknown, Ang: number): Geom_Geometry;
  Scaled(P: gp_Pnt, S: number): Geom_Geometry;
  Transformed(T: unknown): Geom_Geometry;
  Translated(V: unknown): Geom_Geometry;
  Translated(P1: gp_Pnt, P2: gp_Pnt): Geom_Geometry;
  /**
   * Creates a new object which is a copy of this geometric object.
   */
  Copy(): Geom_Geometry;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * The abstract class Curve describes the common behavior of curves in 3D space. The Geom package provides numerous concrete classes of derived curves, including lines, circles, conics, Bezier or BSpline curves, etc. The main characteristic of these curves is that they are parameterized. The {@link Geom_Curve | `Geom_Curve`} class shows:
 *
 * - how to work with the parametric equation of a curve in order to calculate the point of parameter u, together with the vector tangent and the derivative vectors of order 2, 3,..., N at this point;
 * - how to obtain general information about the curve (for example, level of continuity, closed characteristics, periodicity, bounds of the parameter field);
 * - how the parameter changes when a geometric transformation is applied to the curve or when the orientation of the curve is inverted. All curves must have a geometric continuity: a curve is at least "C0". Generally, this property is checked at the time of construction or when the curve is edited. Where this is not the case, the documentation states so explicitly. Warning The Geom package does not prevent the construction of curves with null length or curves which self-intersect.
 */
export declare class Geom_Curve extends Geom_Geometry {
  /**
   * Changes the direction of parametrization of <me>. The "FirstParameter" and the "LastParameter" are not changed but the orientation of the curve is modified. If the curve is bounded the StartPoint of the initial curve becomes the EndPoint of the reversed curve and the EndPoint of the initial curve becomes the StartPoint of the reversed curve.
   */
  Reverse(): void;
  /**
   * Returns the parameter on the reversed curve for the point of parameter U on <me>.
   *
   * me->`Reversed()`->Value(me->ReversedParameter(U))
   *
   * is the same point as
   *
   * me->Value(U)
   */
  ReversedParameter(U: number): number;
  /**
   * Returns the parameter on the transformed curve for the transform of the point of parameter U on <me>.
   *
   * me->Transformed(T)->Value(me->TransformedParameter(U,T))
   *
   * is the same point as
   *
   * me->Value(U).Transformed(T)
   *
   * This methods returns
   *
   * It can be redefined. For example on the Line.
   */
  TransformedParameter(U: number, T: unknown): number;
  /**
   * Returns a coefficient to compute the parameter on the transformed curve for the transform of the point on <me>.
   *
   * Transformed(T)->Value(U * ParametricTransformation(T))
   *
   * is the same point as
   *
   * Value(U).Transformed(T)
   *
   * This methods returns 1.
   *
   * It can be redefined. For example on the Line.
   */
  ParametricTransformation(T: unknown): number;
  /**
   * Returns a copy of <me> reversed.
   */
  Reversed(): Geom_Curve;
  /**
   * Returns the value of the first parameter. Warnings : It can be RealFirst from package {@link Standard | `Standard`} if the curve is infinite.
   */
  FirstParameter(): number;
  /**
   * Returns the value of the last parameter. Warnings : It can be RealLast from package {@link Standard | `Standard`} if the curve is infinite.
   */
  LastParameter(): number;
  /**
   * Returns true if the curve is closed. Some curves such as circle are always closed, others such as line are never closed (by definition). Some Curves such as OffsetCurve can be closed or not. These curves are considered as closed if the distance between the first point and the last point of the curve is lower or equal to the Resolution from package gp which is a fixed criterion independent of the application.
   */
  IsClosed(): boolean;
  /**
   * Is the parametrization of the curve periodic ? It is possible only if the curve is closed and if the following relation is satisfied : for each parametric value U the distance between the point P(u) and the point P (u + T) is lower or equal to Resolution from package gp, T is the period and must be a constant. There are three possibilities : . the curve is never periodic by definition (SegmentLine) . the curve is always periodic by definition (Circle) . the curve can be defined as periodic (BSpline). In this case a function SetPeriodic allows you to give the shape of the curve.
   * The general rule for this case is : if a curve can be periodic or not the default periodicity set is non periodic and you have to turn (explicitly) the curve into a periodic curve if you want the curve to be periodic.
   */
  IsPeriodic(): boolean;
  /**
   * Returns the period of this curve. Exceptions Standard_NoSuchObject if this curve is not periodic.
   */
  Period(): number;
  /**
   * It is the global continuity of the curve C0 : only geometric continuity, C1 : continuity of the first derivative all along the Curve, C2 : continuity of the second derivative all along the Curve, C3 : continuity of the third derivative all along the Curve, G1 : tangency continuity all along the Curve, G2 : curvature continuity all along the Curve, CN : the order of continuity is infinite.
   */
  Continuity(): GeomAbs_Shape;
  /**
   * Returns true if the degree of continuity of this curve is at least N. Exceptions - Standard_RangeError if N is less than 0.
   */
  IsCN(N: number): boolean;
  /**
   * Computes the point of parameter U. Raises an exception on failure (e.g. OffsetCurve at singular point).
   */
  EvalD0(U: number): gp_Pnt;
  /**
   * Computes the point and first derivative at parameter U. Raises an exception if the curve continuity is not C1.
   */
  EvalD1(U: number): Geom_Curve_ResD1;
  /**
   * Computes the point and first two derivatives at parameter U. Raises an exception if the curve continuity is not C2.
   */
  EvalD2(U: number): Geom_Curve_ResD2;
  /**
   * Computes the point and first three derivatives at parameter U. Raises an exception if the curve continuity is not C3.
   */
  EvalD3(U: number): Geom_Curve_ResD3;
  /**
   * Computes the Nth derivative at parameter U. Raises an exception if the curve continuity is not CN, or N < 1.
   */
  EvalDN(U: number, N: number): unknown;
  /**
   * Returns in P the point of parameter U.
   * @param P Mutated in place; read the updated value from this argument after the call.
   */
  D0(U: number, P: gp_Pnt): void;
  /**
   * Returns the point P of parameter U and the first derivative V1.
   * @param P Mutated in place; read the updated value from this argument after the call.
   * @param V1 Mutated in place; read the updated value from this argument after the call.
   */
  D1(U: number, P: gp_Pnt, V1: unknown): void;
  /**
   * Returns the point P of parameter U, the first and second derivatives V1 and V2.
   * @param P Mutated in place; read the updated value from this argument after the call.
   * @param V1 Mutated in place; read the updated value from this argument after the call.
   * @param V2 Mutated in place; read the updated value from this argument after the call.
   */
  D2(U: number, P: gp_Pnt, V1: unknown, V2: unknown): void;
  /**
   * Returns the point P of parameter U, the first, the second and the third derivative.
   * @param P Mutated in place; read the updated value from this argument after the call.
   * @param V1 Mutated in place; read the updated value from this argument after the call.
   * @param V2 Mutated in place; read the updated value from this argument after the call.
   * @param V3 Mutated in place; read the updated value from this argument after the call.
   */
  D3(U: number, P: gp_Pnt, V1: unknown, V2: unknown, V3: unknown): void;
  /**
   * The returned vector gives the value of the derivative for the order of derivation N.
   */
  DN(U: number, N: number): unknown;
  /**
   * Computes the point of parameter U on <me>.
   */
  Value(U: number): gp_Pnt;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

export interface Geom_Curve_ResD1 {
  Point: gp_Pnt;
  D1: unknown;
}

export interface Geom_Curve_ResD2 {
  Point: gp_Pnt;
  D1: unknown;
  D2: unknown;
}

export interface Geom_Curve_ResD3 {
  Point: gp_Pnt;
  D1: unknown;
  D2: unknown;
  D3: unknown;
}

/**
 * The abstract class Conic describes the common behavior of conic curves in 3D space and, in particular, their general characteristics. The Geom package provides four concrete classes of conics: {@link Geom_Circle | `Geom_Circle`}, {@link Geom_Ellipse | `Geom_Ellipse`}, {@link Geom_Hyperbola | `Geom_Hyperbola`} and {@link Geom_Parabola | `Geom_Parabola`}. A conic is positioned in space with a right-handed coordinate system ({@link gp_Ax2 | `gp_Ax2`} object), where:
 *
 * - the origin is the center of the conic (or the apex in the case of a parabola),
 * - the origin, "X Direction" and "Y Direction" define the plane of the conic. This coordinate system is the local coordinate system of the conic. The "main Direction" of this coordinate system is the vector normal to the plane of the conic.
 * The axis, of which the origin and unit vector are respectively the origin and "main Direction" of the local coordinate system, is termed the "Axis" or "main Axis" of the conic.
 * The "main Direction" of the local coordinate system gives an explicit orientation to the conic, determining the direction in which the parameter increases along the conic. The "X Axis" of the local coordinate system also defines the origin of the parameter of the conic.
 */
export declare class Geom_Conic extends Geom_Curve {
  /**
   * Changes the orientation of the conic's plane. The normal axis to the plane is A1. The XAxis and the YAxis are recomputed.
   *
   * raised if the A1 is parallel to the XAxis of the conic.
   */
  SetAxis(theA1: unknown): void;
  /**
   * changes the location point of the conic.
   */
  SetLocation(theP: gp_Pnt): void;
  /**
   * changes the local coordinate system of the conic.
   */
  SetPosition(theA2: gp_Ax2): void;
  /**
   * Returns the "main Axis" of this conic. This axis is normal to the plane of the conic.
   */
  Axis(): unknown;
  /**
   * Returns the location point of the conic. For the circle, the ellipse and the hyperbola it is the center of the conic. For the parabola it is the Apex of the parabola.
   */
  Location(): gp_Pnt;
  /**
   * Returns the local coordinates system of the conic. The main direction of the Axis2Placement is normal to the plane of the conic. The X direction of the Axis2placement is in the plane of the conic and corresponds to the origin for the conic's parametric value u.
   */
  Position(): gp_Ax2;
  /**
   * Returns the eccentricity value of the conic e. e = 0 for a circle 0 < e < 1 for an ellipse (e = 0 if MajorRadius = MinorRadius) e > 1 for a hyperbola e = 1 for a parabola Exceptions Standard_DomainError in the case of a hyperbola if its major radius is null.
   */
  Eccentricity(): number;
  /**
   * Returns the XAxis of the conic. This axis defines the origin of parametrization of the conic. This axis is perpendicular to the Axis of the conic. This axis and the Yaxis define the plane of the conic.
   */
  XAxis(): unknown;
  /**
   * Returns the YAxis of the conic. The YAxis is perpendicular to the Xaxis. This axis and the Xaxis define the plane of the conic.
   */
  YAxis(): unknown;
  /**
   * Reverses the direction of parameterization of <me>. The local coordinate system of the conic is modified.
   */
  Reverse(): void;
  /**
   * Returns the parameter on the reversed curve for the point of parameter U on <me>.
   */
  ReversedParameter(U: number): number;
  /**
   * The continuity of the conic is Cn.
   */
  Continuity(): GeomAbs_Shape;
  /**
   * Returns True. Raised if N < 0.
   */
  IsCN(N: number): boolean;
  static get_type_name(): string;
  static get_type_descriptor(): unknown;
  DynamicType(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes a face which.
 *
 * - references an underlying face with the potential to be given a location and an orientation
 * - has a location for the underlying face, giving its placement in the local coordinate system
 * - has an orientation for the underlying face, in terms of its geometry (as opposed to orientation in relation to other shapes).
 */
export declare class TopoDS_Face extends TopoDS_Shape {
  /**
   * Undefined Face.
   */
  constructor();
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * A Builder is used to create Topological Data Structures. It is the root of the Builder class hierarchy.
 *
 * There are three groups of methods in the Builder:
 *
 * The Make methods create Shapes.
 *
 * The Add method includes a Shape in another Shape.
 *
 * The Remove method removes a Shape from an other Shape.
 *
 * The methods in Builder are not static. They can be redefined in inherited builders.
 *
 * This Builder does not provide methods to Make Vertices, Edges, Faces, Shells or Solids. These methods are provided in the inherited Builders as they must provide the geometry.
 *
 * The Add method check for the following rules:
 *
 * - Any SHAPE can be added in a COMPOUND.
 * - Only SOLID can be added in a COMPSOLID.
 * - Only SHELL, EDGE and VERTEX can be added in a SOLID. EDGE and VERTEX as to be INTERNAL or EXTERNAL.
 * - Only FACE can be added in a SHELL.
 * - Only WIRE and VERTEX can be added in a FACE. VERTEX as to be INTERNAL or EXTERNAL.
 * - Only EDGE can be added in a WIRE.
 * - Only VERTEX can be added in an EDGE.
 * - Nothing can be added in a VERTEX.
 */
export declare class TopoDS_Builder {
  constructor();
  /**
   * Make an empty Wire.
   * @param W Mutated in place; read the updated value from this argument after the call.
   */
  MakeWire(W: TopoDS_Wire): void;
  /**
   * Make an empty Shell.
   * @param S Mutated in place; read the updated value from this argument after the call.
   */
  MakeShell(S: unknown): void;
  /**
   * Make a Solid covering the whole 3D space.
   * @param S Mutated in place; read the updated value from this argument after the call.
   */
  MakeSolid(S: unknown): void;
  /**
   * Make an empty Composite Solid.
   * @param C Mutated in place; read the updated value from this argument after the call.
   */
  MakeCompSolid(C: unknown): void;
  /**
   * Make an empty Compound.
   * @param C Mutated in place; read the updated value from this argument after the call.
   */
  MakeCompound(C: unknown): void;
  /**
   * Add the Shape C in the Shape S. Exceptions.
   *
   * - TopoDS_FrozenShape if S is not free and cannot be modified.
   * - TopoDS__UnCompatibleShapes if S and C are not compatible.
   * @param S Mutated in place; read the updated value from this argument after the call.
   */
  Add(S: TopoDS_Shape, C: TopoDS_Shape): void;
  /**
   * Remove the Shape C from the Shape S. Exceptions TopoDS_FrozenShape if S is frozen and cannot be modified.
   * @param S Mutated in place; read the updated value from this argument after the call.
   */
  Remove(S: TopoDS_Shape, C: TopoDS_Shape): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes an edge which.
 *
 * - references an underlying edge with the potential to be given a location and an orientation
 * - has a location for the underlying edge, giving its placement in the local coordinate system
 * - has an orientation for the underlying edge, in terms of its geometry (as opposed to orientation in relation to other shapes).
 */
export declare class TopoDS_Edge extends TopoDS_Shape {
  /**
   * Undefined Edge.
   */
  constructor();
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes a wire which.
 *
 * - references an underlying wire with the potential to be given a location and an orientation
 * - has a location for the underlying wire, giving its placement in the local coordinate system
 * - has an orientation for the underlying wire, in terms of its geometry (as opposed to orientation in relation to other shapes).
 */
export declare class TopoDS_Wire extends TopoDS_Shape {
  /**
   * Undefined Wire.
   */
  constructor();
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Describes a shape which.
 *
 * - references an underlying shape with the potential to be given a location and an orientation
 * - has a location for the underlying shape, giving its placement in the local coordinate system
 * - has an orientation for the underlying shape, in terms of its geometry (as opposed to orientation in relation to other shapes). Note: A Shape is empty if it references an underlying shape which has an empty list of shapes.
 */
export declare class TopoDS_Shape {
  /**
   * Creates a NULL Shape referring to nothing.
   */
  constructor();
  /**
   * Returns true if this shape is null. In other words, it references no underlying shape with the potential to be given a location and an orientation.
   */
  IsNull(): boolean;
  /**
   * Destroys the reference to the underlying shape stored in this shape. As a result, this shape becomes null.
   */
  Nullify(): void;
  /**
   * Returns the shape local coordinate system.
   */
  Location(): TopLoc_Location;
  /**
   * Sets the shape local coordinate system.
   * @param theLoc the new local coordinate system.
   * @param theRaiseExc flag to raise exception in case of transformation with scale or negative.
   */
  Location(theLoc: TopLoc_Location, theRaiseExc: boolean): void;
  /**
   * Returns a shape similar to <me> with the local coordinate system set to <Loc>.
   * @param theLoc the new local coordinate system.
   * @param theRaiseExc flag to raise exception in case of transformation with scale or negative.
   * @returns the located shape.
   */
  Located(theLoc: TopLoc_Location, theRaiseExc: boolean): TopoDS_Shape;
  /**
   * Returns the shape orientation.
   */
  Orientation(): unknown;
  /**
   * Sets the shape orientation.
   */
  Orientation(theOrient: unknown): void;
  /**
   * Returns a shape similar to <me> with the orientation set to <Or>.
   */
  Oriented(theOrient: unknown): TopoDS_Shape;
  /**
   * Returns a handle to the actual shape implementation.
   */
  TShape(): unknown;
  TShape(theTShape: unknown): void;
  /**
   * Returns the value of the `TopAbs_ShapeEnum` enumeration that corresponds to this shape, for example VERTEX, EDGE, and so on. Exceptions Standard_NullObject if this shape is null.
   */
  ShapeType(): TopAbs_ShapeEnum;
  /**
   * Returns the free flag.
   */
  Free(): boolean;
  /**
   * Sets the free flag.
   */
  Free(theIsFree: boolean): void;
  /**
   * Returns the locked flag.
   */
  Locked(): boolean;
  /**
   * Sets the locked flag.
   */
  Locked(theIsLocked: boolean): void;
  /**
   * Returns the modification flag.
   */
  Modified(): boolean;
  /**
   * Sets the modification flag.
   */
  Modified(theIsModified: boolean): void;
  /**
   * Returns the checked flag.
   */
  Checked(): boolean;
  /**
   * Sets the checked flag.
   */
  Checked(theIsChecked: boolean): void;
  /**
   * Returns the orientability flag.
   */
  Orientable(): boolean;
  /**
   * Sets the orientability flag.
   */
  Orientable(theIsOrientable: boolean): void;
  /**
   * Returns the closedness flag.
   */
  Closed(): boolean;
  /**
   * Sets the closedness flag.
   */
  Closed(theIsClosed: boolean): void;
  /**
   * Returns the infinity flag.
   */
  Infinite(): boolean;
  /**
   * Sets the infinity flag.
   */
  Infinite(theIsInfinite: boolean): void;
  /**
   * Returns the convexness flag.
   */
  Convex(): boolean;
  /**
   * Sets the convexness flag.
   */
  Convex(theIsConvex: boolean): void;
  /**
   * Multiplies the Shape location by thePosition.
   * @param thePosition the transformation to apply.
   * @param theRaiseExc flag to raise exception in case of transformation with scale or negative.
   */
  Move(thePosition: TopLoc_Location, theRaiseExc: boolean): void;
  /**
   * Returns a shape similar to <me> with a location multiplied by thePosition.
   * @param thePosition the transformation to apply.
   * @param theRaiseExc flag to raise exception in case of transformation with scale or negative.
   * @returns the moved shape.
   */
  Moved(thePosition: TopLoc_Location, theRaiseExc: boolean): TopoDS_Shape;
  /**
   * Reverses the orientation, using the Reverse method from the {@link TopAbs | `TopAbs`} package.
   */
  Reverse(): void;
  /**
   * Returns a shape similar to <me> with the orientation reversed, using the Reverse method from the {@link TopAbs | `TopAbs`} package.
   */
  Reversed(): TopoDS_Shape;
  /**
   * Complements the orientation, using the Complement method from the {@link TopAbs | `TopAbs`} package.
   */
  Complement(): void;
  /**
   * Returns a shape similar to <me> with the orientation complemented, using the Complement method from the {@link TopAbs | `TopAbs`} package.
   */
  Complemented(): TopoDS_Shape;
  /**
   * Updates the Shape Orientation by composition with theOrient, using the Compose method from the {@link TopAbs | `TopAbs`} package.
   */
  Compose(theOrient: unknown): void;
  /**
   * Returns a shape similar to <me> with the orientation composed with theOrient, using the Compose method from the {@link TopAbs | `TopAbs`} package.
   */
  Composed(theOrient: unknown): TopoDS_Shape;
  /**
   * Returns the number of direct sub-shapes (children).
   * @see {@link TopoDS_Iterator | `TopoDS_Iterator`}
   */
  NbChildren(): number;
  /**
   * Returns True if two shapes are partners, i.e. if they share the same TShape. Locations and Orientations may differ.
   */
  IsPartner(theOther: TopoDS_Shape): boolean;
  /**
   * Returns True if two shapes are same, i.e. if they share the same TShape with the same Locations. Orientations may differ.
   */
  IsSame(theOther: TopoDS_Shape): boolean;
  /**
   * Returns True if two shapes are equal, i.e. if they share the same TShape with the same Locations and Orientations.
   */
  IsEqual(theOther: TopoDS_Shape): boolean;
  /**
   * Negation of the IsEqual method.
   */
  IsNotEqual(theOther: TopoDS_Shape): boolean;
  /**
   * Replace <me> by a new Shape with the same Orientation and Location and a new TShape with the same geometry and no sub-shapes.
   */
  EmptyCopy(): void;
  /**
   * Returns a new Shape with the same Orientation and Location and a new TShape with the same geometry and no sub-shapes.
   */
  EmptyCopied(): TopoDS_Shape;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * A framework providing advanced tolerance control. It is used to build Shapes. If tolerance control is required, you are advised to:
 *
 * 1. build a default precision for topology, using the classes provided in the BRepAPI package
 * 2. update the tolerance of the resulting shape. Note that only vertices, edges and faces have meaningful tolerance control. The tolerance value must always comply with the condition that face tolerances are more restrictive than edge tolerances which are more restrictive than vertex tolerances. In other words: Tol(Vertex) >= Tol(Edge) >= Tol(Face). Other rules in setting tolerance include:
 *
 * - you can open up tolerance but should never restrict it
 * - an edge cannot be included within the fusion of the tolerance spheres of two vertices
 */
export declare class BRep_Builder extends TopoDS_Builder {
  constructor();
  /**
   * Makes an undefined Face.
   * @param F Mutated in place; read the updated value from this argument after the call.
   */
  MakeFace(F: TopoDS_Face): void;
  /**
   * Makes a theFace with a single triangulation. The triangulation is in the same reference system than the TFace.
   * @param theFace Mutated in place; read the updated value from this argument after the call.
   */
  MakeFace(theFace: TopoDS_Face, theTriangulation: Poly_Triangulation): void;
  /**
   * Makes a Face with a surface.
   * @param F Mutated in place; read the updated value from this argument after the call.
   */
  MakeFace(F: TopoDS_Face, S: unknown, Tol: number): void;
  /**
   * Makes a Face with a list of triangulations and active one. Use NULL active triangulation to set the first triangulation in list as active. The triangulations is in the same reference system than the TFace.
   * @param theFace Mutated in place; read the updated value from this argument after the call.
   */
  MakeFace(theFace: TopoDS_Face, theTriangulations: NCollection_List_handle_Poly_Triangulation, theActiveTriangulation: Poly_Triangulation): void;
  /**
   * Makes a Face with a surface and a location.
   * @param F Mutated in place; read the updated value from this argument after the call.
   */
  MakeFace(F: TopoDS_Face, S: unknown, L: TopLoc_Location, Tol: number): void;
  /**
   * Updates the face F using the tolerance value Tol, surface S and location Location.
   */
  UpdateFace(F: TopoDS_Face, S: unknown, L: TopLoc_Location, Tol: number): void;
  /**
   * Changes a face triangulation. A NULL theTriangulation removes face triangulations. If theToReset is TRUE face triangulations will be reset to new list with only one input triangulation that will be active. Else if theTriangulation is contained in internal triangulations list it will be made active, else the active triangulation will be replaced to theTriangulation one.
   */
  UpdateFace(theFace: TopoDS_Face, theTriangulation: Poly_Triangulation, theToReset: boolean): void;
  /**
   * Updates the face Tolerance.
   */
  UpdateFace(F: TopoDS_Face, Tol: number): void;
  /**
   * Sets the NaturalRestriction flag of the face.
   */
  NaturalRestriction(F: TopoDS_Face, N: boolean): void;
  /**
   * Makes an undefined Edge (no geometry).
   * @param E Mutated in place; read the updated value from this argument after the call.
   */
  MakeEdge(E: TopoDS_Edge): void;
  /**
   * Makes an Edge with a polygon 3d.
   * @param E Mutated in place; read the updated value from this argument after the call.
   */
  MakeEdge(E: TopoDS_Edge, P: unknown): void;
  /**
   * Makes an Edge with a curve.
   * @param E Mutated in place; read the updated value from this argument after the call.
   */
  MakeEdge(E: TopoDS_Edge, C: Geom_Curve, Tol: number): void;
  /**
   * makes an Edge polygon on Triangulation.
   * @param E Mutated in place; read the updated value from this argument after the call.
   */
  MakeEdge(E: TopoDS_Edge, N: unknown, T: Poly_Triangulation): void;
  /**
   * Makes an Edge with a curve and a location.
   * @param E Mutated in place; read the updated value from this argument after the call.
   */
  MakeEdge(E: TopoDS_Edge, C: Geom_Curve, L: TopLoc_Location, Tol: number): void;
  /**
   * makes an Edge polygon on Triangulation.
   * @param E Mutated in place; read the updated value from this argument after the call.
   */
  MakeEdge(E: TopoDS_Edge, N: unknown, T: Poly_Triangulation, L: TopLoc_Location): void;
  /**
   * Changes an Edge 3D polygon. A null Polygon removes the 3d Polygon.
   */
  UpdateEdge(E: TopoDS_Edge, P: unknown): void;
  /**
   * Updates the edge tolerance.
   */
  UpdateEdge(E: TopoDS_Edge, Tol: number): void;
  /**
   * Sets a 3D curve for the edge. If `is a null handle, remove any existing 3d curve.`
   */
  UpdateEdge(E: TopoDS_Edge, C: Geom_Curve, Tol: number): void;
  /**
   * Changes an Edge 3D polygon. A null Polygon removes the 3d Polygon.
   */
  UpdateEdge(E: TopoDS_Edge, P: unknown, L: TopLoc_Location): void;
  /**
   * Changes an Edge polygon on Triangulation.
   */
  UpdateEdge(E: TopoDS_Edge, N: unknown, T: Poly_Triangulation): void;
  /**
   * Changes Edge polygon on a face.
   */
  UpdateEdge(E: TopoDS_Edge, P: unknown, S: TopoDS_Face): void;
  /**
   * Sets a 3D curve for the edge. If `is a null handle, remove any existing 3d curve.`
   */
  UpdateEdge(E: TopoDS_Edge, C: Geom_Curve, L: TopLoc_Location, Tol: number): void;
  /**
   * Sets a pcurve for the edge on the face. If `is a null handle, remove any existing pcurve.`
   */
  UpdateEdge(E: TopoDS_Edge, C: unknown, F: TopoDS_Face, Tol: number): void;
  /**
   * Changes an Edge polygon on Triangulation.
   */
  UpdateEdge(E: TopoDS_Edge, N: unknown, T: Poly_Triangulation, L: TopLoc_Location): void;
  /**
   * Changes an Edge polygon on Triangulation.
   */
  UpdateEdge(E: TopoDS_Edge, N1: unknown, N2: unknown, T: Poly_Triangulation): void;
  /**
   * Changes Edge polygon on a face.
   */
  UpdateEdge(E: TopoDS_Edge, P: unknown, S: unknown, T: TopLoc_Location): void;
  /**
   * Changes Edge polygons on a face.
   *
   * A null Polygon removes the 2d Polygon.
   */
  UpdateEdge(E: TopoDS_Edge, P1: unknown, P2: unknown, S: TopoDS_Face): void;
  /**
   * Sets pcurves for the edge on the closed face. If <C1> or <C2> is a null handle, remove any existing pcurve.
   */
  UpdateEdge(E: TopoDS_Edge, C1: unknown, C2: unknown, F: TopoDS_Face, Tol: number): void;
  /**
   * Sets a pcurve for the edge on the face. If `is a null handle, remove any existing pcurve.`
   */
  UpdateEdge(E: TopoDS_Edge, C: unknown, S: unknown, L: TopLoc_Location, Tol: number): void;
  /**
   * Changes an Edge polygon on Triangulation.
   */
  UpdateEdge(E: TopoDS_Edge, N1: unknown, N2: unknown, T: Poly_Triangulation, L: TopLoc_Location): void;
  /**
   * Changes Edge polygons on a face.
   *
   * A null Polygon removes the 2d Polygon.
   */
  UpdateEdge(E: TopoDS_Edge, P1: unknown, P2: unknown, S: unknown, L: TopLoc_Location): void;
  /**
   * Sets pcurves for the edge on the closed surface. <C1> or <C2> is a null handle, remove any existing pcurve.
   */
  UpdateEdge(E: TopoDS_Edge, C1: unknown, C2: unknown, S: unknown, L: TopLoc_Location, Tol: number): void;
  /**
   * Sets a pcurve for the edge on the face. If `is a null handle, remove any existing pcurve. Sets UV bounds for curve repsentation.`
   */
  UpdateEdge(E: TopoDS_Edge, C: unknown, S: unknown, L: TopLoc_Location, Tol: number, Pf: unknown, Pl: unknown): void;
  /**
   * Sets pcurves for the edge on the closed surface. <C1> or <C2> is a null handle, remove any existing pcurve. Sets UV bounds for curve repsentation.
   */
  UpdateEdge(E: TopoDS_Edge, C1: unknown, C2: unknown, S: unknown, L: TopLoc_Location, Tol: number, Pf: unknown, Pl: unknown): void;
  /**
   * Sets the geometric continuity on the edge.
   */
  Continuity(E: TopoDS_Edge, F1: TopoDS_Face, F2: TopoDS_Face, C: GeomAbs_Shape): void;
  /**
   * Sets the geometric continuity on the edge.
   */
  Continuity(E: TopoDS_Edge, S1: unknown, S2: unknown, L1: TopLoc_Location, L2: TopLoc_Location, C: GeomAbs_Shape): void;
  /**
   * Sets the same parameter flag for the edge <E>.
   */
  SameParameter(E: TopoDS_Edge, S: boolean): void;
  /**
   * Sets the same range flag for the edge <E>.
   */
  SameRange(E: TopoDS_Edge, S: boolean): void;
  /**
   * Sets the degenerated flag for the edge <E>.
   */
  Degenerated(E: TopoDS_Edge, D: boolean): void;
  /**
   * Sets the range of the 3d curve if Only3d=TRUE, otherwise sets the range to all the representations.
   */
  Range(E: TopoDS_Edge, First: number, Last: number, Only3d: boolean): void;
  /**
   * Sets the range of the edge on the pcurve on the face.
   */
  Range(E: TopoDS_Edge, F: TopoDS_Face, First: number, Last: number): void;
  /**
   * Sets the range of the edge on the pcurve on the surface.
   */
  Range(E: TopoDS_Edge, S: unknown, L: TopLoc_Location, First: number, Last: number): void;
  /**
   * Add to <Eout> the geometric representations of <Ein>.
   */
  Transfert(Ein: TopoDS_Edge, Eout: TopoDS_Edge): void;
  /**
   * Transfert the parameters of Vin on Ein as the parameter of Vout on Eout.
   */
  Transfert(Ein: TopoDS_Edge, Eout: TopoDS_Edge, Vin: unknown, Vout: unknown): void;
  /**
   * Makes an udefined vertex without geometry.
   * @param V Mutated in place; read the updated value from this argument after the call.
   */
  MakeVertex(V: unknown): void;
  /**
   * Makes a vertex from a 3D point.
   * @param V Mutated in place; read the updated value from this argument after the call.
   */
  MakeVertex(V: unknown, P: gp_Pnt, Tol: number): void;
  /**
   * Updates the vertex tolerance.
   */
  UpdateVertex(V: unknown, Tol: number): void;
  /**
   * Sets a 3D point on the vertex.
   */
  UpdateVertex(V: unknown, P: gp_Pnt, Tol: number): void;
  /**
   * Sets the parameter for the vertex on the edge curves.
   */
  UpdateVertex(V: unknown, P: number, E: TopoDS_Edge, Tol: number): void;
  /**
   * Sets the parameter for the vertex on the edge pcurve on the face.
   */
  UpdateVertex(V: unknown, P: number, E: TopoDS_Edge, F: TopoDS_Face, Tol: number): void;
  /**
   * Sets the parameters for the vertex on the face.
   */
  UpdateVertex(Ve: unknown, U: number, V: number, F: TopoDS_Face, Tol: number): void;
  /**
   * Sets the parameter for the vertex on the edge pcurve on the surface.
   */
  UpdateVertex(V: unknown, P: number, E: TopoDS_Edge, S: unknown, L: TopLoc_Location, Tol: number): void;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Provides class methods to access to the geometry of BRep shapes.
 */
export declare class BRep_Tool {
  constructor();
  /**
   * If S is Shell, returns True if it has no free boundaries (edges). If S is Wire, returns True if it has no free ends (vertices). (Internal and External sub-shepes are ignored in these checks) If S is Edge, returns True if its vertices are the same. For other shape types returns S.Closed().
   */
  static IsClosed(S: TopoDS_Shape): boolean;
  /**
   * Returns True if <E> has two PCurves in the parametric space of <F>. i.e. <F> is on a closed surface and <E> is on the closing curve.
   */
  static IsClosed(E: TopoDS_Edge, F: TopoDS_Face): boolean;
  /**
   * Returns True if <E> has two PCurves in the parametric space of . i.e. is a closed surface and <E> is on the closing curve.
   */
  static IsClosed(E: TopoDS_Edge, S: unknown, L: TopLoc_Location): boolean;
  /**
   * Returns True if <E> has two arrays of indices in the triangulation <T>.
   */
  static IsClosed(E: TopoDS_Edge, T: Poly_Triangulation, L: TopLoc_Location): boolean;
  /**
   * Returns the geometric surface of the face. Returns in <L> the location for the surface.
   * @param L Mutated in place; read the updated value from this argument after the call.
   */
  static Surface(F: TopoDS_Face, L: TopLoc_Location): unknown;
  /**
   * Returns the geometric surface of the face. It can be a copy if there is a Location.
   */
  static Surface(F: TopoDS_Face): unknown;
  /**
   * Returns the triangulation of the face according to the mesh purpose.
   * @param theFace the input face to find triangulation.
   * @param theLocation the face location. Mutated in place; read the updated value from this argument after the call.
   * @param theMeshPurpose a mesh purpose to find appropriate triangulation (NONE by default).
   * @returns an active triangulation in case of NONE purpose, the first triangulation appropriate for the input purpose, just the first triangulation if none matching other criteria and input purpose is AnyFallback or null handle if there is no any suitable triangulation.
   */
  static Triangulation(theFace: TopoDS_Face, theLocation: TopLoc_Location, theMeshPurpose: number): Poly_Triangulation;
  /**
   * Returns all triangulations of the face.
   * @param theFace the input face.
   * @param theLocation the face location. Mutated in place; read the updated value from this argument after the call.
   * @returns list of all available face triangulations.
   */
  static Triangulations(theFace: TopoDS_Face, theLocation: TopLoc_Location): NCollection_List_handle_Poly_Triangulation;
  /**
   * Returns the tolerance of the face.
   */
  static Tolerance(F: TopoDS_Face): number;
  /**
   * Returns the tolerance for <E>.
   */
  static Tolerance(E: TopoDS_Edge): number;
  /**
   * Returns the tolerance.
   */
  static Tolerance(V: unknown): number;
  /**
   * Returns the NaturalRestriction flag of the face.
   */
  static NaturalRestriction(F: TopoDS_Face): boolean;
  /**
   * Returns True if <F> has a surface, false otherwise.
   */
  static IsGeometric(F: TopoDS_Face): boolean;
  /**
   * Returns True if <E> is a 3d curve or a curve on surface.
   */
  static IsGeometric(E: TopoDS_Edge): boolean;
  /**
   * Returns the 3D curve of the edge. May be a Null handle. Returns in <L> the location for the curve. In <First> and <Last> the parameter range.
   * @param L Mutated in place; read the updated value from this argument after the call.
   * @returns A result object with fields:
   * - `returnValue`: the C++ return value
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static Curve(E: TopoDS_Edge, L: TopLoc_Location, First: number, Last: number): { returnValue: Geom_Curve; First: number; Last: number; [Symbol.dispose](): void };
  /**
   * Returns the 3D curve of the edge. May be a Null handle. In <First> and <Last> the parameter range. It can be a copy if there is a Location.
   * @returns A result object with fields:
   * - `returnValue`: the C++ return value
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static Curve(E: TopoDS_Edge, First: number, Last: number): { returnValue: Geom_Curve; First: number; Last: number; [Symbol.dispose](): void };
  /**
   * Returns the 3D polygon of the edge. May be a Null handle. Returns in <L> the location for the polygon.
   * @param L Mutated in place; read the updated value from this argument after the call.
   */
  static Polygon3D(E: TopoDS_Edge, L: TopLoc_Location): unknown;
  /**
   * Returns the curve associated to the edge in the parametric space of the surface. Returns a NULL handle if this curve does not exist. Returns in <First> and <Last> the parameter range. If the surface is a plane the curve can be not stored but created a new each time. The flag pointed by <theIsStored> serves to indicate storage status. It is valued if the pointer is non-null.
   * @param L Mutated in place; read the updated value from this argument after the call.
   * @returns A result object with fields:
   * - `C`: owned by the returned envelope.
   * - `S`: owned by the returned envelope.
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static CurveOnSurface(E: TopoDS_Edge, L: TopLoc_Location, First: number, Last: number): { C: unknown; S: unknown; First: number; Last: number; [Symbol.dispose](): void };
  /**
   * Returns the curve associated to the edge in the parametric space of the face. Returns a NULL handle if this curve does not exist. Returns in <First> and <Last> the parameter range. If the surface is a plane the curve can be not stored but created a new each time. The flag pointed by <theIsStored> serves to indicate storage status. It is valued if the pointer is non-null.
   * @returns A result object with fields:
   * - `returnValue`: the C++ return value
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static CurveOnSurface(E: TopoDS_Edge, F: TopoDS_Face, First: number, Last: number, theIsStored: boolean): { returnValue: unknown; First: number; Last: number; [Symbol.dispose](): void };
  /**
   * Returns in `, , <L> the 2d curve, the surface and the location for the edge <E> of rank <Index>. and are null if the index is out of range. Returns in <First> and <Last> the parameter range.`
   * @param L Mutated in place; read the updated value from this argument after the call.
   * @returns A result object with fields:
   * - `C`: owned by the returned envelope.
   * - `S`: owned by the returned envelope.
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static CurveOnSurface(E: TopoDS_Edge, L: TopLoc_Location, First: number, Last: number, Index: number): { C: unknown; S: unknown; First: number; Last: number; [Symbol.dispose](): void };
  /**
   * Returns the curve associated to the edge in the parametric space of the surface. Returns a NULL handle if this curve does not exist. Returns in <First> and <Last> the parameter range. If the surface is a plane the curve can be not stored but created a new each time. The flag pointed by <theIsStored> serves to indicate storage status. It is valued if the pointer is non-null.
   * @returns A result object with fields:
   * - `returnValue`: the C++ return value
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static CurveOnSurface(E: TopoDS_Edge, S: unknown, L: TopLoc_Location, First: number, Last: number, theIsStored: boolean): { returnValue: unknown; First: number; Last: number; [Symbol.dispose](): void };
  /**
   * For the planar surface builds the 2d curve for the edge by projection of the edge on plane. Returns a NULL handle if the surface is not planar or the projection failed.
   * @returns A result object with fields:
   * - `returnValue`: the C++ return value
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static CurveOnPlane(E: TopoDS_Edge, S: unknown, L: TopLoc_Location, First: number, Last: number): { returnValue: unknown; First: number; Last: number; [Symbol.dispose](): void };
  /**
   * Returns the polygon associated to the edge in the parametric space of the face. Returns a NULL handle if this polygon does not exist.
   */
  static PolygonOnSurface(E: TopoDS_Edge, F: TopoDS_Face): unknown;
  /**
   * Returns in `, , <L> a 2d curve, a surface and a location for the edge <E>. and are null if the edge has no polygon on surface.`
   * @param L Mutated in place; read the updated value from this argument after the call.
   * @returns A result object with fields:
   * - `C`: owned by the returned envelope.
   * - `S`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static PolygonOnSurface(E: TopoDS_Edge, L: TopLoc_Location): { C: unknown; S: unknown; [Symbol.dispose](): void };
  /**
   * Returns the polygon associated to the edge in the parametric space of the surface. Returns a NULL handle if this polygon does not exist.
   */
  static PolygonOnSurface(E: TopoDS_Edge, S: unknown, L: TopLoc_Location): unknown;
  /**
   * Returns in `, , <L> the 2d curve, the surface and the location for the edge <E> of rank <Index>. and are null if the index is out of range.`
   * @param L Mutated in place; read the updated value from this argument after the call.
   * @returns A result object with fields:
   * - `C`: owned by the returned envelope.
   * - `S`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static PolygonOnSurface(E: TopoDS_Edge, L: TopLoc_Location, Index: number): { C: unknown; S: unknown; [Symbol.dispose](): void };
  /**
   * Returns in.
   *
   * , <T>, <L> a polygon on triangulation, a triangulation and a location for the edge <E>.
   *
   * and <T> are null if the edge has no polygon on triangulation.
   * @param L Mutated in place; read the updated value from this argument after the call.
   * @returns A result object with fields:
   * - `P`: owned by the returned envelope.
   * - `T`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static PolygonOnTriangulation(E: TopoDS_Edge, L: TopLoc_Location): { P: unknown; T: Poly_Triangulation; [Symbol.dispose](): void };
  /**
   * Returns the polygon associated to the edge in the parametric space of the face. Returns a NULL handle if this polygon does not exist.
   */
  static PolygonOnTriangulation(E: TopoDS_Edge, T: Poly_Triangulation, L: TopLoc_Location): unknown;
  /**
   * Returns in.
   *
   * , <T>, <L> a polygon on triangulation, a triangulation and a location for the edge <E> for the range index. `and are null if the edge has no polygon on triangulation.`
   * @param L Mutated in place; read the updated value from this argument after the call.
   * @returns A result object with fields:
   * - `P`: owned by the returned envelope.
   * - `T`: owned by the returned envelope.
   * Dispose the returned envelope to release owned Handle fields.
   */
  static PolygonOnTriangulation(E: TopoDS_Edge, L: TopLoc_Location, Index: number): { P: unknown; T: Poly_Triangulation; [Symbol.dispose](): void };
  /**
   * Returns the SameParameter flag for the edge.
   */
  static SameParameter(E: TopoDS_Edge): boolean;
  /**
   * Returns the SameRange flag for the edge.
   */
  static SameRange(E: TopoDS_Edge): boolean;
  /**
   * Returns True if the edge is degenerated.
   */
  static Degenerated(E: TopoDS_Edge): boolean;
  /**
   * Gets the range of the 3d curve.
   * @returns A result object with fields:
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   */
  static Range(E: TopoDS_Edge, First: number, Last: number): { First: number; Last: number };
  /**
   * Gets the range of the edge on the pcurve on the surface.
   * @returns A result object with fields:
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   */
  static Range(E: TopoDS_Edge, S: unknown, L: TopLoc_Location, First: number, Last: number): { First: number; Last: number };
  /**
   * Gets the range of the edge on the pcurve on the face.
   * @returns A result object with fields:
   * - `First`: updated value from the call.
   * - `Last`: updated value from the call.
   */
  static Range(E: TopoDS_Edge, F: TopoDS_Face, First: number, Last: number): { First: number; Last: number };
  /**
   * Gets the UV locations of the extremities of the edge.
   * @param PFirst Mutated in place; read the updated value from this argument after the call.
   * @param PLast Mutated in place; read the updated value from this argument after the call.
   */
  static UVPoints(E: TopoDS_Edge, S: unknown, L: TopLoc_Location, PFirst: unknown, PLast: unknown): void;
  /**
   * Gets the UV locations of the extremities of the edge.
   * @param PFirst Mutated in place; read the updated value from this argument after the call.
   * @param PLast Mutated in place; read the updated value from this argument after the call.
   */
  static UVPoints(E: TopoDS_Edge, F: TopoDS_Face, PFirst: unknown, PLast: unknown): void;
  /**
   * Sets the UV locations of the extremities of the edge.
   */
  static SetUVPoints(E: TopoDS_Edge, S: unknown, L: TopLoc_Location, PFirst: unknown, PLast: unknown): void;
  /**
   * Sets the UV locations of the extremities of the edge.
   */
  static SetUVPoints(E: TopoDS_Edge, F: TopoDS_Face, PFirst: unknown, PLast: unknown): void;
  /**
   * Returns True if the edge is on the surfaces of the two faces.
   */
  static HasContinuity(E: TopoDS_Edge, F1: TopoDS_Face, F2: TopoDS_Face): boolean;
  /**
   * Returns True if the edge is on the surfaces.
   */
  static HasContinuity(E: TopoDS_Edge, S1: unknown, S2: unknown, L1: TopLoc_Location, L2: TopLoc_Location): boolean;
  /**
   * Returns True if the edge has regularity on some two surfaces.
   */
  static HasContinuity(E: TopoDS_Edge): boolean;
  /**
   * Returns the continuity.
   */
  static Continuity(E: TopoDS_Edge, F1: TopoDS_Face, F2: TopoDS_Face): GeomAbs_Shape;
  /**
   * Returns the continuity.
   */
  static Continuity(E: TopoDS_Edge, S1: unknown, S2: unknown, L1: TopLoc_Location, L2: TopLoc_Location): GeomAbs_Shape;
  /**
   * Returns the max continuity of edge between some surfaces or GeomAbs_C0 if there are no such surfaces.
   */
  static MaxContinuity(theEdge: TopoDS_Edge): GeomAbs_Shape;
  /**
   * Returns the 3d point.
   */
  static Pnt(V: unknown): gp_Pnt;
  /**
   * Returns the parameter of <V> on <E>. Throws Standard_NoSuchObject if no parameter on edge.
   */
  static Parameter(V: unknown, E: TopoDS_Edge): number;
  /**
   * Finds the parameter of <theV> on <theE>.
   * @param theV input vertex
   * @param theE input edge
   * @param theParam calculated parameter on the curve
   * @returns A result object with fields:
   * - `returnValue`: TRUE if done
   * - `theParam`: calculated parameter on the curve
   */
  static Parameter(theV: unknown, theE: TopoDS_Edge, theParam: number): { returnValue: boolean; theParam: number };
  /**
   * Returns the parameters of the vertex on the pcurve of the edge on the face.
   */
  static Parameter(V: unknown, E: TopoDS_Edge, F: TopoDS_Face): number;
  /**
   * Returns the parameters of the vertex on the pcurve of the edge on the surface.
   */
  static Parameter(V: unknown, E: TopoDS_Edge, S: unknown, L: TopLoc_Location): number;
  /**
   * Returns the parameters of the vertex on the face.
   */
  static Parameters(V: unknown, F: TopoDS_Face): unknown;
  /**
   * Returns the maximum tolerance of input shape subshapes.
   */
  static MaxTolerance(theShape: TopoDS_Shape, theSubShape: TopAbs_ShapeEnum): number;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * An Explorer is a Tool to visit a Topological Data Structure from the `TopoDS` package.
 *
 * An Explorer is built with:
 *
 * - The Shape to explore.
 * - The type of Shapes to find: e.g VERTEX, EDGE. This type cannot be SHAPE.
 * - The type of Shapes to avoid. e.g SHELL, EDGE. By default this type is SHAPE which means no restriction on the exploration.
 *
 * The Explorer visits all the structure to find shapes of the requested type which are not contained in the type to avoid.
 *
 * Example to find all the Faces in the Shape S :
 *
 * {@link TopExp_Explorer | `TopExp_Explorer`} Ex; for (Ex.Init(S,TopAbs_FACE); Ex.More(); Ex.Next()) { ProcessFace(Ex.Current()); }
 *
 * // an other way {@link TopExp_Explorer | `TopExp_Explorer`} Ex(S,TopAbs_FACE); while (Ex.More()) { ProcessFace(Ex.Current()); Ex.Next(); }
 *
 * To find all the vertices which are not in an edge :
 *
 * for (Ex.Init(S,TopAbs_VERTEX,TopAbs_EDGE); ...)
 *
 * To find all the faces in a SHELL, then all the faces not in a SHELL :
 *
 * {@link TopExp_Explorer | `TopExp_Explorer`} Ex1, Ex2;
 *
 * for (Ex1.Init(S,TopAbs_SHELL),...) { // visit all shells for (Ex2.Init(Ex1.Current(),TopAbs_FACE),...) { // visit all the faces of the current shell } }
 *
 * for (Ex1.Init(S,TopAbs_FACE,TopAbs_SHELL),...) { // visit all faces not in a shell }
 *
 * If the type to avoid is the same or is less complex than the type to find it has no effect.
 *
 * For example searching edges not in a vertex does not make a difference.
 */
export declare class TopExp_Explorer {
  /**
   * Creates an empty explorer, becomes useful after Init.
   */
  constructor();
  /**
   * Creates an empty explorer, becomes useful after Init.
   */
  constructor(S: TopoDS_Shape, ToFind: TopAbs_ShapeEnum);
  /**
   * Creates an Explorer on the Shape .
   *
   * <ToFind> is the type of shapes to search. TopAbs_VERTEX, TopAbs_EDGE, ...
   *
   * <ToAvoid> is the type of shape to skip in the exploration. If <ToAvoid> is equal or less complex than <ToFind> or if <ToAVoid> is SHAPE it has no effect on the exploration.
   */
  constructor(S: TopoDS_Shape, ToFind: TopAbs_ShapeEnum, ToAvoid?: TopAbs_ShapeEnum);
  /**
   * Resets this explorer on the shape S. It is initialized to search the shape S, for shapes of type ToFind, that are not part of a shape ToAvoid. If the shape ToAvoid is equal to TopAbs_SHAPE, or if it is the same as, or less complex than, the shape ToFind it has no effect on the search.
   */
  Init(S: TopoDS_Shape, ToFind: TopAbs_ShapeEnum, ToAvoid: TopAbs_ShapeEnum): void;
  /**
   * Returns True if there are more shapes in the exploration.
   */
  More(): boolean;
  /**
   * Moves to the next Shape in the exploration.
   */
  Next(): void;
  /**
   * Returns the current shape in the exploration.
   */
  Value(): TopoDS_Shape;
  /**
   * Returns the current shape in the exploration.
   */
  Current(): TopoDS_Shape;
  /**
   * Reinitialize the exploration with the original arguments.
   */
  ReInit(): void;
  /**
   * Return explored shape.
   */
  ExploredShape(): TopoDS_Shape;
  /**
   * Returns the current depth of the exploration. 0 is the shape to explore itself.
   */
  Depth(): number;
  /**
   * Clears the content of the explorer.
   */
  Clear(): void;
  /**
   * Returns a sentinel marking the end of iteration.
   */
  end(): unknown;
  /** Releases the C++ object. The caller must ensure no further access. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Indexed map of ASCII string key-value pairs.
 *
 * Used for metadata exchange in data export operations
 * (e.g. GLTF writer metadata passed to `RWGltf_CafWriter.Perform`).
 */
export declare class TColStd_IndexedDataMapOfStringString {
  constructor();
  /** Release the underlying C++ object to prevent memory leaks. */
  delete(): void;
  [Symbol.dispose](): void;
}

/**
 * Static helper for downcasting generic `TopoDS_Shape` to concrete topology subtypes.
 *
 * OCCT shapes are polymorphic — `BRepAlgoAPI_Fuse.Shape()` returns `TopoDS_Shape`,
 * but algorithms like `BRepFilletAPI_MakeFillet` require `TopoDS_Edge` or `TopoDS_Face`.
 * Use these static casts after verifying the shape type via `TopExp_Explorer`.
 */
export declare class TopoDS {
  /**
   * Downcast a generic shape to an edge.
   *
   * @param shape - The shape to cast (must have `ShapeType() === TopAbs_EDGE`).
   * @returns The same shape typed as `TopoDS_Edge`.
   */
  static Edge(shape: TopoDS_Shape): TopoDS_Edge;
  /**
   * Downcast a generic shape to a wire.
   *
   * @param shape - The shape to cast (must have `ShapeType() === TopAbs_WIRE`).
   * @returns The same shape typed as `TopoDS_Wire`.
   */
  static Wire(shape: TopoDS_Shape): TopoDS_Wire;
  /**
   * Downcast a generic shape to a face.
   *
   * @param shape - The shape to cast (must have `ShapeType() === TopAbs_FACE`).
   * @returns The same shape typed as `TopoDS_Face`.
   */
  static Face(shape: TopoDS_Shape): TopoDS_Face;
  /**
   * Downcast a generic shape to a vertex.
   *
   * @param shape - The shape to cast (must have `ShapeType() === TopAbs_VERTEX`).
   * @returns The same shape typed as `TopoDS_Vertex`.
   */
  static Vertex(shape: TopoDS_Shape): unknown;
  /**
   * Downcast a generic shape to a shell.
   *
   * @param shape - The shape to cast (must have `ShapeType() === TopAbs_SHELL`).
   * @returns The same shape typed as `TopoDS_Shell`.
   */
  static Shell(shape: TopoDS_Shape): unknown;
  /**
   * Downcast a generic shape to a solid.
   *
   * @param shape - The shape to cast (must have `ShapeType() === TopAbs_SOLID`).
   * @returns The same shape typed as `TopoDS_Solid`.
   */
  static Solid(shape: TopoDS_Shape): unknown;
  /**
   * Downcast a generic shape to a compound.
   *
   * @param shape - The shape to cast (must have `ShapeType() === TopAbs_COMPOUND`).
   * @returns The same shape typed as `TopoDS_Compound`.
   */
  static Compound(shape: TopoDS_Shape): unknown;
}

/**
 * OpenCascade.js runtime helpers for exception introspection.
 *
 * Provides access to OCCT exception data when exception handling is enabled
 * (`-fexceptions` / `-sDISABLE_EXCEPTION_CATCHING=0`).
 */
export declare class OCJS {
  /**
   * Extract the `Standard_Failure` data from a caught Emscripten exception pointer.
   *
   * @param exceptionPtr - The raw exception pointer from the Emscripten catch block.
   * @returns The OCCT failure object containing the exception type and message.
   */
  static getStandard_FailureData(exceptionPtr: number): unknown;
  /**
   * Whether this WASM build was compiled with exception handling enabled.
   *
   * @returns `true` if `-fexceptions` / `-sDISABLE_EXCEPTION_CATCHING=0` was used.
   */
  static exceptionsEnabled(): boolean;
  /** Release the underlying C++ object to prevent memory leaks. */
  delete(): void;
  [Symbol.dispose](): void;
}

/** OCCT boolean primitive, mapped to JS `boolean`. */
type Standard_Boolean = boolean;
/** OCCT unsigned byte primitive (0–255), mapped to JS `number`. */
type Standard_Byte = number;
/** OCCT single character, mapped to JS `string`. */
type Standard_Character = string;
/** OCCT null-terminated C string, mapped to JS `string`. */
type Standard_CString = string;
/** OCCT signed integer primitive, mapped to JS `number`. */
type Standard_Integer = number;
/** OCCT double-precision floating-point primitive, mapped to JS `number`. */
type Standard_Real = number;
/** OCCT single-precision floating-point primitive, mapped to JS `number`. */
type Standard_ShortReal = number;
/** OCCT unsigned size/count primitive, mapped to JS `number`. */
type Standard_Size = number;


/**
 * Emscripten virtual filesystem.
 *
 * Provides POSIX-like file operations on the in-memory filesystem backing the
 * WASM module. Use this to load CAD files (STEP, IGES, BREP) into the WASM heap
 * before processing, and to retrieve output files after export.
 *
 * @see {@link https://emscripten.org/docs/api_reference/Filesystem-API.html | Emscripten FS API}
 */
export declare namespace FS {
  /** Result of a path lookup containing the resolved node. */
  interface Lookup {
      path: string;
      node: unknown;
  }

  /** Opaque handle to an open file stream. */
  type FSStream = unknown;
  /** Opaque handle to a filesystem node (file, directory, or device). */
  type FSNode = unknown;
  /** Error thrown by FS operations with an Emscripten errno code. */
  type ErrnoError = unknown;

  /** When `true`, permission checks are bypassed for all FS operations. */
  let ignorePermissions: boolean;
  let trackingDelegate: any;
  let tracking: any;
  let genericErrors: any;

  /**
   * Resolve a path to its filesystem node, optionally following symlinks.
   *
   * @param path - The absolute or relative path to resolve.
   * @param opts - Lookup options (e.g. `{ follow: true }`).
   * @returns The resolved path and filesystem node.
   */
  function lookupPath(path: string, opts: any): unknown;
  /**
   * Get the absolute path for a filesystem node.
   *
   * @param node - The filesystem node.
   * @returns The absolute path string.
   */
  function getPath(node: unknown): string;

  /**
   * Check whether the mode bits indicate a regular file.
   *
   * @param mode - The st_mode value from `stat`.
   * @returns `true` if the mode represents a regular file.
   */
  function isFile(mode: number): boolean;
  /**
   * Check whether the mode bits indicate a directory.
   *
   * @param mode - The st_mode value from `stat`.
   * @returns `true` if the mode represents a directory.
   */
  function isDir(mode: number): boolean;
  /**
   * Check whether the mode bits indicate a symbolic link.
   *
   * @param mode - The st_mode value from `stat`.
   * @returns `true` if the mode represents a symbolic link.
   */
  function isLink(mode: number): boolean;
  /**
   * Check whether the mode bits indicate a character device.
   *
   * @param mode - The st_mode value from `stat`.
   * @returns `true` if the mode represents a character device.
   */
  function isChrdev(mode: number): boolean;
  /**
   * Check whether the mode bits indicate a block device.
   *
   * @param mode - The st_mode value from `stat`.
   * @returns `true` if the mode represents a block device.
   */
  function isBlkdev(mode: number): boolean;
  /**
   * Check whether the mode bits indicate a FIFO (named pipe).
   *
   * @param mode - The st_mode value from `stat`.
   * @returns `true` if the mode represents a FIFO.
   */
  function isFIFO(mode: number): boolean;
  /**
   * Check whether the mode bits indicate a socket.
   *
   * @param mode - The st_mode value from `stat`.
   * @returns `true` if the mode represents a socket.
   */
  function isSocket(mode: number): boolean;

  /**
   * Extract the major device number from a device identifier.
   *
   * @param dev - The combined device identifier.
   * @returns The major device number.
   */
  function major(dev: number): number;
  /**
   * Extract the minor device number from a device identifier.
   *
   * @param dev - The combined device identifier.
   * @returns The minor device number.
   */
  function minor(dev: number): number;
  /**
   * Combine major and minor numbers into a device identifier.
   *
   * @param ma - The major device number.
   * @param mi - The minor device number.
   * @returns The combined device identifier.
   */
  function makedev(ma: number, mi: number): number;
  /**
   * Register a device driver for the given device identifier.
   *
   * @param dev - The combined device identifier.
   * @param ops - Device operation callbacks (read, write, etc.).
   */
  function registerDevice(dev: number, ops: any): void;

  /**
   * Persist or restore the virtual filesystem to/from a backing store.
   *
   * @param populate - When `true`, loads data from the backing store into memory;
   *   when `false`, writes in-memory data to the backing store.
   * @param callback - Called on completion with an optional error.
   */
  function syncfs(populate: boolean, callback: (e: any) => any): void;
  /**
   * Persist or restore the virtual filesystem (callback-first overload).
   *
   * @param callback - Called on completion with an optional error.
   * @param populate - When `true`, loads from backing store (defaults to `false`).
   */
  function syncfs(callback: (e: any) => any, populate?: boolean): void;
  /**
   * Mount a filesystem type at the given mountpoint.
   *
   * @param type - The filesystem type (e.g. `MEMFS`, `IDBFS`).
   * @param opts - Mount options passed to the filesystem driver.
   * @param mountpoint - The path at which to mount.
   * @returns The mount record.
   */
  function mount(type: any, opts: any, mountpoint: string): any;
  /**
   * Unmount the filesystem at the given mountpoint.
   *
   * @param mountpoint - The path to unmount.
   */
  function unmount(mountpoint: string): void;

  /**
   * Create a directory in the virtual filesystem.
   *
   * @param path - The directory path to create.
   * @param mode - Optional POSIX permission bits (default `0o777`).
   * @returns The created directory node.
   */
  function mkdir(path: string, mode?: number): any;
  /**
   * Create a device node in the virtual filesystem.
   *
   * @param path - The path for the device node.
   * @param mode - Optional POSIX permission bits.
   * @param dev - Optional device identifier (from `makedev`).
   * @returns The created device node.
   */
  function mkdev(path: string, mode?: number, dev?: number): any;
  /**
   * Create a symbolic link.
   *
   * @param oldpath - The target path the symlink points to.
   * @param newpath - The path of the symlink itself.
   * @returns The created symlink node.
   */
  function symlink(oldpath: string, newpath: string): any;
  /**
   * Rename (move) a file or directory.
   *
   * @param old_path - The current path.
   * @param new_path - The new path.
   */
  function rename(old_path: string, new_path: string): void;
  /**
   * Remove an empty directory.
   *
   * @param path - The directory to remove.
   */
  function rmdir(path: string): void;
  /**
   * List entries in a directory.
   *
   * @param path - The directory path.
   * @returns Array of entry names (including `.` and `..`).
   */
  function readdir(path: string): any;
  /**
   * Remove a file.
   *
   * @param path - The file to remove.
   */
  function unlink(path: string): void;
  /**
   * Read the target of a symbolic link.
   *
   * @param path - The symlink path.
   * @returns The target path the symlink points to.
   */
  function readlink(path: string): string;
  /**
   * Get file status (size, mode, timestamps, etc.).
   *
   * @param path - The file path.
   * @param dontFollow - When `true`, returns the symlink's own status instead of the target's.
   * @returns An object with POSIX stat fields.
   */
  function stat(path: string, dontFollow?: boolean): any;
  /**
   * Like `stat`, but always returns the symlink's own status.
   *
   * @param path - The file path.
   * @returns An object with POSIX stat fields.
   */
  function lstat(path: string): any;
  /**
   * Change file permission bits.
   *
   * @param path - The file path.
   * @param mode - The new POSIX permission bits.
   * @param dontFollow - When `true`, changes the symlink itself rather than its target.
   */
  function chmod(path: string, mode: number, dontFollow?: boolean): void;
  /**
   * Change permission bits of a symbolic link itself.
   *
   * @param path - The symlink path.
   * @param mode - The new POSIX permission bits.
   */
  function lchmod(path: string, mode: number): void;
  /**
   * Change permission bits of an open file descriptor.
   *
   * @param fd - The file descriptor.
   * @param mode - The new POSIX permission bits.
   */
  function fchmod(fd: number, mode: number): void;
  /**
   * Change file ownership.
   *
   * @param path - The file path.
   * @param uid - The new user ID.
   * @param gid - The new group ID.
   * @param dontFollow - When `true`, changes the symlink itself rather than its target.
   */
  function chown(path: string, uid: number, gid: number, dontFollow?: boolean): void;
  /**
   * Change ownership of a symbolic link itself.
   *
   * @param path - The symlink path.
   * @param uid - The new user ID.
   * @param gid - The new group ID.
   */
  function lchown(path: string, uid: number, gid: number): void;
  /**
   * Change ownership of an open file descriptor.
   *
   * @param fd - The file descriptor.
   * @param uid - The new user ID.
   * @param gid - The new group ID.
   */
  function fchown(fd: number, uid: number, gid: number): void;
  /**
   * Truncate a file to a specified length.
   *
   * @param path - The file path.
   * @param len - The new length in bytes.
   */
  function truncate(path: string, len: number): void;
  /**
   * Truncate an open file descriptor to a specified length.
   *
   * @param fd - The file descriptor.
   * @param len - The new length in bytes.
   */
  function ftruncate(fd: number, len: number): void;
  /**
   * Update access and modification timestamps of a file.
   *
   * @param path - The file path.
   * @param atime - The new access time (seconds since epoch).
   * @param mtime - The new modification time (seconds since epoch).
   */
  function utime(path: string, atime: number, mtime: number): void;
  /**
   * Open a file and return a stream handle.
   *
   * @param path - The file path.
   * @param flags - POSIX open flags as a string (e.g. `'r'`, `'w'`, `'a'`).
   * @param mode - Optional permission bits for newly created files.
   * @param fd_start - Optional starting file descriptor number.
   * @param fd_end - Optional ending file descriptor number.
   * @returns The opened file stream.
   */
  function open(path: string, flags: string, mode?: number, fd_start?: number, fd_end?: number): unknown;
  /**
   * Close an open file stream.
   *
   * @param stream - The stream to close.
   */
  function close(stream: unknown): void;
  /**
   * Reposition the read/write offset of a stream.
   *
   * @param stream - The open file stream.
   * @param offset - The byte offset.
   * @param whence - The reference point (`0` = start, `1` = current, `2` = end).
   * @returns The resulting absolute offset.
   */
  function llseek(stream: unknown, offset: number, whence: number): any;
  /**
   * Read bytes from a stream into a buffer.
   *
   * @param stream - The open file stream.
   * @param buffer - The destination buffer.
   * @param offset - The byte offset within `buffer` to start writing.
   * @param length - Maximum number of bytes to read.
   * @param position - Optional absolute file offset to read from.
   * @returns The number of bytes actually read.
   */
  function read(stream: unknown, buffer: ArrayBufferView, offset: number, length: number, position?: number): number;
  /**
   * Write bytes from a buffer to a stream.
   *
   * @param stream - The open file stream.
   * @param buffer - The source buffer.
   * @param offset - The byte offset within `buffer` to start reading.
   * @param length - Number of bytes to write.
   * @param position - Optional absolute file offset to write at.
   * @param canOwn - When `true`, Emscripten may take ownership of the buffer.
   * @returns The number of bytes actually written.
   */
  function write(
      stream: unknown,
      buffer: ArrayBufferView,
      offset: number,
      length: number,
      position?: number,
      canOwn?: boolean,
  ): number;
  /**
   * Pre-allocate storage for a file region.
   *
   * @param stream - The open file stream.
   * @param offset - Starting byte offset.
   * @param length - Number of bytes to allocate.
   */
  function allocate(stream: unknown, offset: number, length: number): void;
  /**
   * Memory-map a region of a file.
   *
   * @param stream - The open file stream.
   * @param buffer - The target buffer view.
   * @param offset - Byte offset in the buffer.
   * @param length - Length of the mapping in bytes.
   * @param position - Byte offset in the file.
   * @param prot - Memory protection flags.
   * @param flags - Mapping flags.
   * @returns The mapped memory region.
   */
  function mmap(
      stream: unknown,
      buffer: ArrayBufferView,
      offset: number,
      length: number,
      position: number,
      prot: number,
      flags: number,
  ): any;
  /**
   * Perform a device-specific I/O control operation.
   *
   * @param stream - The open file stream.
   * @param cmd - The ioctl command.
   * @param arg - The command argument.
   * @returns The ioctl result.
   */
  function ioctl(stream: unknown, cmd: any, arg: any): any;
  /**
   * Read an entire file as a `Uint8Array` (binary mode).
   *
   * @param path - The file path.
   * @param opts - Options with `encoding: 'binary'`.
   * @returns The file contents as raw bytes.
   */
  function readFile(path: string, opts: { encoding: 'binary'; flags?: string }): Uint8Array;
  /**
   * Read an entire file as a UTF-8 string.
   *
   * @param path - The file path.
   * @param opts - Options with `encoding: 'utf8'`.
   * @returns The file contents as a string.
   */
  function readFile(path: string, opts: { encoding: 'utf8'; flags?: string }): string;
  /**
   * Read an entire file (defaults to binary `Uint8Array`).
   *
   * @param path - The file path.
   * @param opts - Optional flags.
   * @returns The file contents as raw bytes.
   */
  function readFile(path: string, opts?: { flags?: string }): Uint8Array;
  /**
   * Write data to a file, creating it if it does not exist.
   *
   * @param path - The file path.
   * @param data - The content to write (string or binary buffer).
   * @param opts - Optional flags.
   */
  function writeFile(path: string, data: string | ArrayBufferView, opts?: { flags?: string }): void;

  /**
   * Get the current working directory.
   *
   * @returns The absolute path of the current working directory.
   */
  function cwd(): string;
  /**
   * Change the current working directory.
   *
   * @param path - The directory to switch to.
   */
  function chdir(path: string): void;
  /**
   * Initialize the standard I/O streams (stdin, stdout, stderr).
   *
   * @param input - Callback supplying characters for stdin, or `null` for default.
   * @param output - Callback receiving characters from stdout, or `null` for default.
   * @param error - Callback receiving characters from stderr, or `null` for default.
   */
  function init(
      input: null | (() => number | null),
      output: null | ((c: number) => any),
      error: null | ((c: number) => any),
  ): void;

  /**
   * Create a file that is lazily fetched from a URL on first read.
   *
   * @param parent - The parent directory path or node.
   * @param name - The filename.
   * @param url - The URL to fetch the content from.
   * @param canRead - Whether the file is readable.
   * @param canWrite - Whether the file is writable.
   * @returns The created filesystem node.
   */
  function createLazyFile(
      parent: string | FSNode,
      name: string,
      url: string,
      canRead: boolean,
      canWrite: boolean,
  ): unknown;
  /**
   * Create a file that is preloaded (fetched and stored) before the program runs.
   *
   * @param parent - The parent directory path or node.
   * @param name - The filename.
   * @param url - The URL to fetch the content from.
   * @param canRead - Whether the file is readable.
   * @param canWrite - Whether the file is writable.
   * @param onload - Optional callback on successful load.
   * @param onerror - Optional callback on load failure.
   * @param dontCreateFile - When `true`, skips creating the file node.
   * @param canOwn - When `true`, the runtime may take ownership of the data.
   */
  function createPreloadedFile(
      parent: string | FSNode,
      name: string,
      url: string,
      canRead: boolean,
      canWrite: boolean,
      onload?: () => void,
      onerror?: () => void,
      dontCreateFile?: boolean,
      canOwn?: boolean,
  ): void;
  /**
   * Create a file from in-memory data.
   *
   * @param parent - The parent directory path or node.
   * @param name - The filename.
   * @param data - The file contents.
   * @param canRead - Whether the file is readable.
   * @param canWrite - Whether the file is writable.
   * @param canOwn - When `true`, the runtime may take ownership of the data.
   * @returns The created filesystem node.
   */
  function createDataFile(
      parent: string | FSNode,
      name: string,
      data: ArrayBufferView | string,
      canRead: boolean,
      canWrite: boolean,
      canOwn: boolean,
  ): unknown;
  /** Result of analyzing a filesystem path for existence and parent resolution. */
  interface AnalysisResults {
    isRoot: boolean,
    exists: boolean,
    error: Error,
    name: string,
    path: any,
    object: any,
    parentExists: boolean,
    parentPath: any,
    parentObject: any
  }
  /**
   * Analyze a path to determine existence, parent information, and errors.
   *
   * @param path - The path to analyze.
   * @returns Detailed information about the path's resolution.
   */
  function analyzePath(path: string): unknown;
}


/**
 * Extract the exception type and message from a caught `WebAssembly.Exception`.
 *
 * Only available in builds compiled with `-fwasm-exceptions` (native WASM exception handling).
 *
 * @param ex - The caught `WebAssembly.Exception` object.
 * @returns A `[type, message]` tuple where `type` is the C++ exception class name
 *   (e.g. `'Standard_DomainError'`) and `message` is the exception text.
 */
export declare function getExceptionMessage(ex: WebAssembly.Exception): [string, string];
/**
 * Increment the reference count of a `WebAssembly.Exception` to prevent premature disposal.
 *
 * Call this when storing an exception reference beyond its catch scope.
 *
 * @param ex - The exception whose refcount to increment.
 */
export declare function incrementExceptionRefcount(ex: WebAssembly.Exception): void;
/**
 * Decrement the reference count of a `WebAssembly.Exception`, freeing it when count reaches zero.
 *
 * @param ex - The exception whose refcount to decrement.
 */
export declare function decrementExceptionRefcount(ex: WebAssembly.Exception): void;


/**
 * Union of the Emscripten runtime exports and all bound OCCT classes, enums, and functions.
 *
 * Returned by {@link init | `init`} after the WASM module is fully loaded. Access any
 * OCCT binding as a property (e.g. `oc.BRepPrimAPI_MakeBox`) and use
 * the Emscripten virtual filesystem (`oc.FS`).
 */
export type OpenCascadeInstance = {
  /** Emscripten virtual filesystem for reading/writing files in the WASM heap. */
  FS: typeof FS;
} & {
  Message_ProgressRange: typeof Message_ProgressRange;
  NCollection_BaseList: typeof NCollection_BaseList;
  Standard_Transient: typeof Standard_Transient;
  TopLoc_Location: typeof TopLoc_Location;
  gp_Pnt: typeof gp_Pnt;
  gp_Ax2: typeof gp_Ax2;
  gp_Dir: typeof gp_Dir;
  gp_Dir_D: typeof gp_Dir_D;
  GeomAbs_Shape: typeof GeomAbs_Shape;
  Poly_Triangulation: typeof Poly_Triangulation;
  NCollection_Array1_gp_XY: typeof NCollection_Array1_gp_XY;
  NCollection_List_NCollection_List_TopoDS_Shape: typeof NCollection_List_NCollection_List_TopoDS_Shape;
  NCollection_HArray1_handle_StepBasic_NamedUnit: typeof NCollection_HArray1_handle_StepBasic_NamedUnit;
  NCollection_List_handle_BOPDS_PaveBlock: typeof NCollection_List_handle_BOPDS_PaveBlock;
  NCollection_Array1_handle_Geom2d_Curve: typeof NCollection_Array1_handle_Geom2d_Curve;
  NCollection_Sequence_handle_StepFEA_ElementRepresentation: typeof NCollection_Sequence_handle_StepFEA_ElementRepresentation;
  NCollection_DynamicArray_gp_Pnt2d: typeof NCollection_DynamicArray_gp_Pnt2d;
  NCollection_DynamicArray_gp_Pnt2d_1: typeof NCollection_DynamicArray_gp_Pnt2d_1;
  NCollection_DynamicArray_gp_Pnt2d_2: typeof NCollection_DynamicArray_gp_Pnt2d_2;
  NCollection_DynamicArray_gp_Pnt2d_3: typeof NCollection_DynamicArray_gp_Pnt2d_3;
  NCollection_DynamicArray_gp_Pnt2d_4: typeof NCollection_DynamicArray_gp_Pnt2d_4;
  NCollection_HArray1_handle_StepBasic_DerivedUnitElement: typeof NCollection_HArray1_handle_StepBasic_DerivedUnitElement;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeRef: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeRef;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_1: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_1;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_2: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_2;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_3: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_3;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_4: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeRef_4;
  NCollection_Array1_StepShape_ShapeDimensionRepresentationItem: typeof NCollection_Array1_StepShape_ShapeDimensionRepresentationItem;
  NCollection_Array1_StepShape_GeometricSetSelect: typeof NCollection_Array1_StepShape_GeometricSetSelect;
  NCollection_HArray1_handle_StepBasic_Approval: typeof NCollection_HArray1_handle_StepBasic_Approval;
  NCollection_HArray1_StepVisual_CameraModelD3MultiClippingInterectionSelect: typeof NCollection_HArray1_StepVisual_CameraModelD3MultiClippingInterectionSelect;
  NCollection_Sequence_RWGltf_GltfPrimArrayData: typeof NCollection_Sequence_RWGltf_GltfPrimArrayData;
  NCollection_Array1_handle_IGESSolid_Shell: typeof NCollection_Array1_handle_IGESSolid_Shell;
  NCollection_HArray2_gp_XYZ: typeof NCollection_HArray2_gp_XYZ;
  NCollection_Array1_StepAP214_DateAndTimeItem: typeof NCollection_Array1_StepAP214_DateAndTimeItem;
  NCollection_Array1_handle_StepElement_CurveElementSectionDefinition: typeof NCollection_Array1_handle_StepElement_CurveElementSectionDefinition;
  NCollection_DynamicArray_MathRoot_NullInterval: typeof NCollection_DynamicArray_MathRoot_NullInterval;
  NCollection_DynamicArray_MathRoot_NullInterval_1: typeof NCollection_DynamicArray_MathRoot_NullInterval_1;
  NCollection_DynamicArray_MathRoot_NullInterval_2: typeof NCollection_DynamicArray_MathRoot_NullInterval_2;
  NCollection_DynamicArray_MathRoot_NullInterval_3: typeof NCollection_DynamicArray_MathRoot_NullInterval_3;
  NCollection_DynamicArray_MathRoot_NullInterval_4: typeof NCollection_DynamicArray_MathRoot_NullInterval_4;
  NCollection_HArray1_handle_StepGeom_CartesianPoint: typeof NCollection_HArray1_handle_StepGeom_CartesianPoint;
  NCollection_HArray1_StepVisual_PresentationStyleSelect: typeof NCollection_HArray1_StepVisual_PresentationStyleSelect;
  NCollection_Sequence_handle_TDF_Attribute: typeof NCollection_Sequence_handle_TDF_Attribute;
  NCollection_Array2_handle_Standard_Transient: typeof NCollection_Array2_handle_Standard_Transient;
  NCollection_Array1_handle_IGESAppli_Node: typeof NCollection_Array1_handle_IGESAppli_Node;
  NCollection_HArray1_StepVisual_BoxCharacteristicSelect: typeof NCollection_HArray1_StepVisual_BoxCharacteristicSelect;
  NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_RWMesh_NodeAttributes_TopTools_ShapeMapHasher_7;
  NCollection_List_BOPTools_ConnexityBlock: typeof NCollection_List_BOPTools_ConnexityBlock;
  NCollection_Map_BOPDS_Pair: typeof NCollection_Map_BOPDS_Pair;
  NCollection_Map_BOPDS_Pair_2: typeof NCollection_Map_BOPDS_Pair_2;
  NCollection_Map_BOPDS_Pair_3: typeof NCollection_Map_BOPDS_Pair_3;
  NCollection_Map_BOPDS_Pair_4: typeof NCollection_Map_BOPDS_Pair_4;
  NCollection_Map_BOPDS_Pair_5: typeof NCollection_Map_BOPDS_Pair_5;
  NCollection_Map_BOPDS_Pair_6: typeof NCollection_Map_BOPDS_Pair_6;
  NCollection_Map_BOPDS_Pair_7: typeof NCollection_Map_BOPDS_Pair_7;
  NCollection_DynamicArray_BRepGraphInc_CompSolidDef: typeof NCollection_DynamicArray_BRepGraphInc_CompSolidDef;
  NCollection_DynamicArray_BRepGraphInc_CompSolidDef_1: typeof NCollection_DynamicArray_BRepGraphInc_CompSolidDef_1;
  NCollection_DynamicArray_BRepGraphInc_CompSolidDef_2: typeof NCollection_DynamicArray_BRepGraphInc_CompSolidDef_2;
  NCollection_DynamicArray_BRepGraphInc_CompSolidDef_3: typeof NCollection_DynamicArray_BRepGraphInc_CompSolidDef_3;
  NCollection_DynamicArray_BRepGraphInc_CompSolidDef_4: typeof NCollection_DynamicArray_BRepGraphInc_CompSolidDef_4;
  NCollection_Sequence_handle_Poly_Triangulation: typeof NCollection_Sequence_handle_Poly_Triangulation;
  NCollection_Array1_NCollection_Vec3_float: typeof NCollection_Array1_NCollection_Vec3_float;
  NCollection_HArray1_StepAP203_PersonOrganizationItem: typeof NCollection_HArray1_StepAP203_PersonOrganizationItem;
  NCollection_HArray1_StepAP214_DocumentReferenceItem: typeof NCollection_HArray1_StepAP214_DocumentReferenceItem;
  NCollection_Array1_handle_IGESData_IGESEntity: typeof NCollection_Array1_handle_IGESData_IGESEntity;
  NCollection_Sequence_IntTools_CommonPrt: typeof NCollection_Sequence_IntTools_CommonPrt;
  NCollection_HArray1_StepAP203_SpecifiedItem: typeof NCollection_HArray1_StepAP203_SpecifiedItem;
  NCollection_Array1_handle_StepShape_ConnectedFaceSet: typeof NCollection_Array1_handle_StepShape_ConnectedFaceSet;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Compound_4;
  NCollection_Array1_Plate_PinpointConstraint: typeof NCollection_Array1_Plate_PinpointConstraint;
  NCollection_Sequence_IntPatch_Point: typeof NCollection_Sequence_IntPatch_Point;
  NCollection_Array1_handle_StepShape_FaceBound: typeof NCollection_Array1_handle_StepShape_FaceBound;
  NCollection_List_handle_TDF_Delta: typeof NCollection_List_handle_TDF_Delta;
  NCollection_HArray1_handle_StepElement_SurfaceSection: typeof NCollection_HArray1_handle_StepElement_SurfaceSection;
  NCollection_HArray1_handle_StepShape_ConnectedFaceSet: typeof NCollection_HArray1_handle_StepShape_ConnectedFaceSet;
  NCollection_DynamicArray_int: typeof NCollection_DynamicArray_int;
  NCollection_DynamicArray_int_1: typeof NCollection_DynamicArray_int_1;
  NCollection_DynamicArray_int_2: typeof NCollection_DynamicArray_int_2;
  NCollection_DynamicArray_int_3: typeof NCollection_DynamicArray_int_3;
  NCollection_DynamicArray_int_4: typeof NCollection_DynamicArray_int_4;
  NCollection_HSequence_handle_StepElement_ElementMaterial: typeof NCollection_HSequence_handle_StepElement_ElementMaterial;
  NCollection_Array1_handle_IGESSolid_Loop: typeof NCollection_Array1_handle_IGESSolid_Loop;
  NCollection_DynamicArray_handle_Standard_Transient: typeof NCollection_DynamicArray_handle_Standard_Transient;
  NCollection_DynamicArray_handle_Standard_Transient_1: typeof NCollection_DynamicArray_handle_Standard_Transient_1;
  NCollection_DynamicArray_handle_Standard_Transient_2: typeof NCollection_DynamicArray_handle_Standard_Transient_2;
  NCollection_DynamicArray_handle_Standard_Transient_3: typeof NCollection_DynamicArray_handle_Standard_Transient_3;
  NCollection_DynamicArray_handle_Standard_Transient_4: typeof NCollection_DynamicArray_handle_Standard_Transient_4;
  NCollection_Array1_handle_IGESGeom_Boundary: typeof NCollection_Array1_handle_IGESGeom_Boundary;
  NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect: typeof NCollection_Array1_StepVisual_CameraModelD3MultiClippingUnionSelect;
  NCollection_Array1_handle_StepVisual_TessellatedStructuredItem: typeof NCollection_Array1_handle_StepVisual_TessellatedStructuredItem;
  NCollection_Array1_handle_StepElement_SurfaceSection: typeof NCollection_Array1_handle_StepElement_SurfaceSection;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Edge_4;
  NCollection_List_handle_ChFiDS_ElSpine: typeof NCollection_List_handle_ChFiDS_ElSpine;
  NCollection_Sequence_IntTools_PntOn2Faces: typeof NCollection_Sequence_IntTools_PntOn2Faces;
  NCollection_HArray1_gp_Pnt2d: typeof NCollection_HArray1_gp_Pnt2d;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_2: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_2;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_3: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_3;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_4: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_4;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_5: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_5;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_6: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_6;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_7: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_double_7;
  NCollection_Array1_StepAP214_OrganizationItem: typeof NCollection_Array1_StepAP214_OrganizationItem;
  NCollection_Array1_handle_StepDimTol_DatumReference: typeof NCollection_Array1_handle_StepDimTol_DatumReference;
  NCollection_IndexedMap_handle_BOPDS_PaveBlock: typeof NCollection_IndexedMap_handle_BOPDS_PaveBlock;
  NCollection_IndexedMap_handle_BOPDS_PaveBlock_2: typeof NCollection_IndexedMap_handle_BOPDS_PaveBlock_2;
  NCollection_IndexedMap_handle_BOPDS_PaveBlock_3: typeof NCollection_IndexedMap_handle_BOPDS_PaveBlock_3;
  NCollection_IndexedMap_handle_BOPDS_PaveBlock_4: typeof NCollection_IndexedMap_handle_BOPDS_PaveBlock_4;
  NCollection_IndexedMap_handle_BOPDS_PaveBlock_5: typeof NCollection_IndexedMap_handle_BOPDS_PaveBlock_5;
  NCollection_IndexedMap_handle_BOPDS_PaveBlock_6: typeof NCollection_IndexedMap_handle_BOPDS_PaveBlock_6;
  NCollection_IndexedMap_handle_BOPDS_PaveBlock_7: typeof NCollection_IndexedMap_handle_BOPDS_PaveBlock_7;
  NCollection_HArray1_handle_StepVisual_PresentationStyleAssignment: typeof NCollection_HArray1_handle_StepVisual_PresentationStyleAssignment;
  NCollection_Sequence_handle_TDocStd_Document: typeof NCollection_Sequence_handle_TDocStd_Document;
  NCollection_HArray1_ChFiDS_CircSection: typeof NCollection_HArray1_ChFiDS_CircSection;
  NCollection_HArray1_StepAP203_WorkItem: typeof NCollection_HArray1_StepAP203_WorkItem;
  NCollection_Array1_TopoDS_Shape: typeof NCollection_Array1_TopoDS_Shape;
  NCollection_List_IntTools_CurveRangeSample: typeof NCollection_List_IntTools_CurveRangeSample;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeDef: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeDef;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_1: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_1;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_2: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_2;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_3: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_3;
  NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_4: typeof NCollection_DynamicArray_BRepGraphInc_CoEdgeDef_4;
  NCollection_Sequence_TCollection_ExtendedString: typeof NCollection_Sequence_TCollection_ExtendedString;
  NCollection_HArray1_StepShape_ValueQualifier: typeof NCollection_HArray1_StepShape_ValueQualifier;
  NCollection_HArray1_StepVisual_TextOrCharacter: typeof NCollection_HArray1_StepVisual_TextOrCharacter;
  NCollection_HArray1_bool: typeof NCollection_HArray1_bool;
  NCollection_HArray1_StepAP203_StartRequestItem: typeof NCollection_HArray1_StepAP203_StartRequestItem;
  NCollection_List_handle_Law_Function: typeof NCollection_List_handle_Law_Function;
  NCollection_HArray1_handle_IGESDimen_LeaderArrow: typeof NCollection_HArray1_handle_IGESDimen_LeaderArrow;
  NCollection_HArray2_gp_Pnt2d: typeof NCollection_HArray2_gp_Pnt2d;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_7;
  NCollection_HArray1_StepVisual_AnnotationPlaneElement: typeof NCollection_HArray1_StepVisual_AnnotationPlaneElement;
  NCollection_List_double: typeof NCollection_List_double;
  NCollection_HArray1_StepVisual_LayeredItem: typeof NCollection_HArray1_StepVisual_LayeredItem;
  NCollection_List_BOPAlgo_CheckResult: typeof NCollection_List_BOPAlgo_CheckResult;
  NCollection_HArray1_StepAP203_ClassifiedItem: typeof NCollection_HArray1_StepAP203_ClassifiedItem;
  NCollection_Array1_handle_IGESAppli_FiniteElement: typeof NCollection_Array1_handle_IGESAppli_FiniteElement;
  NCollection_HArray1_handle_IGESSolid_VertexList: typeof NCollection_HArray1_handle_IGESSolid_VertexList;
  NCollection_HSequence_handle_Geom_BoundedCurve: typeof NCollection_HSequence_handle_Geom_BoundedCurve;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Wire_4;
  NCollection_Array1_StepVisual_AnnotationPlaneElement: typeof NCollection_Array1_StepVisual_AnnotationPlaneElement;
  NCollection_HArray1_Poly_Triangle: typeof NCollection_HArray1_Poly_Triangle;
  NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem: typeof NCollection_Array1_StepAP214_AutoDesignDateAndTimeItem;
  NCollection_HSequence_handle_StepFEA_ElementGeometricRelationship: typeof NCollection_HSequence_handle_StepFEA_ElementGeometricRelationship;
  NCollection_Sequence_gp_Pnt: typeof NCollection_Sequence_gp_Pnt;
  NCollection_Array1_StepAP214_ExternalIdentificationItem: typeof NCollection_Array1_StepAP214_ExternalIdentificationItem;
  NCollection_Array2_TopoDS_Shape: typeof NCollection_Array2_TopoDS_Shape;
  NCollection_DynamicArray_BOPDS_InterfFZ: typeof NCollection_DynamicArray_BOPDS_InterfFZ;
  NCollection_DynamicArray_BOPDS_InterfFZ_1: typeof NCollection_DynamicArray_BOPDS_InterfFZ_1;
  NCollection_DynamicArray_BOPDS_InterfFZ_2: typeof NCollection_DynamicArray_BOPDS_InterfFZ_2;
  NCollection_DynamicArray_BOPDS_InterfFZ_3: typeof NCollection_DynamicArray_BOPDS_InterfFZ_3;
  NCollection_DynamicArray_BOPDS_InterfFZ_4: typeof NCollection_DynamicArray_BOPDS_InterfFZ_4;
  NCollection_HArray1_handle_StepShape_Face: typeof NCollection_HArray1_handle_StepShape_Face;
  NCollection_Array1_handle_Standard_Transient: typeof NCollection_Array1_handle_Standard_Transient;
  NCollection_DynamicArray_gp_XYZ: typeof NCollection_DynamicArray_gp_XYZ;
  NCollection_DynamicArray_gp_XYZ_1: typeof NCollection_DynamicArray_gp_XYZ_1;
  NCollection_DynamicArray_gp_XYZ_2: typeof NCollection_DynamicArray_gp_XYZ_2;
  NCollection_DynamicArray_gp_XYZ_3: typeof NCollection_DynamicArray_gp_XYZ_3;
  NCollection_DynamicArray_gp_XYZ_4: typeof NCollection_DynamicArray_gp_XYZ_4;
  NCollection_HArray1_gp_Pnt: typeof NCollection_HArray1_gp_Pnt;
  NCollection_List_IntTools_SurfaceRangeSample: typeof NCollection_List_IntTools_SurfaceRangeSample;
  NCollection_DynamicArray_BRepGraphInc_SolidDef: typeof NCollection_DynamicArray_BRepGraphInc_SolidDef;
  NCollection_DynamicArray_BRepGraphInc_SolidDef_1: typeof NCollection_DynamicArray_BRepGraphInc_SolidDef_1;
  NCollection_DynamicArray_BRepGraphInc_SolidDef_2: typeof NCollection_DynamicArray_BRepGraphInc_SolidDef_2;
  NCollection_DynamicArray_BRepGraphInc_SolidDef_3: typeof NCollection_DynamicArray_BRepGraphInc_SolidDef_3;
  NCollection_DynamicArray_BRepGraphInc_SolidDef_4: typeof NCollection_DynamicArray_BRepGraphInc_SolidDef_4;
  NCollection_Sequence_XCAFDimTolObjects_DimensionModif: typeof NCollection_Sequence_XCAFDimTolObjects_DimensionModif;
  NCollection_HArray1_StepVisual_TessellatedEdgeOrVertex: typeof NCollection_HArray1_StepVisual_TessellatedEdgeOrVertex;
  NCollection_Array2_handle_StepGeom_CartesianPoint: typeof NCollection_Array2_handle_StepGeom_CartesianPoint;
  NCollection_HArray1_StepAP214_SecurityClassificationItem: typeof NCollection_HArray1_StepAP214_SecurityClassificationItem;
  NCollection_HArray1_StepShape_GeometricSetSelect: typeof NCollection_HArray1_StepShape_GeometricSetSelect;
  NCollection_HArray1_gp_Vec: typeof NCollection_HArray1_gp_Vec;
  NCollection_Array1_StepAP214_DocumentReferenceItem: typeof NCollection_Array1_StepAP214_DocumentReferenceItem;
  NCollection_Array1_StepAP203_ApprovedItem: typeof NCollection_Array1_StepAP203_ApprovedItem;
  NCollection_Array1_handle_Geom2d_BSplineCurve: typeof NCollection_Array1_handle_Geom2d_BSplineCurve;
  NCollection_Array1_handle_StepFEA_CurveElementEndOffset: typeof NCollection_Array1_handle_StepFEA_CurveElementEndOffset;
  NCollection_HSequence_handle_STEPSelections_AssemblyLink: typeof NCollection_HSequence_handle_STEPSelections_AssemblyLink;
  NCollection_List_BOPDS_Pave: typeof NCollection_List_BOPDS_Pave;
  NCollection_Array1_unsignedchar: typeof NCollection_Array1_unsignedchar;
  NCollection_Array1_gp_Lin: typeof NCollection_Array1_gp_Lin;
  NCollection_DynamicArray_BRepGraphInc_SolidRef: typeof NCollection_DynamicArray_BRepGraphInc_SolidRef;
  NCollection_DynamicArray_BRepGraphInc_SolidRef_1: typeof NCollection_DynamicArray_BRepGraphInc_SolidRef_1;
  NCollection_DynamicArray_BRepGraphInc_SolidRef_2: typeof NCollection_DynamicArray_BRepGraphInc_SolidRef_2;
  NCollection_DynamicArray_BRepGraphInc_SolidRef_3: typeof NCollection_DynamicArray_BRepGraphInc_SolidRef_3;
  NCollection_DynamicArray_BRepGraphInc_SolidRef_4: typeof NCollection_DynamicArray_BRepGraphInc_SolidRef_4;
  NCollection_Array1_handle_IGESSolid_VertexList: typeof NCollection_Array1_handle_IGESSolid_VertexList;
  NCollection_Array1_StepAP203_CertifiedItem: typeof NCollection_Array1_StepAP203_CertifiedItem;
  NCollection_Sequence_handle_Expr_GeneralExpression: typeof NCollection_Sequence_handle_Expr_GeneralExpression;
  NCollection_HArray1_handle_StepRepr_PropertyDefinitionRepresentation: typeof NCollection_HArray1_handle_StepRepr_PropertyDefinitionRepresentation;
  NCollection_Sequence_IntSurf_PathPoint: typeof NCollection_Sequence_IntSurf_PathPoint;
  NCollection_Sequence_handle_MAT_Arc: typeof NCollection_Sequence_handle_MAT_Arc;
  NCollection_HArray1_handle_StepElement_CurveElementEndReleasePacket: typeof NCollection_HArray1_handle_StepElement_CurveElementEndReleasePacket;
  NCollection_HArray1_StepAP214_AutoDesignGeneralOrgItem: typeof NCollection_HArray1_StepAP214_AutoDesignGeneralOrgItem;
  NCollection_HArray1_StepVisual_FillStyleSelect: typeof NCollection_HArray1_StepVisual_FillStyleSelect;
  NCollection_Array1_handle_IGESSolid_Face: typeof NCollection_Array1_handle_IGESSolid_Face;
  NCollection_List_handle_TDF_AttributeDelta: typeof NCollection_List_handle_TDF_AttributeDelta;
  NCollection_HSequence_TCollection_ExtendedString: typeof NCollection_HSequence_TCollection_ExtendedString;
  NCollection_List_BRepOffset_Interval: typeof NCollection_List_BRepOffset_Interval;
  NCollection_Array1_handle_IGESDimen_LeaderArrow: typeof NCollection_Array1_handle_IGESDimen_LeaderArrow;
  NCollection_Array1_double: typeof NCollection_Array1_double;
  NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape: typeof NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape;
  NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_2: typeof NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_2;
  NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_3: typeof NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_3;
  NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_4: typeof NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_4;
  NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_5: typeof NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_5;
  NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_6: typeof NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_6;
  NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_7: typeof NCollection_IndexedDataMap_TDF_Label_TopoDS_Shape_7;
  NCollection_Array1_StepDimTol_DatumSystemOrReference: typeof NCollection_Array1_StepDimTol_DatumSystemOrReference;
  NCollection_Array1_handle_HLRAlgo_PolyData: typeof NCollection_Array1_handle_HLRAlgo_PolyData;
  NCollection_Sequence_handle_IFSelect_Selection: typeof NCollection_Sequence_handle_IFSelect_Selection;
  NCollection_Array1_TCollection_ExtendedString: typeof NCollection_Array1_TCollection_ExtendedString;
  NCollection_HArray1_handle_StepShape_OrientedClosedShell: typeof NCollection_HArray1_handle_StepShape_OrientedClosedShell;
  NCollection_Sequence_Plate_PinpointConstraint: typeof NCollection_Sequence_Plate_PinpointConstraint;
  NCollection_HArray1_StepDimTol_DatumSystemOrReference: typeof NCollection_HArray1_StepDimTol_DatumSystemOrReference;
  NCollection_HArray1_gp_Vec2d: typeof NCollection_HArray1_gp_Vec2d;
  NCollection_Sequence_IntTools_Root: typeof NCollection_Sequence_IntTools_Root;
  NCollection_List_TCollection_AsciiString: typeof NCollection_List_TCollection_AsciiString;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_1: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_1;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_2: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_2;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_3: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_3;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_4: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_CoEdge_4;
  NCollection_Array1_gp_Dir: typeof NCollection_Array1_gp_Dir;
  NCollection_HSequence_handle_ShapeAnalysis_FreeBoundData: typeof NCollection_HSequence_handle_ShapeAnalysis_FreeBoundData;
  NCollection_DynamicArray_BRepMesh_Circle: typeof NCollection_DynamicArray_BRepMesh_Circle;
  NCollection_DynamicArray_BRepMesh_Circle_1: typeof NCollection_DynamicArray_BRepMesh_Circle_1;
  NCollection_DynamicArray_BRepMesh_Circle_2: typeof NCollection_DynamicArray_BRepMesh_Circle_2;
  NCollection_DynamicArray_BRepMesh_Circle_3: typeof NCollection_DynamicArray_BRepMesh_Circle_3;
  NCollection_DynamicArray_BRepMesh_Circle_4: typeof NCollection_DynamicArray_BRepMesh_Circle_4;
  NCollection_Array1_handle_IGESGeom_TransformationMatrix: typeof NCollection_Array1_handle_IGESGeom_TransformationMatrix;
  NCollection_Array1_StepAP214_GroupItem: typeof NCollection_Array1_StepAP214_GroupItem;
  NCollection_Array1_StepAP214_AutoDesignReferencingItem: typeof NCollection_Array1_StepAP214_AutoDesignReferencingItem;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_1: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_1;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_2: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_2;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_3: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_3;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_4: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Shell_4;
  NCollection_HArray1_StepAP214_DateItem: typeof NCollection_HArray1_StepAP214_DateItem;
  NCollection_HArray1_handle_Geom_Curve: typeof NCollection_HArray1_handle_Geom_Curve;
  NCollection_IndexedMap_Message_MetricType: typeof NCollection_IndexedMap_Message_MetricType;
  NCollection_IndexedMap_Message_MetricType_2: typeof NCollection_IndexedMap_Message_MetricType_2;
  NCollection_IndexedMap_Message_MetricType_3: typeof NCollection_IndexedMap_Message_MetricType_3;
  NCollection_IndexedMap_Message_MetricType_4: typeof NCollection_IndexedMap_Message_MetricType_4;
  NCollection_IndexedMap_Message_MetricType_5: typeof NCollection_IndexedMap_Message_MetricType_5;
  NCollection_IndexedMap_Message_MetricType_6: typeof NCollection_IndexedMap_Message_MetricType_6;
  NCollection_IndexedMap_Message_MetricType_7: typeof NCollection_IndexedMap_Message_MetricType_7;
  NCollection_IndexedMap_TCollection_AsciiString: typeof NCollection_IndexedMap_TCollection_AsciiString;
  NCollection_IndexedMap_TCollection_AsciiString_2: typeof NCollection_IndexedMap_TCollection_AsciiString_2;
  NCollection_IndexedMap_TCollection_AsciiString_3: typeof NCollection_IndexedMap_TCollection_AsciiString_3;
  NCollection_IndexedMap_TCollection_AsciiString_4: typeof NCollection_IndexedMap_TCollection_AsciiString_4;
  NCollection_IndexedMap_TCollection_AsciiString_5: typeof NCollection_IndexedMap_TCollection_AsciiString_5;
  NCollection_IndexedMap_TCollection_AsciiString_6: typeof NCollection_IndexedMap_TCollection_AsciiString_6;
  NCollection_IndexedMap_TCollection_AsciiString_7: typeof NCollection_IndexedMap_TCollection_AsciiString_7;
  NCollection_Array1_handle_StepRepr_RepresentationItem: typeof NCollection_Array1_handle_StepRepr_RepresentationItem;
  NCollection_HArray2_handle_StepGeom_SurfacePatch: typeof NCollection_HArray2_handle_StepGeom_SurfacePatch;
  NCollection_DynamicArray_BRepGraphInc_ShellRef: typeof NCollection_DynamicArray_BRepGraphInc_ShellRef;
  NCollection_DynamicArray_BRepGraphInc_ShellRef_1: typeof NCollection_DynamicArray_BRepGraphInc_ShellRef_1;
  NCollection_DynamicArray_BRepGraphInc_ShellRef_2: typeof NCollection_DynamicArray_BRepGraphInc_ShellRef_2;
  NCollection_DynamicArray_BRepGraphInc_ShellRef_3: typeof NCollection_DynamicArray_BRepGraphInc_ShellRef_3;
  NCollection_DynamicArray_BRepGraphInc_ShellRef_4: typeof NCollection_DynamicArray_BRepGraphInc_ShellRef_4;
  NCollection_DataMap_TDF_Label_int: typeof NCollection_DataMap_TDF_Label_int;
  NCollection_DataMap_TDF_Label_int_2: typeof NCollection_DataMap_TDF_Label_int_2;
  NCollection_DataMap_TDF_Label_int_3: typeof NCollection_DataMap_TDF_Label_int_3;
  NCollection_DataMap_TDF_Label_int_4: typeof NCollection_DataMap_TDF_Label_int_4;
  NCollection_DataMap_TDF_Label_int_5: typeof NCollection_DataMap_TDF_Label_int_5;
  NCollection_DataMap_TDF_Label_int_6: typeof NCollection_DataMap_TDF_Label_int_6;
  NCollection_DataMap_TDF_Label_int_7: typeof NCollection_DataMap_TDF_Label_int_7;
  NCollection_Array1_handle_IGESGraph_Color: typeof NCollection_Array1_handle_IGESGraph_Color;
  NCollection_HArray1_handle_StepShape_Edge: typeof NCollection_HArray1_handle_StepShape_Edge;
  NCollection_Sequence_handle_MAT_BasicElt: typeof NCollection_Sequence_handle_MAT_BasicElt;
  NCollection_HArray1_TCollection_AsciiString: typeof NCollection_HArray1_TCollection_AsciiString;
  NCollection_DataMap_int_bool: typeof NCollection_DataMap_int_bool;
  NCollection_DataMap_int_bool_2: typeof NCollection_DataMap_int_bool_2;
  NCollection_DataMap_int_bool_3: typeof NCollection_DataMap_int_bool_3;
  NCollection_DataMap_int_bool_4: typeof NCollection_DataMap_int_bool_4;
  NCollection_DataMap_int_bool_5: typeof NCollection_DataMap_int_bool_5;
  NCollection_DataMap_int_bool_6: typeof NCollection_DataMap_int_bool_6;
  NCollection_DataMap_int_bool_7: typeof NCollection_DataMap_int_bool_7;
  NCollection_List_IntSurf_PntOn2S: typeof NCollection_List_IntSurf_PntOn2S;
  NCollection_HArray1_StepShape_Shell: typeof NCollection_HArray1_StepShape_Shell;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Shell_4;
  NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute: typeof NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute;
  NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_2: typeof NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_2;
  NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_3: typeof NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_3;
  NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_4: typeof NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_4;
  NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_5: typeof NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_5;
  NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_6: typeof NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_6;
  NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_7: typeof NCollection_DataMap_handle_TDF_Attribute_handle_TDF_Attribute_7;
  NCollection_HArray1_StepGeom_PcurveOrSurface: typeof NCollection_HArray1_StepGeom_PcurveOrSurface;
  NCollection_Array1_handle_StepBasic_Document: typeof NCollection_Array1_handle_StepBasic_Document;
  NCollection_Sequence_BRepExtrema_SolutionElem: typeof NCollection_Sequence_BRepExtrema_SolutionElem;
  NCollection_Map_handle_TNaming_NamedShape: typeof NCollection_Map_handle_TNaming_NamedShape;
  NCollection_Map_handle_TNaming_NamedShape_2: typeof NCollection_Map_handle_TNaming_NamedShape_2;
  NCollection_Map_handle_TNaming_NamedShape_3: typeof NCollection_Map_handle_TNaming_NamedShape_3;
  NCollection_Map_handle_TNaming_NamedShape_4: typeof NCollection_Map_handle_TNaming_NamedShape_4;
  NCollection_Map_handle_TNaming_NamedShape_5: typeof NCollection_Map_handle_TNaming_NamedShape_5;
  NCollection_Map_handle_TNaming_NamedShape_6: typeof NCollection_Map_handle_TNaming_NamedShape_6;
  NCollection_Map_handle_TNaming_NamedShape_7: typeof NCollection_Map_handle_TNaming_NamedShape_7;
  NCollection_HArray1_handle_StepShape_OrientedEdge: typeof NCollection_HArray1_handle_StepShape_OrientedEdge;
  NCollection_Sequence_handle_Standard_Transient: typeof NCollection_Sequence_handle_Standard_Transient;
  NCollection_HArray1_Bnd_Box: typeof NCollection_HArray1_Bnd_Box;
  NCollection_Array1_gp_XYZ: typeof NCollection_Array1_gp_XYZ;
  NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId: typeof NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId;
  NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_2: typeof NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_2;
  NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_3: typeof NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_3;
  NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_4: typeof NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_4;
  NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_5: typeof NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_5;
  NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_6: typeof NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_6;
  NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_7: typeof NCollection_DataMap_BRepGraph_RefUID_BRepGraph_RefId_7;
  NCollection_Array1_bool: typeof NCollection_Array1_bool;
  NCollection_Array1_AppParCurves_ConstraintCouple: typeof NCollection_Array1_AppParCurves_ConstraintCouple;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Product_4;
  NCollection_HArray1_StepAP214_PresentedItemSelect: typeof NCollection_HArray1_StepAP214_PresentedItemSelect;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CompSolid_4;
  NCollection_Array1_handle_Standard_Persistent: typeof NCollection_Array1_handle_Standard_Persistent;
  NCollection_Array1_handle_StepBasic_Organization: typeof NCollection_Array1_handle_StepBasic_Organization;
  NCollection_Array1_handle_StepGeom_CartesianPoint: typeof NCollection_Array1_handle_StepGeom_CartesianPoint;
  NCollection_Array1_int: typeof NCollection_Array1_int;
  NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect: typeof NCollection_Array1_StepAP214_AutoDesignPresentedItemSelect;
  NCollection_Sequence_ShapeFix_WireSegment: typeof NCollection_Sequence_ShapeFix_WireSegment;
  NCollection_Sequence_handle_StepElement_ElementMaterial: typeof NCollection_Sequence_handle_StepElement_ElementMaterial;
  NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId: typeof NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId;
  NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_2: typeof NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_2;
  NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_3: typeof NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_3;
  NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_4: typeof NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_4;
  NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_5: typeof NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_5;
  NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_6: typeof NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_6;
  NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_7: typeof NCollection_DataMap_BRepGraph_NodeId_BRepGraph_NodeId_7;
  NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_BRepTopAdaptor_Tool_TopTools_ShapeMapHasher_7;
  NCollection_DynamicArray_handle_Adaptor3d_Surface: typeof NCollection_DynamicArray_handle_Adaptor3d_Surface;
  NCollection_DynamicArray_handle_Adaptor3d_Surface_1: typeof NCollection_DynamicArray_handle_Adaptor3d_Surface_1;
  NCollection_DynamicArray_handle_Adaptor3d_Surface_2: typeof NCollection_DynamicArray_handle_Adaptor3d_Surface_2;
  NCollection_DynamicArray_handle_Adaptor3d_Surface_3: typeof NCollection_DynamicArray_handle_Adaptor3d_Surface_3;
  NCollection_DynamicArray_handle_Adaptor3d_Surface_4: typeof NCollection_DynamicArray_handle_Adaptor3d_Surface_4;
  NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher: typeof NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher;
  NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_2: typeof NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_2;
  NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_3: typeof NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_3;
  NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_4: typeof NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_4;
  NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_5: typeof NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_5;
  NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_6: typeof NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_6;
  NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_7: typeof NCollection_IndexedDataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_7;
  NCollection_HArray1_TDF_Label: typeof NCollection_HArray1_TDF_Label;
  NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher: typeof NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher;
  NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_2: typeof NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_2;
  NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_3: typeof NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_3;
  NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_4: typeof NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_4;
  NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_5: typeof NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_5;
  NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_6: typeof NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_6;
  NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_7: typeof NCollection_IndexedMap_TopoDS_Shape_TopTools_ShapeMapHasher_7;
  NCollection_HArray1_StepVisual_InvisibleItem: typeof NCollection_HArray1_StepVisual_InvisibleItem;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_1: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_1;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_2: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_2;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_3: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_3;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_4: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Occurrence_4;
  NCollection_HArray1_StepAP214_PersonAndOrganizationItem: typeof NCollection_HArray1_StepAP214_PersonAndOrganizationItem;
  NCollection_DynamicArray_double: typeof NCollection_DynamicArray_double;
  NCollection_DynamicArray_double_1: typeof NCollection_DynamicArray_double_1;
  NCollection_DynamicArray_double_2: typeof NCollection_DynamicArray_double_2;
  NCollection_DynamicArray_double_3: typeof NCollection_DynamicArray_double_3;
  NCollection_DynamicArray_double_4: typeof NCollection_DynamicArray_double_4;
  NCollection_HArray1_StepFEA_DegreeOfFreedom: typeof NCollection_HArray1_StepFEA_DegreeOfFreedom;
  NCollection_DataMap_TCollection_AsciiString_int: typeof NCollection_DataMap_TCollection_AsciiString_int;
  NCollection_DataMap_TCollection_AsciiString_int_2: typeof NCollection_DataMap_TCollection_AsciiString_int_2;
  NCollection_DataMap_TCollection_AsciiString_int_3: typeof NCollection_DataMap_TCollection_AsciiString_int_3;
  NCollection_DataMap_TCollection_AsciiString_int_4: typeof NCollection_DataMap_TCollection_AsciiString_int_4;
  NCollection_DataMap_TCollection_AsciiString_int_5: typeof NCollection_DataMap_TCollection_AsciiString_int_5;
  NCollection_DataMap_TCollection_AsciiString_int_6: typeof NCollection_DataMap_TCollection_AsciiString_int_6;
  NCollection_DataMap_TCollection_AsciiString_int_7: typeof NCollection_DataMap_TCollection_AsciiString_int_7;
  NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject: typeof NCollection_Sequence_handle_XCAFDimTolObjects_DimensionObject;
  NCollection_HArray1_float: typeof NCollection_HArray1_float;
  NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_BRepOffset_Offset_TopTools_ShapeMapHasher_7;
  NCollection_IndexedMap_TDF_Label: typeof NCollection_IndexedMap_TDF_Label;
  NCollection_IndexedMap_TDF_Label_2: typeof NCollection_IndexedMap_TDF_Label_2;
  NCollection_IndexedMap_TDF_Label_3: typeof NCollection_IndexedMap_TDF_Label_3;
  NCollection_IndexedMap_TDF_Label_4: typeof NCollection_IndexedMap_TDF_Label_4;
  NCollection_IndexedMap_TDF_Label_5: typeof NCollection_IndexedMap_TDF_Label_5;
  NCollection_IndexedMap_TDF_Label_6: typeof NCollection_IndexedMap_TDF_Label_6;
  NCollection_IndexedMap_TDF_Label_7: typeof NCollection_IndexedMap_TDF_Label_7;
  NCollection_DynamicArray_BRepGraphInc_ShellDef: typeof NCollection_DynamicArray_BRepGraphInc_ShellDef;
  NCollection_DynamicArray_BRepGraphInc_ShellDef_1: typeof NCollection_DynamicArray_BRepGraphInc_ShellDef_1;
  NCollection_DynamicArray_BRepGraphInc_ShellDef_2: typeof NCollection_DynamicArray_BRepGraphInc_ShellDef_2;
  NCollection_DynamicArray_BRepGraphInc_ShellDef_3: typeof NCollection_DynamicArray_BRepGraphInc_ShellDef_3;
  NCollection_DynamicArray_BRepGraphInc_ShellDef_4: typeof NCollection_DynamicArray_BRepGraphInc_ShellDef_4;
  NCollection_HArray1_handle_StepVisual_CurveStyleFontPattern: typeof NCollection_HArray1_handle_StepVisual_CurveStyleFontPattern;
  NCollection_Array1_StepGeom_SurfaceBoundary: typeof NCollection_Array1_StepGeom_SurfaceBoundary;
  NCollection_Array2_handle_Geom_Surface: typeof NCollection_Array2_handle_Geom_Surface;
  NCollection_HArray1_handle_StepRepr_MaterialPropertyRepresentation: typeof NCollection_HArray1_handle_StepRepr_MaterialPropertyRepresentation;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_CoEdge_4;
  NCollection_List_handle_TNaming_NamedShape: typeof NCollection_List_handle_TNaming_NamedShape;
  NCollection_HSequence_handle_Units_Quantity: typeof NCollection_HSequence_handle_Units_Quantity;
  NCollection_Array1_StepAP214_AutoDesignDatedItem: typeof NCollection_Array1_StepAP214_AutoDesignDatedItem;
  NCollection_Sequence_TCollection_AsciiString: typeof NCollection_Sequence_TCollection_AsciiString;
  NCollection_List_TopoDS_Shape: typeof NCollection_List_TopoDS_Shape;
  NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_TopoDS_Shape_TopTools_ShapeMapHasher_7;
  NCollection_Sequence_AppParCurves_MultiCurve: typeof NCollection_Sequence_AppParCurves_MultiCurve;
  NCollection_HSequence_handle_Geom2d_BoundedCurve: typeof NCollection_HSequence_handle_Geom2d_BoundedCurve;
  NCollection_HArray1_handle_StepElement_VolumeElementPurposeMember: typeof NCollection_HArray1_handle_StepElement_VolumeElementPurposeMember;
  NCollection_HSequence_handle_Standard_Transient: typeof NCollection_HSequence_handle_Standard_Transient;
  NCollection_Array1_StepAP203_DateTimeItem: typeof NCollection_Array1_StepAP203_DateTimeItem;
  NCollection_HSequence_handle_Units_Unit: typeof NCollection_HSequence_handle_Units_Unit;
  NCollection_Array1_StepGeom_TrimmingSelect: typeof NCollection_Array1_StepGeom_TrimmingSelect;
  NCollection_Array2_gp_Vec: typeof NCollection_Array2_gp_Vec;
  NCollection_Sequence_handle_ChFiDS_SurfData: typeof NCollection_Sequence_handle_ChFiDS_SurfData;
  NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher: typeof NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher;
  NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_2: typeof NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_2;
  NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_3: typeof NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_3;
  NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_4: typeof NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_4;
  NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_5: typeof NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_5;
  NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_6: typeof NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_6;
  NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_7: typeof NCollection_IndexedDataMap_TopoDS_Shape_XCAFPrs_Style_TopTools_ShapeMapHasher_7;
  NCollection_HArray1_handle_IGESAppli_FiniteElement: typeof NCollection_HArray1_handle_IGESAppli_FiniteElement;
  NCollection_HArray1_handle_Geom_BSplineCurve: typeof NCollection_HArray1_handle_Geom_BSplineCurve;
  NCollection_DynamicArray_BRepGraphInc_FaceRef: typeof NCollection_DynamicArray_BRepGraphInc_FaceRef;
  NCollection_DynamicArray_BRepGraphInc_FaceRef_1: typeof NCollection_DynamicArray_BRepGraphInc_FaceRef_1;
  NCollection_DynamicArray_BRepGraphInc_FaceRef_2: typeof NCollection_DynamicArray_BRepGraphInc_FaceRef_2;
  NCollection_DynamicArray_BRepGraphInc_FaceRef_3: typeof NCollection_DynamicArray_BRepGraphInc_FaceRef_3;
  NCollection_DynamicArray_BRepGraphInc_FaceRef_4: typeof NCollection_DynamicArray_BRepGraphInc_FaceRef_4;
  NCollection_List_int: typeof NCollection_List_int;
  NCollection_Map_int: typeof NCollection_Map_int;
  NCollection_Map_int_2: typeof NCollection_Map_int_2;
  NCollection_Map_int_3: typeof NCollection_Map_int_3;
  NCollection_Map_int_4: typeof NCollection_Map_int_4;
  NCollection_Map_int_5: typeof NCollection_Map_int_5;
  NCollection_Map_int_6: typeof NCollection_Map_int_6;
  NCollection_Map_int_7: typeof NCollection_Map_int_7;
  NCollection_DynamicArray_BOPDS_InterfVF: typeof NCollection_DynamicArray_BOPDS_InterfVF;
  NCollection_DynamicArray_BOPDS_InterfVF_1: typeof NCollection_DynamicArray_BOPDS_InterfVF_1;
  NCollection_DynamicArray_BOPDS_InterfVF_2: typeof NCollection_DynamicArray_BOPDS_InterfVF_2;
  NCollection_DynamicArray_BOPDS_InterfVF_3: typeof NCollection_DynamicArray_BOPDS_InterfVF_3;
  NCollection_DynamicArray_BOPDS_InterfVF_4: typeof NCollection_DynamicArray_BOPDS_InterfVF_4;
  NCollection_DynamicArray_BOPDS_Curve: typeof NCollection_DynamicArray_BOPDS_Curve;
  NCollection_DynamicArray_BOPDS_Curve_1: typeof NCollection_DynamicArray_BOPDS_Curve_1;
  NCollection_DynamicArray_BOPDS_Curve_2: typeof NCollection_DynamicArray_BOPDS_Curve_2;
  NCollection_DynamicArray_BOPDS_Curve_3: typeof NCollection_DynamicArray_BOPDS_Curve_3;
  NCollection_DynamicArray_BOPDS_Curve_4: typeof NCollection_DynamicArray_BOPDS_Curve_4;
  NCollection_Array2_gp_Pnt: typeof NCollection_Array2_gp_Pnt;
  NCollection_HSequence_handle_Transfer_Finder: typeof NCollection_HSequence_handle_Transfer_Finder;
  NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape: typeof NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape;
  NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_2: typeof NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_2;
  NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_3: typeof NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_3;
  NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_4: typeof NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_4;
  NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_5: typeof NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_5;
  NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_6: typeof NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_6;
  NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_7: typeof NCollection_DataMap_handle_StepRepr_RepresentationItem_TopoDS_Shape_7;
  NCollection_List_handle_Poly_Triangulation: typeof NCollection_List_handle_Poly_Triangulation;
  NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect: typeof NCollection_Array1_StepVisual_CameraModelD3MultiClippingInterectionSelect;
  NCollection_HArray1_StepAP214_AutoDesignDatedItem: typeof NCollection_HArray1_StepAP214_AutoDesignDatedItem;
  NCollection_Array1_StepAP214_SecurityClassificationItem: typeof NCollection_Array1_StepAP214_SecurityClassificationItem;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_2: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_2;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_3: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_3;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_4: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_4;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_5: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_5;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_6: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_6;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_7: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_int_7;
  NCollection_HArray1_handle_StepFEA_CurveElementInterval: typeof NCollection_HArray1_handle_StepFEA_CurveElementInterval;
  NCollection_DataMap_int_int: typeof NCollection_DataMap_int_int;
  NCollection_DataMap_int_int_2: typeof NCollection_DataMap_int_int_2;
  NCollection_DataMap_int_int_3: typeof NCollection_DataMap_int_int_3;
  NCollection_DataMap_int_int_4: typeof NCollection_DataMap_int_int_4;
  NCollection_DataMap_int_int_5: typeof NCollection_DataMap_int_int_5;
  NCollection_DataMap_int_int_6: typeof NCollection_DataMap_int_int_6;
  NCollection_DataMap_int_int_7: typeof NCollection_DataMap_int_int_7;
  NCollection_Array1_handle_Geom2d_BezierCurve: typeof NCollection_Array1_handle_Geom2d_BezierCurve;
  NCollection_Array1_AppParCurves_MultiPoint: typeof NCollection_Array1_AppParCurves_MultiPoint;
  NCollection_Array2_handle_StepGeom_SurfacePatch: typeof NCollection_Array2_handle_StepGeom_SurfacePatch;
  NCollection_Sequence_handle_STEPSelections_AssemblyLink: typeof NCollection_Sequence_handle_STEPSelections_AssemblyLink;
  NCollection_HArray1_handle_StepFEA_CurveElementEndOffset: typeof NCollection_HArray1_handle_StepFEA_CurveElementEndOffset;
  NCollection_Sequence_handle_Storage_Root: typeof NCollection_Sequence_handle_Storage_Root;
  NCollection_HSequence_double: typeof NCollection_HSequence_double;
  NCollection_HArray1_StepAP214_ExternalIdentificationItem: typeof NCollection_HArray1_StepAP214_ExternalIdentificationItem;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Occurrence_4;
  NCollection_HArray2_handle_StepGeom_CartesianPoint: typeof NCollection_HArray2_handle_StepGeom_CartesianPoint;
  NCollection_DynamicArray_BOPDS_InterfZZ: typeof NCollection_DynamicArray_BOPDS_InterfZZ;
  NCollection_DynamicArray_BOPDS_InterfZZ_1: typeof NCollection_DynamicArray_BOPDS_InterfZZ_1;
  NCollection_DynamicArray_BOPDS_InterfZZ_2: typeof NCollection_DynamicArray_BOPDS_InterfZZ_2;
  NCollection_DynamicArray_BOPDS_InterfZZ_3: typeof NCollection_DynamicArray_BOPDS_InterfZZ_3;
  NCollection_DynamicArray_BOPDS_InterfZZ_4: typeof NCollection_DynamicArray_BOPDS_InterfZZ_4;
  NCollection_Array1_HLRAlgo_PolyHidingData: typeof NCollection_Array1_HLRAlgo_PolyHidingData;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_1: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_1;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_2: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_2;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_3: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_3;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_4: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Child_4;
  NCollection_List_unsignedchar: typeof NCollection_List_unsignedchar;
  NCollection_HSequence_int: typeof NCollection_HSequence_int;
  NCollection_HArray1_handle_StepBasic_UncertaintyMeasureWithUnit: typeof NCollection_HArray1_handle_StepBasic_UncertaintyMeasureWithUnit;
  NCollection_Array1_handle_StepDimTol_DatumReferenceElement: typeof NCollection_Array1_handle_StepDimTol_DatumReferenceElement;
  NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif: typeof NCollection_Sequence_XCAFDimTolObjects_GeomToleranceModif;
  NCollection_HArray1_handle_StepFEA_NodeRepresentation: typeof NCollection_HArray1_handle_StepFEA_NodeRepresentation;
  NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_Bnd_Box_TopTools_ShapeMapHasher_7;
  NCollection_HSequence_handle_StepFEA_ElementRepresentation: typeof NCollection_HSequence_handle_StepFEA_ElementRepresentation;
  NCollection_Sequence_handle_MAT2d_Connexion: typeof NCollection_Sequence_handle_MAT2d_Connexion;
  NCollection_DynamicArray_BRepGraphInc_CompoundDef: typeof NCollection_DynamicArray_BRepGraphInc_CompoundDef;
  NCollection_DynamicArray_BRepGraphInc_CompoundDef_1: typeof NCollection_DynamicArray_BRepGraphInc_CompoundDef_1;
  NCollection_DynamicArray_BRepGraphInc_CompoundDef_2: typeof NCollection_DynamicArray_BRepGraphInc_CompoundDef_2;
  NCollection_DynamicArray_BRepGraphInc_CompoundDef_3: typeof NCollection_DynamicArray_BRepGraphInc_CompoundDef_3;
  NCollection_DynamicArray_BRepGraphInc_CompoundDef_4: typeof NCollection_DynamicArray_BRepGraphInc_CompoundDef_4;
  NCollection_Sequence_gp_Pnt2d: typeof NCollection_Sequence_gp_Pnt2d;
  NCollection_Array1_StepVisual_DraughtingCalloutElement: typeof NCollection_Array1_StepVisual_DraughtingCalloutElement;
  NCollection_Sequence_handle_Geom2d_BoundedCurve: typeof NCollection_Sequence_handle_Geom2d_BoundedCurve;
  NCollection_Array1_handle_StepFEA_NodeRepresentation: typeof NCollection_Array1_handle_StepFEA_NodeRepresentation;
  NCollection_HArray1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember: typeof NCollection_HArray1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember;
  NCollection_Array1_StepAP214_PresentedItemSelect: typeof NCollection_Array1_StepAP214_PresentedItemSelect;
  NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient: typeof NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient;
  NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_2: typeof NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_2;
  NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_3: typeof NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_3;
  NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_4: typeof NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_4;
  NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_5: typeof NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_5;
  NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_6: typeof NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_6;
  NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_7: typeof NCollection_DataMap_TCollection_AsciiString_handle_Standard_Transient_7;
  NCollection_HArray1_AppParCurves_ConstraintCouple: typeof NCollection_HArray1_AppParCurves_ConstraintCouple;
  NCollection_Sequence_Extrema_POnCurv2d: typeof NCollection_Sequence_Extrema_POnCurv2d;
  NCollection_Array1_StepAP214_ApprovalItem: typeof NCollection_Array1_StepAP214_ApprovalItem;
  NCollection_DynamicArray_BRepGraphInc_OccurrenceDef: typeof NCollection_DynamicArray_BRepGraphInc_OccurrenceDef;
  NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_1: typeof NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_1;
  NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_2: typeof NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_2;
  NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_3: typeof NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_3;
  NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_4: typeof NCollection_DynamicArray_BRepGraphInc_OccurrenceDef_4;
  NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment: typeof NCollection_Array1_handle_StepDimTol_DatumReferenceCompartment;
  NCollection_HArray2_double: typeof NCollection_HArray2_double;
  NCollection_List_NCollection_Sequence_int: typeof NCollection_List_NCollection_Sequence_int;
  NCollection_Array1_StepAP203_ContractedItem: typeof NCollection_Array1_StepAP203_ContractedItem;
  NCollection_HArray1_handle_IGESGeom_TransformationMatrix: typeof NCollection_HArray1_handle_IGESGeom_TransformationMatrix;
  NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData: typeof NCollection_Sequence_handle_ShapeAnalysis_FreeBoundData;
  NCollection_HArray1_handle_StepBasic_Organization: typeof NCollection_HArray1_handle_StepBasic_Organization;
  NCollection_Map_TCollection_AsciiString: typeof NCollection_Map_TCollection_AsciiString;
  NCollection_Map_TCollection_AsciiString_2: typeof NCollection_Map_TCollection_AsciiString_2;
  NCollection_Map_TCollection_AsciiString_3: typeof NCollection_Map_TCollection_AsciiString_3;
  NCollection_Map_TCollection_AsciiString_4: typeof NCollection_Map_TCollection_AsciiString_4;
  NCollection_Map_TCollection_AsciiString_5: typeof NCollection_Map_TCollection_AsciiString_5;
  NCollection_Map_TCollection_AsciiString_6: typeof NCollection_Map_TCollection_AsciiString_6;
  NCollection_Map_TCollection_AsciiString_7: typeof NCollection_Map_TCollection_AsciiString_7;
  NCollection_DynamicArray_BOPDS_InterfEF: typeof NCollection_DynamicArray_BOPDS_InterfEF;
  NCollection_DynamicArray_BOPDS_InterfEF_1: typeof NCollection_DynamicArray_BOPDS_InterfEF_1;
  NCollection_DynamicArray_BOPDS_InterfEF_2: typeof NCollection_DynamicArray_BOPDS_InterfEF_2;
  NCollection_DynamicArray_BOPDS_InterfEF_3: typeof NCollection_DynamicArray_BOPDS_InterfEF_3;
  NCollection_DynamicArray_BOPDS_InterfEF_4: typeof NCollection_DynamicArray_BOPDS_InterfEF_4;
  NCollection_List_BRepCheck_Status: typeof NCollection_List_BRepCheck_Status;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Face_4;
  NCollection_Array2_int: typeof NCollection_Array2_int;
  NCollection_HArray1_handle_StepBasic_Person: typeof NCollection_HArray1_handle_StepBasic_Person;
  NCollection_Array1_handle_StepBasic_Product: typeof NCollection_Array1_handle_StepBasic_Product;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_1: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_1;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_2: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_2;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_3: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_3;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_4: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_Triangulation_4;
  NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg: typeof NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg;
  NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_2: typeof NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_2;
  NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_3: typeof NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_3;
  NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_4: typeof NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_4;
  NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_5: typeof NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_5;
  NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_6: typeof NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_6;
  NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_7: typeof NCollection_DataMap_handle_Standard_Transient_NCollection_List_Message_Msg_7;
  NCollection_DynamicArray_BRepGraphInc_FaceDef: typeof NCollection_DynamicArray_BRepGraphInc_FaceDef;
  NCollection_DynamicArray_BRepGraphInc_FaceDef_1: typeof NCollection_DynamicArray_BRepGraphInc_FaceDef_1;
  NCollection_DynamicArray_BRepGraphInc_FaceDef_2: typeof NCollection_DynamicArray_BRepGraphInc_FaceDef_2;
  NCollection_DynamicArray_BRepGraphInc_FaceDef_3: typeof NCollection_DynamicArray_BRepGraphInc_FaceDef_3;
  NCollection_DynamicArray_BRepGraphInc_FaceDef_4: typeof NCollection_DynamicArray_BRepGraphInc_FaceDef_4;
  NCollection_Array1_handle_StepShape_OrientedClosedShell: typeof NCollection_Array1_handle_StepShape_OrientedClosedShell;
  NCollection_Array1_handle_StepShape_OrientedEdge: typeof NCollection_Array1_handle_StepShape_OrientedEdge;
  NCollection_Array1_ChFiDS_CircSection: typeof NCollection_Array1_ChFiDS_CircSection;
  NCollection_Sequence_IntTools_Curve: typeof NCollection_Sequence_IntTools_Curve;
  NCollection_Array1_StepShape_Shell: typeof NCollection_Array1_StepShape_Shell;
  NCollection_Array1_StepAP203_WorkItem: typeof NCollection_Array1_StepAP203_WorkItem;
  NCollection_HArray1_TCollection_ExtendedString: typeof NCollection_HArray1_TCollection_ExtendedString;
  NCollection_DataMap_int_NCollection_PackedMap_int: typeof NCollection_DataMap_int_NCollection_PackedMap_int;
  NCollection_DataMap_int_NCollection_PackedMap_int_2: typeof NCollection_DataMap_int_NCollection_PackedMap_int_2;
  NCollection_DataMap_int_NCollection_PackedMap_int_3: typeof NCollection_DataMap_int_NCollection_PackedMap_int_3;
  NCollection_DataMap_int_NCollection_PackedMap_int_4: typeof NCollection_DataMap_int_NCollection_PackedMap_int_4;
  NCollection_DataMap_int_NCollection_PackedMap_int_5: typeof NCollection_DataMap_int_NCollection_PackedMap_int_5;
  NCollection_DataMap_int_NCollection_PackedMap_int_6: typeof NCollection_DataMap_int_NCollection_PackedMap_int_6;
  NCollection_DataMap_int_NCollection_PackedMap_int_7: typeof NCollection_DataMap_int_NCollection_PackedMap_int_7;
  NCollection_DynamicArray_BRepMesh_Vertex: typeof NCollection_DynamicArray_BRepMesh_Vertex;
  NCollection_DynamicArray_BRepMesh_Vertex_1: typeof NCollection_DynamicArray_BRepMesh_Vertex_1;
  NCollection_DynamicArray_BRepMesh_Vertex_2: typeof NCollection_DynamicArray_BRepMesh_Vertex_2;
  NCollection_DynamicArray_BRepMesh_Vertex_3: typeof NCollection_DynamicArray_BRepMesh_Vertex_3;
  NCollection_DynamicArray_BRepMesh_Vertex_4: typeof NCollection_DynamicArray_BRepMesh_Vertex_4;
  NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher: typeof NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher;
  NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_2: typeof NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_2;
  NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_3: typeof NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_3;
  NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_4: typeof NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_4;
  NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_5: typeof NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_5;
  NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_6: typeof NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_6;
  NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_7: typeof NCollection_IndexedDataMap_TopoDS_Shape_NCollection_List_TopoDS_Shape_TopTools_ShapeMapHasher_7;
  NCollection_Array1_HLRAlgo_TriangleData: typeof NCollection_Array1_HLRAlgo_TriangleData;
  NCollection_HArray1_StepVisual_DirectionCountSelect: typeof NCollection_HArray1_StepVisual_DirectionCountSelect;
  NCollection_DoubleMap_int_TDF_Label: typeof NCollection_DoubleMap_int_TDF_Label;
  NCollection_DoubleMap_int_TDF_Label_2: typeof NCollection_DoubleMap_int_TDF_Label_2;
  NCollection_DoubleMap_int_TDF_Label_3: typeof NCollection_DoubleMap_int_TDF_Label_3;
  NCollection_IndexedMap_handle_Standard_Transient: typeof NCollection_IndexedMap_handle_Standard_Transient;
  NCollection_IndexedMap_handle_Standard_Transient_2: typeof NCollection_IndexedMap_handle_Standard_Transient_2;
  NCollection_IndexedMap_handle_Standard_Transient_3: typeof NCollection_IndexedMap_handle_Standard_Transient_3;
  NCollection_IndexedMap_handle_Standard_Transient_4: typeof NCollection_IndexedMap_handle_Standard_Transient_4;
  NCollection_IndexedMap_handle_Standard_Transient_5: typeof NCollection_IndexedMap_handle_Standard_Transient_5;
  NCollection_IndexedMap_handle_Standard_Transient_6: typeof NCollection_IndexedMap_handle_Standard_Transient_6;
  NCollection_IndexedMap_handle_Standard_Transient_7: typeof NCollection_IndexedMap_handle_Standard_Transient_7;
  NCollection_Array1_handle_StepFEA_CurveElementEndRelease: typeof NCollection_Array1_handle_StepFEA_CurveElementEndRelease;
  NCollection_HArray1_StepAP214_ApprovalItem: typeof NCollection_HArray1_StepAP214_ApprovalItem;
  NCollection_HArray2_handle_Standard_Transient: typeof NCollection_HArray2_handle_Standard_Transient;
  NCollection_HArray1_StepAP214_GroupItem: typeof NCollection_HArray1_StepAP214_GroupItem;
  NCollection_DynamicArray_TopoDS_Face: typeof NCollection_DynamicArray_TopoDS_Face;
  NCollection_DynamicArray_TopoDS_Face_1: typeof NCollection_DynamicArray_TopoDS_Face_1;
  NCollection_DynamicArray_TopoDS_Face_2: typeof NCollection_DynamicArray_TopoDS_Face_2;
  NCollection_DynamicArray_TopoDS_Face_3: typeof NCollection_DynamicArray_TopoDS_Face_3;
  NCollection_DynamicArray_TopoDS_Face_4: typeof NCollection_DynamicArray_TopoDS_Face_4;
  NCollection_Array1_handle_IGESDraw_ConnectPoint: typeof NCollection_Array1_handle_IGESDraw_ConnectPoint;
  NCollection_HArray1_StepAP214_DateAndTimeItem: typeof NCollection_HArray1_StepAP214_DateAndTimeItem;
  NCollection_DynamicArray_TopoDS_Shape: typeof NCollection_DynamicArray_TopoDS_Shape;
  NCollection_DynamicArray_TopoDS_Shape_1: typeof NCollection_DynamicArray_TopoDS_Shape_1;
  NCollection_DynamicArray_TopoDS_Shape_2: typeof NCollection_DynamicArray_TopoDS_Shape_2;
  NCollection_DynamicArray_TopoDS_Shape_3: typeof NCollection_DynamicArray_TopoDS_Shape_3;
  NCollection_DynamicArray_TopoDS_Shape_4: typeof NCollection_DynamicArray_TopoDS_Shape_4;
  NCollection_Array1_StepAP203_ClassifiedItem: typeof NCollection_Array1_StepAP203_ClassifiedItem;
  NCollection_HArray1_StepGeom_TrimmingSelect: typeof NCollection_HArray1_StepGeom_TrimmingSelect;
  NCollection_Array1_handle_Expr_NamedUnknown: typeof NCollection_Array1_handle_Expr_NamedUnknown;
  NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem: typeof NCollection_Array1_StepAP214_AutoDesignGeneralOrgItem;
  NCollection_Sequence_handle_IntPatch_Line: typeof NCollection_Sequence_handle_IntPatch_Line;
  NCollection_Sequence_handle_Geom2d_Curve: typeof NCollection_Sequence_handle_Geom2d_Curve;
  NCollection_DynamicArray_BOPDS_InterfVV: typeof NCollection_DynamicArray_BOPDS_InterfVV;
  NCollection_DynamicArray_BOPDS_InterfVV_1: typeof NCollection_DynamicArray_BOPDS_InterfVV_1;
  NCollection_DynamicArray_BOPDS_InterfVV_2: typeof NCollection_DynamicArray_BOPDS_InterfVV_2;
  NCollection_DynamicArray_BOPDS_InterfVV_3: typeof NCollection_DynamicArray_BOPDS_InterfVV_3;
  NCollection_DynamicArray_BOPDS_InterfVV_4: typeof NCollection_DynamicArray_BOPDS_InterfVV_4;
  NCollection_Sequence_handle_TCollection_HAsciiString: typeof NCollection_Sequence_handle_TCollection_HAsciiString;
  NCollection_Array1_handle_StepBasic_Approval: typeof NCollection_Array1_handle_StepBasic_Approval;
  NCollection_Sequence_handle_Expr_NamedFunction: typeof NCollection_Sequence_handle_Expr_NamedFunction;
  NCollection_Array1_StepVisual_FillStyleSelect: typeof NCollection_Array1_StepVisual_FillStyleSelect;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_2: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_2;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_3: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_3;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_4: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_4;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_5: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_5;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_6: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_6;
  NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_7: typeof NCollection_DataMap_TCollection_ExtendedString_handle_NCollection_HArray1_int_7;
  NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject: typeof NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject;
  NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_2: typeof NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_2;
  NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_3: typeof NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_3;
  NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_4: typeof NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_4;
  NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_5: typeof NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_5;
  NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_6: typeof NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_6;
  NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_7: typeof NCollection_DataMap_handle_XCAFDimTolObjects_GeomToleranceObject_handle_XCAFDimTolObjects_DatumObject_7;
  NCollection_Sequence_PCDM_Reference: typeof NCollection_Sequence_PCDM_Reference;
  NCollection_Array1_StepAP214_PersonAndOrganizationItem: typeof NCollection_Array1_StepAP214_PersonAndOrganizationItem;
  NCollection_HSequence_handle_TCollection_HAsciiString: typeof NCollection_HSequence_handle_TCollection_HAsciiString;
  NCollection_HArray1_StepAP214_AutoDesignGroupedItem: typeof NCollection_HArray1_StepAP214_AutoDesignGroupedItem;
  NCollection_Array1_handle_Geom_Curve: typeof NCollection_Array1_handle_Geom_Curve;
  NCollection_HArray1_StepDimTol_ToleranceZoneTarget: typeof NCollection_HArray1_StepDimTol_ToleranceZoneTarget;
  NCollection_IndexedMap_handle_TDF_Attribute: typeof NCollection_IndexedMap_handle_TDF_Attribute;
  NCollection_IndexedMap_handle_TDF_Attribute_2: typeof NCollection_IndexedMap_handle_TDF_Attribute_2;
  NCollection_IndexedMap_handle_TDF_Attribute_3: typeof NCollection_IndexedMap_handle_TDF_Attribute_3;
  NCollection_IndexedMap_handle_TDF_Attribute_4: typeof NCollection_IndexedMap_handle_TDF_Attribute_4;
  NCollection_IndexedMap_handle_TDF_Attribute_5: typeof NCollection_IndexedMap_handle_TDF_Attribute_5;
  NCollection_IndexedMap_handle_TDF_Attribute_6: typeof NCollection_IndexedMap_handle_TDF_Attribute_6;
  NCollection_IndexedMap_handle_TDF_Attribute_7: typeof NCollection_IndexedMap_handle_TDF_Attribute_7;
  NCollection_DynamicArray_BOPDS_InterfFF: typeof NCollection_DynamicArray_BOPDS_InterfFF;
  NCollection_DynamicArray_BOPDS_InterfFF_1: typeof NCollection_DynamicArray_BOPDS_InterfFF_1;
  NCollection_DynamicArray_BOPDS_InterfFF_2: typeof NCollection_DynamicArray_BOPDS_InterfFF_2;
  NCollection_DynamicArray_BOPDS_InterfFF_3: typeof NCollection_DynamicArray_BOPDS_InterfFF_3;
  NCollection_DynamicArray_BOPDS_InterfFF_4: typeof NCollection_DynamicArray_BOPDS_InterfFF_4;
  NCollection_Array1_StepDimTol_GeometricToleranceModifier: typeof NCollection_Array1_StepDimTol_GeometricToleranceModifier;
  NCollection_HArray1_handle_IGESData_LineFontEntity: typeof NCollection_HArray1_handle_IGESData_LineFontEntity;
  NCollection_Array1_handle_IGESData_ViewKindEntity: typeof NCollection_Array1_handle_IGESData_ViewKindEntity;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_1: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_1;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_2: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_2;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_3: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_3;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_4: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Solid_4;
  NCollection_HArray1_HLRAlgo_PolyHidingData: typeof NCollection_HArray1_HLRAlgo_PolyHidingData;
  NCollection_Array1_Quantity_Color: typeof NCollection_Array1_Quantity_Color;
  NCollection_Array1_handle_StepBasic_Person: typeof NCollection_Array1_handle_StepBasic_Person;
  NCollection_List_gp_Pnt2d: typeof NCollection_List_gp_Pnt2d;
  NCollection_Array1_StepVisual_TessellatedEdgeOrVertex: typeof NCollection_Array1_StepVisual_TessellatedEdgeOrVertex;
  NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId: typeof NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId;
  NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_2: typeof NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_2;
  NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_3: typeof NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_3;
  NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_4: typeof NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_4;
  NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_5: typeof NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_5;
  NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_6: typeof NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_6;
  NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_7: typeof NCollection_DataMap_BRepGraph_UID_BRepGraph_NodeId_7;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_1;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_2;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_3;
  NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_Typed_BRepGraph_NodeId_Kind_Solid_4;
  NCollection_HArray1_handle_Geom2d_BSplineCurve: typeof NCollection_HArray1_handle_Geom2d_BSplineCurve;
  NCollection_IndexedDataMap_TCollection_AsciiString_int: typeof NCollection_IndexedDataMap_TCollection_AsciiString_int;
  NCollection_IndexedDataMap_TCollection_AsciiString_int_2: typeof NCollection_IndexedDataMap_TCollection_AsciiString_int_2;
  NCollection_IndexedDataMap_TCollection_AsciiString_int_3: typeof NCollection_IndexedDataMap_TCollection_AsciiString_int_3;
  NCollection_IndexedDataMap_TCollection_AsciiString_int_4: typeof NCollection_IndexedDataMap_TCollection_AsciiString_int_4;
  NCollection_IndexedDataMap_TCollection_AsciiString_int_5: typeof NCollection_IndexedDataMap_TCollection_AsciiString_int_5;
  NCollection_IndexedDataMap_TCollection_AsciiString_int_6: typeof NCollection_IndexedDataMap_TCollection_AsciiString_int_6;
  NCollection_IndexedDataMap_TCollection_AsciiString_int_7: typeof NCollection_IndexedDataMap_TCollection_AsciiString_int_7;
  NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher: typeof NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher;
  NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_2: typeof NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_2;
  NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_3: typeof NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_3;
  NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_4: typeof NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_4;
  NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_5: typeof NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_5;
  NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_6: typeof NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_6;
  NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_7: typeof NCollection_Map_TopoDS_Shape_TopTools_ShapeMapHasher_7;
  NCollection_Array1_handle_IGESDimen_GeneralNote: typeof NCollection_Array1_handle_IGESDimen_GeneralNote;
  NCollection_HArray1_StepVisual_RenderingPropertiesSelect: typeof NCollection_HArray1_StepVisual_RenderingPropertiesSelect;
  NCollection_DataMap_TCollection_ExtendedString_int: typeof NCollection_DataMap_TCollection_ExtendedString_int;
  NCollection_DataMap_TCollection_ExtendedString_int_2: typeof NCollection_DataMap_TCollection_ExtendedString_int_2;
  NCollection_DataMap_TCollection_ExtendedString_int_3: typeof NCollection_DataMap_TCollection_ExtendedString_int_3;
  NCollection_DataMap_TCollection_ExtendedString_int_4: typeof NCollection_DataMap_TCollection_ExtendedString_int_4;
  NCollection_DataMap_TCollection_ExtendedString_int_5: typeof NCollection_DataMap_TCollection_ExtendedString_int_5;
  NCollection_DataMap_TCollection_ExtendedString_int_6: typeof NCollection_DataMap_TCollection_ExtendedString_int_6;
  NCollection_DataMap_TCollection_ExtendedString_int_7: typeof NCollection_DataMap_TCollection_ExtendedString_int_7;
  NCollection_List_HLRAlgo_BiPoint: typeof NCollection_List_HLRAlgo_BiPoint;
  NCollection_Array2_gp_XYZ: typeof NCollection_Array2_gp_XYZ;
  NCollection_HArray1_HLRAlgo_TriangleData: typeof NCollection_HArray1_HLRAlgo_TriangleData;
  NCollection_List_HLRAlgo_Interference: typeof NCollection_List_HLRAlgo_Interference;
  NCollection_HArray1_StepShape_ShapeDimensionRepresentationItem: typeof NCollection_HArray1_StepShape_ShapeDimensionRepresentationItem;
  NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString: typeof NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString;
  NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_2: typeof NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_2;
  NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_3: typeof NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_3;
  NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_4: typeof NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_4;
  NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_5: typeof NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_5;
  NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_6: typeof NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_6;
  NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_7: typeof NCollection_DataMap_TCollection_AsciiString_TCollection_AsciiString_7;
  NCollection_HArray1_StepVisual_StyleContextSelect: typeof NCollection_HArray1_StepVisual_StyleContextSelect;
  NCollection_HArray1_StepAP214_AutoDesignReferencingItem: typeof NCollection_HArray1_StepAP214_AutoDesignReferencingItem;
  NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember: typeof NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember;
  NCollection_Sequence_int: typeof NCollection_Sequence_int;
  NCollection_Sequence_handle_Geom_BoundedCurve: typeof NCollection_Sequence_handle_Geom_BoundedCurve;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_1: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_1;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_2: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_2;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_3: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_3;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_4: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Wire_4;
  NCollection_Sequence_IntSurf_InteriorPoint: typeof NCollection_Sequence_IntSurf_InteriorPoint;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_double_TopTools_ShapeMapHasher_7;
  NCollection_Array1_handle_StepShape_Edge: typeof NCollection_Array1_handle_StepShape_Edge;
  NCollection_Array1_gp_Trsf: typeof NCollection_Array1_gp_Trsf;
  NCollection_Array2_handle_NCollection_HArray1_int: typeof NCollection_Array2_handle_NCollection_HArray1_int;
  NCollection_Array1_StepVisual_DirectionCountSelect: typeof NCollection_Array1_StepVisual_DirectionCountSelect;
  NCollection_Array1_handle_Geom_BezierCurve: typeof NCollection_Array1_handle_Geom_BezierCurve;
  NCollection_Sequence_gp_XY: typeof NCollection_Sequence_gp_XY;
  NCollection_List_gp_Pnt: typeof NCollection_List_gp_Pnt;
  NCollection_DynamicArray_BRepGraphInc_VertexRef: typeof NCollection_DynamicArray_BRepGraphInc_VertexRef;
  NCollection_DynamicArray_BRepGraphInc_VertexRef_1: typeof NCollection_DynamicArray_BRepGraphInc_VertexRef_1;
  NCollection_DynamicArray_BRepGraphInc_VertexRef_2: typeof NCollection_DynamicArray_BRepGraphInc_VertexRef_2;
  NCollection_DynamicArray_BRepGraphInc_VertexRef_3: typeof NCollection_DynamicArray_BRepGraphInc_VertexRef_3;
  NCollection_DynamicArray_BRepGraphInc_VertexRef_4: typeof NCollection_DynamicArray_BRepGraphInc_VertexRef_4;
  NCollection_DynamicArray_BOPDS_Point: typeof NCollection_DynamicArray_BOPDS_Point;
  NCollection_DynamicArray_BOPDS_Point_1: typeof NCollection_DynamicArray_BOPDS_Point_1;
  NCollection_DynamicArray_BOPDS_Point_2: typeof NCollection_DynamicArray_BOPDS_Point_2;
  NCollection_DynamicArray_BOPDS_Point_3: typeof NCollection_DynamicArray_BOPDS_Point_3;
  NCollection_DynamicArray_BOPDS_Point_4: typeof NCollection_DynamicArray_BOPDS_Point_4;
  NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation: typeof NCollection_Array1_handle_StepRepr_MaterialPropertyRepresentation;
  NCollection_HArray1_StepAP214_OrganizationItem: typeof NCollection_HArray1_StepAP214_OrganizationItem;
  NCollection_HArray1_StepAP203_DateTimeItem: typeof NCollection_HArray1_StepAP203_DateTimeItem;
  NCollection_Array1_float: typeof NCollection_Array1_float;
  NCollection_Sequence_double: typeof NCollection_Sequence_double;
  NCollection_Array1_Bnd_Box: typeof NCollection_Array1_Bnd_Box;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_1: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_1;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_2: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_2;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_3: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_3;
  NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_4: typeof NCollection_DynamicArray_BRepGraph_RepId_Typed_BRepGraph_RepId_Kind_PolygonOnTri_4;
  NCollection_HArray1_handle_StepDimTol_DatumReferenceCompartment: typeof NCollection_HArray1_handle_StepDimTol_DatumReferenceCompartment;
  NCollection_Array1_StepVisual_TextOrCharacter: typeof NCollection_Array1_StepVisual_TextOrCharacter;
  NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_handle_NCollection_HArray2_TopoDS_Shape_TopTools_ShapeMapHasher_7;
  NCollection_Array1_StepAP203_StartRequestItem: typeof NCollection_Array1_StepAP203_StartRequestItem;
  NCollection_HArray1_handle_Standard_Transient: typeof NCollection_HArray1_handle_Standard_Transient;
  NCollection_Array1_StepElement_MeasureOrUnspecifiedValue: typeof NCollection_Array1_StepElement_MeasureOrUnspecifiedValue;
  NCollection_Sequence_IntRes2d_IntersectionPoint: typeof NCollection_Sequence_IntRes2d_IntersectionPoint;
  NCollection_List_TCollection_ExtendedString: typeof NCollection_List_TCollection_ExtendedString;
  NCollection_Array1_StepVisual_SurfaceStyleElementSelect: typeof NCollection_Array1_StepVisual_SurfaceStyleElementSelect;
  NCollection_HArray1_double: typeof NCollection_HArray1_double;
  NCollection_HArray1_handle_StepShape_FaceBound: typeof NCollection_HArray1_handle_StepShape_FaceBound;
  NCollection_List_handle_BRep_PointRepresentation: typeof NCollection_List_handle_BRep_PointRepresentation;
  NCollection_HArray1_handle_StepFEA_ElementRepresentation: typeof NCollection_HArray1_handle_StepFEA_ElementRepresentation;
  NCollection_DynamicArray_BOPDS_InterfVE: typeof NCollection_DynamicArray_BOPDS_InterfVE;
  NCollection_DynamicArray_BOPDS_InterfVE_1: typeof NCollection_DynamicArray_BOPDS_InterfVE_1;
  NCollection_DynamicArray_BOPDS_InterfVE_2: typeof NCollection_DynamicArray_BOPDS_InterfVE_2;
  NCollection_DynamicArray_BOPDS_InterfVE_3: typeof NCollection_DynamicArray_BOPDS_InterfVE_3;
  NCollection_DynamicArray_BOPDS_InterfVE_4: typeof NCollection_DynamicArray_BOPDS_InterfVE_4;
  NCollection_Sequence_Extrema_POnSurf: typeof NCollection_Sequence_Extrema_POnSurf;
  NCollection_Array1_TCollection_AsciiString: typeof NCollection_Array1_TCollection_AsciiString;
  NCollection_Array1_StepAP203_PersonOrganizationItem: typeof NCollection_Array1_StepAP203_PersonOrganizationItem;
  NCollection_Array1_StepAP214_AutoDesignGroupedItem: typeof NCollection_Array1_StepAP214_AutoDesignGroupedItem;
  NCollection_HArray1_StepAP203_ChangeRequestItem: typeof NCollection_HArray1_StepAP203_ChangeRequestItem;
  NCollection_HArray1_StepVisual_DraughtingCalloutElement: typeof NCollection_HArray1_StepVisual_DraughtingCalloutElement;
  NCollection_Sequence_handle_TCollection_HExtendedString: typeof NCollection_Sequence_handle_TCollection_HExtendedString;
  NCollection_HArray1_StepAP214_AutoDesignDateAndPersonItem: typeof NCollection_HArray1_StepAP214_AutoDesignDateAndPersonItem;
  NCollection_HArray1_handle_IGESGraph_TextDisplayTemplate: typeof NCollection_HArray1_handle_IGESGraph_TextDisplayTemplate;
  NCollection_HArray1_handle_IGESDraw_ConnectPoint: typeof NCollection_HArray1_handle_IGESDraw_ConnectPoint;
  NCollection_HArray1_handle_IGESDefs_TabularData: typeof NCollection_HArray1_handle_IGESDefs_TabularData;
  NCollection_Array1_StepVisual_StyleContextSelect: typeof NCollection_Array1_StepVisual_StyleContextSelect;
  NCollection_DynamicArray_BRepGraphInc_ChildRef: typeof NCollection_DynamicArray_BRepGraphInc_ChildRef;
  NCollection_DynamicArray_BRepGraphInc_ChildRef_1: typeof NCollection_DynamicArray_BRepGraphInc_ChildRef_1;
  NCollection_DynamicArray_BRepGraphInc_ChildRef_2: typeof NCollection_DynamicArray_BRepGraphInc_ChildRef_2;
  NCollection_DynamicArray_BRepGraphInc_ChildRef_3: typeof NCollection_DynamicArray_BRepGraphInc_ChildRef_3;
  NCollection_DynamicArray_BRepGraphInc_ChildRef_4: typeof NCollection_DynamicArray_BRepGraphInc_ChildRef_4;
  NCollection_DynamicArray_BOPDS_InterfEE: typeof NCollection_DynamicArray_BOPDS_InterfEE;
  NCollection_DynamicArray_BOPDS_InterfEE_1: typeof NCollection_DynamicArray_BOPDS_InterfEE_1;
  NCollection_DynamicArray_BOPDS_InterfEE_2: typeof NCollection_DynamicArray_BOPDS_InterfEE_2;
  NCollection_DynamicArray_BOPDS_InterfEE_3: typeof NCollection_DynamicArray_BOPDS_InterfEE_3;
  NCollection_DynamicArray_BOPDS_InterfEE_4: typeof NCollection_DynamicArray_BOPDS_InterfEE_4;
  NCollection_Sequence_handle_TDocStd_ApplicationDelta: typeof NCollection_Sequence_handle_TDocStd_ApplicationDelta;
  NCollection_Array1_gp_Vec: typeof NCollection_Array1_gp_Vec;
  NCollection_Array1_StepVisual_BoxCharacteristicSelect: typeof NCollection_Array1_StepVisual_BoxCharacteristicSelect;
  NCollection_HSequence_handle_StepElement_CurveElementSectionDefinition: typeof NCollection_HSequence_handle_StepElement_CurveElementSectionDefinition;
  NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit: typeof NCollection_Array1_handle_StepBasic_UncertaintyMeasureWithUnit;
  NCollection_Array1_StepShape_ValueQualifier: typeof NCollection_Array1_StepShape_ValueQualifier;
  NCollection_Array1_handle_StepFEA_CurveElementInterval: typeof NCollection_Array1_handle_StepFEA_CurveElementInterval;
  NCollection_Array1_gp_Pnt: typeof NCollection_Array1_gp_Pnt;
  NCollection_HSequence_TopoDS_Shape: typeof NCollection_HSequence_TopoDS_Shape;
  NCollection_Sequence_handle_Units_Quantity: typeof NCollection_Sequence_handle_Units_Quantity;
  NCollection_Array1_StepVisual_PresentationStyleSelect: typeof NCollection_Array1_StepVisual_PresentationStyleSelect;
  NCollection_HSequence_handle_Storage_Root: typeof NCollection_HSequence_handle_Storage_Root;
  NCollection_DataMap_TCollection_AsciiString_RWObj_Material: typeof NCollection_DataMap_TCollection_AsciiString_RWObj_Material;
  NCollection_DataMap_TCollection_AsciiString_RWObj_Material_2: typeof NCollection_DataMap_TCollection_AsciiString_RWObj_Material_2;
  NCollection_DataMap_TCollection_AsciiString_RWObj_Material_3: typeof NCollection_DataMap_TCollection_AsciiString_RWObj_Material_3;
  NCollection_DataMap_TCollection_AsciiString_RWObj_Material_4: typeof NCollection_DataMap_TCollection_AsciiString_RWObj_Material_4;
  NCollection_DataMap_TCollection_AsciiString_RWObj_Material_5: typeof NCollection_DataMap_TCollection_AsciiString_RWObj_Material_5;
  NCollection_DataMap_TCollection_AsciiString_RWObj_Material_6: typeof NCollection_DataMap_TCollection_AsciiString_RWObj_Material_6;
  NCollection_DataMap_TCollection_AsciiString_RWObj_Material_7: typeof NCollection_DataMap_TCollection_AsciiString_RWObj_Material_7;
  NCollection_DataMap_int_TopoDS_Shape: typeof NCollection_DataMap_int_TopoDS_Shape;
  NCollection_DataMap_int_TopoDS_Shape_2: typeof NCollection_DataMap_int_TopoDS_Shape_2;
  NCollection_DataMap_int_TopoDS_Shape_3: typeof NCollection_DataMap_int_TopoDS_Shape_3;
  NCollection_DataMap_int_TopoDS_Shape_4: typeof NCollection_DataMap_int_TopoDS_Shape_4;
  NCollection_DataMap_int_TopoDS_Shape_5: typeof NCollection_DataMap_int_TopoDS_Shape_5;
  NCollection_DataMap_int_TopoDS_Shape_6: typeof NCollection_DataMap_int_TopoDS_Shape_6;
  NCollection_DataMap_int_TopoDS_Shape_7: typeof NCollection_DataMap_int_TopoDS_Shape_7;
  NCollection_HArray1_handle_IGESData_IGESEntity: typeof NCollection_HArray1_handle_IGESData_IGESEntity;
  NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_TDF_Label_TopTools_ShapeMapHasher_7;
  NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_handle_Standard_Transient_TopTools_ShapeMapHasher_7;
  NCollection_Array1_StepAP203_ChangeRequestItem: typeof NCollection_Array1_StepAP203_ChangeRequestItem;
  NCollection_List_handle_Message_Alert: typeof NCollection_List_handle_Message_Alert;
  NCollection_HArray1_handle_IGESGeom_Boundary: typeof NCollection_HArray1_handle_IGESGeom_Boundary;
  NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData: typeof NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData;
  NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_2: typeof NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_2;
  NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_3: typeof NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_3;
  NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_4: typeof NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_4;
  NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_5: typeof NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_5;
  NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_6: typeof NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_6;
  NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_7: typeof NCollection_DataMap_TCollection_ExtendedString_handle_CDM_MetaData_7;
  NCollection_DynamicArray_BRepGraphInc_VertexDef: typeof NCollection_DynamicArray_BRepGraphInc_VertexDef;
  NCollection_DynamicArray_BRepGraphInc_VertexDef_1: typeof NCollection_DynamicArray_BRepGraphInc_VertexDef_1;
  NCollection_DynamicArray_BRepGraphInc_VertexDef_2: typeof NCollection_DynamicArray_BRepGraphInc_VertexDef_2;
  NCollection_DynamicArray_BRepGraphInc_VertexDef_3: typeof NCollection_DynamicArray_BRepGraphInc_VertexDef_3;
  NCollection_DynamicArray_BRepGraphInc_VertexDef_4: typeof NCollection_DynamicArray_BRepGraphInc_VertexDef_4;
  NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape: typeof NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape;
  NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_2: typeof NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_2;
  NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_3: typeof NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_3;
  NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_4: typeof NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_4;
  NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_5: typeof NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_5;
  NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_6: typeof NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_6;
  NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_7: typeof NCollection_DataMap_handle_StepShape_TopologicalRepresentationItem_TopoDS_Shape_7;
  NCollection_List_handle_BRep_CurveRepresentation: typeof NCollection_List_handle_BRep_CurveRepresentation;
  NCollection_HArray1_handle_StepDimTol_DatumReference: typeof NCollection_HArray1_handle_StepDimTol_DatumReference;
  NCollection_Map_handle_TDF_Attribute: typeof NCollection_Map_handle_TDF_Attribute;
  NCollection_Map_handle_TDF_Attribute_2: typeof NCollection_Map_handle_TDF_Attribute_2;
  NCollection_Map_handle_TDF_Attribute_3: typeof NCollection_Map_handle_TDF_Attribute_3;
  NCollection_Map_handle_TDF_Attribute_4: typeof NCollection_Map_handle_TDF_Attribute_4;
  NCollection_Map_handle_TDF_Attribute_5: typeof NCollection_Map_handle_TDF_Attribute_5;
  NCollection_Map_handle_TDF_Attribute_6: typeof NCollection_Map_handle_TDF_Attribute_6;
  NCollection_Map_handle_TDF_Attribute_7: typeof NCollection_Map_handle_TDF_Attribute_7;
  NCollection_Array1_handle_StepBasic_NamedUnit: typeof NCollection_Array1_handle_StepBasic_NamedUnit;
  NCollection_HArray1_handle_Geom2d_Curve: typeof NCollection_HArray1_handle_Geom2d_Curve;
  NCollection_Array1_handle_IGESDefs_TabularData: typeof NCollection_Array1_handle_IGESDefs_TabularData;
  NCollection_DynamicArray_BRepGraphInc_EdgeDef: typeof NCollection_DynamicArray_BRepGraphInc_EdgeDef;
  NCollection_DynamicArray_BRepGraphInc_EdgeDef_1: typeof NCollection_DynamicArray_BRepGraphInc_EdgeDef_1;
  NCollection_DynamicArray_BRepGraphInc_EdgeDef_2: typeof NCollection_DynamicArray_BRepGraphInc_EdgeDef_2;
  NCollection_DynamicArray_BRepGraphInc_EdgeDef_3: typeof NCollection_DynamicArray_BRepGraphInc_EdgeDef_3;
  NCollection_DynamicArray_BRepGraphInc_EdgeDef_4: typeof NCollection_DynamicArray_BRepGraphInc_EdgeDef_4;
  NCollection_Array1_handle_StepGeom_CompositeCurveSegment: typeof NCollection_Array1_handle_StepGeom_CompositeCurveSegment;
  NCollection_Array1_handle_IGESGeom_CurveOnSurface: typeof NCollection_Array1_handle_IGESGeom_CurveOnSurface;
  NCollection_Array1_StepFEA_DegreeOfFreedom: typeof NCollection_Array1_StepFEA_DegreeOfFreedom;
  NCollection_Sequence_TDF_Label: typeof NCollection_Sequence_TDF_Label;
  NCollection_Array1_handle_StepElement_VolumeElementPurposeMember: typeof NCollection_Array1_handle_StepElement_VolumeElementPurposeMember;
  NCollection_Sequence_handle_PCDM_Document: typeof NCollection_Sequence_handle_PCDM_Document;
  NCollection_HArray1_handle_IGESGraph_Color: typeof NCollection_HArray1_handle_IGESGraph_Color;
  NCollection_HArray1_handle_IGESDimen_GeneralNote: typeof NCollection_HArray1_handle_IGESDimen_GeneralNote;
  NCollection_HArray1_StepVisual_CameraModelD3MultiClippingUnionSelect: typeof NCollection_HArray1_StepVisual_CameraModelD3MultiClippingUnionSelect;
  NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition: typeof NCollection_Sequence_handle_StepElement_CurveElementSectionDefinition;
  NCollection_DynamicArray_BRepGraph_NodeId: typeof NCollection_DynamicArray_BRepGraph_NodeId;
  NCollection_DynamicArray_BRepGraph_NodeId_1: typeof NCollection_DynamicArray_BRepGraph_NodeId_1;
  NCollection_DynamicArray_BRepGraph_NodeId_2: typeof NCollection_DynamicArray_BRepGraph_NodeId_2;
  NCollection_DynamicArray_BRepGraph_NodeId_3: typeof NCollection_DynamicArray_BRepGraph_NodeId_3;
  NCollection_DynamicArray_BRepGraph_NodeId_4: typeof NCollection_DynamicArray_BRepGraph_NodeId_4;
  NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship: typeof NCollection_Sequence_handle_StepFEA_ElementGeometricRelationship;
  NCollection_Array1_handle_StepRepr_ShapeAspect: typeof NCollection_Array1_handle_StepRepr_ShapeAspect;
  NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject: typeof NCollection_Sequence_handle_XCAFDimTolObjects_DatumObject;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_1: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_1;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_2: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_2;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_3: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_3;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_4: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Face_4;
  NCollection_DynamicArray_BOPDS_InterfEZ: typeof NCollection_DynamicArray_BOPDS_InterfEZ;
  NCollection_DynamicArray_BOPDS_InterfEZ_1: typeof NCollection_DynamicArray_BOPDS_InterfEZ_1;
  NCollection_DynamicArray_BOPDS_InterfEZ_2: typeof NCollection_DynamicArray_BOPDS_InterfEZ_2;
  NCollection_DynamicArray_BOPDS_InterfEZ_3: typeof NCollection_DynamicArray_BOPDS_InterfEZ_3;
  NCollection_DynamicArray_BOPDS_InterfEZ_4: typeof NCollection_DynamicArray_BOPDS_InterfEZ_4;
  NCollection_List_Poly_Triangle: typeof NCollection_List_Poly_Triangle;
  NCollection_DataMap_int_NCollection_List_TopoDS_Shape: typeof NCollection_DataMap_int_NCollection_List_TopoDS_Shape;
  NCollection_DataMap_int_NCollection_List_TopoDS_Shape_2: typeof NCollection_DataMap_int_NCollection_List_TopoDS_Shape_2;
  NCollection_DataMap_int_NCollection_List_TopoDS_Shape_3: typeof NCollection_DataMap_int_NCollection_List_TopoDS_Shape_3;
  NCollection_DataMap_int_NCollection_List_TopoDS_Shape_4: typeof NCollection_DataMap_int_NCollection_List_TopoDS_Shape_4;
  NCollection_DataMap_int_NCollection_List_TopoDS_Shape_5: typeof NCollection_DataMap_int_NCollection_List_TopoDS_Shape_5;
  NCollection_DataMap_int_NCollection_List_TopoDS_Shape_6: typeof NCollection_DataMap_int_NCollection_List_TopoDS_Shape_6;
  NCollection_DataMap_int_NCollection_List_TopoDS_Shape_7: typeof NCollection_DataMap_int_NCollection_List_TopoDS_Shape_7;
  NCollection_HArray1_handle_StepBasic_Document: typeof NCollection_HArray1_handle_StepBasic_Document;
  NCollection_Array1_handle_StepVisual_TessellatedItem: typeof NCollection_Array1_handle_StepVisual_TessellatedItem;
  NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile: typeof NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile;
  NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_2: typeof NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_2;
  NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_3: typeof NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_3;
  NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_4: typeof NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_4;
  NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_5: typeof NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_5;
  NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_6: typeof NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_6;
  NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_7: typeof NCollection_DataMap_TCollection_AsciiString_handle_STEPCAFControl_ExternFile_7;
  NCollection_Array1_StepDimTol_ToleranceZoneTarget: typeof NCollection_Array1_StepDimTol_ToleranceZoneTarget;
  NCollection_Sequence_bool: typeof NCollection_Sequence_bool;
  NCollection_HArray1_unsignedchar: typeof NCollection_HArray1_unsignedchar;
  NCollection_Array1_handle_Geom_BSplineCurve: typeof NCollection_Array1_handle_Geom_BSplineCurve;
  NCollection_DataMap_gp_Pnt_handle_Standard_Transient: typeof NCollection_DataMap_gp_Pnt_handle_Standard_Transient;
  NCollection_DataMap_gp_Pnt_handle_Standard_Transient_2: typeof NCollection_DataMap_gp_Pnt_handle_Standard_Transient_2;
  NCollection_DataMap_gp_Pnt_handle_Standard_Transient_3: typeof NCollection_DataMap_gp_Pnt_handle_Standard_Transient_3;
  NCollection_DataMap_gp_Pnt_handle_Standard_Transient_4: typeof NCollection_DataMap_gp_Pnt_handle_Standard_Transient_4;
  NCollection_DataMap_gp_Pnt_handle_Standard_Transient_5: typeof NCollection_DataMap_gp_Pnt_handle_Standard_Transient_5;
  NCollection_DataMap_gp_Pnt_handle_Standard_Transient_6: typeof NCollection_DataMap_gp_Pnt_handle_Standard_Transient_6;
  NCollection_DataMap_gp_Pnt_handle_Standard_Transient_7: typeof NCollection_DataMap_gp_Pnt_handle_Standard_Transient_7;
  NCollection_Array1_handle_Expr_GeneralExpression: typeof NCollection_Array1_handle_Expr_GeneralExpression;
  NCollection_HArray1_handle_IGESSolid_Loop: typeof NCollection_HArray1_handle_IGESSolid_Loop;
  NCollection_Sequence_handle_Transfer_Finder: typeof NCollection_Sequence_handle_Transfer_Finder;
  NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock: typeof NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock;
  NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_1: typeof NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_1;
  NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_2: typeof NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_2;
  NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_3: typeof NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_3;
  NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_4: typeof NCollection_DynamicArray_NCollection_List_handle_BOPDS_PaveBlock_4;
  NCollection_List_handle_TDF_Attribute: typeof NCollection_List_handle_TDF_Attribute;
  NCollection_HArray1_handle_StepVisual_TessellatedStructuredItem: typeof NCollection_HArray1_handle_StepVisual_TessellatedStructuredItem;
  NCollection_HSequence_handle_Units_Token: typeof NCollection_HSequence_handle_Units_Token;
  NCollection_HArray1_StepDimTol_GeometricToleranceModifier: typeof NCollection_HArray1_StepDimTol_GeometricToleranceModifier;
  NCollection_List_TDF_Label: typeof NCollection_List_TDF_Label;
  NCollection_List_Standard_GUID: typeof NCollection_List_Standard_GUID;
  NCollection_Sequence_Extrema_POnCurv: typeof NCollection_Sequence_Extrema_POnCurv;
  NCollection_HSequence_handle_ChFiDS_SurfData: typeof NCollection_HSequence_handle_ChFiDS_SurfData;
  NCollection_Array1_Poly_Triangle: typeof NCollection_Array1_Poly_Triangle;
  NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient: typeof NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient;
  NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_2: typeof NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_2;
  NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_3: typeof NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_3;
  NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_4: typeof NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_4;
  NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_5: typeof NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_5;
  NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_6: typeof NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_6;
  NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_7: typeof NCollection_DataMap_handle_Standard_Transient_handle_Standard_Transient_7;
  NCollection_HArray2_TopoDS_Shape: typeof NCollection_HArray2_TopoDS_Shape;
  NCollection_HArray1_handle_StepRepr_ShapeAspect: typeof NCollection_HArray1_handle_StepRepr_ShapeAspect;
  NCollection_Sequence_gp_Trsf: typeof NCollection_Sequence_gp_Trsf;
  NCollection_HArray1_handle_StepDimTol_DatumReferenceElement: typeof NCollection_HArray1_handle_StepDimTol_DatumReferenceElement;
  NCollection_HArray1_handle_StepElement_CurveElementSectionDefinition: typeof NCollection_HArray1_handle_StepElement_CurveElementSectionDefinition;
  NCollection_HArray1_handle_IGESAppli_Node: typeof NCollection_HArray1_handle_IGESAppli_Node;
  NCollection_Sequence_handle_Units_Token: typeof NCollection_Sequence_handle_Units_Token;
  NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember: typeof NCollection_Array1_handle_NCollection_HSequence_handle_StepElement_SurfaceElementPurposeMember;
  NCollection_Array1_handle_StepShape_ConnectedEdgeSet: typeof NCollection_Array1_handle_StepShape_ConnectedEdgeSet;
  NCollection_HArray1_StepAP203_ApprovedItem: typeof NCollection_HArray1_StepAP203_ApprovedItem;
  NCollection_Sequence_TopoDS_Shape: typeof NCollection_Sequence_TopoDS_Shape;
  NCollection_HArray1_handle_StepBasic_Product: typeof NCollection_HArray1_handle_StepBasic_Product;
  NCollection_HSequence_TCollection_AsciiString: typeof NCollection_HSequence_TCollection_AsciiString;
  NCollection_Array1_std_pair_unsignedint_int: typeof NCollection_Array1_std_pair_unsignedint_int;
  NCollection_List_Bnd_Range: typeof NCollection_List_Bnd_Range;
  NCollection_HArray1_gp_XYZ: typeof NCollection_HArray1_gp_XYZ;
  NCollection_HArray1_handle_IGESData_ViewKindEntity: typeof NCollection_HArray1_handle_IGESData_ViewKindEntity;
  NCollection_Array1_handle_HLRAlgo_PolyShellData: typeof NCollection_Array1_handle_HLRAlgo_PolyShellData;
  NCollection_Array1_gp_Pnt2d: typeof NCollection_Array1_gp_Pnt2d;
  NCollection_List_BOPTools_CoupleOfShape: typeof NCollection_List_BOPTools_CoupleOfShape;
  NCollection_Array1_handle_IGESData_LineFontEntity: typeof NCollection_Array1_handle_IGESData_LineFontEntity;
  NCollection_HArray1_StepAP203_ContractedItem: typeof NCollection_HArray1_StepAP203_ContractedItem;
  NCollection_Map_TDF_Label: typeof NCollection_Map_TDF_Label;
  NCollection_Map_TDF_Label_2: typeof NCollection_Map_TDF_Label_2;
  NCollection_Map_TDF_Label_3: typeof NCollection_Map_TDF_Label_3;
  NCollection_Map_TDF_Label_4: typeof NCollection_Map_TDF_Label_4;
  NCollection_Map_TDF_Label_5: typeof NCollection_Map_TDF_Label_5;
  NCollection_Map_TDF_Label_6: typeof NCollection_Map_TDF_Label_6;
  NCollection_Map_TDF_Label_7: typeof NCollection_Map_TDF_Label_7;
  NCollection_HArray1_handle_StepShape_ConnectedEdgeSet: typeof NCollection_HArray1_handle_StepShape_ConnectedEdgeSet;
  NCollection_HArray2_int: typeof NCollection_HArray2_int;
  NCollection_HArray1_StepAP203_CertifiedItem: typeof NCollection_HArray1_StepAP203_CertifiedItem;
  NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_gp_XYZ_TopTools_ShapeMapHasher_7;
  NCollection_Array1_handle_StepBasic_DerivedUnitElement: typeof NCollection_Array1_handle_StepBasic_DerivedUnitElement;
  NCollection_HArray1_handle_StepFEA_CurveElementEndRelease: typeof NCollection_HArray1_handle_StepFEA_CurveElementEndRelease;
  NCollection_Sequence_handle_Units_Unit: typeof NCollection_Sequence_handle_Units_Unit;
  NCollection_Array1_StepDimTol_DatumReferenceModifier: typeof NCollection_Array1_StepDimTol_DatumReferenceModifier;
  NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient: typeof NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient;
  NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_2: typeof NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_2;
  NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_3: typeof NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_3;
  NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_4: typeof NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_4;
  NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_5: typeof NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_5;
  NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_6: typeof NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_6;
  NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_7: typeof NCollection_IndexedDataMap_handle_Standard_Transient_handle_Standard_Transient_7;
  NCollection_DynamicArray_BOPDS_FaceInfo: typeof NCollection_DynamicArray_BOPDS_FaceInfo;
  NCollection_DynamicArray_BOPDS_FaceInfo_1: typeof NCollection_DynamicArray_BOPDS_FaceInfo_1;
  NCollection_DynamicArray_BOPDS_FaceInfo_2: typeof NCollection_DynamicArray_BOPDS_FaceInfo_2;
  NCollection_DynamicArray_BOPDS_FaceInfo_3: typeof NCollection_DynamicArray_BOPDS_FaceInfo_3;
  NCollection_DynamicArray_BOPDS_FaceInfo_4: typeof NCollection_DynamicArray_BOPDS_FaceInfo_4;
  NCollection_DataMap_TCollection_ExtendedString_unsignedchar: typeof NCollection_DataMap_TCollection_ExtendedString_unsignedchar;
  NCollection_DataMap_TCollection_ExtendedString_unsignedchar_2: typeof NCollection_DataMap_TCollection_ExtendedString_unsignedchar_2;
  NCollection_DataMap_TCollection_ExtendedString_unsignedchar_3: typeof NCollection_DataMap_TCollection_ExtendedString_unsignedchar_3;
  NCollection_DataMap_TCollection_ExtendedString_unsignedchar_4: typeof NCollection_DataMap_TCollection_ExtendedString_unsignedchar_4;
  NCollection_DataMap_TCollection_ExtendedString_unsignedchar_5: typeof NCollection_DataMap_TCollection_ExtendedString_unsignedchar_5;
  NCollection_DataMap_TCollection_ExtendedString_unsignedchar_6: typeof NCollection_DataMap_TCollection_ExtendedString_unsignedchar_6;
  NCollection_DataMap_TCollection_ExtendedString_unsignedchar_7: typeof NCollection_DataMap_TCollection_ExtendedString_unsignedchar_7;
  NCollection_DynamicArray_handle_NCollection_HSequence_int: typeof NCollection_DynamicArray_handle_NCollection_HSequence_int;
  NCollection_DynamicArray_handle_NCollection_HSequence_int_1: typeof NCollection_DynamicArray_handle_NCollection_HSequence_int_1;
  NCollection_DynamicArray_handle_NCollection_HSequence_int_2: typeof NCollection_DynamicArray_handle_NCollection_HSequence_int_2;
  NCollection_DynamicArray_handle_NCollection_HSequence_int_3: typeof NCollection_DynamicArray_handle_NCollection_HSequence_int_3;
  NCollection_DynamicArray_handle_NCollection_HSequence_int_4: typeof NCollection_DynamicArray_handle_NCollection_HSequence_int_4;
  NCollection_HArray1_handle_StepBasic_ProductContext: typeof NCollection_HArray1_handle_StepBasic_ProductContext;
  NCollection_Array1_StepVisual_RenderingPropertiesSelect: typeof NCollection_Array1_StepVisual_RenderingPropertiesSelect;
  NCollection_Array1_StepAP214_DateItem: typeof NCollection_Array1_StepAP214_DateItem;
  NCollection_HArray1_StepDimTol_DatumReferenceModifier: typeof NCollection_HArray1_StepDimTol_DatumReferenceModifier;
  NCollection_HArray1_handle_IGESGraph_TextFontDef: typeof NCollection_HArray1_handle_IGESGraph_TextFontDef;
  NCollection_IndexedMap_double: typeof NCollection_IndexedMap_double;
  NCollection_IndexedMap_double_2: typeof NCollection_IndexedMap_double_2;
  NCollection_IndexedMap_double_3: typeof NCollection_IndexedMap_double_3;
  NCollection_IndexedMap_double_4: typeof NCollection_IndexedMap_double_4;
  NCollection_IndexedMap_double_5: typeof NCollection_IndexedMap_double_5;
  NCollection_IndexedMap_double_6: typeof NCollection_IndexedMap_double_6;
  NCollection_IndexedMap_double_7: typeof NCollection_IndexedMap_double_7;
  NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject: typeof NCollection_Sequence_handle_XCAFDimTolObjects_GeomToleranceObject;
  NCollection_Array1_StepVisual_LayeredItem: typeof NCollection_Array1_StepVisual_LayeredItem;
  NCollection_DynamicArray_BOPDS_InterfVZ: typeof NCollection_DynamicArray_BOPDS_InterfVZ;
  NCollection_DynamicArray_BOPDS_InterfVZ_1: typeof NCollection_DynamicArray_BOPDS_InterfVZ_1;
  NCollection_DynamicArray_BOPDS_InterfVZ_2: typeof NCollection_DynamicArray_BOPDS_InterfVZ_2;
  NCollection_DynamicArray_BOPDS_InterfVZ_3: typeof NCollection_DynamicArray_BOPDS_InterfVZ_3;
  NCollection_DynamicArray_BOPDS_InterfVZ_4: typeof NCollection_DynamicArray_BOPDS_InterfVZ_4;
  NCollection_Array1_handle_TCollection_HAsciiString: typeof NCollection_Array1_handle_TCollection_HAsciiString;
  NCollection_HArray2_handle_NCollection_HArray1_double: typeof NCollection_HArray2_handle_NCollection_HArray1_double;
  NCollection_HArray1_handle_StepGeom_CompositeCurveSegment: typeof NCollection_HArray1_handle_StepGeom_CompositeCurveSegment;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_1: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_1;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_2: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_2;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_3: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_3;
  NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_4: typeof NCollection_DynamicArray_BRepGraph_RefId_Typed_BRepGraph_RefId_Kind_Vertex_4;
  NCollection_HArray2_handle_Geom_Surface: typeof NCollection_HArray2_handle_Geom_Surface;
  NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket: typeof NCollection_Array1_handle_StepElement_CurveElementEndReleasePacket;
  NCollection_Array2_double: typeof NCollection_Array2_double;
  NCollection_Array2_handle_NCollection_HArray1_double: typeof NCollection_Array2_handle_NCollection_HArray1_double;
  NCollection_HArray1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember: typeof NCollection_HArray1_handle_NCollection_HSequence_handle_StepElement_CurveElementPurposeMember;
  NCollection_DynamicArray_BRepGraphInc_WireRef: typeof NCollection_DynamicArray_BRepGraphInc_WireRef;
  NCollection_DynamicArray_BRepGraphInc_WireRef_1: typeof NCollection_DynamicArray_BRepGraphInc_WireRef_1;
  NCollection_DynamicArray_BRepGraphInc_WireRef_2: typeof NCollection_DynamicArray_BRepGraphInc_WireRef_2;
  NCollection_DynamicArray_BRepGraphInc_WireRef_3: typeof NCollection_DynamicArray_BRepGraphInc_WireRef_3;
  NCollection_DynamicArray_BRepGraphInc_WireRef_4: typeof NCollection_DynamicArray_BRepGraphInc_WireRef_4;
  NCollection_HArray1_int: typeof NCollection_HArray1_int;
  NCollection_Array1_StepGeom_PcurveOrSurface: typeof NCollection_Array1_StepGeom_PcurveOrSurface;
  NCollection_Array1_handle_StepVisual_CurveStyleFontPattern: typeof NCollection_Array1_handle_StepVisual_CurveStyleFontPattern;
  NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation: typeof NCollection_Array1_handle_StepRepr_PropertyDefinitionRepresentation;
  NCollection_Map_handle_BOPDS_PaveBlock: typeof NCollection_Map_handle_BOPDS_PaveBlock;
  NCollection_Map_handle_BOPDS_PaveBlock_2: typeof NCollection_Map_handle_BOPDS_PaveBlock_2;
  NCollection_Map_handle_BOPDS_PaveBlock_3: typeof NCollection_Map_handle_BOPDS_PaveBlock_3;
  NCollection_Map_handle_BOPDS_PaveBlock_4: typeof NCollection_Map_handle_BOPDS_PaveBlock_4;
  NCollection_Map_handle_BOPDS_PaveBlock_5: typeof NCollection_Map_handle_BOPDS_PaveBlock_5;
  NCollection_Map_handle_BOPDS_PaveBlock_6: typeof NCollection_Map_handle_BOPDS_PaveBlock_6;
  NCollection_Map_handle_BOPDS_PaveBlock_7: typeof NCollection_Map_handle_BOPDS_PaveBlock_7;
  NCollection_Array1_StepAP203_SpecifiedItem: typeof NCollection_Array1_StepAP203_SpecifiedItem;
  NCollection_DynamicArray_BRepGraph_RefId: typeof NCollection_DynamicArray_BRepGraph_RefId;
  NCollection_DynamicArray_BRepGraph_RefId_1: typeof NCollection_DynamicArray_BRepGraph_RefId_1;
  NCollection_DynamicArray_BRepGraph_RefId_2: typeof NCollection_DynamicArray_BRepGraph_RefId_2;
  NCollection_DynamicArray_BRepGraph_RefId_3: typeof NCollection_DynamicArray_BRepGraph_RefId_3;
  NCollection_DynamicArray_BRepGraph_RefId_4: typeof NCollection_DynamicArray_BRepGraph_RefId_4;
  NCollection_HArray2_handle_NCollection_HArray1_int: typeof NCollection_HArray2_handle_NCollection_HArray1_int;
  NCollection_HArray1_handle_IGESSolid_Face: typeof NCollection_HArray1_handle_IGESSolid_Face;
  NCollection_HSequence_handle_TCollection_HExtendedString: typeof NCollection_HSequence_handle_TCollection_HExtendedString;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_2: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_2;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_3: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_3;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_4: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_4;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_5: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_5;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_6: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_6;
  NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_7: typeof NCollection_IndexedDataMap_handle_BOPDS_PaveBlock_NCollection_List_handle_BOPDS_PaveBlock_7;
  NCollection_DataMap_TDF_Label_TDF_Label: typeof NCollection_DataMap_TDF_Label_TDF_Label;
  NCollection_DataMap_TDF_Label_TDF_Label_2: typeof NCollection_DataMap_TDF_Label_TDF_Label_2;
  NCollection_DataMap_TDF_Label_TDF_Label_3: typeof NCollection_DataMap_TDF_Label_TDF_Label_3;
  NCollection_DataMap_TDF_Label_TDF_Label_4: typeof NCollection_DataMap_TDF_Label_TDF_Label_4;
  NCollection_DataMap_TDF_Label_TDF_Label_5: typeof NCollection_DataMap_TDF_Label_TDF_Label_5;
  NCollection_DataMap_TDF_Label_TDF_Label_6: typeof NCollection_DataMap_TDF_Label_TDF_Label_6;
  NCollection_DataMap_TDF_Label_TDF_Label_7: typeof NCollection_DataMap_TDF_Label_TDF_Label_7;
  NCollection_Sequence_handle_Geom_Curve: typeof NCollection_Sequence_handle_Geom_Curve;
  NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString: typeof NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString;
  NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_2: typeof NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_2;
  NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_3: typeof NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_3;
  NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_4: typeof NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_4;
  NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_5: typeof NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_5;
  NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_6: typeof NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_6;
  NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_7: typeof NCollection_DataMap_TCollection_ExtendedString_TCollection_ExtendedString_7;
  NCollection_Array1_handle_IGESGraph_TextDisplayTemplate: typeof NCollection_Array1_handle_IGESGraph_TextDisplayTemplate;
  NCollection_DataMap_int_handle_MAT_BasicElt: typeof NCollection_DataMap_int_handle_MAT_BasicElt;
  NCollection_DataMap_int_handle_MAT_BasicElt_2: typeof NCollection_DataMap_int_handle_MAT_BasicElt_2;
  NCollection_DataMap_int_handle_MAT_BasicElt_3: typeof NCollection_DataMap_int_handle_MAT_BasicElt_3;
  NCollection_DataMap_int_handle_MAT_BasicElt_4: typeof NCollection_DataMap_int_handle_MAT_BasicElt_4;
  NCollection_DataMap_int_handle_MAT_BasicElt_5: typeof NCollection_DataMap_int_handle_MAT_BasicElt_5;
  NCollection_DataMap_int_handle_MAT_BasicElt_6: typeof NCollection_DataMap_int_handle_MAT_BasicElt_6;
  NCollection_DataMap_int_handle_MAT_BasicElt_7: typeof NCollection_DataMap_int_handle_MAT_BasicElt_7;
  NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif: typeof NCollection_Sequence_XCAFDimTolObjects_DatumSingleModif;
  NCollection_HArray1_StepVisual_SurfaceStyleElementSelect: typeof NCollection_HArray1_StepVisual_SurfaceStyleElementSelect;
  NCollection_List_handle_ChFiDS_Stripe: typeof NCollection_List_handle_ChFiDS_Stripe;
  NCollection_Array1_handle_StepFEA_ElementRepresentation: typeof NCollection_Array1_handle_StepFEA_ElementRepresentation;
  NCollection_Array2_gp_Pnt2d: typeof NCollection_Array2_gp_Pnt2d;
  NCollection_List_handle_BRepMesh_DiscretAlgoFactory: typeof NCollection_List_handle_BRepMesh_DiscretAlgoFactory;
  NCollection_HArray1_StepAP214_AutoDesignPresentedItemSelect: typeof NCollection_HArray1_StepAP214_AutoDesignPresentedItemSelect;
  NCollection_Array2_handle_Geom_BezierSurface: typeof NCollection_Array2_handle_Geom_BezierSurface;
  NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId: typeof NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId;
  NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_2: typeof NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_2;
  NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_3: typeof NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_3;
  NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_4: typeof NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_4;
  NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_5: typeof NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_5;
  NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_6: typeof NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_6;
  NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_7: typeof NCollection_DataMap_BRepGraph_NodeId_NCollection_DynamicArray_BRepGraph_NodeId_7;
  NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_double_TopTools_ShapeMapHasher_7;
  NCollection_DynamicArray_BRepGraph_RefUID: typeof NCollection_DynamicArray_BRepGraph_RefUID;
  NCollection_DynamicArray_BRepGraph_RefUID_1: typeof NCollection_DynamicArray_BRepGraph_RefUID_1;
  NCollection_DynamicArray_BRepGraph_RefUID_2: typeof NCollection_DynamicArray_BRepGraph_RefUID_2;
  NCollection_DynamicArray_BRepGraph_RefUID_3: typeof NCollection_DynamicArray_BRepGraph_RefUID_3;
  NCollection_DynamicArray_BRepGraph_RefUID_4: typeof NCollection_DynamicArray_BRepGraph_RefUID_4;
  NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem: typeof NCollection_Array1_StepAP214_AutoDesignDateAndPersonItem;
  NCollection_Sequence_IntTools_Range: typeof NCollection_Sequence_IntTools_Range;
  NCollection_Array1_gp_Vec2d: typeof NCollection_Array1_gp_Vec2d;
  NCollection_HArray1_gp_XY: typeof NCollection_HArray1_gp_XY;
  NCollection_HArray1_Quantity_Color: typeof NCollection_HArray1_Quantity_Color;
  NCollection_Array1_handle_StepShape_Face: typeof NCollection_Array1_handle_StepShape_Face;
  NCollection_HArray1_handle_Standard_Persistent: typeof NCollection_HArray1_handle_Standard_Persistent;
  NCollection_Array1_handle_IGESGraph_TextFontDef: typeof NCollection_Array1_handle_IGESGraph_TextFontDef;
  NCollection_HArray1_StepGeom_SurfaceBoundary: typeof NCollection_HArray1_StepGeom_SurfaceBoundary;
  NCollection_Sequence_NCollection_Sequence_handle_Geom2d_Geometry: typeof NCollection_Sequence_NCollection_Sequence_handle_Geom2d_Geometry;
  NCollection_Array1_TDF_Label: typeof NCollection_Array1_TDF_Label;
  NCollection_Array1_handle_StepVisual_PresentationStyleAssignment: typeof NCollection_Array1_handle_StepVisual_PresentationStyleAssignment;
  NCollection_DataMap_TCollection_ExtendedString_double: typeof NCollection_DataMap_TCollection_ExtendedString_double;
  NCollection_DataMap_TCollection_ExtendedString_double_2: typeof NCollection_DataMap_TCollection_ExtendedString_double_2;
  NCollection_DataMap_TCollection_ExtendedString_double_3: typeof NCollection_DataMap_TCollection_ExtendedString_double_3;
  NCollection_DataMap_TCollection_ExtendedString_double_4: typeof NCollection_DataMap_TCollection_ExtendedString_double_4;
  NCollection_DataMap_TCollection_ExtendedString_double_5: typeof NCollection_DataMap_TCollection_ExtendedString_double_5;
  NCollection_DataMap_TCollection_ExtendedString_double_6: typeof NCollection_DataMap_TCollection_ExtendedString_double_6;
  NCollection_DataMap_TCollection_ExtendedString_double_7: typeof NCollection_DataMap_TCollection_ExtendedString_double_7;
  NCollection_DynamicArray_ExtremaPC_ExtremumResult: typeof NCollection_DynamicArray_ExtremaPC_ExtremumResult;
  NCollection_DynamicArray_ExtremaPC_ExtremumResult_1: typeof NCollection_DynamicArray_ExtremaPC_ExtremumResult_1;
  NCollection_DynamicArray_ExtremaPC_ExtremumResult_2: typeof NCollection_DynamicArray_ExtremaPC_ExtremumResult_2;
  NCollection_DynamicArray_ExtremaPC_ExtremumResult_3: typeof NCollection_DynamicArray_ExtremaPC_ExtremumResult_3;
  NCollection_DynamicArray_ExtremaPC_ExtremumResult_4: typeof NCollection_DynamicArray_ExtremaPC_ExtremumResult_4;
  NCollection_HArray1_TopoDS_Shape: typeof NCollection_HArray1_TopoDS_Shape;
  NCollection_HArray2_gp_Pnt: typeof NCollection_HArray2_gp_Pnt;
  NCollection_HArray1_handle_IGESSolid_Shell: typeof NCollection_HArray1_handle_IGESSolid_Shell;
  NCollection_Sequence_handle_Expr_NamedExpression: typeof NCollection_Sequence_handle_Expr_NamedExpression;
  NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape: typeof NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape;
  NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_2: typeof NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_2;
  NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_3: typeof NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_3;
  NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_4: typeof NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_4;
  NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_5: typeof NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_5;
  NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_6: typeof NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_6;
  NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_7: typeof NCollection_DataMap_TCollection_AsciiString_TopoDS_Shape_7;
  NCollection_Sequence_HLRBRep_ShapeBounds: typeof NCollection_Sequence_HLRBRep_ShapeBounds;
  NCollection_HArray1_handle_IGESGeom_CurveOnSurface: typeof NCollection_HArray1_handle_IGESGeom_CurveOnSurface;
  NCollection_HArray1_StepElement_MeasureOrUnspecifiedValue: typeof NCollection_HArray1_StepElement_MeasureOrUnspecifiedValue;
  NCollection_DynamicArray_BRepGraph_UID: typeof NCollection_DynamicArray_BRepGraph_UID;
  NCollection_DynamicArray_BRepGraph_UID_1: typeof NCollection_DynamicArray_BRepGraph_UID_1;
  NCollection_DynamicArray_BRepGraph_UID_2: typeof NCollection_DynamicArray_BRepGraph_UID_2;
  NCollection_DynamicArray_BRepGraph_UID_3: typeof NCollection_DynamicArray_BRepGraph_UID_3;
  NCollection_DynamicArray_BRepGraph_UID_4: typeof NCollection_DynamicArray_BRepGraph_UID_4;
  NCollection_HArray1_handle_TCollection_HAsciiString: typeof NCollection_HArray1_handle_TCollection_HAsciiString;
  NCollection_HArray1_StepAP214_AutoDesignDateAndTimeItem: typeof NCollection_HArray1_StepAP214_AutoDesignDateAndTimeItem;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_2: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_2;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_3: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_3;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_4: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_4;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_5: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_5;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_6: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_6;
  NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_7: typeof NCollection_DataMap_TopoDS_Shape_NCollection_List_Message_Msg_TopTools_ShapeMapHasher_7;
  NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial: typeof NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial;
  NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_2: typeof NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_2;
  NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_3: typeof NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_3;
  NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_4: typeof NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_4;
  NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_5: typeof NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_5;
  NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_6: typeof NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_6;
  NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_7: typeof NCollection_DataMap_handle_XCAFDoc_VisMaterial_handle_XCAFDoc_VisMaterial_7;
  NCollection_Array1_StepVisual_InvisibleItem: typeof NCollection_Array1_StepVisual_InvisibleItem;
  NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance: typeof NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance;
  NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_2: typeof NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_2;
  NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_3: typeof NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_3;
  NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_4: typeof NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_4;
  NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_5: typeof NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_5;
  NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_6: typeof NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_6;
  NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_7: typeof NCollection_DataMap_handle_TopoDS_TShape_handle_VrmlData_Appearance_7;
  NCollection_Array1_handle_StepBasic_ProductContext: typeof NCollection_Array1_handle_StepBasic_ProductContext;
  NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient: typeof NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient;
  NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_2: typeof NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_2;
  NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_3: typeof NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_3;
  NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_4: typeof NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_4;
  NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_5: typeof NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_5;
  NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_6: typeof NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_6;
  NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_7: typeof NCollection_IndexedDataMap_TCollection_AsciiString_handle_Standard_Transient_7;
  NCollection_HArray1_handle_StepRepr_RepresentationItem: typeof NCollection_HArray1_handle_StepRepr_RepresentationItem;
  NCollection_DynamicArray_BRepGraphInc_WireDef: typeof NCollection_DynamicArray_BRepGraphInc_WireDef;
  NCollection_DynamicArray_BRepGraphInc_WireDef_1: typeof NCollection_DynamicArray_BRepGraphInc_WireDef_1;
  NCollection_DynamicArray_BRepGraphInc_WireDef_2: typeof NCollection_DynamicArray_BRepGraphInc_WireDef_2;
  NCollection_DynamicArray_BRepGraphInc_WireDef_3: typeof NCollection_DynamicArray_BRepGraphInc_WireDef_3;
  NCollection_DynamicArray_BRepGraphInc_WireDef_4: typeof NCollection_DynamicArray_BRepGraphInc_WireDef_4;
  BRepAlgoAPI_Cut: typeof BRepAlgoAPI_Cut;
  BRepAlgoAPI_BuilderAlgo: typeof BRepAlgoAPI_BuilderAlgo;
  BRepAlgoAPI_Algo: typeof BRepAlgoAPI_Algo;
  BRepAlgoAPI_BooleanOperation: typeof BRepAlgoAPI_BooleanOperation;
  BRepAlgoAPI_Fuse: typeof BRepAlgoAPI_Fuse;
  BRepPrimAPI_MakeSweep: typeof BRepPrimAPI_MakeSweep;
  BRepPrimAPI_MakeCylinder: typeof BRepPrimAPI_MakeCylinder;
  BRepPrimAPI_MakeOneAxis: typeof BRepPrimAPI_MakeOneAxis;
  BRepPrimAPI_MakeBox: typeof BRepPrimAPI_MakeBox;
  BRepMesh_DiscretRoot: typeof BRepMesh_DiscretRoot;
  BRepMesh_IncrementalMesh: typeof BRepMesh_IncrementalMesh;
  BRepOffsetAPI_MakeFilling: typeof BRepOffsetAPI_MakeFilling;
  BRepOffsetAPI_ThruSections: typeof BRepOffsetAPI_ThruSections;
  BRepOffsetAPI_MakePipeShell: typeof BRepOffsetAPI_MakePipeShell;
  BRepBuilderAPI_MakeEdge: typeof BRepBuilderAPI_MakeEdge;
  BRepBuilderAPI_ModifyShape: typeof BRepBuilderAPI_ModifyShape;
  BRepBuilderAPI_MakeShape: typeof BRepBuilderAPI_MakeShape;
  BRepBuilderAPI_MakeWire: typeof BRepBuilderAPI_MakeWire;
  BRepBuilderAPI_Command: typeof BRepBuilderAPI_Command;
  ChFi3d_FilletShape: typeof ChFi3d_FilletShape;
  BRepFilletAPI_MakeFillet: typeof BRepFilletAPI_MakeFillet;
  BRepFilletAPI_LocalOperation: typeof BRepFilletAPI_LocalOperation;
  TopAbs_ShapeEnum: typeof TopAbs_ShapeEnum;
  Geom_Circle: typeof Geom_Circle;
  Geom_Geometry: typeof Geom_Geometry;
  Geom_Curve: typeof Geom_Curve;
  Geom_Conic: typeof Geom_Conic;
  TopoDS_Face: typeof TopoDS_Face;
  TopoDS_Builder: typeof TopoDS_Builder;
  TopoDS_Edge: typeof TopoDS_Edge;
  TopoDS_Wire: typeof TopoDS_Wire;
  TopoDS_Shape: typeof TopoDS_Shape;
  BRep_Builder: typeof BRep_Builder;
  BRep_Tool: typeof BRep_Tool;
  TopExp_Explorer: typeof TopExp_Explorer;
  TColStd_IndexedDataMapOfStringString: typeof TColStd_IndexedDataMapOfStringString;
  TopoDS: typeof TopoDS;
  OCJS: typeof OCJS;
  getExceptionMessage: typeof getExceptionMessage;
  incrementExceptionRefcount: typeof incrementExceptionRefcount;
  decrementExceptionRefcount: typeof decrementExceptionRefcount;
};

/**
 * Initialize the OpenCASCADE WASM module and return the fully populated instance.
 *
 * Downloads, compiles, and instantiates the WASM binary. The returned
 * `OpenCascadeInstance` provides access to all bound OCCT classes and the
 * Emscripten virtual filesystem.
 *
 * @param options - Emscripten module overrides (e.g. `locateFile`, `print`, `instantiateWasm`).
 * @returns The initialized instance with all OCCT bindings and the `FS` namespace.
 */
export default function init(options?: Record<string, unknown>): Promise<OpenCascadeInstance>;
