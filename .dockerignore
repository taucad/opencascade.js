# Version control and editor metadata
.git
.github
.gitignore
.gitattributes
.vscode
.cursor
.devcontainer

# Nx / pnpm caches and workspace metadata
.nx
node_modules

# Python venv and bytecode
.venv
**/__pycache__/
*.pyc

# Build outputs and host-side dependency checkouts
deps/
build/
dist/
docker-e2e-output/
docs-site/
tarballs/
tmp/
tools/

# Prebuilt host-side artifacts left next to YAML configs
# (the docker build re-creates these inside the image; shipping them as
# build context bloats the build-configs/ COPY layer by ~63 MB).
build-configs/opencascade_single.js
build-configs/opencascade_single.d.ts
build-configs/opencascade_single.wasm
build-configs/opencascade_single.js.symbols
build-configs/opencascade_single.provenance.json
build-configs/provenance.json

# Pre-V8 source checkout (excluded so the build context stays minimal)
occt-src/

# Packaging artifacts and logs
*.tgz
*.log
