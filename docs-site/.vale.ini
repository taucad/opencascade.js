StylesPath = .vale/styles
MinAlertLevel = suggestion

# Documentation pages.
[*.mdx]
BasedOnStyles = Ocjs

# Repository Markdown — the README is a reader-facing surface too, and is held
# to the same standard as the docs it links into.
[*.md]
BasedOnStyles = Ocjs
